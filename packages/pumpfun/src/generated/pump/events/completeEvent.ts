// @ts-nocheck
/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from '@solana/kit';

export const COMPLETE_EVENT_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  95, 114, 97, 156, 212, 46, 152, 8,
]);

export function getCompleteEventEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(COMPLETE_EVENT_EVENT_DISCRIMINATOR);
}

export type CompleteEventEvent = {
  user: Address;
  mint: Address;
  bondingCurve: Address;
  timestamp: bigint;
  quoteMint: Address;
};

export type CompleteEventEventArgs = {
  user: Address;
  mint: Address;
  bondingCurve: Address;
  timestamp: number | bigint;
  quoteMint: Address;
};

/** Gets the encoder for {@link CompleteEventEventArgs} event data. */
export function getCompleteEventEventEncoder(): FixedSizeEncoder<CompleteEventEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ['user', getAddressEncoder()],
      ['mint', getAddressEncoder()],
      ['bondingCurve', getAddressEncoder()],
      ['timestamp', getI64Encoder()],
      ['quoteMint', getAddressEncoder()],
    ]),
    [getConstantEncoder(COMPLETE_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link CompleteEventEvent} event data. */
export function getCompleteEventEventDecoder(): FixedSizeDecoder<CompleteEventEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ['user', getAddressDecoder()],
      ['mint', getAddressDecoder()],
      ['bondingCurve', getAddressDecoder()],
      ['timestamp', getI64Decoder()],
      ['quoteMint', getAddressDecoder()],
    ]),
    [getConstantDecoder(COMPLETE_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link CompleteEventEvent} event data. */
export function getCompleteEventEventCodec(): FixedSizeCodec<
  CompleteEventEventArgs,
  CompleteEventEvent
> {
  return combineCodec(getCompleteEventEventEncoder(), getCompleteEventEventDecoder());
}

/** Parses the provided bytes into {@link CompleteEventEvent} event data. */
export function parseCompleteEventEvent(data: ReadonlyUint8Array | Uint8Array): CompleteEventEvent {
  if (containsBytes(data, COMPLETE_EVENT_EVENT_DISCRIMINATOR, 0)) {
    return getCompleteEventEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "CompleteEventEvent" event discriminators.',
  );
}
