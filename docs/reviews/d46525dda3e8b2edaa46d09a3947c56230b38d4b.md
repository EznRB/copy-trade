# Review d46525dda3e8b2edaa46d09a3947c56230b38d4b

- Branch: sprint/f1-5-enrichment
- Data: 2026-09-25 02:29:23
- Gates: lint=1 typecheck=1 test=1 (0 = ok)
- Veredito: **REJEITADO**

## Saida do code-reviewer

opencode.exe : [0m
No C:\Users\enzo\AppData\Roaming\npm\opencode.ps1:14 caractere:3
+   & "$basedir/node_modules/opencode-ai/bin/opencode.exe"   $args
+   ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : NotSpecified: ([0m:String) [], RemoteException
    + FullyQualifiedErrorId : NativeCommandError
 
> code-reviewer ┬À big-pickle
[0m
[0m$ [0mgit show d46525dda3e8b2edaa46d09a3947c56230b38d4b --stat
commit d46525dda3e8b2edaa46d09a3947c56230b38d4b
Author: EznRB <EznRB@users.noreply.github.com>
Date:   Fri Sep 25 02:17:51 2026 -0300

    feat(f1.5): schema ObservedEvent com enrichment + migration + repositories com guarda TOCTOU + testes
    
    - colunas nullable direction/amountSol/tokenAmount/mint/counterparty/enrichSource/enrichedAt/enrichAttempts + 
@@index
    - migration offline (20260924142844_f15_enrichment) sem BOM
    - repositories: markEnriched/markUndecodable/findUnenriched/incrementEnrichAttempt
    - TOCTOU: updateMany WHERE enrichedAt IS NULL (idempotencia por construcao)
    - observed-events.enrich.test.ts: 9 testes com fakes (sem prisma singleton)
    - schema.prisma sanitizado ASCII (remove U+FFFD/CJK)
    
    Resolve achados: review 88152bbf (HIGH-1 cobertura repos, MEDIUM TOCTOU, MEDIUM encoding), 5299d25*.

 .../20260924142844_f15_enrichment/migration.sql    |  14 ++
 packages/database/prisma/schema.prisma             |  33 ++--
 packages/database/src/index.ts                     |   7 +
 .../repositories/observed-events.enrich.test.ts    | 194 +++++++++++++++++++++
 .../database/src/repositories/observed-events.ts   | 141 +++++++++++++++
 5 files changed, 379 insertions(+), 10 deletions(-)
[0m
[0m$ [0mgit log --oneline -5 d46525dda3e8b2edaa46d09a3947c56230b38d4b
d46525d feat(f1.5): schema ObservedEvent com enrichment + migration + repositories com guarda TOCTOU + testes
d016b8b feat(f1.5): decoder pump.fun/PumpSwap via codama da IDL oficial (pump-public-docs @81091419)
ae69bef feat(ops): higiene mensal (report-only) + bootstrap obrigatorio de sessao para sincronizar chats
93c975b refactor(docs): ADRs em arquivos individuais + indice + protocolo anti-alucinacao (8.3)
551243b feat(ops): monitoramento do ecossistema em 3 camadas (dependabot + monitor semanal + regra 8.2)
[0m
[0m$ [0mgit show d46525dda3e8b2edaa46d09a3947c56230b38d4b -- packages/database/prisma/schema.prisma 
packages/database/src/index.ts
commit d46525dda3e8b2edaa46d09a3947c56230b38d4b
Author: EznRB <EznRB@users.noreply.github.com>
Date:   Fri Sep 25 02:17:51 2026 -0300

    feat(f1.5): schema ObservedEvent com enrichment + migration + repositories com guarda TOCTOU + testes
    
    - colunas nullable direction/amountSol/tokenAmount/mint/counterparty/enrichSource/enrichedAt/enrichAttempts + 
@@index
    - migration offline (20260924142844_f15_enrichment) sem BOM
    - repositories: markEnriched/markUndecodable/findUnenriched/incrementEnrichAttempt
    - TOCTOU: updateMany WHERE enrichedAt IS NULL (idempotencia por construcao)
    - observed-events.enrich.test.ts: 9 testes com fakes (sem prisma singleton)
    - schema.prisma sanitizado ASCII (remove U+FFFD/CJK)
    
    Resolve achados: review 88152bbf (HIGH-1 cobertura repos, MEDIUM TOCTOU, MEDIUM encoding), 5299d25*.

diff --git a/packages/database/prisma/schema.prisma b/packages/database/prisma/schema.prisma
index 87679b0..ee0b14e 100644
--- a/packages/database/prisma/schema.prisma
+++ b/packages/database/prisma/schema.prisma
@@ -1,5 +1,5 @@
-// @ct/database ÔÇö Prisma schema v1 (Fases 1ÔÇô9)
-// Fonte de verdade para persist├¬ncia. Nunca adicionar secrets aqui (somente env refs).
+// @ct/database ? Prisma schema v1 (Fases 1?9)
+// Fonte de verdade para persist?ncia. Nunca adicionar secrets aqui (somente env refs).
 
 datasource db {
   provider = "postgresql"
@@ -25,7 +25,7 @@ model Wallet {
   metrics        WalletMetric[]
   trades         WalletTrade[]
   clusterMembers WalletClusterMember[]
-  // ObservedEvent usa `wallet` como address (String), n├úo FK ÔÇö sem relation aqui.
+  // ObservedEvent usa `wallet` como address (String), n?o FK ? sem relation aqui.
 }
 
 model WalletMetric {
@@ -130,7 +130,7 @@ model Creator {
 // ---------------------------------------------------------------------------
 
 model ObservedEvent {
-  id               String   @id @default(cuid())
+  id               String    @id @default(cuid())
   signature        String
   instructionIndex Int
   wallet           String
@@ -138,14 +138,27 @@ model ObservedEvent {
   slot             BigInt?
   blockTime        DateTime?
   payload          Json?
-  receivedAt       DateTime @default(now())
-
-  // Chave de dedup (idempot├¬ncia ÔÇö AGENTS.md regra 6)
+  receivedAt       DateTime  @default(now())
+
+  // Enrichment F1.5 (decoder pump.fun/PumpSwap) ? tudo nullable:
+  // eventos antigos e nao decodificaveis ficam null/UNKNOWN, nunca inferidos.
+  direction      String? // 'buy' | 'sell'
+  amountSol      Decimal?
+  tokenAmount    Decimal?
+  mint           String?
+  counterparty   String? // bonding curve (? curve) ou pool PumpSwap
+  enrichSource   String? // ex.: 'pump-decoder-v1'
+  enrichedAt     DateTime?
+  enrichAttempts Int      @default(0)
+
+  // Chave de dedup (idempotencia - AGENTS.md regra 6)
   @@unique([signature, instructionIndex, wallet])
+  @@index([mint])
+  @@index([eventType])
 }
 
 // ---------------------------------------------------------------------------
-// Signals (Fases 4ÔÇô7)
+// Signals (Fases 4?7)
 // ---------------------------------------------------------------------------
 
 model Signal {
@@ -176,7 +189,7 @@ model SignalFeature {
 }
 
 // ---------------------------------------------------------------------------
-// Execution (Fases 9ÔÇô10) ÔÇö nunca habilitar LIVE sem aprova├º├úo humana
+// Execution (Fases 9?10) ? nunca habilitar LIVE sem aprova??o humana
 // ---------------------------------------------------------------------------
 
 model Order {
@@ -233,7 +246,7 @@ model ExecutionMetric {
 }
 
 // ---------------------------------------------------------------------------
-// Backtest / ML (Fases 5ÔÇô7)
+// Backtest / ML (Fases 5?7)
 // ---------------------------------------------------------------------------
 
 model BacktestRun {
diff --git a/packages/database/src/index.ts b/packages/database/src/index.ts
index ad87218..eb4e6de 100644
--- a/packages/database/src/index.ts
+++ b/packages/database/src/index.ts
@@ -26,9 +26,16 @@ export type * from '@prisma/client';
 export {
   insertObservedEvent,
   existsObservedEvent,
+  markEnriched,
+  markUndecodable,
+  findUnenriched,
+  incrementEnrichAttempt,
   type InsertResult,
   type ObservedEventInsertData,
   type ObservedEventDelegate,
+  type EnrichmentData,
+  type UnenrichedRow,
+  type ObservedEventEnrichDelegate,
 } from './repositories/observed-events.js';
 export {
   recordSystemEvent,
[0m
[0m$ [0mgit show d46525dda3e8b2edaa46d09a3947c56230b38d4b -- 
"packages/database/prisma/migrations/20260924142844_f15_enrichment/migration.sql"
commit d46525dda3e8b2edaa46d09a3947c56230b38d4b
Author: EznRB <EznRB@users.noreply.github.com>
Date:   Fri Sep 25 02:17:51 2026 -0300

    feat(f1.5): schema ObservedEvent com enrichment + migration + repositories com guarda TOCTOU + testes
    
    - colunas nullable direction/amountSol/tokenAmount/mint/counterparty/enrichSource/enrichedAt/enrichAttempts + 
@@index
    - migration offline (20260924142844_f15_enrichment) sem BOM
    - repositories: markEnriched/markUndecodable/findUnenriched/incrementEnrichAttempt
    - TOCTOU: updateMany WHERE enrichedAt IS NULL (idempotencia por construcao)
    - observed-events.enrich.test.ts: 9 testes com fakes (sem prisma singleton)
    - schema.prisma sanitizado ASCII (remove U+FFFD/CJK)
    
    Resolve achados: review 88152bbf (HIGH-1 cobertura repos, MEDIUM TOCTOU, MEDIUM encoding), 5299d25*.

diff --git a/packages/database/prisma/migrations/20260924142844_f15_enrichment/migration.sql 
b/packages/database/prisma/migrations/20260924142844_f15_enrichment/migration.sql
new file mode 100644
index 0000000..d1a2430
--- /dev/null
+++ b/packages/database/prisma/migrations/20260924142844_f15_enrichment/migration.sql
@@ -0,0 +1,14 @@
+-- F1.5 enrichment: colunas nullable em "ObservedEvent".
+-- Eventos antigos e nao decodificaveis permanecem NULL/UNKNOWN (nunca inferidos).
+ALTER TABLE "ObservedEvent"
+  ADD COLUMN "direction" TEXT,
+  ADD COLUMN "amountSol" DECIMAL(65,30),
+  ADD COLUMN "tokenAmount" DECIMAL(65,30),
+  ADD COLUMN "mint" TEXT,
+  ADD COLUMN "counterparty" TEXT,
+  ADD COLUMN "enrichSource" TEXT,
+  ADD COLUMN "enrichedAt" TIMESTAMP(3),
+  ADD COLUMN "enrichAttempts" INTEGER NOT NULL DEFAULT 0;
+
+CREATE INDEX "ObservedEvent_mint_idx" ON "ObservedEvent"("mint");
+CREATE INDEX "ObservedEvent_eventType_idx" ON "ObservedEvent"("eventType");
[0m
[0m$ [0mgit show d46525dda3e8b2edaa46d09a3947c56230b38d4b -- packages/database/src/repositories/observed-events.ts
commit d46525dda3e8b2edaa46d09a3947c56230b38d4b
Author: EznRB <EznRB@users.noreply.github.com>
Date:   Fri Sep 25 02:17:51 2026 -0300

    feat(f1.5): schema ObservedEvent com enrichment + migration + repositories com guarda TOCTOU + testes
    
    - colunas nullable direction/amountSol/tokenAmount/mint/counterparty/enrichSource/enrichedAt/enrichAttempts + 
@@index
    - migration offline (20260924142844_f15_enrichment) sem BOM
    - repositories: markEnriched/markUndecodable/findUnenriched/incrementEnrichAttempt
    - TOCTOU: updateMany WHERE enrichedAt IS NULL (idempotencia por construcao)
    - observed-events.enrich.test.ts: 9 testes com fakes (sem prisma singleton)
    - schema.prisma sanitizado ASCII (remove U+FFFD/CJK)
    
    Resolve achados: review 88152bbf (HIGH-1 cobertura repos, MEDIUM TOCTOU, MEDIUM encoding), 5299d25*.

diff --git a/packages/database/src/repositories/observed-events.ts 
b/packages/database/src/repositories/observed-events.ts
index 1ebaab6..878cbdd 100644
--- a/packages/database/src/repositories/observed-events.ts
+++ b/packages/database/src/repositories/observed-events.ts
@@ -76,3 +76,144 @@ export async function existsObservedEvent(
     );
   }
 }
+
+// ---------------------------------------------------------------------------
+// F1.5 ÔÇö Enrichment (decoder pump.fun/PumpSwap)
+// ---------------------------------------------------------------------------
+
+export interface EnrichmentData {
+  id: string;
+  direction: 'buy' | 'sell';
+  amountSol: string | number | null;
+  tokenAmount: string | number | null;
+  mint: string;
+  counterparty: string;
+  enrichSource: string;
+}
+
+export interface UnenrichedRow {
+  id: string;
+  signature: string;
+  instructionIndex: number;
+  wallet: string;
+}
+
+export interface ObservedEventEnrichDelegate {
+  update(args: { where: { id: string }; data: Record<string, unknown> }): Promise<unknown>;
+  findMany(args: {
+    where: Record<string, unknown>;
+    orderBy: Record<string, unknown>;
+    take: number;
+    select: Record<string, boolean>;
+  }): Promise<UnenrichedRow[]>;
+  updateMany?(args: { where: Record<string, unknown>; data: Record<string, unknown> }): Promise<unknown>;
+}
+
+/**
+ * Marca um evento como enriquecido com os campos decodificados.
+ * TOCTOU-safe (review 88152bbf): guarda `enrichedAt: null` no WHERE ÔÇö
+ * se outro worker enriqueceu antes, o update n├úo afeta linha alguma e
+ * reportamos que n├úo foi gravado (falso positivo de escrita eliminado).
+ */
+export async function markEnriched(
+  client: { observedEvent: ObservedEventEnrichDelegate },
+  data: EnrichmentData,
+): Promise<void> {
+  // Fallback deterministico quando o delegate nao tem updateMany com guarda.
+  const supportsGuardedWrite = typeof client.observedEvent.updateMany === 'function';
+  const payload = {
+    direction: data.direction,
+    amountSol: data.amountSol,
+    tokenAmount: data.tokenAmount,
+    mint: data.mint,
+    counterparty: data.counterparty,
+    enrichSource: data.enrichSource,
+    enrichedAt: new Date(),
+  };
+  try {
+    if (supportsGuardedWrite && client.observedEvent.updateMany) {
+      const res = (await client.observedEvent.updateMany({
+        where: { id: data.id, enrichedAt: null },
+        data: payload,
+      })) as { count?: number } | undefined;
+      if (res && typeof res.count === 'number' && res.count === 0) {
+        throw new RepositoryError(
+          'DATABASE_ERROR',
+          `markEnriched ignorado: ObservedEvent ${data.id} ja estava enriquecido (TOCTOU)`,
+          new Error('guarda enrichedAt IS NULL falhou'),
+        );
+      }
+      return;
+    }
+    await client.observedEvent.update({
+      where: { id: data.id },
+      data: payload,
+    });
+  } catch (err) {
+    if (err instanceof RepositoryError) throw err;
+    throw new RepositoryError('DATABASE_ERROR', `Falha ao enriquecer ObservedEvent ${data.id}`, err);
+  }
+}
+
+/** Marca o evento como processado sem decodificar (UNKNOWN expl├¡cito). */
+export async function markUndecodable(
+  client: { observedEvent: ObservedEventEnrichDelegate },
+  id: string,
+  reason: string,
+): Promise<void> {
+  try {
+    const guarded = typeof client.observedEvent.updateMany === 'function';
+    const payload = {
+      enrichSource: `undecodable:${reason}`.slice(0, 128),
+      enrichedAt: new Date(),
+    };
+    if (guarded && client.observedEvent.updateMany) {
+      await client.observedEvent.updateMany({
+        where: { id, enrichedAt: null },
+        data: payload,
+      });
+      return;
+    }
+    await client.observedEvent.update({
+      where: { id },
+      data: payload,
+    });
+  } catch (err) {
+    throw new RepositoryError('DATABASE_ERROR', `Falha ao marcar undecodable ${id}`, err);
+  }
+}
+
+/** Busca lote de eventos ainda n├úo enriquecidos para processar (backfill/enricher). */
+export async function findUnenriched(
+  client: { observedEvent: ObservedEventEnrichDelegate },
+  opts: { limit: number; maxAttempts: number },
+): Promise<UnenrichedRow[]> {
+  try {
+    return await client.observedEvent.findMany({
+      where: { enrichedAt: null, enrichAttempts: { lt: opts.maxAttempts } },
+      orderBy: { receivedAt: 'asc' },
+      take: opts.limit,
+      select: { id: true, signature: true, instructionIndex: true, wallet: true },
+    });
+  } catch (err) {
+    throw new RepositoryError('DATABASE_ERROR', 'Falha ao buscar eventos n├úo enriquecidos', err);
+  }
+}
+
+/** Incrementa o contador de tentativas de enrichment (evita retry infinito). */
+export async function incrementEnrichAttempt(
+  client: { observedEvent: ObservedEventEnrichDelegate },
+  id: string,
+): Promise<void> {
+  if (!client.observedEvent.updateMany) {
+    throw new RepositoryError('DATABASE_ERROR', 'delegate sem updateMany', new Error('unsupported'));
+  }
+  try {
+    await client.observedEvent.updateMany({
+      where: { id },
+      data: { enrichAttempts: { increment: 1 } },
+    });
+  } catch (err) {
+    throw new RepositoryError('DATABASE_ERROR', `Falha ao incrementar enrichAttempts ${id}`, err);
+  }
+}
[0m
[0m$ [0mgit show d46525dda3e8b2edaa46d09a3947c56230b38d4b -- 
packages/database/src/repositories/observed-events.enrich.test.ts
commit d46525dda3e8b2edaa46d09a3947c56230b38d4b
Author: EznRB <EznRB@users.noreply.github.com>
Date:   Fri Sep 25 02:17:51 2026 -0300

    feat(f1.5): schema ObservedEvent com enrichment + migration + repositories com guarda TOCTOU + testes
    
    - colunas nullable direction/amountSol/tokenAmount/mint/counterparty/enrichSource/enrichedAt/enrichAttempts + 
@@index
    - migration offline (20260924142844_f15_enrichment) sem BOM
    - repositories: markEnriched/markUndecodable/findUnenriched/incrementEnrichAttempt
    - TOCTOU: updateMany WHERE enrichedAt IS NULL (idempotencia por construcao)
    - observed-events.enrich.test.ts: 9 testes com fakes (sem prisma singleton)
    - schema.prisma sanitizado ASCII (remove U+FFFD/CJK)
    
    Resolve achados: review 88152bbf (HIGH-1 cobertura repos, MEDIUM TOCTOU, MEDIUM encoding), 5299d25*.

diff --git a/packages/database/src/repositories/observed-events.enrich.test.ts 
b/packages/database/src/repositories/observed-events.enrich.test.ts
new file mode 100644
index 0000000..6fa4180
--- /dev/null
+++ b/packages/database/src/repositories/observed-events.enrich.test.ts
@@ -0,0 +1,194 @@
+/**
+ * Testes dos metodos de enrichment do ObservedEvent (review 88152bbf/5299d25):
+ * markEnriched, markUndecodable, findUnenriched, incrementEnrichAttempt.
+ * Fakes em memoria; nunca importa o singleton prisma.
+ */
+import { describe, it, expect } from 'vitest';
+import {
+  markEnriched,
+  markUndecodable,
+  findUnenriched,
+  incrementEnrichAttempt,
+  type UnenrichedRow,
+} from './observed-events.js';
+import { RepositoryError } from './errors.js';
+
+interface Row {
+  id: string;
+  signature: string;
+  instructionIndex: number;
+  wallet: string;
+  enrichedAt: Date | null;
+  enrichAttempts: number;
+  receivedAt: Date;
+  direction?: string | null;
+  mint?: string | null;
+}
+
+/** Delegate fake com update guardado (TOCTOU), updateMany, findMany ordenado. */
+function fakeDelegate(initial: Row[]) {
+  const rows = [...initial];
+  return {
+    rows,
+    async update(args: { where: { id: string }; data: Record<string, unknown> }) {
+      const r = rows.find((x) => x.id === args.where.id);
+      if (!r) throw new Error('not found');
+      Object.assign(r, args.data);
+      return r;
+    },
+    async updateMany(args: { where: Record<string, unknown>; data: Record<string, unknown> }) {
+      const { id, enrichedAt } = args.where as { id?: string; enrichedAt?: Date | null };
+      let count = 0;
+      for (const r of rows) {
+        if (id !== undefined && r.id !== id) continue;
+        if ('enrichedAt' in args.where) {
+          if (enrichedAt === null) {
+            if (r.enrichedAt !== null) continue;
+          }
+        }
+        Object.assign(r, args.data);
+        count++;
+      }
+      return { count };
+    },
+    async findMany(args: {
+      where: Record<string, unknown>;
+      orderBy: Record<string, unknown>;
+      take: number;
+      select: Record<string, boolean>;
+    }): Promise<UnenrichedRow[]> {
+      const out = rows
+        .filter((r) => {
+          const w = args.where as { enrichedAt?: null; enrichAttempts?: { lt?: number } };
+          if (w.enrichedAt === null && r.enrichedAt !== null) return false;
+          if (w.enrichAttempts && r.enrichAttempts >= (w.enrichAttempts.lt ?? Infinity))
+            return false;
+          return true;
+        })
+        .sort((a, b) => a.receivedAt.getTime() - b.receivedAt.getTime())
+        .slice(0, args.take)
+        .map((r) => ({
+          id: r.id,
+          signature: r.signature,
+          instructionIndex: r.instructionIndex,
+          wallet: r.wallet,
+        }));
+      return out;
+    },
+    async updateManyInc(args: { where: { id: string }; data: Record<string, unknown> }) {
+      const r = rows.find((x) => x.id === args.where.id);
+      if (!r) return { count: 0 };
+      const inc = (args.data['enrichAttempts'] as { increment: number }).increment;
+      r.enrichAttempts += inc;
+      return { count: 1 };
+    },
+  };
+}
+
+const row = (id: string, over: Partial<Row> = {}): Row => ({
+  id,
+  signature: id,
+  instructionIndex: 0,
+  wallet: 'W',
+  enrichedAt: null,
+  enrichAttempts: 0,
+  receivedAt: new Date('2026-01-01T00:00:00Z'),
+  ...over,
+});
+
+describe('markEnriched', () => {
+  it('persiste todos os campos + enrichedAt', async () => {
+    const d = fakeDelegate([row('a')]);
+    await markEnriched({ observedEvent: d }, {
+      id: 'a',
+      direction: 'buy',
+      amountSol: '0.5',
+      tokenAmount: '1000',
+      mint: 'M'.repeat(44),
+      counterparty: 'C'.repeat(44),
+      enrichSource: 'pump-decoder-v1:pump_buy',
+    });
+    expect(d.rows[0]!.direction).toBe('buy');
+    expect(d.rows[0]!.enrichedAt).toBeInstanceOf(Date);
+  });
+
+  it('TOCTOU: escrita guardada nao sobrescreve evento ja enriquecido', async () => {
+    const d = fakeDelegate([row('a', { enrichedAt: new Date() })]);
+    await expect(
+      markEnriched({ observedEvent: d }, {
+        id: 'a',
+        direction: 'sell',
+        amountSol: '1',
+        tokenAmount: '1',
+        mint: 'M'.repeat(44),
+        counterparty: 'C'.repeat(44),
+        enrichSource: 'pump-decoder-v1:pump_sell',
+      }),
+    ).rejects.toThrowError(RepositoryError);
+  });
+
+  it('delegate sem updateMany cai no update simples (compat)', async () => {
+    const d = fakeDelegate([row('a')]);
+    const noUM = { ...d } as Omit<typeof d, 'updateMany'>;
+    delete (noUM as Record<string, unknown>).updateMany;
+    await markEnriched({ observedEvent: noUM }, {
+      id: 'a',
+      direction: 'buy',
+      amountSol: null,
+      tokenAmount: '10',
+      mint: 'M'.repeat(44),
+      counterparty: 'C'.repeat(44),
+      enrichSource: 'pump-decoder-v1:pump_buy',
+    });
+    expect(d.rows[0]!.enrichedAt).toBeInstanceOf(Date);
+  });
+});
+
+describe('markUndecodable', () => {
+  it('marca com reason truncada a 128 chars', async () => {
+    const d = fakeDelegate([row('a')]);
+    const long = 'x'.repeat(500);
+    await markUndecodable({ observedEvent: d }, 'a', long);
+    const src = (d.rows[0] as unknown as Record<string, unknown>)['enrichSource'] as string;
+    expect(src.startsWith('undecodable:')).toBe(true);
+    expect(src.length).toBeLessThanOrEqual(128);
+    expect(d.rows[0]!.enrichedAt).toBeInstanceOf(Date);
+  });
+});
+
+describe('findUnenriched', () => {
+  it('filtra enrichedAt null e enrichAttempts < max, ordena por receivedAt asc, respeita take', async () => {
+    const d = fakeDelegate([
+      row('old', { receivedAt: new Date('2026-01-01') }),
+      row('new', { receivedAt: new Date('2026-02-01') }),
+      row('enriched', { enrichedAt: new Date() }),
+      row('maxed', { enrichAttempts: 5 }),
+    ]);
+    const out = await findUnenriched({ observedEvent: d }, { limit: 10, maxAttempts: 5 });
+    expect(out.map((r) => r.id)).toEqual(['old', 'new']);
+    const one = await findUnenriched({ observedEvent: d }, { limit: 1, maxAttempts: 5 });
+    expect(one.map((r) => r.id)).toEqual(['old']);
+  });
+});
+
+describe('incrementEnrichAttempt', () => {
+  it('incrementa via updateMany', async () => {
+    const d = fakeDelegate([row('a')]);
+    // usa o updateMany de incremento do prisma (fake aproxima shape do Prisma)
+    const shaped = {
+      ...d,
+      updateMany: d.updateManyInc.bind(d),
+    };
+    await incrementEnrichAttempt({ observedEvent: shaped }, 'a');
+    expect(d.rows[0]!.enrichAttempts).toBe(1);
+  });
+
+  it('delegate sem updateMany lanca RepositoryError', async () => {
+    const d = fakeDelegate([row('a')]);
+    const noUM = { ...d } as Omit<typeof d, 'updateMany'>;
+    delete (noUM as Record<string, unknown>).updateMany;
+    await expect(incrementEnrichAttempt({ observedEvent: noUM }, 'a')).rejects.toBeInstanceOf(
+      RepositoryError,
+    );
+  });
+});
[0m
[0mÔåÆ [0mRead packages/database/src/repositories/observed-events.ts
[0mÔåÆ [0mRead packages/database/src/repositories/errors.ts
[0mÔåÆ [0mRead packages/database/prisma/schema.prisma
[0m
[0m$ [0mGet-ChildItem "packages\database\prisma\migrations" -Recurse | Select-Object FullName
FullName                                                                                             
--------                                                                                             
C:\Users\enzo\Desktop\copytrade 2\packages\database\prisma\migrations\000000000000_init              
C:\Users\enzo\Desktop\copytrade 2\packages\database\prisma\migrations\migration_lock.toml            
C:\Users\enzo\Desktop\copytrade 2\packages\database\prisma\migrations\000000000000_init\migration.sql
[0m
[0m$ [0mgit grep -n "markEnriched\|findUnenriched\|markUndecodable\|incrementEnrichAttempt" 
d46525dda3e8b2edaa46d09a3947c56230b38d4b -- "*.ts" | Select-String -NotMatch 
"observed-events.ts|observed-events.enrich.test.ts|index.ts"
(no output)
[0m
[0m$ [0m$blob = git show "d46525dda3e8b2edaa46d09a3947c56230b38d4b:packages/database/prisma/schema.prisma" 2>$null; 
$bytes = [System.Text.Encoding]::UTF8.GetBytes(($blob -join "`n")); $nonAscii = @(); foreach ($b in $bytes) { if ($b 
-gt 127) { $nonAscii += $b } }; "Total bytes: $($bytes.Length)"; "Non-ASCII bytes: $($nonAscii.Count)"; if 
($nonAscii.Count -gt 0) { "First 20: $($nonAscii[0..19] -join ',')" }
Total bytes: 9601
Non-ASCII bytes: 0
[0m
[0m$ [0mgit show d016b8b --name-only --format="%s" | Select-Object -First 40
feat(f1.5): decoder pump.fun/PumpSwap via codama da IDL oficial (pump-public-docs @81091419)

packages/pumpfun/codama.pump.mjs
packages/pumpfun/idl/pump.json
packages/pumpfun/idl/pump_amm.json
packages/pumpfun/package.json
packages/pumpfun/scripts/fix-esm-imports.mjs
packages/pumpfun/src/decode.test.ts
packages/pumpfun/src/decode.ts
packages/pumpfun/src/generated/pump/accounts/bondingCurve.ts
packages/pumpfun/src/generated/pump/accounts/feeConfig.ts
packages/pumpfun/src/generated/pump/accounts/global.ts
packages/pumpfun/src/generated/pump/accounts/globalVolumeAccumulator.ts
packages/pumpfun/src/generated/pump/accounts/index.ts
packages/pumpfun/src/generated/pump/accounts/quoteControl.ts
packages/pumpfun/src/generated/pump/accounts/sharingConfig.ts
packages/pumpfun/src/generated/pump/accounts/userVolumeAccumulator.ts
packages/pumpfun/src/generated/pump/errors/index.ts
packages/pumpfun/src/generated/pump/errors/pump.ts
packages/pumpfun/src/generated/pump/events/addQuoteControlMintEvent.ts
packages/pumpfun/src/generated/pump/events/adminCtoEvent.ts
packages/pumpfun/src/generated/pump/events/adminSetIdlAuthorityEvent.ts
packages/pumpfun/src/generated/pump/events/adminUpdateTokenIncentivesEvent.ts
packages/pumpfun/src/generated/pump/events/claimCashbackEvent.ts
packages/pumpfun/src/generated/pump/events/claimTokenIncentivesEvent.ts
packages/pumpfun/src/generated/pump/events/closeUserVolumeAccumulatorEvent.ts
packages/pumpfun/src/generated/pump/events/collectCreatorFeeEvent.ts
packages/pumpfun/src/generated/pump/events/completeEvent.ts
packages/pumpfun/src/generated/pump/events/completePumpAmmMigrationEvent.ts
packages/pumpfun/src/generated/pump/events/createEvent.ts
packages/pumpfun/src/generated/pump/events/distributeCreatorFeesEvent.ts
packages/pumpfun/src/generated/pump/events/distributeFeeToHoldersEvent.ts
packages/pumpfun/src/generated/pump/events/extendAccountEvent.ts
packages/pumpfun/src/generated/pump/events/index.ts
packages/pumpfun/src/generated/pump/events/initUserVolumeAccumulatorEvent.ts
packages/pumpfun/src/generated/pump/events/migrateBondingCurveCreatorEvent.ts
packages/pumpfun/src/generated/pump/events/minimumDistributableFeeEvent.ts
packages/pumpfun/src/generated/pump/events/removeQuoteControlMintEvent.ts
packages/pumpfun/src/generated/pump/events/reservedFeeRecipientsEvent.ts
packages/pumpfun/src/generated/pump/events/setCreatorEvent.ts
[0m
[0m$ [0m$mig = git show "d46525dda3e8b2edaa46d09a3947c56230b38d4b:packages/database/prisma/migrations/20260924142844_
f15_enrichment/migration.sql" 2>$null; $mbytes = [System.Text.Encoding]::UTF8.GetBytes(($mig -join "`n")); $bom = 
($mbytes.Length -ge 3 -and $mbytes[0] -eq 0xEF -and $mbytes[1] -eq 0xBB -and $mbytes[2] -eq 0xBF); "Has BOM: $bom"; 
"Non-ASCII in migration: $(($mbytes | Where-Object { $_ -gt 127 }).Count)"; ""; "--- decoder commit stat ---"; git 
show d016b8b --stat | Select-Object -First 30
Has BOM: False
Non-ASCII in migration: 0

--- decoder commit stat ---
commit d016b8b22f89cd2350ee5dacdbc9ed0a06525f9c
Author: EznRB <EznRB@users.noreply.github.com>
Date:   Fri Sep 25 02:16:10 2026 -0300

    feat(f1.5): decoder pump.fun/PumpSwap via codama da IDL oficial (pump-public-docs @81091419)
    
    - packages/pumpfun/idl/: IDLs oficiais vendored (pump.json, pump_amm.json), pin de SHA em sources.ts
    - codama.pump.mjs: geracao reproduzivel + postprocess documentado (@ts-nocheck apenas nos gerados)
    - scripts/fix-esm-imports.mjs: corrige imports sem extensao do codama para Node ESM
    - decode.ts: wrapper 100% tipado, nunca throw em dados hostis, validacao base58; normaliza lado do token quando 
baseMint=wSOL (inverte buy/sell)
    - decode.test.ts: 15 testes adversariais (truncado, contas ausentes/fora de ordem, mint invalido, discriminador 
desconhecido, inversao wSOL)
    
    Resolve findings antigos do round-1: falta de decoder oficial (RT decoder). Review antigo: 2ab477f*.

 packages/pumpfun/codama.pump.mjs                   |    59 +
 packages/pumpfun/idl/pump.json                     | 12387 +++++++++++++++++++
 packages/pumpfun/idl/pump_amm.json                 |  7681 ++++++++++++
 packages/pumpfun/package.json                      |    46 +-
 packages/pumpfun/scripts/fix-esm-imports.mjs       |    36 +
 packages/pumpfun/src/decode.test.ts                |   183 +
 packages/pumpfun/src/decode.ts                     |   161 +
 .../src/generated/pump/accounts/bondingCurve.ts    |   184 +
 .../src/generated/pump/accounts/feeConfig.ts       |   162 +
 .../pumpfun/src/generated/pump/accounts/global.ts  |   254 +
 .../pump/accounts/globalVolumeAccumulator.ts       |   164 +
 .../pumpfun/src/generated/pump/accounts/index.ts   |    16 +
 .../src/generated/pump/accounts/quoteControl.ts    |   144 +
 .../src/generated/pump/accounts/sharingConfig.ts   |   168 +
 .../pump/accounts/userVolumeAccumulator.ts         |   184 +
 .../pumpfun/src/generated/pump/errors/index.ts     |    10 +
[0m
[0m$ [0mGet-ChildItem packages\database -Depth 1 | Select-Object FullName; ""; "--- root tsconfig files ---"; 
Get-ChildItem -Filter "tsconfig*.json" -Recurse -Depth 2 | Where-Object { $_.FullName -notmatch "node_modules" } | 
Select-Object FullName; ""; "--- package.json scripts (root) ---"; Get-Content package.json -Raw | Select-Object 
-First 1
FullName                                                                     
--------                                                                     
C:\Users\enzo\Desktop\copytrade 2\packages\database\prisma                   
C:\Users\enzo\Desktop\copytrade 2\packages\database\src                      
C:\Users\enzo\Desktop\copytrade 2\packages\database\package.json             
C:\Users\enzo\Desktop\copytrade 2\packages\database\tsconfig.json            
C:\Users\enzo\Desktop\copytrade 2\packages\database\prisma\migrations        
C:\Users\enzo\Desktop\copytrade 2\packages\database\prisma\schema.prisma     
C:\Users\enzo\Desktop\copytrade 2\packages\database\src\repositories         
C:\Users\enzo\Desktop\copytrade 2\packages\database\src\index.ts             

--- root tsconfig files ---
C:\Users\enzo\Desktop\copytrade 2\tsconfig.base.json                         
C:\Users\enzo\Desktop\copytrade 2\tsconfig.json                              
C:\Users\enzo\Desktop\copytrade 2\backtester\tsconfig.json                   
C:\Users\enzo\Desktop\copytrade 2\packages\config\tsconfig.json              
C:\Users\enzo\Desktop\copytrade 2\packages\database\tsconfig.json            
C:\Users\enzo\Desktop\copytrade 2\packages\dex\tsconfig.json                 
C:\Users\enzo\Desktop\copytrade 2\packages\logging\tsconfig.json             
C:\Users\enzo\Desktop\copytrade 2\packages\metrics\tsconfig.json             
C:\Users\enzo\Desktop\copytrade 2\packages\pumpfun\tsconfig.json             
C:\Users\enzo\Desktop\copytrade 2\packages\solana\tsconfig.json              
C:\Users\enzo\Desktop\copytrade 2\packages\types\tsconfig.json               
C:\Users\enzo\Desktop\copytrade 2\services\data-ingestion\tsconfig.json      
C:\Users\enzo\Desktop\copytrade 2\services\execution-engine\tsconfig.json    
C:\Users\enzo\Desktop\copytrade 2\services\feature-engine\tsconfig.json      
C:\Users\enzo\Desktop\copytrade 2\services\notification-service\tsconfig.json
C:\Users\enzo\Desktop\copytrade 2\services\position-manager\tsconfig.json    
C:\Users\enzo\Desktop\copytrade 2\services\risk-engine\tsconfig.json         
C:\Users\enzo\Desktop\copytrade 2\services\signal-engine\tsconfig.json       
C:\Users\enzo\Desktop\copytrade 2\services\token-monitor\tsconfig.json       
C:\Users\enzo\Desktop\copytrade 2\services\wallet-monitor\tsconfig.json      

--- package.json scripts (root) ---
{
  "name": "solana-copytrade-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Solana Smart-Money Intelligence + Pump.fun Copy Trading Engine (PAPER-first)",
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*",
    "backtester"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "lint": "eslint . --ext .ts,.tsx --max-warnings 0",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc -b --pretty",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "prisma:generate": "prisma generate --schema packages/database/prisma/schema.prisma",
    "prisma:migrate": "prisma migrate dev --schema packages/database/prisma/schema.prisma",
    "check:secrets": "node infrastructure/scripts/check-secrets.mjs",
    "check:security": "npm run check:secrets && npm audit --audit-level=high"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@typescript-eslint/eslint-plugin": "^7.13.0",
    "@typescript-eslint/parser": "^7.13.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "fast-check": "^3.19.0",
    "prettier": "^3.3.0",
    "typescript": "^5.4.5",
    "vitest": "^3.2.7"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
[0m
[0mÔ£▒ [0mGrep "insertObservedEvent|existsObservedEvent"[90m in packages ┬À 17 matches[0m
[0m
[0m$ [0mTest-Path "node_modules\typescript\bin\tsc"; Test-Path "node_modules\.bin\tsc.cmd"; node --version
False
False
v25.7.0
[0m
[0m$ [0mnpm ls -g typescript --depth=0 2>$null; Get-ChildItem -Path "C:\Users\enzo\Desktop\copytrade 2" -Filter 
"node_modules" -Directory -Recurse -Depth 3 -ErrorAction SilentlyContinue | Select-Object -First 10 FullName; npx 
--no-install tsc --version 2>$null
C:\Users\enzo\AppData\Roaming\npm
`-- (empty)


FullName                                                
--------                                                
C:\Users\enzo\Desktop\copytrade 2\node_modules          
C:\Users\enzo\Desktop\copytrade 2\.opencode\node_modules
[0m
[0m$ [0mGet-ChildItem node_modules -Directory | Select-Object -First 40 Name; ""; Test-Path "node_modules\.bin\tsc"; 
Get-ChildItem node_modules\.bin -ErrorAction SilentlyContinue | Select-Object -First 10 Name
False
[0m
[0mÔåÉ [0mWrite C:/Users/enzo/AppData/Local/Temp/opencode/delegate-compat-test.ts
Wrote file successfully.
[0m
[0m$ [0mnpx -y typescript@5.4.5 --noEmit --strict --target es2022 --module nodenext --moduleResolution nodenext 
C:\Users\enzo\AppData\Local\Temp\opencode\delegate-compat-test.ts 2>&1 | Select-Object -First 40
node.exe : npm error could not determine executable to run
No linha:1 caractere:1
+ & "C:\Program Files\nodejs/node.exe" "C:\Program Files\nodejs/node_mo ...
+ ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : NotSpecified: (npm error could...ecutable to run:String) [], RemoteException
    + FullyQualifiedErrorId : NativeCommandError
 
npm error A complete log of this run can be found in: 
C:\Users\enzo\AppData\Local\npm-cache\_logs\2026-09-25T05_26_13_286Z-debug-0.log
[0m
[0m$ [0mnpx -y -p typescript@5.4.5 tsc --noEmit --strict --target es2022 --module nodenext --moduleResolution 
nodenext C:\Users\enzo\AppData\Local\Temp\opencode\delegate-compat-test.ts 2>&1 | Select-Object -First 40
../../AppData/Local/Temp/opencode/delegate-compat-test.ts(73,7): error TS2322: Type '{ observedEvent: RealDelegate; }' 
is not assignable to type '{ observedEvent: ObservedEventEnrichDelegate; }'.
  Types of property 'observedEvent' are incompatible.
    Property 'findMany' is missing in type 'RealDelegate' but required in type 'ObservedEventEnrichDelegate'.
[0m
[0mÔåÉ [0mEdit C:/Users/enzo/AppData/Local/Temp/opencode/delegate-compat-test.ts
Index: C:\Users\enzo\AppData\Local\Temp\opencode\delegate-compat-test.ts
===================================================================
--- C:\Users\enzo\AppData\Local\Temp\opencode\delegate-compat-test.ts
+++ C:\Users\enzo\AppData\Local\Temp\opencode\delegate-compat-test.ts
@@ -38,15 +38,25 @@
 interface BatchPayload {
   count: number;
 }
 
+type ObservedEventFindManyArgs = {
+  where?: Record<string, unknown>;
+  orderBy?: unknown;
+  take?: number;
+  select?: Record<string, boolean>;
+};
+
 interface RealDelegate {
   update<T extends ObservedEventUpdateArgs>(
     args: SelectSubset<T, ObservedEventUpdateArgs>,
   ): Promise<unknown>;
   updateMany<T extends { where: unknown; data: unknown }>(
     args?: SelectSubset<T, { where: unknown; data: unknown }>,
   ): Promise<BatchPayload>;
+  findMany<T extends ObservedEventFindManyArgs>(
+    args?: SelectSubset<T, ObservedEventFindManyArgs>,
+  ): Promise<UnenrichedRow[]>;
 }
 
 // --- Interface do repositorio (commit d46525d) ---
 interface UnenrichedRow {

[0m
[0m$ [0mnpx -y -p typescript@5.4.5 tsc --noEmit --strict --target es2022 --module nodenext --moduleResolution 
nodenext C:\Users\enzo\AppData\Local\Temp\opencode\delegate-compat-test.ts 2>&1 | Select-Object -First 40
(no output)
[0m
The mimic may not be faithful enough. Let me test against **real** Prisma-generated types in a temp project.
[0m$ [0mNew-Item -ItemType Directory -Force -Path "C:\Users\enzo\AppData\Local\Temp\opencode\prisma-compat-test" | 
Out-Null; Set-Content -Path "C:\Users\enzo\AppData\Local\Temp\opencode\prisma-compat-test\package.json" -Value '{"name"
:"prisma-compat-test","private":true,"dependencies":{"@prisma/client":"^5.22.0"},"devDependencies":{"prisma":"^5.22.0",
"typescript":"^5.4.5"}}'; Test-Path "C:\Users\enzo\AppData\Local\Temp\opencode\prisma-compat-test\package.json"
True
[0m
[0mÔåÉ [0mWrite C:/Users/enzo/AppData/Local/Temp/opencode/prisma-compat-test/schema.prisma
Wrote file successfully.
[0m
[0m$ [0mnpm install --no-audit --no-fund 2>&1 | Select-Object -Last 3; npx prisma generate --schema schema.prisma 
2>&1 | Select-Object -Last 5
added 8 packages in 6s

Start by importing your Prisma Client (See: https://pris.ly/d/importing-client)

Tip: Need your database queries to be 1000x faster? Accelerate offers you that and more: 
https://pris.ly/tip-2-accelerate
[0m
[0mÔåÉ [0mWrite C:/Users/enzo/AppData/Local/Temp/opencode/prisma-compat-test/compat.ts
Wrote file successfully.
[0m
[0m$ [0mnpx tsc --noEmit --strict --target es2022 --module nodenext --moduleResolution nodenext --esModuleInterop 
compat.ts 2>&1 | Select-Object -First 30
compat.ts(28,7): error TS2322: Type 'PrismaClient<PrismaClientOptions, never, DefaultArgs>' is not assignable to type 
'{ observedEvent: ObservedEventEnrichDelegate; }'.
  The types of 'observedEvent.findMany(...).then' are incompatible between these types.
    Type '<TResult1 = { [x: string]: never; }[], TResult2 = never>(onfulfilled?: ((value: { [x: string]: never; }[]) 
=> TResult1 | PromiseLike<TResult1>) | null | undefined, onrejected?: ((reason: any) => TResult2 | PromiseLike<...>) | 
null | undefined) => Promise<...>' is not assignable to type '<TResult1 = UnenrichedRow[], TResult2 = 
never>(onfulfilled?: ((value: UnenrichedRow[]) => TResult1 | PromiseLike<TResult1>) | null | undefined, onrejected?: 
((reason: any) => TResult2 | PromiseLike<...>) | ... 1 more ... | undefined) => Promise<...>'.
      Types of parameters 'onfulfilled' and 'onfulfilled' are incompatible.
        Types of parameters 'value' and 'value' are incompatible.
          Type '{ [x: string]: never; }[]' is not assignable to type 'UnenrichedRow[]'.
            Type '{ [x: string]: never; }' is missing the following properties from type 'UnenrichedRow': id, 
signature, instructionIndex, wallet
[0m
[0m$ [0mnpx tsc --noEmit --strict --target es2022 --module nodenext --moduleResolution nodenext --esModuleInterop 
compat.ts 2>&1 | Select-Object -First 60
compat.ts(28,7): error TS2322: Type 'PrismaClient<PrismaClientOptions, never, DefaultArgs>' is not assignable to type 
'{ observedEvent: ObservedEventEnrichDelegate; }'.
  The types of 'observedEvent.findMany(...).then' are incompatible between these types.
    Type '<TResult1 = { [x: string]: never; }[], TResult2 = never>(onfulfilled?: ((value: { [x: string]: never; }[]) 
=> TResult1 | PromiseLike<TResult1>) | null | undefined, onrejected?: ((reason: any) => TResult2 | PromiseLike<...>) | 
null | undefined) => Promise<...>' is not assignable to type '<TResult1 = UnenrichedRow[], TResult2 = 
never>(onfulfilled?: ((value: UnenrichedRow[]) => TResult1 | PromiseLike<TResult1>) | null | undefined, onrejected?: 
((reason: any) => TResult2 | PromiseLike<...>) | ... 1 more ... | undefined) => Promise<...>'.
      Types of parameters 'onfulfilled' and 'onfulfilled' are incompatible.
        Types of parameters 'value' and 'value' are incompatible.
          Type '{ [x: string]: never; }[]' is not assignable to type 'UnenrichedRow[]'.
            Type '{ [x: string]: never; }' is missing the following properties from type 'UnenrichedRow': id, 
signature, instructionIndex, wallet
[0m
The delegation test is **confirmed**: the real `PrismaClient` is not assignable to `ObservedEventEnrichDelegate`. Let me check the decoder contract (previous commit) to assess whether `findUnenriched`'s select is sufficient for its consumer.
[0m$ [0mgit show d016b8b:packages/pumpfun/src/decode.ts | Select-Object -First 120
/**
 * decode.ts ÔÇö decoder F1.5 sobre os clientes gerados por codama a partir da
 * IDL oficial pump-fun/pump-public-docs (ADR-016: fonte oficial; ADR-020:
 * codama em vez de discriminadores ├á m├úo).
 *
 * Regras (dados on-chain hostis):
 * - NUNCA lan├ºa exce├º├úo em input adversarial ÔÇö retorna null.
 * - Endere├ºos validados com allowlist base58 (RT-004/006 mantidos fechados).
 * - Valores num├®ricos da instru├º├úo s├úo PAR├éMETROS/LIMITES da instru├º├úo
 *   (amount = tokens; em buy, sol = maxSolCost/maxQuoteAmountIn; em sell,
 *   sol = minSolOutput/minQuoteAmountOut) ÔÇö N├âO s├úo valores realizados.
 *   Valores realizados v├¬m de balance changes (fora do escopo deste decoder).
 */
import { z } from 'zod';
import {
  BUY_DISCRIMINATOR as PUMP_BUY,
  SELL_DISCRIMINATOR as PUMP_SELL,
  getBuyInstructionDataDecoder as pumpBuyDecoder,
  getSellInstructionDataDecoder as pumpSellDecoder,
} from './generated/pump/instructions/index.js';
import { PUMP_PROGRAM_ADDRESS } from './generated/pump/programs/index.js';
import {
  BUY_DISCRIMINATOR as AMM_BUY,
  SELL_DISCRIMINATOR as AMM_SELL,
  getBuyInstructionDataDecoder as ammBuyDecoder,
  getSellInstructionDataDecoder as ammSellDecoder,
} from './generated/pump_amm/instructions/index.js';
import { PUMP_AMM_PROGRAM_ADDRESS } from './generated/pump_amm/programs/index.js';

const BASE58 = /^[1-9A-HJ-NP-Za-km-z]{32,44}$/;
const addressSchema = z.string().regex(BASE58);

export interface DecodedSwap {
  kind: 'pump_buy' | 'pump_sell' | 'pumpswap_buy' | 'pumpswap_sell';
  direction: 'buy' | 'sell';
  mint: string;
  user: string;
  /** Bonding curve (pump.*) ou pool AMM (pumpswap.*). */
  counterparty: string;
  /** Tokens em unidades base (instru├º├úo `amount`/`baseAmount*`). */
  tokenAmount: string | null;
  /** SOL em lamports ÔÇö LIMITE da instru├º├úo (maxSolCost / minSolOutput etc.), n├úo realizado. */
  solAmount: string | null;
}

function discEq(data: ArrayLike<number>, disc: ArrayLike<number>): boolean {
  if (data.length < 8 || disc.length < 8) return false;
  for (let i = 0; i < 8; i++) if (data[i] !== disc[i]) return false;
  return true;
}

function validAddr(a: unknown): a is string {
  return typeof a === 'string' && addressSchema.safeParse(a).success;
}

/** Contas m├¡nimas esperadas: pump.buy/sell = 16; pump_amm.buy/sell = 22. */
const PUMP_MIN_ACCOUNTS = 16;
const AMM_MIN_ACCOUNTS = 22;

export function decodePumpInstruction(
  programId: string,
  data: ArrayLike<number>,
  accounts: string[],
): DecodedSwap | null {
  try {
    // Normaliza para Uint8Array: os decoders do codama exigem Uint8Array.
    const bytes = data instanceof Uint8Array ? data : Uint8Array.from(Array.from(data));
    if (programId === PUMP_PROGRAM_ADDRESS) {
      if (accounts.length < PUMP_MIN_ACCOUNTS) return null;
      // IDL: mint=2, bondingCurve=3, user=6
      const mint = accounts[2];
      const counterparty = accounts[3];
      const user = accounts[6];
      if (!validAddr(mint) || !validAddr(counterparty) || !validAddr(user)) return null;

      if (discEq(bytes, PUMP_BUY)) {
        const d = pumpBuyDecoder().decode(bytes);
        return {
          kind: 'pump_buy',
          direction: 'buy',
          mint: mint!,
          user: user!,
          counterparty: counterparty!,
          tokenAmount: d.amount.toString(),
          solAmount: d.maxSolCost.toString(),
        };
      }
      if (discEq(bytes, PUMP_SELL)) {
        const d = pumpSellDecoder().decode(bytes);
        return {
          kind: 'pump_sell',
          direction: 'sell',
          mint: mint!,
          user: user!,
          counterparty: counterparty!,
          tokenAmount: d.amount.toString(),
          solAmount: d.minSolOutput.toString(),
        };
      }
      return null;
    }

    if (programId === PUMP_AMM_PROGRAM_ADDRESS) {
      if (accounts.length < AMM_MIN_ACCOUNTS) return null;
      // IDL: pool=0, user=1, baseMint=3, quoteMint=4
      const counterparty = accounts[0];
      const user = accounts[1];
      const baseMint = accounts[3];
      const quoteMint = accounts[4];
      if (!validAddr(baseMint) || !validAddr(quoteMint) || !validAddr(counterparty) || !validAddr(user)) return null;

      const WSOL = 'So11111111111111111111111111111111111111112';
      // Normaliza├º├úo pelo lado do TOKEN (review 2ab477f/fixtures): se a base
      // do pool for wSOL, a sem├óntica buy/sell da instru├º├úo se inverte do ponto
      // de vista do token (usu├írio d├í wSOL e recebe token => buy do token).
      const quoteIsWsol = quoteMint === WSOL;
      const baseIsWsol = baseMint === WSOL;
      const tokenMint = quoteIsWsol ? baseMint! : baseIsWsol ? quoteMint! : baseMint!;

      const mapDirection = (ix: 'buy' | 'sell'): 'buy' | 'sell' => {
[0m
[0m$ [0mgit grep -n "getTransaction\|fetchTransaction\|instructionIndex" d46525dda3e8b2edaa46d09a3947c56230b38d4b -- 
"packages/solana/src/*.ts" "packages/solana/src/**/*.ts" | Select-Object -First 20
d46525dda3e8b2edaa46d09a3947c56230b38d4b:packages/solana/src/chain-event.ts:7:  instructionIndex: number;
d46525dda3e8b2edaa46d09a3947c56230b38d4b:packages/solana/src/helius-provider.ts:129:   * (instructionIndex 0, wallet 
''): logsNotification n├úo identifica a wallet
d46525dda3e8b2edaa46d09a3947c56230b38d4b:packages/solana/src/helius-provider.ts:315:        instructionIndex: 0,
d46525dda3e8b2edaa46d09a3947c56230b38d4b:packages/solana/src/rpc-provider.ts:218:   * getTransaction com encoding 
jsonParsed e maxSupportedTransactionVersion: 0
d46525dda3e8b2edaa46d09a3947c56230b38d4b:packages/solana/src/rpc-provider.ts:221:  async getTransaction(signature: 
string): Promise<unknown | null> {
d46525dda3e8b2edaa46d09a3947c56230b38d4b:packages/solana/src/rpc-provider.ts:222:    return this.call<unknown | 
null>('getTransaction', [
[0m
[0m$ [0mgit grep -n "payload" d46525dda3e8b2edaa46d09a3947c56230b38d4b -- 
"packages/database/src/repositories/observed-events.ts" 
"packages/database/src/repositories/observed-events.enrich.test.ts" 
"packages/database/src/repositories/observed-events.test.ts" | Select-Object -First 25
d46525dda3e8b2edaa46d09a3947c56230b38d4b:packages/database/src/repositories/observed-events.ts:17:  payload?: unknown;
d46525dda3e8b2edaa46d09a3947c56230b38d4b:packages/database/src/repositories/observed-events.ts:124:  const payload = {
d46525dda3e8b2edaa46d09a3947c56230b38d4b:packages/database/src/repositories/observed-events.ts:137:        data: 
payload,
d46525dda3e8b2edaa46d09a3947c56230b38d4b:packages/database/src/repositories/observed-events.ts:150:      data: payload,
d46525dda3e8b2edaa46d09a3947c56230b38d4b:packages/database/src/repositories/observed-events.ts:166:    const payload = 
{
d46525dda3e8b2edaa46d09a3947c56230b38d4b:packages/database/src/repositories/observed-events.ts:173:        data: 
payload,
d46525dda3e8b2edaa46d09a3947c56230b38d4b:packages/database/src/repositories/observed-events.ts:179:      data: payload,
[0m
[0m$ [0m$f = git show 
"d46525dda3e8b2edaa46d09a3947c56230b38d4b:packages/database/src/repositories/observed-events.ts"; $i = 0; $f | 
ForEach-Object { $i++; if ($_ -match "ObservedEventEnrichDelegate|export async function 
markEnriched|updateMany\(|markUndecodable|findUnenriched|incrementEnrichAttempt|supportsGuardedWrite|count === 0") { 
"{0}: {1}" -f $i, $_.Trim() } }
101: export interface ObservedEventEnrichDelegate {
118: export async function markEnriched(
119: client: { observedEvent: ObservedEventEnrichDelegate },
123: const supportsGuardedWrite = typeof client.observedEvent.updateMany === 'function';
134: if (supportsGuardedWrite && client.observedEvent.updateMany) {
135: const res = (await client.observedEvent.updateMany({
139: if (res && typeof res.count === 'number' && res.count === 0) {
159: export async function markUndecodable(
160: client: { observedEvent: ObservedEventEnrichDelegate },
171: await client.observedEvent.updateMany({
187: export async function findUnenriched(
188: client: { observedEvent: ObservedEventEnrichDelegate },
204: export async function incrementEnrichAttempt(
205: client: { observedEvent: ObservedEventEnrichDelegate },
212: await client.observedEvent.updateMany({
[0m
Let me get exact line numbers in the branch's file for precise citations.
[0m$ [0m$t = git show 
"d46525dda3e8b2edaa46d09a3947c56230b38d4b:packages/database/src/repositories/observed-events.enrich.test.ts"; ($t | 
Select-String -Pattern "^\s*it\(").Count; ($t | Select-String -Pattern "^\s*it\(" | ForEach-Object { $_.Line.Trim() })
7
it('persiste todos os campos + enrichedAt', async () => {
it('TOCTOU: escrita guardada nao sobrescreve evento ja enriquecido', async () => {
it('delegate sem updateMany cai no update simples (compat)', async () => {
it('marca com reason truncada a 128 chars', async () => {
it('filtra enrichedAt null e enrichAttempts < max, ordena por receivedAt asc, respeita take', async () => {
it('incrementa via updateMany', async () => {
it('delegate sem updateMany lanca RepositoryError', async () => {
[0m
[0m$ [0m$s = git show "d46525dda3e8b2edaa46d09a3947c56230b38d4b:packages/database/prisma/schema.prisma"; $i = 0; $s 
| ForEach-Object { $i++; if ($_ -match "enrichAttempts|@@index|amountSol|mint ") { "{0}: {1}" -f $i, $_.Trim() } }
43: @@index([walletId, window])
50: mint               String
86: mint      String   @unique
106: @@index([tokenId, computedAt])
146: amountSol      Decimal?
148: mint           String?
152: enrichAttempts Int      @default(0)
156: @@index([mint])
157: @@index([eventType])
169: mint            String
178: @@index([strategyId, strategyVersion, createdAt])
201: mint          String
232: mint          String
268: mint         String
[0m
## VEREDITO: REJEITADO

---

## ACHADOS

### HIGH-1 ÔÇö `ObservedEventEnrichDelegate` ├® estruturalmente incompat├¡vel com o `PrismaClient` real (bloqueia o pr├│ximo passo e mascara o problema com fakes)

- **Arquivo:** `packages/database/src/repositories/observed-events.ts:101-112` (interface), consumida por `markEnriched`/`markUndecodable`/`findUnenriched`/`incrementEnrichAttempt` (linhas 118-215).
- **Evid├¬ncia (reproduzido, n├úo opini├úo):** criei um projeto tempor├írio com o **schema.id├¬ntico ao do commit**, rodei `prisma generate` (Prisma 5.22.0, mesma fam├¡lia do repo) e compilei com `tsc --strict`:
  ```ts
  const client = new PrismaClient();
  const c: { observedEvent: ObservedEventEnrichDelegate } = client; // TS2322
  ```
  Erro real: `findMany(...).then` ÔÇö o delegate real retorna `Promise<{ [x: string]: never; }[]>` (via `GetFindResult`), que **n├úo ├® atribu├¡vel** a `Promise<UnenrichedRow[]>`; `update`/`updateMany` tamb├®m s├úo gen├®ricos (`<T extends ObservedEventUpdateArgs>(args: SelectSubset<T, ...>)`) e n├úo casam com os par├ómetros n├úo-gen├®ricos `{ where: { id: string }; data: Record<string, unknown> }`.
- **Como reproduzir:** qualquer chamada real do enricher ÔÇö `markEnriched(db, data)` com `db: PrismaClient` ÔÇö falha em `npm run typecheck` (gate obrigat├│rio, AGENTS.md ┬º6). Hoje n├úo h├í call sites (`git grep` confirma: s├│ o teste com fakes e os exports em `index.ts`), ent├úo o commit est├í "verde" ÔÇö mas o **prop├│sito declarado do commit** ├® fornecer essas fun├º├Áes ao enricher, e o wiring ser├í o primeiro commit a quebrar.
- **Por que ├® grave al├®m do erro de tipo:** os 7 testes usam fakes que casam *exatamente* com a interface inventada, n├úo com a forma real do Prisma ÔÇö a "cobertura" (que o commit diz resolver o achado HIGH-1 da review 88152bbf) valida o contrato errado. O falso positivo fica invis├¡vel at├® a primeira execu├º├úo com o cliente real.
- **Sugest├úo:** tipar contra o delegate real do Prisma (ex.: aceitar `PrismaClient['observedEvent']` com `select` tipado e retorno de `findMany` compat├¡vel com `UnenrichedRow[]` via `Prisma.validator`), ou tornar as fun├º├Áes gen├®ricas; e adicionar um teste *de compila├º├úo* que atribua o `PrismaClient` real ├á interface.

### MEDIUM-1 ÔÇö A query principal do feature (`findUnenriched`) n├úo tem ├¡ndice: seq scan em tabela de eventos que cresce sem limite

- **Arquivo:** `packages/database/prisma/schema.prisma:156-157` ÔÇö s├│ `@@index([mint])` e `@@index([eventType])`; `packages/database/src/repositories/observed-events.ts:193-195` (`WHERE enrichedAt IS NULL AND enrichAttempts < max ORDER BY receivedAt`).
- **Evid├¬ncia:** o commit adiciona ├¡ndices para `mint` e `eventType`, mas n├úo para exatamente o filtro/ordena├º├úo que o pr├│prio feature usa. Em Fase 1+ a tabela `ObservedEvent` recebe 1+ linha por trade observado (milh├Áes); `findUnenriched` varreria a tabela inteira a cada poll do enricher.
- **Sugest├úo:** `@@index([enrichedAt, receivedAt])`, ou partial index via migra├º├úo manual (`WHERE enrichedAt IS NULL`).

### MEDIUM-2 ÔÇö Sem estado terminal quando `enrichAttempts >= maxAttempts`: linha ├│rf├ú invis├¡vel

- **Arquivo:** `observed-events.ts:193-195`.
- **Evid├¬ncia:** ap├│s `maxAttempts` tentativas, o evento fica `enrichedAt = null`, **exclu├¡do** de `findUnenriched` (`enrichAttempts: { lt: max }`) e sem `markUndecodable` ÔÇö n├úo ├® enriquecido, n├úo ├® UNKNOWN expl├¡cito, nunca mais ├® tocado, e **nenhuma API do reposit├│rio permite list├í-lo**. ├ë uma perda silenciosa de dados + impossibilidade de auditoria (AGENTS.md: observabilidade > performance).
- **Sugest├úo:** ao atingir o cap, terminal expl├¡cito (`enrichSource='undecodable:max_attempts'` + `enrichedAt`), mantendo a distin├º├úo do motivo.

### MEDIUM-3 ÔÇö `markUndecodable` queima o evento permanentemente; sem├óntica de "nunca inferido" vs "├║nica tentativa" conflita com a pr├│pria doc

- **Arquivo:** `observed-events.ts:159-185`.
- **Evid├¬ncia:** `enrichedAt = now()` remove o evento de `findUnenriched` para sempre. Duas consequ├¬ncias: (1) quando o decoder evoluir (v2 conhecer├í discriminadores novos), n├úo h├í re-decode path ÔÇö o dado fica ├│rf├úo; (2) falhas **transit├│rias** (RPC down, rate-limit) s├úo classificadas como "undecodable" se o caller errar ÔÇö o que **n├úo** ├® tratado aqui (o retry correto exigiria `incrementEnrichAttempt`, mas nada na API impede o uso errado). E `enrichSource` ("fonte") ├® overloaded como status (`undecodable:<reason>` at├® 128 chars).
- **Sugest├úo:** separar status (`decoded | undecodable | errored`) de `enrichSource`; considerar re-decode peri├│dico com `enrichAttempts` tamb├®m para undecodable.

### MEDIUM-4 ÔÇö Sem├óntica/unidade de `amountSol` indefinida entre coluna, tipo e decoder (risco de validade estat├¡stica a jusante)

- **Arquivo:** `schema.prisma:146` (`amountSol Decimal?`), `observed-events.ts:91` (`amountSol: string | number | null`); contrato do decoder no commit anterior `d016b8b` (`packages/pumpfun/src/decode.ts`): `solAmount: string | null` ÔÇö **"SOL em lamports ÔÇö LIMITE da instru├º├úo (maxSolCost/minSolOutput), n├úo realizado"**.
- **Evid├¬ncia:** o nome `amountSol` sugere SOL realizados; o decoder entrega **lamports** de um **limite**. Um enricher que grave `decoder.solAmount` cru na coluna produz `5 lamports ÔåÆ 0.000000005` (erro de 9 ordens de grandeza) e, pior, features de F4/backtest interpretando limite como valor realizado violam AGENTS.md ┬º9 ("fills imposs├¡veis", "ignorar fees/slippage", validade estat├¡stica). Nada no schema/tipo documenta unidade nem "limite".
- **Sugest├úo:** renomear/tipar explicitamente (ex.: `amountSolLamports` + doc `-- LIMITE, n├úo realizado`), ou converter na fronteira com contrato testado.

### MEDIUM-5 ÔÇö Fallback n├úo-guardado em `markEnriched` contorna a pr├│pria guarda TOCTOU (destaque do commit)

- **Arquivo:** `observed-events.ts:147-151`.
- **Evid├¬ncia:** quando o delegate n├úo exp├Áe `updateMany`, cai em `update` incondicional **sem** `WHERE enrichedAt IS NULL` ÔÇö a escrita sobrescreve dados/`enrichedAt` de um enriquecimento concorrente. Com o Prisma real o caminho ├® inalcan├º├ível (`updateMany` sempre existe), mas a interface o anuncia como v├ílido (`updateMany?` opcional + teste dedicado "compat"), e o commit vende "idempot├¬ncia por constru├º├úo" sem ressalva. Um wrapper delegado (ex.: camada de cache/retry) reativa a race silenciosamente.
- **Sugest├úo:** remover o fallback, ou marc├í-lo explicitamente como n├úo-TOCTOU-safe e lan├ºar se `updateMany` ausente.

### LOW-1 ÔÇö Sanitiza├º├úo ASCII corrompeu coment├írios: caracteres v├ílidos viraram `?` literal

- **Arquivo:** `schema.prisma:1` ("Fases 1?9"), `:28` ("n?o FK"), `:145` ("? tudo nullable"), entre outros.
- **Evid├¬ncia:** verifica├º├úo byte a byte do blob da branch: 0 bytes n├úo-ASCII (a sanitiza├º├úo "funcionou"), mas `ÔÇö`, `ÔÇô`, `├ú`, `├á` foram substitu├¡dos por `?` **literal** (0x3F). Coment├írios ficaram ileg├¡veis ("Fases 1?9" em vez de "Fases 1ÔÇô9"). Cosm├®tico, mas degrada a documenta├º├úo do schema ÔÇö fonte de verdade do projeto.
- **Sugest├úo:** reescrever coment├írios com texto ASCII intencional, n├úo substitui├º├úo cega.

### LOW-2 ÔÇö Commit declara "9 testes"; o arquivo tem 7 `it()`

- **Arquivo:** `observed-events.enrich.test.ts` ÔÇö contagem real: 3 (markEnriched) + 1 (markUndecodable) + 1 (findUnenriched) + 2 (incrementEnrichAttempt) = **7**.

### LOW-3 ÔÇö TOCTOU tratado via exce├º├úo com mensagem enganosa; conten├º├úo esperada vira erro de banco

- **Arquivo:** `observed-events.ts:139-145`.
- **Evid├¬ncia:** `count === 0` lan├ºa `RepositoryError(DATABASE_ERROR, "... ja estava enriquecido (TOCTOU)")` ÔÇö a mesma condi├º├úo ocorre quando a **linha n├úo existe** (ex.: deletada por reten├º├úo), e o caller (worker em lote) pode interpretar como falha de decodifica├º├úo e incrementar `enrichAttempts` de um evento **j├í enriquecido** por outro worker ÔÇö corrompendo o bookkeeping de tentativas.
- **Sugest├úo:** retornar `'written' | 'skipped'` em vez de lan├ºar para conten├º├úo leg├¡tima.

### LOW-4 ÔÇö `findUnenriched` n├úo seleciona `payload`/`eventType`: contrato incompleto para o consumidor

- **Arquivo:** `observed-events.ts:199-201`.
- **Evid├¬ncia:** o select retorna s├│ `id/signature/instructionIndex/wallet`; o decoder `decodePumpInstruction(programId, data, accounts)` (d016b8b) precisa dos bytes da instru├º├úo e contas ÔÇö o enricher ter├í que refetch via RPC (`getTransaction`, `packages/solana/src/rpc-provider.ts:221`) ou uma query nova. Decis├úo de design aceit├ível, mas n├úo documentada nem testada.

### LOW-5 ÔÇö Inconsist├¬ncia: `markUndecodable` ignora `count === 0` silenciosamente

- **Arquivo:** `observed-events.ts:171-176` ÔÇö linha inexistente = no-op silencioso; `markEnriched` lan├ºa na mesma condi├º├úo. Comportamento divergente dificulta auditoria de perdas.

---

## TESTES EXECUTADOS (tentativas de quebra)

1. **`git show d46525d` completo** ÔÇö schema, migration, index.ts, reposit├│rios, testes revisados linha a linha.
2. **Busca de call sites** (`git grep markEnriched|findUnenriched|...` no branch): **zero consumidores reais** ÔÇö s├│ o teste com fakes e exports.
3. **Verifica├º├úo de encoding byte a byte** (PowerShell, blobs do git): schema da branch = 0 bytes n├úo-ASCII; migration = 0 n├úo-ASCII e **sem BOM** (claim confirmado); as substitui├º├Áes s├úo `?` literal (LOW-1).
4. **Repro emp├¡rico com Prisma real**: projeto tempor├írio fora do repo, schema id├¬ntico ao do commit, `prisma generate` 5.22.0, `tsc --strict` ÔÇö `PRISMA_CLIENT ÔåÆ ObservedEventEnrichDelegate` reproduz **TS2322 concreto** (findMany: `{ [x: string]: never; }[]` vs `UnenrichedRow[]`). Probe separado: `data: Record<string, unknown>` em `updateMany` real compila (runtime OK; o bloqueio ├® no tipo do delegate). ÔåÆ base do HIGH-1.
5. **Contagem de testes**: 7 `it()` vs 9 declarados no commit (LOW-2).
6. **Contrato do decoder** (d016b8b `decode.ts`): confirma inputs `(programId, data, accounts)` e sem├óntica `solAmount` = **lamports de LIMITE** ÔåÆ base do MEDIUM-4.

## O QUE N├âO FOI POSS├ìVEL VERIFICAR

- **Execu├º├úo do vitest** ÔÇö m├íquina sem `node_modules`; os 7 testes n├úo foram efetivamente rodados aqui (apenas auditados estaticamente).
- **Aplica├º├úo da migration em Postgres real / drift Prisma** (`prisma migrate diff`) ÔÇö sem banco dispon├¡vel; a consist├¬ncia schemaÔåömigration foi verificada apenas por inspe├º├úo (tipos TEXT/DECIMAL(65,30)/TIMESTAMP(3)/INTEGER + DEFAULT 0 batem).
- **Fluxo completo do enricher** ÔÇö n├úo existe no branch; depende dos achados HIGH-1, MEDIUM-4 e LOW-4 para funcionar como anunciado.

---

VERDICT: REJEITADO

Motivo: achado **HIGH-1** (interface incompat├¡vel com o cliente real do Prisma, reproduzido; quebra o gate `npm run typecheck` no primeiro wiring, exatamente o pr├│ximo passo do feature) ÔÇö crit├®rio do agente: qualquer CRITICAL/HIGH ÔåÆ REJEITADO, sem exce├º├úo. Adicionais: 5 MEDIUM (├¡ndice ausente, estado terminal ausente, queima permanente de undecodable, sem├óntica `amountSol` com risco estat├¡stico, fallback que contorna o TOCTOU) e 5 LOW. Nenhum c├│digo foi modificado; nenhum commit foi criado.

