// @ts-nocheck
/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from '@solana/kit';

export const INIT_USER_VOLUME_ACCUMULATOR_EVENT_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([134, 36, 13, 72, 232, 101, 130, 216]);

export function getInitUserVolumeAccumulatorEventEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INIT_USER_VOLUME_ACCUMULATOR_EVENT_EVENT_DISCRIMINATOR,
  );
}

export type InitUserVolumeAccumulatorEventEvent = {
  payer: Address;
  user: Address;
  timestamp: bigint;
};

export type InitUserVolumeAccumulatorEventEventArgs = {
  payer: Address;
  user: Address;
  timestamp: number | bigint;
};

/** Gets the encoder for {@link InitUserVolumeAccumulatorEventEventArgs} event data. */
export function getInitUserVolumeAccumulatorEventEventEncoder(): FixedSizeEncoder<InitUserVolumeAccumulatorEventEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ['payer', getAddressEncoder()],
      ['user', getAddressEncoder()],
      ['timestamp', getI64Encoder()],
    ]),
    [getConstantEncoder(INIT_USER_VOLUME_ACCUMULATOR_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link InitUserVolumeAccumulatorEventEvent} event data. */
export function getInitUserVolumeAccumulatorEventEventDecoder(): FixedSizeDecoder<InitUserVolumeAccumulatorEventEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ['payer', getAddressDecoder()],
      ['user', getAddressDecoder()],
      ['timestamp', getI64Decoder()],
    ]),
    [getConstantDecoder(INIT_USER_VOLUME_ACCUMULATOR_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link InitUserVolumeAccumulatorEventEvent} event data. */
export function getInitUserVolumeAccumulatorEventEventCodec(): FixedSizeCodec<
  InitUserVolumeAccumulatorEventEventArgs,
  InitUserVolumeAccumulatorEventEvent
> {
  return combineCodec(
    getInitUserVolumeAccumulatorEventEventEncoder(),
    getInitUserVolumeAccumulatorEventEventDecoder(),
  );
}

/** Parses the provided bytes into {@link InitUserVolumeAccumulatorEventEvent} event data. */
export function parseInitUserVolumeAccumulatorEventEvent(
  data: ReadonlyUint8Array | Uint8Array,
): InitUserVolumeAccumulatorEventEvent {
  if (containsBytes(data, INIT_USER_VOLUME_ACCUMULATOR_EVENT_EVENT_DISCRIMINATOR, 0)) {
    return getInitUserVolumeAccumulatorEventEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "InitUserVolumeAccumulatorEventEvent" event discriminators.',
  );
}
