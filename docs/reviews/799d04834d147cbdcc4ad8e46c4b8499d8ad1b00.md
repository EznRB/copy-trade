# Review 799d04834d147cbdcc4ad8e46c4b8499d8ad1b00

- Branch: sprint/f1-5-enrichment
- Data: 2026-09-25 01:09:21
- Gates: lint=1 typecheck=1 test=1 (0 = ok)
- Veredito: **REJEITADO (gates falharam)**

## Saida do code-reviewer

opencode.exe : [0m
No C:\Users\enzo\AppData\Roaming\npm\opencode.ps1:14 caractere:3
+   & "$basedir/node_modules/opencode-ai/bin/opencode.exe"   $args
+   ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : NotSpecified: ([0m:String) [], RemoteException
    + FullyQualifiedErrorId : NativeCommandError
 
> code-reviewer ┬À big-pickle
[0m
[0m$ [0mgit show 799d04834d147cbdcc4ad8e46c4b8499d8ad1b00 --stat
commit 799d04834d147cbdcc4ad8e46c4b8499d8ad1b00
Author: EznRB <EznRB@users.noreply.github.com>
Date:   Fri Sep 25 01:08:42 2026 -0300

    fix(review): HIGH-3 - instance lock com staleness por idade (PID reusado) + testes

 services/data-ingestion/src/instance-lock.test.ts | 19 +++++++++++++++++++
 services/data-ingestion/src/instance-lock.ts      | 19 +++++++++++++++----
 2 files changed, 34 insertions(+), 4 deletions(-)
[0m
[0m$ [0mgit log --oneline -5 799d04834d147cbdcc4ad8e46c4b8499d8ad1b00; git branch --contains 
799d04834d147cbdcc4ad8e46c4b8499d8ad1b00
799d048 fix(review): HIGH-3 - instance lock com staleness por idade (PID reusado) + testes
6cd436b fix(review): MEDIUM - fixtures rigorosos campo-a-campo (direction/mint/user), divergencias listadas, 
decodificacao normaliza lado do token quando base=wSOL, relatorio sem BOM
cc64b8d fix(review): HIGH-1 testes helius-provider (per-wallet subs, base58, unsubscribe, reconnect storm) + 
anti-orfao de sockets + teto maxReconnects + HIGH-2 leak resolvido
fd6a5a6 fix(review): testes para markEnriched/markUndecodable/findUnenriched/incrementEnrichAttempt + guarda TOCTOU 
(updateMany where enrichedAt null) + schema.prisma ASCII puro
5299d25 docs(f1.5): roadmap/handoff/ADR-025 + artefatos de evidencia (fixtures + backfill 10k = 19.1%)
+ sprint/f1-5-enrichment
[0m
[0m$ [0mgit show 799d04834d147cbdcc4ad8e46c4b8499d8ad1b00
commit 799d04834d147cbdcc4ad8e46c4b8499d8ad1b00
Author: EznRB <EznRB@users.noreply.github.com>
Date:   Fri Sep 25 01:08:42 2026 -0300

    fix(review): HIGH-3 - instance lock com staleness por idade (PID reusado) + testes

diff --git a/services/data-ingestion/src/instance-lock.test.ts b/services/data-ingestion/src/instance-lock.test.ts
index cb995e9..50f13da 100644
--- a/services/data-ingestion/src/instance-lock.test.ts
+++ b/services/data-ingestion/src/instance-lock.test.ts
@@ -57,6 +57,25 @@ describe('InstanceLock', () => {
     lock.release();
   });
 
+  it('lock velho (timestamp ultrapassado) ├® tratado como stale', () => {
+    const stale = Date.now() - 25 * 3_600_000; // 25h atras
+    writeFileSync(lockPath, `${process.ppid || process.pid}\n${stale}\n`);
+    // Mesmo que o PID do lock esteja "vivo" (ppid), a idade > 24h vence => substitui.
+    const lock = new InstanceLock(lockPath);
+    // ppid vivo + recente NAO passaria aqui; velho passa.
+    try {
+      lock.acquire();
+    } catch (e) {
+      // se ppid nao existir ou lock antigo: ambos caminhos levam a aquisicao ok
+      // (caminho 2: pid vivo + velho => tambem deve substituir)
+      throw e;
+    }
+    expect(existsSync(lockPath)).toBe(true);
+    lock.release();
+  });
+
+  it('lock recente com PID vivo rejeita (nao stale)');
+
   it('lock corrompido (n├úo-n├║mero) ├® substitu├¡do', () => {
     writeFileSync(lockPath, 'garbage\n');
     const lock = new InstanceLock(lockPath);
diff --git a/services/data-ingestion/src/instance-lock.ts b/services/data-ingestion/src/instance-lock.ts
index 2004324..2d9c93a 100644
--- a/services/data-ingestion/src/instance-lock.ts
+++ b/services/data-ingestion/src/instance-lock.ts
@@ -29,15 +29,26 @@ export class InstanceLock {
 
   acquire(): void {
     if (existsSync(this.lockPath)) {
-      const raw = readFileSync(this.lockPath, 'utf8').trim();
-      const pid = Number(raw);
-      if (Number.isInteger(pid) && pid > 0 && pid !== process.pid && isProcessAlive(pid)) {
+      const lines = readFileSync(this.lockPath, 'utf8').trim().split('\n');
+      const pid = Number(lines[0]);
+      const createdAt = Number(lines[1] ?? 0);
+      // Stale por idade (review: PID reuse no Windows): lock mais velho que
+      // LOCK_STALE_MS (default 24h) ├® considerado morto mesmo se o PID existir.
+      const staleMs = Number(process.env['INGESTION_LOCK_STALE_MS'] ?? 86_400_000);
+      const isStaleByAge = createdAt > 0 && this.now() - createdAt > staleMs;
+      if (
+        !isStaleByAge &&
+        Number.isInteger(pid) &&
+        pid > 0 &&
+        pid !== process.pid &&
+        isProcessAlive(pid)
+      ) {
         throw new Error(
           `CONFIG_ERROR: j├í existe inst├óncia ativa (PID ${pid}) ÔÇö lock em ${this.lockPath}. ` +
             'Encerre a inst├óncia anterior ou remova o lock se for stale.',
         );
       }
-      // Lock stale (processo morreu sem cleanup) ÔÇö remover e seguir.
+      // Lock stale (processo morreu sem cleanup OU idade superada) ÔÇö remover e seguir.
       unlinkSync(this.lockPath);
     }
     mkdirSync(dirname(this.lockPath), { recursive: true });
[0m
[0mÔ£ù [0mRead services/data-ingestion/src/instance-lock.ts failed
[91m[1mError: [0mFile not found: C:\Users\enzo\Desktop\copytrade 2\services\data-ingestion\src\instance-lock.ts
[0mÔ£ù [0mRead services/data-ingestion/src/instance-lock.test.ts failed
[91m[1mError: [0mFile not found: C:\Users\enzo\Desktop\copytrade 2\services\data-ingestion\src\instance-lock.test.ts
Now let me read the full files to understand the complete context:
[0m
[0m$ [0mgit worktree list; git status --short | Select-Object -First 10
C:/Users/enzo/Desktop/copytrade 2                      d119914 [master]
C:/Users/enzo/Desktop/copytrade-wt-f1-5-enrichment     799d048 [sprint/f1-5-enrichment]
C:/Users/enzo/Desktop/copytrade-wt-f1-postgres-live    cbd49d4 [sprint/f1-postgres-live]
C:/Users/enzo/Desktop/copytrade-wt-f1-security-fixes   19ffacc [sprint/f1-security-fixes]
C:/Users/enzo/Desktop/copytrade-wt-f1-security-fixes-2 df98063 [sprint/f1-security-fixes-2]
[0m
[93m[1m! [0mpermission requested: external_directory 
(C:\Users\enzo\Desktop\copytrade-wt-f1-5-enrichment\services\data-ingestion\src\*); auto-rejecting
[0mÔ£ù [0mRead C:/Users/enzo/Desktop/copytrade-wt-f1-5-enrichment/services/data-ingestion/src/instance-lock.ts failed
[91m[1mError: [0mThe user rejected permission to use this specific tool call.
[93m[1m! [0mpermission requested: external_directory 
(C:\Users\enzo\Desktop\copytrade-wt-f1-5-enrichment\services\data-ingestion\src\*); auto-rejecting
[0mÔ£ù [0mRead C:/Users/enzo/Desktop/copytrade-wt-f1-5-enrichment/services/data-ingestion/src/instance-lock.test.ts 
failed
[91m[1mError: [0mThe user rejected permission to use this specific tool call.

