# Review 2ab477f7dc60361db18e2106cd4b9ef48aea977e

- Branch: sprint/f1-5-enrichment
- Data: 2026-09-24 20:42:59
- Gates: lint=1 typecheck=1 test=1 (0 = ok)
- Veredito: **REJEITADO**

## Saida do code-reviewer

opencode.exe : [0m
No C:\Users\enzo\AppData\Roaming\npm\opencode.ps1:14 caractere:3
+   & "$basedir/node_modules/opencode-ai/bin/opencode.exe"   $args
+   ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : NotSpecified: ([0m:String) [], RemoteException
    + FullyQualifiedErrorId : NativeCommandError
 
> code-reviewer ┬À big-pickle
[0m
[0m$ [0mgit show 2ab477f7dc60361db18e2106cd4b9ef48aea977e --stat
commit 2ab477f7dc60361db18e2106cd4b9ef48aea977e
Author: EznRB <EznRB@users.noreply.github.com>
Date:   Thu Sep 24 20:36:20 2026 -0300

    feat(f1.5): fixtures reais mainnet 10/10 vs Helius Enhanced (8 PumpSwap + 2 curve) + logsSubscribe 1-endereco + 
getTransaction v1

 data/locks/data-ingestion.lock               |   2 +
 f15-fixtures-report.json                     |   1 +
 packages/pumpfun/scripts/fix-esm-imports.mjs |  17 ++--
 packages/solana/src/helius-provider.ts       |  20 +++--
 packages/solana/src/rpc-provider.ts          |   6 +-
 scripts/f15-backfill.mjs                     |  69 ++++++++++++++++
 scripts/f15-fixtures.mjs                     | 117 +++++++++++++++++++++++++++
 7 files changed, 214 insertions(+), 18 deletions(-)
[0m
[0m$ [0mgit log --oneline -5 2ab477f7dc60361db18e2106cd4b9ef48aea977e
2ab477f feat(f1.5): fixtures reais mainnet 10/10 vs Helius Enhanced (8 PumpSwap + 2 curve) + logsSubscribe 1-endereco 
+ getTransaction v1
c438ef1 feat(f1.5): enricher (decode pump/pumpswap) + runner batch + wiring no boot + exports do database
dddb30b feat(f1.5): decoder pump.fun/PumpSwap via codama a partir da IDL oficial (pump-public-docs @81091419) + 
wrapper decodePumpInstruction + testes adversariais
afe614c chore: remove cols.sql acidental
88152bb feat(f1.5): schema ObservedEvent com colunas de enrichment + migration + repositories 
(markEnriched/findUnenriched)
[0m
[0m$ [0mgit show 2ab477f7dc60361db18e2106cd4b9ef48aea977e
commit 2ab477f7dc60361db18e2106cd4b9ef48aea977e
Author: EznRB <EznRB@users.noreply.github.com>
Date:   Thu Sep 24 20:36:20 2026 -0300

    feat(f1.5): fixtures reais mainnet 10/10 vs Helius Enhanced (8 PumpSwap + 2 curve) + logsSubscribe 1-endereco + 
getTransaction v1

diff --git a/data/locks/data-ingestion.lock b/data/locks/data-ingestion.lock
new file mode 100644
index 0000000..44768e4
--- /dev/null
+++ b/data/locks/data-ingestion.lock
@@ -0,0 +1,2 @@
+19816
+1790291576311
diff --git a/f15-fixtures-report.json b/f15-fixtures-report.json
new file mode 100644
index 0000000..547030a
--- /dev/null
+++ b/f15-fixtures-report.json
@@ -0,0 +1 @@
+´╗┐{  "sample": 10,  "pumpswapFixtures": 8,  "pumpFixtures": 2,  "probes": {    "decoded": 10,    "undecodable": 13,  
  "errors": 14  },  "helisMatch": 10,  "heliusMismatch": 0,  "results": [    {      "signature": 
"63ABsoyM7T47de2EsAvaVok9ndQ9o83sg1uxRNGcExPxunnWY3yDpfqqEqihpsRw6i4GG3VSAp6HJcgPynZbpoSt",      "local": {        
"kind": "pumpswap_sell",        "direction": "sell",        "mint": "Bg75GaeTN7uVqtUEsquh61hwENeaA8TD5K8HwX4Lfomo",    
    "user": "8z9FqZed5Pg9oPFdum5A9RBZvW8kk8aKxeidezfEH7mf",        "counterparty": 
"EJXEbdsXFJ9AFEu1EqNHycfAEoBN7BFtfLVWx4DsEWUG"      },      "helius": {        "type": "SWAP",        "source": 
"PUMP_AMM",        "feePayer": "8z9FqZed5Pg9oPFdum5A9RBZvW8kk8aKxeidezfEH7mf"      }    },    {      "signature": 
"4W2Nvqsf38KtXpVAnpr9VR7tznJUP9YJiYmjqjDWuppkUpszyyHZ4kCNJdmRiM6kgoDPpVCeyqaEp1si6KYPFhLb",      "local": {        
"kind": "pump_buy",        "direction": "buy",        "mint": "DDmfov3tmsddkfb3EhZKSQ6Ti4GKjRJtpDMVMtRspump",        
"user": "BwWK17cbHxwWBKZkUYvzxLcNQ1YVyaFezduWbtm2de6s",        "counterparty": 
"DQbHNJrqn288wQT5Ba1PGgF1XFbqZZeU5fUa3iJwBFH"      },      "helius": {        "type": "SWAP",        "source": 
"PUMP_FUN",        "feePayer": "Gygj9QQby4j2jryqyqBHvLP7ctv2SaANgh4sCb69BUpA"      }    },    {      "signature": 
"2zaGNZ9XC3EsCpA8SJ37zJH9mpcDdJ7wuK74ib62NYqtmsSjMGyy6kcYyAgN62FWqEQDBWy3cZsJizNmvzWPiouW",      "local": {        
"kind": "pumpswap_buy",        "direction": "buy",        "mint": "Bg75GaeTN7uVqtUEsquh61hwENeaA8TD5K8HwX4Lfomo",      
  "user": "41n8gyoVAuDjZdUeHRGrPscG2DzJawDHpa1Y6VLV4VqJ",        "counterparty": 
"EJXEbdsXFJ9AFEu1EqNHycfAEoBN7BFtfLVWx4DsEWUG"      },      "helius": {        "type": "SWAP",        "source": 
"PUMP_AMM",        "feePayer": "41n8gyoVAuDjZdUeHRGrPscG2DzJawDHpa1Y6VLV4VqJ"      }    },    {      "signature": 
"JmU7nhkH65Mhw8DAjSScQPVt5HDsL2yC1MntsabzFf9GW6ATMcVSWDCxDLtWaBagaVoGvigmmURpr8P7uXuLaLZ",      "local": {        
"kind": "pumpswap_sell",        "direction": "sell",        "mint": "Bg75GaeTN7uVqtUEsquh61hwENeaA8TD5K8HwX4Lfomo",    
    "user": "8JjVNyj65NyhCuit2n4xAPHaMJHvJsQarg23wFrhawAc",        "counterparty": 
"EJXEbdsXFJ9AFEu1EqNHycfAEoBN7BFtfLVWx4DsEWUG"      },      "helius": {        "type": "SWAP",        "source": 
"PUMP_AMM",        "feePayer": "8JjVNyj65NyhCuit2n4xAPHaMJHvJsQarg23wFrhawAc"      }    },    {      "signature": 
"3CVqDq7Rg1W8hEWNqPtAnnEaX2RQK5zyL7PHY4Up5Q7gFKtqdvrHUbfKjcGtCtWGAvCJSaj2enYMso2jJ7t5gwn4",      "local": {        
"kind": "pumpswap_buy",        "direction": "buy",        "mint": "Bg75GaeTN7uVqtUEsquh61hwENeaA8TD5K8HwX4Lfomo",      
  "user": "41n8gyoVAuDjZdUeHRGrPscG2DzJawDHpa1Y6VLV4VqJ",        "counterparty": 
"EJXEbdsXFJ9AFEu1EqNHycfAEoBN7BFtfLVWx4DsEWUG"      },      "helius": {        "type": "SWAP",        "source": 
"PUMP_AMM",        "feePayer": "41n8gyoVAuDjZdUeHRGrPscG2DzJawDHpa1Y6VLV4VqJ"      }    },    {      "signature": 
"4PH2sLQw1FMgYUvTpoRw8ximo8PL2tVbLxyfxpdjPMzXS9mBAftJDBajVpFuNT1Wx1Sc9B1cB4HpxJnu3a1LeZde",      "local": {        
"kind": "pumpswap_buy",        "direction": "buy",        "mint": "spCPNAAmhvXakcEkwEvwLnqTcqJKqbVRwv3tEscpump",       
 "user": "2VpN7GhUKVTVSfxYK57V1UNJkMhYVmUqp94jVdsuFmPW",        "counterparty": 
"J9QCk2L78vG6evcCDmmYhVD9QLnLMz2s3M6bUa2mWE6U"      },      "helius": {        "type": "SWAP",        "source": 
"PUMP_AMM",        "feePayer": "2VpN7GhUKVTVSfxYK57V1UNJkMhYVmUqp94jVdsuFmPW"      }    },    {      "signature": 
"2TjRyNUT8PLWYu45fKgVBTMXHVdE5vP432Ciy6J9dWPPmQu9nap8C5p1BymSdw7YYH1DbFF8gvzBL6KPBVnNKUmC",      "local": {        
"kind": "pumpswap_sell",        "direction": "sell",        "mint": "So11111111111111111111111111111111111111112",     
   "user": "D5UfnNFnKrUt8Hby8rdwLY5Pz3gFnZMRb7MhTA9B4Fyb",        "counterparty": 
"AjXLNqoYkVJPBYwQHujPtXmFLjWoGdNDkixjoMz8uPDZ"      },      "helius": {        "type": "SWAP",        "source": 
"PUMP_AMM",        "feePayer": "D5UfnNFnKrUt8Hby8rdwLY5Pz3gFnZMRb7MhTA9B4Fyb"      }    },    {      "signature": 
"26qtmtLSDjEdHa1e6mHsBZd8C8Eyu7vaSAaGW3fDwwsro2LpovS63WAKAwYwaRYyimLD1aacp53SAwYG91NeXyzc",      "local": {        
"kind": "pump_sell",        "direction": "sell",        "mint": "AUWfxprFcJ7tjnQzveuP5nerLZ6e2nnrBzdQBrLgpump",        
"user": "3rD9CzmLGfyaAgFbT8GM6mJi3LYoceGi2JL1EZecDhGs",        "counterparty": 
"89LxouuDZP1mG92iek4gXZFPm28iC4YcCSMZgZkpSgzW"      },      "helius": {        "type": "SWAP",        "source": 
"PUMP_FUN",        "feePayer": "3rD9CzmLGfyaAgFbT8GM6mJi3LYoceGi2JL1EZecDhGs"      }    },    {      "signature": 
"brWS5gaMspaHVnh7usT4mWdrm6AoCsiVx8YgBymK66nACKmmH8zLNJW1LwtoZxsryeK8u2S4GmXDheC8bu4vjvZ",      "local": {        
"kind": "pumpswap_sell",        "direction": "sell",        "mint": "Bg75GaeTN7uVqtUEsquh61hwENeaA8TD5K8HwX4Lfomo",    
    "user": "8JjVNyj65NyhCuit2n4xAPHaMJHvJsQarg23wFrhawAc",        "counterparty": 
"EJXEbdsXFJ9AFEu1EqNHycfAEoBN7BFtfLVWx4DsEWUG"      },      "helius": {        "type": "SWAP",        "source": 
"PUMP_AMM",        "feePayer": "8JjVNyj65NyhCuit2n4xAPHaMJHvJsQarg23wFrhawAc"      }    },    {      "signature": 
"2UJCRsDWRp1iJRVdS8urbyhLShAb7VwMmpwCsYkw3WYCLuApH7n4hKGXc9z9E9Usu4PASGqMqDwk3FvQKCMXffRj",      "local": {        
"kind": "pumpswap_sell",        "direction": "sell",        "mint": "So11111111111111111111111111111111111111112",     
   "user": "EbcTeft8Y7t5YaWgqi2BQCzNzgKckwtF3zZKHQdWxhjK",        "counterparty": 
"6CSsc8jmVUR8fk7o8jwxsQbGfocKBNueVyWycp7ReYW4"      },      "helius": {        "type": "SWAP",        "source": 
"PUMP_AMM",        "feePayer": "EbcTeft8Y7t5YaWgqi2BQCzNzgKckwtF3zZKHQdWxhjK"      }    }  ]}
\ No newline at end of file
diff --git a/packages/pumpfun/scripts/fix-esm-imports.mjs b/packages/pumpfun/scripts/fix-esm-imports.mjs
index 55488d4..764d43d 100644
--- a/packages/pumpfun/scripts/fix-esm-imports.mjs
+++ b/packages/pumpfun/scripts/fix-esm-imports.mjs
@@ -15,16 +15,19 @@ function* walk(dir) {
   }
 }
 
-const importRe = /(from|import|export)[^'"]*['"](\.{1,2}\/[^'"]+)['"]/g;
+const importRe = /(from|import|export)[^'"]*['"](\.{1,2}(?:\/[^'"]*)?)['"]/g;
+function resolveSpec(fileDir, spec) {
+  if (spec.endsWith('.js')) return spec;
+  const abs = join(fileDir, spec);
+  if (existsSync(`${abs}.js`)) return `${spec}.js`;
+  if (existsSync(join(abs, 'index.js'))) return `${spec === '.' || spec === '..' ? spec + '/' : spec + '/'}index.js`;
+  return spec;
+}
 let changed = 0;
 for (const file of walk(dist)) {
   let src = readFileSync(file, 'utf8');
-  const out = src.replace(importRe, (m, kw, spec) => {
-    if (spec.endsWith('.js')) return m;
-    const abs = join(pdirname(file), spec);
-    const fixed = existsSync(`${abs}.js`) ? `${spec}.js` : `${spec}/index.js`;
-    return m.replace(spec, fixed);
-  });
+  const fileDir = pdirname(file);
+  const out = src.replace(importRe, (m, kw, spec) => m.replace(spec, resolveSpec(fileDir, spec)));
   if (out !== src) {
     writeFileSync(file, out);
     changed++;
diff --git a/packages/solana/src/helius-provider.ts b/packages/solana/src/helius-provider.ts
index d5919db..40e395b 100644
--- a/packages/solana/src/helius-provider.ts
+++ b/packages/solana/src/helius-provider.ts
@@ -216,18 +216,24 @@ export class HeliusProvider implements BlockchainDataProvider {
     });
   }
 
-  private sendLogsSubscribe(): Promise<void> {
-    if (this.wallets.size === 0 || this.ws === null) return Promise.resolve();
+  private async sendLogsSubscribe(): Promise<void> {
+    if (this.wallets.size === 0 || this.ws === null) return;
+    // FACT (Helius, observado 2026-09-24): logsSubscribe aceita apenas 1
+    // endereco em `mentions` neste plano. Resolver: uma subscription por
+    // endereco (Promise.all; se alguma falhar, o connect falha de forma
+    // audivel e a rotina de reconnect refaz).
+    const wallets = [...this.wallets];
+    await Promise.all(wallets.map((wallet) => this.sendSingleSubscribe(wallet)));
+  }
+
+  private sendSingleSubscribe(wallet: string): Promise<void> {
     return new Promise((resolve, reject) => {
       const id = this.nextRequestId++;
       const message = JSON.stringify({
         jsonrpc: '2.0',
         id,
         method: 'logsSubscribe',
-        params: [
-          { mentions: [...this.wallets] },
-          { commitment: this.commitment },
-        ],
+        params: [{ mentions: [wallet] }, { commitment: this.commitment }],
       });
       const ws = this.ws;
       if (!ws) {
@@ -239,8 +245,6 @@ export class HeliusProvider implements BlockchainDataProvider {
           reject(new ClassifiedError('RPC_ERROR', `logsSubscribe falhou: ${err.message}`, err));
           return;
         }
-        // Confirma├º├úo chega como resposta JSON-RPC com result = subscription id;
-        // resolu├º├úo ocorre em handleMessage via pending map simplificado:
         this.pendingSubscribes.set(id, { resolve, reject });
       });
     });
diff --git a/packages/solana/src/rpc-provider.ts b/packages/solana/src/rpc-provider.ts
index 42ca1f2..5cb5334 100644
--- a/packages/solana/src/rpc-provider.ts
+++ b/packages/solana/src/rpc-provider.ts
@@ -215,13 +215,13 @@ export class RpcProvider {
   }
 
   /**
-   * getTransaction com encoding jsonParsed e maxSupportedTransactionVersion: 0
-   * (FACT: requerido para transa├º├Áes versionadas/v0 com address lookup tables).
+   * getTransaction com encoding jsonParsed e maxSupportedTransactionVersion: 1
+   * (FACT 2026-09-24: versao 1 introduzida pelo runtime; 0 insuficiente). Requerido para transa├º├Áes versionadas/v0 
com address lookup tables).
    */
   async getTransaction(signature: string): Promise<unknown | null> {
     return this.call<unknown | null>('getTransaction', [
       signature,
-      { encoding: 'jsonParsed', maxSupportedTransactionVersion: 0 },
+      { encoding: 'jsonParsed', maxSupportedTransactionVersion: 1 },
     ]);
   }
 
diff --git a/scripts/f15-backfill.mjs b/scripts/f15-backfill.mjs
new file mode 100644
index 0000000..332d479
--- /dev/null
+++ b/scripts/f15-backfill.mjs
@@ -0,0 +1,69 @@
+// scripts/f15-backfill.mjs - GATE F1.5 (backfill one-shot):
+// Processa um lote de eventos persistidos e reporta cobertura (decoded vs UNKNOWN).
+// Uso: node --env-file=.env scripts/f15-backfill.mjs [batchSize] [maxBatches]
+import { prisma } from '../packages/database/dist/index.js';
+import { RpcProvider } from '../packages/solana/dist/index.js';
+import { Enricher } from '../services/data-ingestion/dist/enricher.js';
+import { EnrichRunner } from '../services/data-ingestion/dist/enrich-runner.js';
+import {
+  markEnriched,
+  markUndecodable,
+  findUnenriched,
+  incrementEnrichAttempt,
+} from '../packages/database/dist/index.js';
+
+const log = { info: console.error, warn: console.error, error: console.error }; // stderr (stdout = relatorio)
+
+const rpc = new RpcProvider(
+  [{ name: 'primary', url: process.env.SOLANA_RPC_HTTP, tier: 'primary' }],
+  { logger: log },
+);
+
+const enricher = new Enricher(
+  { getTransaction: (sig) => rpc.getTransaction(sig) },
+  {
+    bumpAttempt: (id) => incrementEnrichAttempt({ observedEvent: prisma.observedEvent }, id),
+    markEnriched: (id, d) =>
+      markEnriched({ observedEvent: prisma.observedEvent }, {
+        id,
+        direction: d.direction,
+        amountSol: d.solAmount,
+        tokenAmount: d.tokenAmount,
+        mint: d.mint,
+        counterparty: d.counterparty,
+        enrichSource: `pump-decoder-v1:${d.kind}`,
+      }),
+    markUndecodable: (id, reason) =>
+      markUndecodable({ observedEvent: prisma.observedEvent }, id, reason),
+  },
+  log,
+);
+
+const batchSize = Number(process.argv[2] ?? 50);
+const maxBatches = Number(process.argv[3] ?? 200); // default: ate 10k eventos
+const runner = new EnrichRunner(
+  {
+    findUnenriched: (limit, maxAttempts) =>
+      findUnenriched({ observedEvent: prisma.observedEvent }, { limit, maxAttempts }),
+  },
+  enricher,
+  { batchSize, maxAttempts: 5, sleepMs: 120 },
+);
+
+const started = Date.now();
+const stats = await runner.runUntilEmpty(maxBatches);
+const elapsedSec = ((Date.now() - started) / 1000).toFixed(1);
+
+const bySource = await prisma.observedEvent.groupBy({
+  by: ['enrichSource'],
+  _count: { _all: true },
+});
+const coverage = {
+  ...stats,
+  elapsedSec,
+  coveragePct:
+    stats.seen > 0 ? ((stats.enriched / stats.seen) * 100).toFixed(1) + '%' : 'n/a',
+  enrichSourceBreakdown: bySource,
+};
+process.stdout.write(JSON.stringify(coverage, null, 2) + '\n');
+await prisma.$disconnect();
diff --git a/scripts/f15-fixtures.mjs b/scripts/f15-fixtures.mjs
new file mode 100644
index 0000000..b33d751
--- /dev/null
+++ b/scripts/f15-fixtures.mjs
@@ -0,0 +1,117 @@
+// scripts/f15-fixtures.mjs - GATE F1.5 (fixtures):
+// Amostra transacoes reais do ObservedEvent, decodifica localmente e compara
+// 1:1 com a Enhanced API da Helius (apenas eventos decodificados; o resto e
+// contado como UNKNOWN). Uso: node --env-file=.env scripts/f15-fixtures.mjs [n]
+import { prisma } from '../packages/database/dist/index.js';
+import { decodePumpInstruction } from '../packages/pumpfun/dist/index.js';
+import { collectInstructionCalls } from '../services/data-ingestion/dist/enricher.js';
+
+const N = Number(process.argv[2] ?? 10);
+const TARGET_AMM = 2;
+const MAX_TRIES = 300;
+const HELIUS_KEY = process.env.HELIUS_API_KEY;
+const RPC = process.env.SOLANA_RPC_HTTP;
+if (!HELIUS_KEY || !RPC) throw new Error('env incompleto (HELIUS_API_KEY/SOLANA_RPC_HTTP)');
+
+async function heliusEnhanced(signature) {
+  const res = await fetch(`https://api-mainnet.helius-rpc.com/v0/transactions?api-key=${HELIUS_KEY}`, {
+    method: 'POST',
+    headers: { 'content-type': 'application/json' },
+    body: JSON.stringify({ transactions: [signature] }),
+  });
+  if (!res.ok) throw new Error(`Enhanced API HTTP ${res.status}`);
+  const arr = await res.json();
+  return arr[0] ?? null;
+}
+
+async function rpcGetTransaction(signature) {
+  const res = await fetch(RPC, {
+    method: 'POST',
+    headers: { 'content-type': 'application/json' },
+    body: JSON.stringify({
+      jsonrpc: '2.0',
+      id: 1,
+      method: 'getTransaction',
+      params: [signature, { encoding: 'jsonParsed', maxSupportedTransactionVersion: 1 }],
+    }),
+  });
+  const j = await res.json();
+  return j.result ?? null;
+}
+
+const bigRows = await prisma.$queryRawUnsafe(
+  `SELECT signature FROM "ObservedEvent" WHERE "receivedAt" > now() - interval '30 minutes' ORDER BY random() LIMIT 
${MAX_TRIES}`,
+);
+
+const results = [];
+const probes = { decoded: 0, undecodable: 0, errors: 0 };
+let pumpOk = 0,
+  ammOk = 0,
+  i = 0;
+
+while (i < bigRows.length && (results.length < N || ammOk < TARGET_AMM)) {
+  const { signature } = bigRows[i++];
+  try {
+    const [tx, enhanced] = await Promise.all([
+      rpcGetTransaction(signature),
+      heliusEnhanced(signature),
+    ]);
+    let local = null;
+    for (const c of collectInstructionCalls(tx)) {
+      const d = decodePumpInstruction(c.programId, c.data, c.accounts);
+      if (d) {
+        local = d;
+        break;
+      }
+    }
+    if (!local) {
+      probes.undecodable++;
+      await new Promise((r) => setTimeout(r, 150));
+      continue;
+    }
+    probes.decoded++;
+    if (local.kind.startsWith('pumpswap')) ammOk++;
+    else pumpOk++;
+    results.push({
+      signature,
+      local: {
+        kind: local.kind,
+        direction: local.direction,
+        mint: local.mint,
+        user: local.user,
+        counterparty: local.counterparty,
+      },
+      helius: enhanced
+        ? {
+            type: enhanced.type ?? null,
+            source: enhanced.source ?? null,
+            feePayer: enhanced.feePayer ?? null,
+          }
+        : null,
+    });
+    await new Promise((r) => setTimeout(r, 300));
+  } catch (e) {
+    probes.errors++;
+  }
+}
+
+let match = 0,
+  mismatch = 0;
+for (const r of results) {
+  if (!r.helius) continue;
+  const heliusSwap = r.helius.type === 'SWAP' || r.helius.type === 'SWAP_PARTIAL';
+  if (heliusSwap) match++;
+  else mismatch++;
+}
+
+const report = {
+  sample: results.length,
+  pumpswapFixtures: ammOk,
+  pumpFixtures: pumpOk,
+  probes,
+  helisMatch: match,
+  heliusMismatch: mismatch,
+  results,
+};
+process.stdout.write(JSON.stringify(report, null, 2) + '\n');
+await prisma.$disconnect();
[0m
[0mÔåÆ [0mRead packages/solana/src/helius-provider.ts
[0m
[0m$ [0mgit branch --show-current; git status --short | Select-Object -First 20
master
 M docs/security/red-team/FINDINGS.md
 M docs/security/red-team/SPRINTS.md
 M docs/security/red-team/poc/sprint2-ingestion.poc.test.ts
[0m
[0mÔåÆ [0mRead packages/solana/src/rpc-provider.ts
[0m
[0m$ [0mGet-ChildItem -Recurse -Path services\data-ingestion\src -File | Select-Object FullName
FullName                                                                            
--------                                                                            
C:\Users\enzo\Desktop\copytrade 2\services\data-ingestion\src\dedup-store.ts        
C:\Users\enzo\Desktop\copytrade 2\services\data-ingestion\src\dedup.test.ts         
C:\Users\enzo\Desktop\copytrade 2\services\data-ingestion\src\factory.ts            
C:\Users\enzo\Desktop\copytrade 2\services\data-ingestion\src\index.ts              
C:\Users\enzo\Desktop\copytrade 2\services\data-ingestion\src\main.ts               
C:\Users\enzo\Desktop\copytrade 2\services\data-ingestion\src\normalizer.test.ts    
C:\Users\enzo\Desktop\copytrade 2\services\data-ingestion\src\normalizer.ts         
C:\Users\enzo\Desktop\copytrade 2\services\data-ingestion\src\pipeline.ts           
C:\Users\enzo\Desktop\copytrade 2\services\data-ingestion\src\schemas.ts            
C:\Users\enzo\Desktop\copytrade 2\services\data-ingestion\src\security-fixes.test.ts
[0m
[0m$ [0mgit show 2ab477f:packages/solana/src/helius-provider.ts | Select-String -Pattern 
"subscription|sendLogsSubscribe|sendSingleSubscribe|subscriptionId|mentions" -Context 2
* FACT (documenta├º├úo Helius Enhanced Websockets): endpoint
   *   wss://mainnet.helius-rpc.com/?api-key=<HELIUS_API_KEY>
>  * aceita os mesmos m├®todos de subscription do WSS padr├úo da Solana
>  * (logsSubscribe / logsUnsubscribe), com filtro `mentions` por endere├ºo.
   * Fallback: sem API key, conecta no WSS padr├úo da Solana
   * (wss://api.mainnet-beta.solana.com) ÔÇö o formato das mensagens ├® o mesmo
   *
   * Features:
>  * - logsSubscribe com { mentions: wallets } e commitment configur├ível.
   * - Reconnect com backoff exponencial + jitter (cap 30s).
   * - Heartbeat: ping a cada 30s; se nenhuma mensagem por 60s ÔåÆ reconnect for├ºado.
        value?: LogsNotificationValue;
      };
>     subscription?: unknown;
    };
    id?: number | string;
  
    private ws: WebSocket | null = null;
>   private subscriptionId: number | null = null;
    private nextRequestId = 1;
    private reconnectBackoff: ExponentialBackoff;
      }
      if (this.connected) {
>       await this.sendLogsSubscribe();
      }
    }
      this.ws = null;
      this.connected = false;
>     this.subscriptionId = null;
      if (ws) {
        await new Promise<void>((resolve) => {
          this.startHeartbeat();
          this.logger.info('wss conectado', { urlHost: safeHost(this.wsUrl) });
>         void this.sendLogsSubscribe()
            .then(() => resolve())
            .catch((err: unknown) => reject(err));
        ws.on('close', (code: number, reason: Buffer) => {
          this.connected = false;
>         this.subscriptionId = null;
          this.clearTimers();
          this.logger.warn('wss fechado', { code, reason: reason.toString() });
    }
  
>   private async sendLogsSubscribe(): Promise<void> {
      if (this.wallets.size === 0 || this.ws === null) return;
      // FACT (Helius, observado 2026-09-24): logsSubscribe aceita apenas 1
>     // endereco em `mentions` neste plano. Resolver: uma subscription por
      // endereco (Promise.all; se alguma falhar, o connect falha de forma
      // audivel e a rotina de reconnect refaz).
      const wallets = [...this.wallets];
>     await Promise.all(wallets.map((wallet) => this.sendSingleSubscribe(wallet)));
    }
  
>   private sendSingleSubscribe(wallet: string): Promise<void> {
      return new Promise((resolve, reject) => {
        const id = this.nextRequestId++;
          id,
          method: 'logsSubscribe',
>         params: [{ mentions: [wallet] }, { commitment: this.commitment }],
        });
        const ws = this.ws;
          );
        } else {
>         if (typeof parsed.result === 'number') this.subscriptionId = parsed.result;
          this.logger.info('logsSubscribe ativo', {
>           subscriptionId: this.subscriptionId,
            wallets: this.wallets.size,
          });
[0m
I'm on `master`; the commit under review is on `sprint/f1-5-enrichment`. I need to review the code as it exists AT that commit, not the master working tree.
[0m$ [0mgit ls-tree -r --name-only 2ab477f | Select-String -Pattern "enricher|enrich-runner|data-ingestion|lock"
data/locks/data-ingestion.lock
package-lock.json
packages/database/prisma/migrations/migration_lock.toml
services/data-ingestion/README.md
services/data-ingestion/package.json
services/data-ingestion/src/dedup-store.ts
services/data-ingestion/src/dedup.test.ts
services/data-ingestion/src/enrich-runner.ts
services/data-ingestion/src/enricher.test.ts
services/data-ingestion/src/enricher.ts
services/data-ingestion/src/factory.ts
services/data-ingestion/src/index.ts
services/data-ingestion/src/instance-lock.test.ts
services/data-ingestion/src/instance-lock.ts
services/data-ingestion/src/main.ts
services/data-ingestion/src/normalizer.test.ts
services/data-ingestion/src/normalizer.ts
services/data-ingestion/src/pipeline.ts
services/data-ingestion/src/retention.test.ts
services/data-ingestion/src/retention.ts
services/data-ingestion/src/schemas.ts
services/data-ingestion/src/security-fixes.test.ts
services/data-ingestion/tsconfig.json
[0m
[0m$ [0mgit show 2ab477f:services/data-ingestion/src/instance-lock.ts
/**
 * InstanceLock ÔÇö garante inst├óncia ├║nica do servi├ºo (ADR-024, d├®bito F1).
 *
 * Dois processos de ingest├úo rodando simultaneamente (como ocorreu no smoke
 * da F1) duplicam trabalho e press├úo no provider/DB. Implementa├º├úo: lock file
 * com PID; na inicializa├º├úo, se o lock existir E o PID estiver vivo ÔåÆ abortar
 * boot (falha fechada, CONFIG_ERROR). PID morto ÔåÆ lock stale, removido.
 */
import { existsSync, readFileSync, writeFileSync, unlinkSync, mkdirSync } from 'node:fs';
import { dirname } from 'node:path';

export function isProcessAlive(pid: number): boolean {
  try {
    process.kill(pid, 0);
    return true;
  } catch (err) {
    // EPERM = processo existe mas sem permiss├úo de sinal ÔÇö considerar vivo.
    return (err as NodeJS.ErrnoException).code === 'EPERM';
  }
}

export class InstanceLock {
  private acquired = false;

  constructor(
    private readonly lockPath: string,
    private readonly now: () => number = Date.now,
  ) {}

  acquire(): void {
    if (existsSync(this.lockPath)) {
      const raw = readFileSync(this.lockPath, 'utf8').trim();
      const pid = Number(raw);
      if (Number.isInteger(pid) && pid > 0 && pid !== process.pid && isProcessAlive(pid)) {
        throw new Error(
          `CONFIG_ERROR: j├í existe inst├óncia ativa (PID ${pid}) ÔÇö lock em ${this.lockPath}. ` +
            'Encerre a inst├óncia anterior ou remova o lock se for stale.',
        );
      }
      // Lock stale (processo morreu sem cleanup) ÔÇö remover e seguir.
      unlinkSync(this.lockPath);
    }
    mkdirSync(dirname(this.lockPath), { recursive: true });
    // Conte├║do: PID + timestamp (auditoria).
    writeFileSync(this.lockPath, `${process.pid}\n${this.now()}\n`, { flag: 'wx' });
    this.acquired = true;
  }

  release(): void {
    if (!this.acquired) return;
    this.acquired = false;
    try {
      const raw = readFileSync(this.lockPath, 'utf8').trim().split('\n')[0];
      // S├│ remove se o lock ainda ├® nosso (evita apagar lock de outra inst├óncia).
      if (raw === String(process.pid) && existsSync(this.lockPath)) unlinkSync(this.lockPath);
    } catch {
      // Lock file perdido/corrompido n├úo deve impedir shutdown.
    }
  }
}
[0m
[0m$ [0mgit show 2ab477f:packages/solana/src/helius-provider.ts | Select-Object -Skip 215 -First 120
});
  }

  private async sendLogsSubscribe(): Promise<void> {
    if (this.wallets.size === 0 || this.ws === null) return;
    // FACT (Helius, observado 2026-09-24): logsSubscribe aceita apenas 1
    // endereco em `mentions` neste plano. Resolver: uma subscription por
    // endereco (Promise.all; se alguma falhar, o connect falha de forma
    // audivel e a rotina de reconnect refaz).
    const wallets = [...this.wallets];
    await Promise.all(wallets.map((wallet) => this.sendSingleSubscribe(wallet)));
  }

  private sendSingleSubscribe(wallet: string): Promise<void> {
    return new Promise((resolve, reject) => {
      const id = this.nextRequestId++;
      const message = JSON.stringify({
        jsonrpc: '2.0',
        id,
        method: 'logsSubscribe',
        params: [{ mentions: [wallet] }, { commitment: this.commitment }],
      });
      const ws = this.ws;
      if (!ws) {
        resolve();
        return;
      }
      ws.send(message, (err?: Error) => {
        if (err) {
          reject(new ClassifiedError('RPC_ERROR', `logsSubscribe falhou: ${err.message}`, err));
          return;
        }
        this.pendingSubscribes.set(id, { resolve, reject });
      });
    });
  }

  private readonly pendingSubscribes = new Map<
    number,
    { resolve: () => void; reject: (e: Error) => void }
  >();

  private handleMessage(data: unknown): void {
    this.lastMessageAt = Date.now();
    let text: string;
    if (typeof data === 'string') {
      text = data;
    } else if (Buffer.isBuffer(data)) {
      text = data.toString('utf8');
    } else if (Array.isArray(data)) {
      text = Buffer.concat(data).toString('utf8');
    } else {
      this.logger.warn('wss mensagem de tipo inesperado descartada');
      return;
    }

    let parsed: JsonRpcNotification;
    try {
      parsed = JSON.parse(text) as JsonRpcNotification;
    } catch {
      this.logger.warn('wss mensagem n├úo-JSON descartada');
      return;
    }

    // Resposta a logsSubscribe
    if (parsed.id !== undefined && this.pendingSubscribes.has(Number(parsed.id))) {
      const pending = this.pendingSubscribes.get(Number(parsed.id));
      this.pendingSubscribes.delete(Number(parsed.id));
      if (parsed.error) {
        pending?.reject(
          new ClassifiedError(
            'RPC_ERROR',
            `logsSubscribe rejeitado: ${parsed.error.message}`,
          ),
        );
      } else {
        if (typeof parsed.result === 'number') this.subscriptionId = parsed.result;
        this.logger.info('logsSubscribe ativo', {
          subscriptionId: this.subscriptionId,
          wallets: this.wallets.size,
        });
        pending?.resolve();
      }
      return;
    }

    // Notifica├º├úo de logs
    if (parsed.method === 'logsNotification') {
      const slot = Number(parsed.params?.result?.context?.slot ?? 0);
      const value = parsed.params?.result?.value;
      const signature = typeof value?.signature === 'string' ? value.signature : null;
      if (signature === null) {
        // Dados hostis/malformados: nunca inferir ÔÇö m├®trica/log e descarte.
        this.logger.warn('logsNotification malformada descartada', { slot });
        return;
      }
      const notification: RawTransactionNotification = {
        signature,
        slot: Number.isFinite(slot) ? slot : 0,
        value,
      };
      const observed: ObservedChainEvent = {
        signature,
        instructionIndex: 0,
        wallet: '',
        slot: BigInt(notification.slot),
        payload: value,
      };
      for (const cb of this.eventListeners) {
        try {
          cb(observed);
        } catch (err) {
          this.logger.error('listener onEvent lan├ºou exce├º├úo', {
            error: err instanceof Error ? err.message : String(err),
          });
        }
      }
      for (const cb of this.rawListeners) {
        try {
          cb(notification);
[0m
[0m$ [0mgit show 2ab477f:services/data-ingestion/src/enricher.ts
/**
 * enricher.ts ÔÇö F1.5: transforma ObservedEvent crus (UNKNOWN) em eventos com
 * dire├º├úo/valores via decode da transa├º├úo completa (getTransaction), usando o
 * decoder oficial pump.fun/PumpSwap de @ct/pumpfun (IDL oficial + codama).
 *
 * Regras inviol├íveis:
 * - Sem eventType real decodificado ÔåÆ UNKNOWN expl├¡cito (markUndecodable).
 * - Amounts vindos da INSTRU├ç├âO s├úo par├ómetros/limites, n├úo realizados ÔÇö
 *   documentado em decode.ts. O pipeline F1.5 persiste o que decode fornece
 *   e marca enrichSource='pump-decoder-v1' (rastre├ível; revog├ível).
 */
import { decodePumpInstruction, type DecodedSwap } from '@ct/pumpfun';

/** Shape m├¡nimo do getTransaction(jsonParsed) que usamos. */
export interface ParsedTx {
  meta?: {
    innerInstructions?: Array<{
      index: number;
      instructions: ParsedInstruction[];
    }> | null;
  } | null;
  transaction?: {
    message?: {
      accountKeys?: Array<{ pubkey?: string } | string> | null;
      instructions?: ParsedInstruction[] | null;
    } | null;
  } | null;
}

export interface ParsedInstruction {
  programId?: string;
  /** jsonParsed "partially decoded": accounts ├® indireto pela message. */
  accounts?: string[]; // j├í resolvidas para endere├ºos (eta)
  data?: string; // base58
  /** jsonParsed fully parsed */
  parsed?: unknown;
}

export interface TxFetcher {
  getTransaction(signature: string): Promise<unknown>;
}

export interface EnrichStore {
  markEnriched(id: string, d: DecodedSwap): Promise<void>;
  markUndecodable(id: string, reason: string): Promise<void>;
  bumpAttempt(id: string): Promise<void>;
}

/** Converte base58 ÔåÆ Uint8Array (alphabet Bitcoin/Solana, sem libs externas). */
export function base58ToBytes(s: string): Uint8Array | null {
  if (!/^[1-9A-HJ-NP-Za-km-z]+$/.test(s)) return null;
  const map = new Map(
    '123456789ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz'
      .split('')
      .map((c, i) => [c, BigInt(i)] as const),
  );
  let num = 0n;
  for (const ch of s) {
    const v = map.get(ch);
    if (v === undefined) return null;
    num = num * 58n + v;
  }
  const bytes: number[] = [];
  let n = num;
  if (n === 0n) bytes.push(0);
  while (n > 0n) {
    bytes.unshift(Number(n % 256n));
    n /= 256n;
  }
  // Preserva 0s ├á esquerda ('1')
  for (const ch of s) {
    if (ch === '1') bytes.unshift(0);
    else break;
  }
  return Uint8Array.from(bytes.reverse().reverse());
}

/**
 * Resolve o array de pubkeys da message para endere├ºos string, combinando
 * instru├º├Áes externas e internas com ├¡ndices de conta. Retorna lista de
 * { programId, accountKeys }, ou null se a tx estiver inutiliz├ível.
 */
export function collectInstructionCalls(
  tx: ParsedTx,
): Array<{ programId: string; data: Uint8Array; accounts: string[] }> {
  const keys = tx?.transaction?.message?.accountKeys;
  if (!Array.isArray(keys)) return [];
  const keyAt = (i: number): string | null => {
    const k = keys[i];
    if (typeof k === 'string') return k;
    return k?.pubkey ?? null;
  };

  const calls: Array<{ programId: string; data: Uint8Array; accounts: string[] }> = [];
  const push = (ix: ParsedInstruction & { programIdIndex?: number; accountKeys?: number[] }) => {
    // jsonParsed: programId j├í string; "partially decoded" traz programIdIndex + data base58 + accounts (indices)
    const programId = ix.programId ?? (ix.programIdIndex != null ? keyAt(ix.programIdIndex) : null);
    if (!programId) return;
    const accounts = Array.isArray(ix.accounts)
      ? (ix.accounts as unknown[]).map((a) =>
          typeof a === 'string' ? a : typeof a === 'number' ? keyAt(a) : null,
        )
      : null;
    if (!accounts || accounts.some((a) => a === null)) return;
    const dataB58 = typeof ix.data === 'string' ? ix.data : null;
    if (!dataB58) return;
    const bytes = base58ToBytes(dataB58);
    if (!bytes) return;
    calls.push({ programId, data: bytes, accounts: accounts as string[] });
  };

  for (const ix of tx.transaction?.message?.instructions ?? []) push(ix);
  for (const inner of tx.meta?.innerInstructions ?? []) {
    for (const ix of inner?.instructions ?? []) push(ix);
  }
  return calls;
}

export class Enricher {
  private running = 0;

  constructor(
    private readonly fetcher: TxFetcher,
    private readonly store: EnrichStore,
    private readonly logger: {
      info(m: string, f?: Record<string, unknown>): void;
      warn(m: string, f?: Record<string, unknown>): void;
      error(m: string, f?: Record<string, unknown>): void;
    },
    private readonly maxAttempts = 5,
  ) {}

  /** Enriquece um evento por signature. Idempotente por coluna (update por id). */
  async enrich(id: string, signature: string): Promise<'enriched' | 'undecodable' | 'retry'> {
    this.running++;
    try {
      await this.store.bumpAttempt(id);
      const rawTx = await this.fetcher.getTransaction(signature);
      const calls = collectInstructionCalls(rawTx as ParsedTx);
      let decoded: DecodedSwap | null = null;
      for (const c of calls) {
        decoded = decodePumpInstruction(c.programId, c.data, c.accounts);
        if (decoded) break;
      }
      if (!decoded) {
        await this.store.markUndecodable(id, 'no-decodable-instruction');
        return 'undecodable';
      }
      await this.store.markEnriched(id, decoded);
      return 'enriched';
    } catch (err) {
      // Erro transit├│rio de RPC: retry at├® maxAttempts (sem marca├º├úo definitiva).
      this.logger.warn('enrich falhou (retry se attempts < max)', {
        errorClass: 'RPC_ERROR',
        id,
        error: err instanceof Error ? err.message : String(err),
      });
      return 'retry';
    } finally {
      this.running--;
    }
  }
}

export { type DecodedSwap };
[0m
[0m$ [0mgit show 2ab477f:services/data-ingestion/src/enrich-runner.ts
/**
 * enrich-runner.ts ÔÇö loop/worker de enrichment F1.5 (usado pelo servi├ºo e
 * pelo script one-shot de backfill). Batch de eventos n├úo enriquecidos,
 * rate-limit configur├ível, sem Throw no loop.
 */
import type { Enricher } from './enricher.js';

export interface UnenrichedStore {
  findUnenriched(limit: number, maxAttempts: number): Promise<
    Array<{ id: string; signature: string }>
  >;
}

export interface RunnerStats {
  seen: number;
  enriched: number;
  undecodable: number;
  retried: number;
}

export class EnrichRunner {
  readonly stats: RunnerStats = { seen: 0, enriched: 0, undecodable: 0, retried: 0 };
  private stopFlag = false;

  constructor(
    private readonly store: UnenrichedStore,
    private readonly enricher: Enricher,
    private readonly opts: { batchSize: number; maxAttempts: number; sleepMs: number },
  ) {}

  stop(): void {
    this.stopFlag = true;
  }

  /** Uma passada completa at├® esgotar a fila ou stopFlag. */
  async runUntilEmpty(maxBatches: number = Number.MAX_SAFE_INTEGER): Promise<RunnerStats> {
    for (let b = 0; b < maxBatches && !this.stopFlag; b++) {
      const batch = await this.store.findUnenriched(this.opts.batchSize, this.opts.maxAttempts);
      if (batch.length === 0) break;
      for (const row of batch) {
        if (this.stopFlag) break;
        const r = await this.enricher.enrich(row.id, row.signature);
        this.stats.seen++;
        if (r === 'enriched') this.stats.enriched++;
        else if (r === 'undecodable') this.stats.undecodable++;
        else this.stats.retried++;
        if (this.opts.sleepMs > 0) await new Promise((r2) => setTimeout(r2, this.opts.sleepMs));
      }
    }
    return this.stats;
  }
}
[0m
[0m$ [0mgit ls-tree -r --name-only 2ab477f packages/database/src | Select-String -Pattern "enrich|observed"
packages/database/src/repositories/observed-events.test.ts
packages/database/src/repositories/observed-events.ts
[0m
[0m$ [0mgit show 2ab477f:packages/database/src/repositories/enrichment-status.ts 2>$null; git ls-tree -r --name-only 
2ab477f packages/database/src
packages/database/src/index.ts
packages/database/src/repositories/audit-log.test.ts
packages/database/src/repositories/audit-log.ts
packages/database/src/repositories/errors.ts
packages/database/src/repositories/observed-events.test.ts
packages/database/src/repositories/observed-events.ts
packages/database/src/repositories/system-events.test.ts
packages/database/src/repositories/system-events.ts
[0m
[0m$ [0mgit show 2ab477f:packages/database/src/repositories/observed-events.ts
/**
 * Repository de ObservedEvent ÔÇö dedup de eventos on-chain (AGENTS.md regra 6).
 * Chave de idempot├¬ncia: signature + instructionIndex + wallet
 * (@@unique no schema ÔåÆ constraint ObservedEvent_signature_instructionIndex_wallet_key).
 *
 * O client ├® injetado (nunca importa o singleton) para permitir fakes em testes.
 */
import { isUniqueViolation, RepositoryError } from './errors.js';

export interface ObservedEventInsertData {
  signature: string;
  instructionIndex: number;
  wallet: string;
  eventType: string;
  slot?: bigint | null;
  blockTime?: Date | null;
  payload?: unknown;
}

export interface ObservedEventDelegate {
  create(args: { data: ObservedEventInsertData }): Promise<unknown>;
  findUnique(args: {
    where: {
      signature_instructionIndex_wallet: {
        signature: string;
        instructionIndex: number;
        wallet: string;
      };
    };
    select?: { id: true };
  }): Promise<{ id: string } | null>;
}

export type InsertResult = 'inserted' | 'duplicate';

/**
 * Insere um evento observado. Idempotente:
 * - unique violation (P2002) na chave de dedup ÔåÆ 'duplicate' (n├úo ├® erro);
 * - qualquer outro erro ÔåÆ RepositoryError(DATABASE_ERROR).
 */
export async function insertObservedEvent(
  client: { observedEvent: ObservedEventDelegate },
  data: ObservedEventInsertData,
): Promise<InsertResult> {
  try {
    await client.observedEvent.create({ data });
    return 'inserted';
  } catch (err) {
    if (isUniqueViolation(err)) {
      return 'duplicate';
    }
    throw new RepositoryError(
      'DATABASE_ERROR',
      `Falha ao inserir ObservedEvent (${data.signature}:${data.instructionIndex}:${data.wallet})`,
      err,
    );
  }
}

/** Verifica exist├¬ncia pela chave de dedup, sem lan├ºar. */
export async function existsObservedEvent(
  client: { observedEvent: ObservedEventDelegate },
  key: { signature: string; instructionIndex: number; wallet: string },
): Promise<boolean> {
  try {
    const found = await client.observedEvent.findUnique({
      where: { signature_instructionIndex_wallet: key },
      select: { id: true },
    });
    return found !== null;
  } catch (err) {
    throw new RepositoryError(
      'DATABASE_ERROR',
      `Falha ao consultar ObservedEvent (${key.signature}:${key.instructionIndex}:${key.wallet})`,
      err,
    );
  }
}

// ---------------------------------------------------------------------------
// F1.5 ÔÇö Enrichment (decoder pump.fun/PumpSwap)
// ---------------------------------------------------------------------------

export interface EnrichmentData {
  id: string;
  direction: 'buy' | 'sell';
  amountSol: string | number | null;
  tokenAmount: string | number | null;
  mint: string;
  counterparty: string;
  enrichSource: string;
}

export interface UnenrichedRow {
  id: string;
  signature: string;
  instructionIndex: number;
  wallet: string;
}

export interface ObservedEventEnrichDelegate {
  update(args: { where: { id: string }; data: Record<string, unknown> }): Promise<unknown>;
  findMany(args: {
    where: Record<string, unknown>;
    orderBy: Record<string, unknown>;
    take: number;
    select: Record<string, boolean>;
  }): Promise<UnenrichedRow[]>;
  updateMany?(args: { where: Record<string, unknown>; data: Record<string, unknown> }): Promise<unknown>;
}

/** Marca um evento como enriquecido com os campos decodificados. */
export async function markEnriched(
  client: { observedEvent: ObservedEventEnrichDelegate },
  data: EnrichmentData,
): Promise<void> {
  try {
    await client.observedEvent.update({
      where: { id: data.id },
      data: {
        direction: data.direction,
        amountSol: data.amountSol,
        tokenAmount: data.tokenAmount,
        mint: data.mint,
        counterparty: data.counterparty,
        enrichSource: data.enrichSource,
        enrichedAt: new Date(),
      },
    });
  } catch (err) {
    throw new RepositoryError('DATABASE_ERROR', `Falha ao enriquecer ObservedEvent ${data.id}`, err);
  }
}

/** Marca o evento como processado sem decodificar (UNKNOWN expl├¡cito). */
export async function markUndecodable(
  client: { observedEvent: ObservedEventEnrichDelegate },
  id: string,
  reason: string,
): Promise<void> {
  try {
    await client.observedEvent.update({
      where: { id },
      data: { enrichSource: `undecodable:${reason}`.slice(0, 128), enrichedAt: new Date() },
    });
  } catch (err) {
    throw new RepositoryError('DATABASE_ERROR', `Falha ao marcar undecodable ${id}`, err);
  }
}

/** Busca lote de eventos ainda n├úo enriquecidos para processar (backfill/enricher). */
export async function findUnenriched(
  client: { observedEvent: ObservedEventEnrichDelegate },
  opts: { limit: number; maxAttempts: number },
): Promise<UnenrichedRow[]> {
  try {
    return await client.observedEvent.findMany({
      where: { enrichedAt: null, enrichAttempts: { lt: opts.maxAttempts } },
      orderBy: { receivedAt: 'asc' },
      take: opts.limit,
      select: { id: true, signature: true, instructionIndex: true, wallet: true },
    });
  } catch (err) {
    throw new RepositoryError('DATABASE_ERROR', 'Falha ao buscar eventos n├úo enriquecidos', err);
  }
}

/** Incrementa o contador de tentativas de enrichment (evita retry infinito). */
export async function incrementEnrichAttempt(
  client: { observedEvent: ObservedEventEnrichDelegate },
  id: string,
): Promise<void> {
  if (!client.observedEvent.updateMany) {
    throw new RepositoryError('DATABASE_ERROR', 'delegate sem updateMany', new Error('unsupported'));
  }
  try {
    await client.observedEvent.updateMany({
      where: { id },
      data: { enrichAttempts: { increment: 1 } },
    });
  } catch (err) {
    throw new RepositoryError('DATABASE_ERROR', `Falha ao incrementar enrichAttempts ${id}`, err);
  }
}
[0m
[0m$ [0mgit show 2ab477f:packages/database/prisma/schema.prisma | Select-String -Pattern "model ObservedEvent" 
-Context 0,40
> model ObservedEvent {
    id               String    @id @default(cuid())
    signature        String
    instructionIndex Int
    wallet           String
    eventType        String
    slot             BigInt?
    blockTime        DateTime?
    payload          Json?
    receivedAt       DateTime  @default(now())
  
    // Enrichment F1.5 (decoder pump.fun/PumpSwap) ÔÇö tudo nullable:
    // eventos antigos e nao decodificaveis ficam null/UNKNOWN, nunca inferidos.
    direction      String? // 'buy' | 'sell'
    amountSol      Decimal?
    tokenAmount    Decimal?
    mint           String?
    counterparty   String? // bonding curve (µ│Á curve) ou pool PumpSwap
    enrichSource   String? // ex.: 'pump-decoder-v1'
    enrichedAt     DateTime?
    enrichAttempts Int      @default(0)
  
    // Chave de dedup (idempot´┐¢ncia - AGENTS.md regra 6)
    @@unique([signature, instructionIndex, wallet])
    @@index([mint])
    @@index([eventType])
  }
  
  // ---------------------------------------------------------------------------
  // Signals (Fases 4ÔÇô7)
  // ---------------------------------------------------------------------------
  
  model Signal {
    id              String   @id @default(cuid())
    correlationId   String
    strategyId      String
    strategyVersion String
    mint            String
    direction       String // "buy" | "sell"
    score           Float?
    status          String   @default("pending") // pending | approved | vetoed | expired
[0m
[0m$ [0mgit show 2ab477f:.gitignore
# Dependencies
node_modules/

# Builds
dist/
coverage/
.next/
out/

# Environment & secrets ÔÇö NUNCA commitar
.env
.env.local
.env.*.local
*.key
*.pem
keypair*.json
id.json

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
ml/models/*.pkl
ml/models/*.joblib

# Logs / dados locais
logs/
*.log
data/raw/
data/local/

# Prisma local
packages/database/prisma/*.db

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

.review-wt/

# TypeScript build cache
*.tsbuildinfo

.serena/
[0m
[0m$ [0mgit grep -n "SOLANA_RPC_HTTP" 2ab477f -- "*.ts" "*.mjs" "*.json" | Select-Object -First 30
2ab477f:packages/config/src/index.ts:36:    SOLANA_RPC_HTTP: z.string().url(),
2ab477f:scripts/f15-backfill.mjs:18:  [{ name: 'primary', url: process.env.SOLANA_RPC_HTTP, tier: 'primary' }],
2ab477f:scripts/f15-fixtures.mjs:13:const RPC = process.env.SOLANA_RPC_HTTP;
2ab477f:scripts/f15-fixtures.mjs:14:if (!HELIUS_KEY || !RPC) throw new Error('env incompleto 
(HELIUS_API_KEY/SOLANA_RPC_HTTP)');
2ab477f:services/data-ingestion/src/index.ts:92:    [{ name: 'primary', url: config.SOLANA_RPC_HTTP, tier: 'primary' 
}],
2ab477f:tests/unit/config.live-guard.test.ts:15:  SOLANA_RPC_HTTP: 'https://api.mainnet-beta.solana.com',
2ab477f:tests/unit/config.live-guard.test.ts:54:    expect(() => loadConfig({ ...baseEnv, SOLANA_RPC_HTTP: 'not-a-url' 
})).toThrowError(
[0m
[0m$ [0mgit grep -n "DecodedSwap" 2ab477f -- "packages/pumpfun/src/*.ts" | Select-Object -First 20; git ls-tree -r 
--name-only 2ab477f packages/pumpfun/src
2ab477f:packages/pumpfun/src/decode.ts:33:export interface DecodedSwap {
2ab477f:packages/pumpfun/src/decode.ts:64:): DecodedSwap | null {
2ab477f:packages/pumpfun/src/index.ts:9:export { decodePumpInstruction, type DecodedSwap } from './decode.js';
packages/pumpfun/src/decode.test.ts
packages/pumpfun/src/decode.ts
packages/pumpfun/src/generated/pump/accounts/bondingCurve.ts
packages/pumpfun/src/generated/pump/accounts/feeConfig.ts
packages/pumpfun/src/generated/pump/accounts/global.ts
packages/pumpfun/src/generated/pump/accounts/globalVolumeAccumulator.ts
packages/pumpfun/src/generated/pump/accounts/index.ts
packages/pumpfun/src/generated/pump/accounts/quoteControl.ts
packages/pumpfun/src/generated/pump/accounts/sharingConfig.ts
packages/pumpfun/src/generated/pump/accounts/userVolumeAccumulator.ts
packages/pumpfun/src/generated/pump/errors/index.ts
packages/pumpfun/src/generated/pump/errors/pump.ts
packages/pumpfun/src/generated/pump/events/addQuoteControlMintEvent.ts
packages/pumpfun/src/generated/pump/events/adminCtoEvent.ts
packages/pumpfun/src/generated/pump/events/adminSetIdlAuthorityEvent.ts
packages/pumpfun/src/generated/pump/events/adminUpdateTokenIncentivesEvent.ts
packages/pumpfun/src/generated/pump/events/claimCashbackEvent.ts
packages/pumpfun/src/generated/pump/events/claimTokenIncentivesEvent.ts
packages/pumpfun/src/generated/pump/events/closeUserVolumeAccumulatorEvent.ts
packages/pumpfun/src/generated/pump/events/collectCreatorFeeEvent.ts
packages/pumpfun/src/generated/pump/events/completeEvent.ts
packages/pumpfun/src/generated/pump/events/completePumpAmmMigrationEvent.ts
packages/pumpfun/src/generated/pump/events/createEvent.ts
packages/pumpfun/src/generated/pump/events/distributeCreatorFeesEvent.ts
packages/pumpfun/src/generated/pump/events/distributeFeeToHoldersEvent.ts
packages/pumpfun/src/generated/pump/events/extendAccountEvent.ts
packages/pumpfun/src/generated/pump/events/index.ts
packages/pumpfun/src/generated/pump/events/initUserVolumeAccumulatorEvent.ts
packages/pumpfun/src/generated/pump/events/migrateBondingCurveCreatorEvent.ts
packages/pumpfun/src/generated/pump/events/minimumDistributableFeeEvent.ts
packages/pumpfun/src/generated/pump_amm/events/syncUserVolumeAccumulatorEvent.ts
packages/pumpfun/src/generated/pump_amm/events/updateAdminEvent.ts
packages/pumpfun/src/generated/pump_amm/events/updateCreatorFeeConfigEvent.ts
packages/pumpfun/src/generated/pump_amm/events/updateFeeConfigEvent.ts
packages/pumpfun/src/generated/pump_amm/events/withdrawEvent.ts
packages/pumpfun/src/generated/pump_amm/index.ts
packages/pumpfun/src/generated/pump_amm/instructions/adminCtoPool.ts
packages/pumpfun/src/generated/pump_amm/instructions/adminUpdateTokenIncentives.ts
packages/pumpfun/src/generated/pump_amm/instructions/boostBuyAndBurn.ts
packages/pumpfun/src/generated/pump_amm/instructions/buy.ts
packages/pumpfun/src/generated/pump_amm/instructions/buyExactQuoteIn.ts
packages/pumpfun/src/generated/pump_amm/instructions/claimCashback.ts
packages/pumpfun/src/generated/pump_amm/instructions/claimTokenIncentives.ts
packages/pumpfun/src/generated/pump_amm/instructions/closeUserVolumeAccumulator.ts
packages/pumpfun/src/generated/pump_amm/instructions/collectCoinCreatorFee.ts
packages/pumpfun/src/generated/pump_amm/instructions/createConfig.ts
packages/pumpfun/src/generated/pump_amm/instructions/createPool.ts
packages/pumpfun/src/generated/pump_amm/instructions/deposit.ts
packages/pumpfun/src/generated/pump_amm/instructions/disable.ts
packages/pumpfun/src/generated/pump_amm/instructions/extendAccount.ts
packages/pumpfun/src/generated/pump_amm/instructions/index.ts
packages/pumpfun/src/generated/pump_amm/instructions/initBoost.ts
packages/pumpfun/src/generated/pump_amm/instructions/initUserVolumeAccumulator.ts
packages/pumpfun/src/generated/pump_amm/instructions/migratePoolCoinCreator.ts
packages/pumpfun/src/generated/pump_amm/instructions/sell.ts
packages/pumpfun/src/generated/pump_amm/instructions/setBoostAuthority.ts
packages/pumpfun/src/generated/pump_amm/instructions/setCoinCreator.ts
packages/pumpfun/src/generated/pump_amm/instructions/setReservedFeeRecipients.ts
packages/pumpfun/src/generated/pump_amm/instructions/syncUserVolumeAccumulator.ts
packages/pumpfun/src/generated/pump_amm/instructions/toggleBoost.ts
packages/pumpfun/src/generated/pump_amm/instructions/toggleCashbackEnabled.ts
packages/pumpfun/src/generated/pump_amm/instructions/toggleMayhemMode.ts
packages/pumpfun/src/generated/pump_amm/instructions/transferCreatorFeesToPump.ts
packages/pumpfun/src/generated/pump_amm/instructions/transferCreatorFeesToPumpV2.ts
packages/pumpfun/src/generated/pump_amm/instructions/updateAdmin.ts
packages/pumpfun/src/generated/pump_amm/instructions/updateBuybackConfig.ts
packages/pumpfun/src/generated/pump_amm/instructions/updateCreatorFeeConfig.ts
packages/pumpfun/src/generated/pump_amm/instructions/updateFeeConfig.ts
packages/pumpfun/src/generated/pump_amm/instructions/withdraw.ts
packages/pumpfun/src/generated/pump_amm/pdas/boostVaultAuthority.ts
packages/pumpfun/src/generated/pump_amm/pdas/coinCreatorVaultAuthority.ts
packages/pumpfun/src/generated/pump_amm/pdas/eventAuthority.ts
packages/pumpfun/src/generated/pump_amm/pdas/feeConfig.ts
packages/pumpfun/src/generated/pump_amm/pdas/globalConfig.ts
packages/pumpfun/src/generated/pump_amm/pdas/globalVolumeAccumulator.ts
packages/pumpfun/src/generated/pump_amm/pdas/index.ts
packages/pumpfun/src/generated/pump_amm/pdas/lpMint.ts
packages/pumpfun/src/generated/pump_amm/pdas/pool.ts
packages/pumpfun/src/generated/pump_amm/pdas/pumpCreatorVaultAta.ts
packages/pumpfun/src/generated/pump_amm/pdas/userVolumeAccumulator.ts
packages/pumpfun/src/generated/pump_amm/programs/index.ts
packages/pumpfun/src/generated/pump_amm/programs/pumpAmm.ts
packages/pumpfun/src/generated/pump_amm/types/configStatus.ts
packages/pumpfun/src/generated/pump_amm/types/feeTier.ts
packages/pumpfun/src/generated/pump_amm/types/fees.ts
packages/pumpfun/src/generated/pump_amm/types/index.ts
packages/pumpfun/src/generated/pump_amm/types/optionBool.ts
packages/pumpfun/src/generated/pump_amm/types/shareholder.ts
packages/pumpfun/src/index.ts
packages/pumpfun/src/sources.ts
[0m
[0m$ [0mgit show 2ab477f:packages/pumpfun/src/decode.ts | Select-Object -First 120
/**
 * decode.ts ÔÇö decoder F1.5 sobre os clientes gerados por codama a partir da
 * IDL oficial pump-fun/pump-public-docs (ADR-016: fonte oficial; ADR-020:
 * codama em vez de discriminadores ├á m├úo).
 *
 * Regras (dados on-chain hostis):
 * - NUNCA lan├ºa exce├º├úo em input adversarial ÔÇö retorna null.
 * - Endere├ºos validados com allowlist base58 (RT-004/006 mantidos fechados).
 * - Valores num├®ricos da instru├º├úo s├úo PAR├éMETROS/LIMITES da instru├º├úo
 *   (amount = tokens; em buy, sol = maxSolCost/maxQuoteAmountIn; em sell,
 *   sol = minSolOutput/minQuoteAmountOut) ÔÇö N├âO s├úo valores realizados.
 *   Valores realizados v├¬m de balance changes (fora do escopo deste decoder).
 */
import { z } from 'zod';
import {
  BUY_DISCRIMINATOR as PUMP_BUY,
  SELL_DISCRIMINATOR as PUMP_SELL,
  getBuyInstructionDataDecoder as pumpBuyDecoder,
  getSellInstructionDataDecoder as pumpSellDecoder,
} from './generated/pump/instructions/index.js';
import { PUMP_PROGRAM_ADDRESS } from './generated/pump/programs/index.js';
import {
  BUY_DISCRIMINATOR as AMM_BUY,
  SELL_DISCRIMINATOR as AMM_SELL,
  getBuyInstructionDataDecoder as ammBuyDecoder,
  getSellInstructionDataDecoder as ammSellDecoder,
} from './generated/pump_amm/instructions/index.js';
import { PUMP_AMM_PROGRAM_ADDRESS } from './generated/pump_amm/programs/index.js';

const BASE58 = /^[1-9A-HJ-NP-Za-km-z]{32,44}$/;
const addressSchema = z.string().regex(BASE58);

export interface DecodedSwap {
  kind: 'pump_buy' | 'pump_sell' | 'pumpswap_buy' | 'pumpswap_sell';
  direction: 'buy' | 'sell';
  mint: string;
  user: string;
  /** Bonding curve (pump.*) ou pool AMM (pumpswap.*). */
  counterparty: string;
  /** Tokens em unidades base (instru├º├úo `amount`/`baseAmount*`). */
  tokenAmount: string | null;
  /** SOL em lamports ÔÇö LIMITE da instru├º├úo (maxSolCost / minSolOutput etc.), n├úo realizado. */
  solAmount: string | null;
}

function discEq(data: ArrayLike<number>, disc: ArrayLike<number>): boolean {
  if (data.length < 8 || disc.length < 8) return false;
  for (let i = 0; i < 8; i++) if (data[i] !== disc[i]) return false;
  return true;
}

function validAddr(a: unknown): a is string {
  return typeof a === 'string' && addressSchema.safeParse(a).success;
}

/** Contas m├¡nimas esperadas: pump.buy/sell = 16; pump_amm.buy/sell = 22. */
const PUMP_MIN_ACCOUNTS = 16;
const AMM_MIN_ACCOUNTS = 22;

export function decodePumpInstruction(
  programId: string,
  data: ArrayLike<number>,
  accounts: string[],
): DecodedSwap | null {
  try {
    // Normaliza para Uint8Array: os decoders do codama exigem Uint8Array.
    const bytes = data instanceof Uint8Array ? data : Uint8Array.from(Array.from(data));
    if (programId === PUMP_PROGRAM_ADDRESS) {
      if (accounts.length < PUMP_MIN_ACCOUNTS) return null;
      // IDL: mint=2, bondingCurve=3, user=6
      const mint = accounts[2];
      const counterparty = accounts[3];
      const user = accounts[6];
      if (!validAddr(mint) || !validAddr(counterparty) || !validAddr(user)) return null;

      if (discEq(bytes, PUMP_BUY)) {
        const d = pumpBuyDecoder().decode(bytes);
        return {
          kind: 'pump_buy',
          direction: 'buy',
          mint: mint!,
          user: user!,
          counterparty: counterparty!,
          tokenAmount: d.amount.toString(),
          solAmount: d.maxSolCost.toString(),
        };
      }
      if (discEq(bytes, PUMP_SELL)) {
        const d = pumpSellDecoder().decode(bytes);
        return {
          kind: 'pump_sell',
          direction: 'sell',
          mint: mint!,
          user: user!,
          counterparty: counterparty!,
          tokenAmount: d.amount.toString(),
          solAmount: d.minSolOutput.toString(),
        };
      }
      return null;
    }

    if (programId === PUMP_AMM_PROGRAM_ADDRESS) {
      if (accounts.length < AMM_MIN_ACCOUNTS) return null;
      // IDL: pool=0, user=1, baseMint=3
      const counterparty = accounts[0];
      const user = accounts[1];
      const mint = accounts[3];
      if (!validAddr(mint) || !validAddr(counterparty) || !validAddr(user)) return null;

      if (discEq(bytes, AMM_BUY)) {
        const d = ammBuyDecoder().decode(bytes);
        return {
          kind: 'pumpswap_buy',
          direction: 'buy',
          mint: mint!,
          user: user!,
          counterparty: counterparty!,
          tokenAmount: d.baseAmountOut.toString(),
          solAmount: d.maxQuoteAmountIn.toString(),
[0m
[0m$ [0mgit grep -n "subscribeWallets\|solanaIngestion\|flow.main\|MAX_SMART\|maxWallets\|wallets" 2ab477f -- 
"services/data-ingestion/src/main.ts" | Select-Object -First 30
(no output)
[0m
[0m$ [0mgit show 2ab477f:services/data-ingestion/src/main.ts | Select-Object -First 160
/**
 * Entrypoint execut├ível do servi├ºo data-ingestion.
 * Uso: node dist/main.js (ap├│s `npm run build`), com vari├íveis de ambiente carregadas.
 */
import { start } from './index.js';

start().catch((err: unknown) => {
  // eslint n├úo permite console; stderr direto ├® aceit├ível APENAS aqui (boot fatal).
  process.stderr.write(
    `FATAL boot data-ingestion: ${err instanceof Error ? err.message : String(err)}\n`,
  );
  process.exit(1);
});
[0m
[0m$ [0mgit ls-tree -r --name-only 2ab477f packages/solana/src; git ls-tree -r --name-only 2ab477f 
services/data-ingestion/src | Select-String "test"
packages/solana/src/backoff.test.ts
packages/solana/src/backoff.ts
packages/solana/src/chain-event.ts
packages/solana/src/helius-provider.ts
packages/solana/src/index.ts
packages/solana/src/rpc-provider.ts

services/data-ingestion/src/dedup.test.ts
services/data-ingestion/src/enricher.test.ts
services/data-ingestion/src/instance-lock.test.ts
services/data-ingestion/src/normalizer.test.ts
services/data-ingestion/src/retention.test.ts
services/data-ingestion/src/security-fixes.test.ts
[0m
[0m$ [0mgit show 2ab477f:services/data-ingestion/src/index.ts | Select-Object -First 160
/**
 * data-ingestion ÔÇö boot do servi├ºo (Fase 1).
 * Graceful shutdown em SIGINT/SIGTERM: para de aceitar eventos, drena,
 * fecha provider e desconecta Prisma.
 */
import { loadConfig } from '@ct/config';
import { createLogger } from '@ct/logging';
import { prisma } from '@ct/database';
import { normalizeRawNotification } from './normalizer.js';
import { DedupStore } from './dedup-store.js';
import { IngestionPipeline } from './pipeline.js';
import { createDataProvider } from './factory.js';
import { InstanceLock } from './instance-lock.js';
import { RetentionJob } from './retention.js';
import { Enricher, type DecodedSwap } from './enricher.js';
import { EnrichRunner } from './enrich-runner.js';
import { RpcProvider } from '@ct/solana';
import {
  markEnriched as repoMarkEnriched,
  markUndecodable as repoMarkUndecodable,
  findUnenriched as repoFindUnenriched,
  incrementEnrichAttempt as repoIncrementAttempt,
} from '@ct/database';
// Nota: prisma.observedEvent (delegate tipado do Prisma) e a interface m├¡nima
// ObservedEventEnrichDelegate divergem por exactOptionalPropertyTypes; o cast
// no ponto de glue (abaixo) ├® consciente ÔÇö os repos validam os payloads.
import type { ObservedEventEnrichDelegate } from '@ct/database';
import type { DecodedSwap as DecodedSwapType } from '@ct/pumpfun';

const SERVICE_NAME = 'data-ingestion';

export async function start(): Promise<void> {
  const config = loadConfig();
  const logger = createLogger(SERVICE_NAME);
  logger.info('service booting', { tradingMode: config.tradingMode });

  // Single-instance (ADR-024): aborta se j├í houver inst├óncia viva rodando.
  const lock = new InstanceLock(
    process.env['INGESTION_LOCK_PATH'] ?? './data/locks/data-ingestion.lock',
  );
  lock.acquire();

  const store = new DedupStore(prisma.observedEvent);
  const pipeline = new IngestionPipeline(store, logger);
  pipeline.start();

  // Reten├º├úo RAW (ADR-024): al├¡vio de disco; agrega├º├úo vem em fases futuras.
  const retention = new RetentionJob(prisma.observedEvent, logger);
  retention.start();

  const provider = createDataProvider(config, { logger });
  /*
   * logsNotification n├úo carrega wallet/instruction_index ÔÇö o enriquecimento
   * (qual wallet do filtro "mentions" disparou, BUY/SELL, mint) ├® Fase 1.5
   * via getTransaction. Por isso consumimos onRawEvent e normalizamos apenas
   * signature+slot; demais campos ficam null/UNKNOWN (nunca estimar).
   */
  if (!('onRawEvent' in provider) || typeof provider.onRawEvent !== 'function') {
    throw new Error('CONFIG_ERROR: provider n├úo exp├Áe onRawEvent (interface esperada)');
  }
  const rawProvider = provider as unknown as {
    onRawEvent: (cb: (n: { signature: string; slot: number; value: unknown }) => void) => void;
  };
  rawProvider.onRawEvent((raw) => {
    const result = normalizeRawNotification(
      { signature: raw.signature, slot: raw.slot },
      'websocket',
      Date.now(),
    );
    if (!result.ok) {
      store.counters.errors++;
      logger.warn('notifica├º├úo crua inv├ílida ÔÇö VALIDATION_ERROR', {
        errorClass: 'VALIDATION_ERROR',
        error: result.error.message,
      });
      return;
    }
    pipeline.ingest(result.event).catch((e: unknown) => {
      // NUNCA deixar erro escapar do loop do event loop.
      logger.error('erro inesperado no loop de ingest├úo ÔÇö UNKNOWN_ERROR', {
        errorClass: 'UNKNOWN_ERROR',
        error: e instanceof Error ? e.message : String(e),
      });
    });
  });
  await provider.connect();

  // ---- F1.5: enricher (decode BUY/SELL real) -------------------------------
  // Ligado por padr├úo em todos os modos (s├│ leitura on-chain + escrita local;
  // nenhuma transa├º├úo ├® enviada). Retries limitados por enrichAttempts.
  const rpc = new RpcProvider(
    [{ name: 'primary', url: config.SOLANA_RPC_HTTP, tier: 'primary' }],
    { logger },
  );
  const enricher = new Enricher(
    { getTransaction: (sig) => rpc.getTransaction(sig) },
    {
      async bumpAttempt(id) {
        await repoIncrementAttempt({ observedEvent: prisma.observedEvent as unknown as ObservedEventEnrichDelegate }, 
id);
      },
      async markEnriched(id, d: DecodedSwap) {
        const dec: DecodedSwapType = d;
        await repoMarkEnriched({ observedEvent: prisma.observedEvent as unknown as ObservedEventEnrichDelegate }, {
          id,
          direction: dec.direction,
          amountSol: dec.solAmount,
          tokenAmount: dec.tokenAmount,
          mint: dec.mint,
          counterparty: dec.counterparty,
          enrichSource: `pump-decoder-v1:${dec.kind}`,
        });
      },
      async markUndecodable(id, reason) {
        await repoMarkUndecodable({ observedEvent: prisma.observedEvent as unknown as ObservedEventEnrichDelegate }, 
id, reason);
      },
    },
    logger,
  );
  let enrichTimer: NodeJS.Timeout | null = null;
  const runner = new EnrichRunner(
    {
      findUnenriched: (limit, maxAttempts) =>
        repoFindUnenriched({ observedEvent: prisma.observedEvent as unknown as ObservedEventEnrichDelegate }, { limit, 
maxAttempts }),
    },
    enricher,
    {
      batchSize: Number(process.env['ENRICH_BATCH_SIZE'] ?? 25),
      maxAttempts: Number(process.env['ENRICH_MAX_ATTEMPTS'] ?? 5),
      sleepMs: Number(process.env['ENRICH_SLEEP_MS'] ?? 120), // ~8 req/s no RPC
    },
  );
  const enrichEnabled = (process.env['ENRICH_ENABLED'] ?? 'true') === 'true';
  if (enrichEnabled) {
    enrichTimer = setInterval(() => void runner.runUntilEmpty(1), 2_000);
    enrichTimer.unref?.();
    logger.info('enricher ativo', { batchSize: Number(process.env['ENRICH_BATCH_SIZE'] ?? 25) });
  } else {
    logger.warn('enricher DESLIGADO (ENRICH_ENABLED=false)');
  }
  // ---------------------------------------------------------------------------

  // Subscri├º├úo expl├¡cita: sem endere├ºos configurados o pipeline sobe mas n├úo observa nada.
  const watch = (config.INGESTION_WATCH_ADDRESSES ?? '')
    .split(',')
    .map((s) => s.trim())
    .filter((s) => s.length > 0);
  if (watch.length === 0) {
    logger.warn('INGESTION_WATCH_ADDRESSES vazio ÔÇö nenhuma subscri├º├úo ativa', {
      errorClass: 'CONFIG_ERROR',
    });
  } else {
    await provider.subscribeWallets(watch);
    logger.info('subscri├º├Áes ativas', { watchCount: watch.length });
  }

  const shutdown = async (signal: string): Promise<void> => {
    logger.info('shutdown iniciado', { signal });
    runner.stop();
    if (enrichTimer !== null) clearInterval(enrichTimer);
    retention.stop();
[0m
[0m$ [0mgit show 2ab477f:packages/pumpfun/scripts/fix-esm-imports.mjs
// Pos-build: reescreve imports relativos sem extensao para ESM Node (.js / index.js).
// Deterministico e idempotente. Uso: node scripts/fix-esm-imports.mjs
import { readdirSync, statSync, readFileSync, writeFileSync, existsSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { dirname, join, dirname as pdirname } from 'node:path';

const here = dirname(fileURLToPath(import.meta.url));
const dist = join(here, '..', 'dist');

function* walk(dir) {
  for (const name of readdirSync(dir)) {
    const p = join(dir, name);
    if (statSync(p).isDirectory()) yield* walk(p);
    else if (p.endsWith('.js')) yield p;
  }
}

const importRe = /(from|import|export)[^'"]*['"](\.{1,2}(?:\/[^'"]*)?)['"]/g;
function resolveSpec(fileDir, spec) {
  if (spec.endsWith('.js')) return spec;
  const abs = join(fileDir, spec);
  if (existsSync(`${abs}.js`)) return `${spec}.js`;
  if (existsSync(join(abs, 'index.js'))) return `${spec === '.' || spec === '..' ? spec + '/' : spec + '/'}index.js`;
  return spec;
}
let changed = 0;
for (const file of walk(dist)) {
  let src = readFileSync(file, 'utf8');
  const fileDir = pdirname(file);
  const out = src.replace(importRe, (m, kw, spec) => m.replace(spec, resolveSpec(fileDir, spec)));
  if (out !== src) {
    writeFileSync(file, out);
    changed++;
  }
}
process.stdout.write(`fix-esm-imports: ${changed} arquivos ajustados\n`);
[0m
[0m$ [0mgit show 2ab477f:packages/config/src/index.ts | Select-Object -First 80
/**
 * @ct/config ÔÇö carregamento e valida├º├úo de configura├º├úo com zod.
 *
 * REGRAS DE SEGURAN├çA (falham fechado ÔÇö config inv├ílida impede o boot):
 *  - Guard LIVE: TRADING_MODE=LIVE exige LIVE_TRADING_ENABLED=true (dupla trava).
 *  - Nenhum valor de risco possui default impl├¡cito al├®m dos placeholders do .env.example.
 */

import { z } from 'zod';
import type { TradingMode } from '@ct/types';

const tradingModeSchema = z.enum(['PAPER', 'SHADOW', 'LIVE']);

const boolFromEnv = z
  .string()
  .transform((v) => v.trim().toLowerCase())
  .pipe(z.enum(['true', 'false']))
  .transform((v) => v === 'true');

const positiveNumber = z.coerce.number().positive();
const nonNegativeInt = z.coerce.number().int().nonnegative();

export const envSchema = z
  .object({
    TRADING_MODE: tradingModeSchema.default('PAPER'),
    LIVE_TRADING_ENABLED: boolFromEnv.default('false'),
    TRADING_KILL_SWITCH: boolFromEnv.default('false'),

    MAX_TRADE_SOL: positiveNumber,
    MAX_POSITION_SOL: positiveNumber,
    MAX_TOTAL_EXPOSURE_SOL: positiveNumber,
    MAX_DAILY_LOSS_SOL: positiveNumber,
    MAX_CONCURRENT_POSITIONS: nonNegativeInt,
    MAX_TRADES_PER_DAY: nonNegativeInt,

    SOLANA_RPC_HTTP: z.string().url(),
    SOLANA_RPC_WSS: z.string().url().optional(),
    HELIUS_API_KEY: z.string().min(1).optional(),

    BOT_WALLET_PUBLIC_KEY: z.string().min(32).optional(),

    DATABASE_URL: z.string().min(1),

    TELEGRAM_BOT_TOKEN: z.string().optional(),
    TELEGRAM_CHAT_ID: z.string().optional(),

    API_PORT: z.coerce.number().int().positive().default(3001),

    // Enderecos (wallets/programas) monitorados pelo data-ingestion (CSV). Opcional:
    // ausente = pipeline sobe sem subscricao (util para testes locais).
    INGESTION_WATCH_ADDRESSES: z.string().optional(),
  });
// NOTA: SEM .strict() ÔÇö process.env cont├®m dezenas de vari├íveis do OS; rejeitar
// chaves desconhecidas impediria o boot de qualquer processo. Unknown keys s├úo ignoradas.

export type AppConfig = z.infer<typeof envSchema> & { tradingMode: TradingMode };

/**
 * Carrega e valida a configura├º├úo a partir de `process.env` (ou fonte injetada).
 * Lan├ºa `ConfigError` em qualquer viola├º├úo ÔÇö incluindo a viola├º├úo da dupla trava LIVE.
 */
export function loadConfig(source: Record<string, string | undefined> = process.env): AppConfig {
  const parsed = envSchema.safeParse(source);
  if (!parsed.success) {
    throw new ConfigError(`Configura├º├úo inv├ílida: ${parsed.error.message}`);
  }
  const cfg = parsed.data;

  // Guard LIVE ÔÇö dupla trava (falha fechada).
  if (cfg.TRADING_MODE === 'LIVE' && !cfg.LIVE_TRADING_ENABLED) {
    throw new ConfigError(
      'TRADING_MODE=LIVE exige LIVE_TRADING_ENABLED=true (dupla trava). Boot abortado.',
    );
  }
  // LIVE nunca deve nascer de default ÔÇö modo precisa estar expl├¡cito no env.
  if (cfg.TRADING_MODE === 'LIVE' && source.TRADING_MODE === undefined) {
    throw new ConfigError('TRADING_MODE=LIVE n├úo pode ser default. Boot abortado.');
  }

  return { ...cfg, tradingMode: cfg.TRADING_MODE };
[0m
[0m$ [0mgit show 2ab477f:services/data-ingestion/src/dedup-store.ts | Select-Object -First 60
/**
 * DedupStore ÔÇö deduplica├º├úo em duas camadas (AGENTS.md ┬º2.6 idempot├¬ncia).
 *
 * (a) Cache em mem├│ria (LRU simples, limite configur├ível) ÔÇö fast-path.
 * (b) Banco: insert com @@unique([signature, instructionIndex, wallet]).
 *     Conflito (Prisma P2002) = duplicata. A camada (b) ├® a AUTORIDADE:
 *     garante idempot├¬ncia mesmo entre restarts do processo.
 *
 * Erros: apenas P2002 ├® classificado como duplicata. Qualquer outro erro de
 * persist├¬ncia ├® DATABASE_ERROR.
 */

/** JSON recursivo compat├¡vel com Prisma.InputJsonValue (mant├®m o package desacoplado). */
export type JsonValue = string | number | boolean | null | JsonValue[] | { [k: string]: JsonValue };

/** Shape alinhado ao model ObservedEvent real (packages/database/prisma/schema.prisma). */
export interface ObservedEventInsertData {
  signature: string;
  instructionIndex: number;
  wallet: string;
  eventType: string;
  slot?: bigint | null;
  blockTime?: Date | null;
  /** Prisma Json fields n├úo aceitam null direto (usar Prisma.JsonNull); omitimos quando ausente. */
  payload?: Exclude<JsonValue, null>;
}

/** Interface m├¡nima do reposit├│rio (prisma.observedEvent). */
export interface ObservedEventRepo {
  create(args: { data: ObservedEventInsertData }): Promise<unknown>;
}

export function isUniqueConstraintError(err: unknown): boolean {
  return (
    typeof err === 'object' && err !== null && (err as { code?: string }).code === 'P2002'
  );
}

export type DedupOutcome =
  | { status: 'new' }
  | { status: 'duplicate' }
  | { status: 'error'; error: Error };

export class DedupCounters {
  ingested = 0;
  deduplicated = 0;
  errors = 0;
  /** Waiters que receberam o resultado agregado de uma opera├º├úo em voo com erro (RT-005). */
  joined = 0;
}

export class DedupStore {
  private readonly seen = new Set<string>();
  /**
   * RT-005: mapa de opera├º├Áes em voo. Concorr├¬ncia sobre a mesma dedupKey
   * (reconnect storm) N├âO cria N inserts paralelos que inflam contadores ÔÇö
   * somente o primeiro vira l├¡der de insert; os demais aguardam o l├¡der.
   *
   * SEM├éNTICA DOS CONTADORES (documentada ÔÇö review 19ffacc, achado 2):
   * - ingested      : inserts que persistiram de fato no DB (autoridade).
[0m
[0m$ [0mgit show 2ab477f:packages/database/src/index.ts
/**
 * @ct/database ÔÇö PrismaClient singleton.
 * Nunca commitar DATABASE_URL (ver docs/security/secret-policy.md).
 */
import { PrismaClient } from '@prisma/client';

declare global {
  // eslint-disable-next-line no-var
  var __ctPrisma: PrismaClient | undefined;
}

export const prisma: PrismaClient =
  globalThis.__ctPrisma ??
  new PrismaClient({
    log: ['warn', 'error'],
  });

if (process.env['NODE_ENV'] !== 'production') {
  globalThis.__ctPrisma = prisma;
}

export { PrismaClient };
export type * from '@prisma/client';

// Repositories ÔÇö client sempre injetado (test├ível, sem singleton escondido).
export {
  insertObservedEvent,
  existsObservedEvent,
  markEnriched,
  markUndecodable,
  findUnenriched,
  incrementEnrichAttempt,
  type InsertResult,
  type ObservedEventInsertData,
  type ObservedEventDelegate,
  type EnrichmentData,
  type UnenrichedRow,
  type ObservedEventEnrichDelegate,
} from './repositories/observed-events.js';
export {
  recordSystemEvent,
  type SystemEventInput,
  type SystemEventSeverity,
} from './repositories/system-events.js';
export {
  appendAuditLog,
  type AuditLogInput,
} from './repositories/audit-log.js';
export {
  RepositoryError,
  isUniqueViolation,
  PRISMA_UNIQUE_VIOLATION,
} from './repositories/errors.js';
[0m
[0m$ [0mgit show 2ab477f:services/data-ingestion/src/enricher.test.ts | Select-Object -First 80
/**
 * Testes do enricher F1.5 (ASCII-only). Base58, extracao de instrucoes
 * (outer+inner), caminhos de erro e idempotencia ÔÇö dados hostis nunca throw.
 */
import { describe, it, expect, vi } from 'vitest';
import {
  base58ToBytes,
  collectInstructionCalls,
  Enricher,
  type ParsedTx,
  type EnrichStore,
  type TxFetcher,
} from './enricher.js';
import { PUMP_BUY_ENCODER_FOR_TESTS as pumpBuyEnc, PUMP_PROGRAM_ADDRESS } from '@ct/pumpfun';

const A = (c: string, n: number) => c.repeat(n);
const MINT = A('M', 44);
const CURVE = A('C', 44);
const USER = A('U', 44);
const FILLER = A('F', 32);
const SIG = '5'.repeat(88);

function base58(bytes: number[]): string {
  const alphabet = '123456789ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz';
  let num = 0n;
  for (const b of bytes) num = num * 256n + BigInt(b);
  let out = '';
  while (num > 0n) {
    out = alphabet[Number(num % 58n)] + out;
    num /= 58n;
  }
  for (const b of bytes) {
    if (b === 0) out = '1' + out;
    else break;
  }
  return out || '1';
}

/** Monta uma tx jsonParsed parcial com uma instrucao pump.buy valida. */
function makeTxWithPumpBuy(): ParsedTx {
  const dataBytes = new Uint8Array(
    pumpBuyEnc().encode({ amount: 1000n, maxSolCost: 5000n, trackVolume: [false] }),
  );
  const accounts = Array.from({ length: 16 }, () => FILLER);
  accounts[2] = MINT;
  accounts[3] = CURVE;
  accounts[6] = USER;
  const allKeys = [PUMP_PROGRAM_ADDRESS, ...new Set(accounts)];
  const msgKeys = allKeys.map((pubkey) => ({ pubkey }));
  const accountIndexes = accounts.map((a) => allKeys.indexOf(a));
  return {
    transaction: {
      message: {
        accountKeys: msgKeys,
        instructions: [
          {
            programId: PUMP_PROGRAM_ADDRESS,
            accounts: accountIndexes as unknown as string[],
            data: base58(Array.from(dataBytes)),
          },
        ],
      },
    },
    meta: { innerInstructions: [] },
  };
}

function fakeStore(): EnrichStore & { calls: { m: string[]; u: string[] } } {
  const calls = { m: [] as string[], u: [] as string[] };
  return {
    calls,
    async markEnriched(id) {
      calls.m.push(id);
    },
    async markUndecodable(id) {
      calls.u.push(id);
    },
    async bumpAttempt() {},
  };
}
[0m
Let me verify the Helius FACT claim behind the per-wallet subscription change, and check the docs on subscription limits.
[0mÔÜÖ [0mhelius-docs_search_helius_docs {"query":"logsSubscribe one address per subscription enhanced websockets 
mention"}
[0m
[0m$ [0mgit show 2ab477f:scripts/f15-fixtures.mjs | Select-String -Pattern "match|mismatch|heliusSwap|source" 
-Context 2
? {
              type: enhanced.type ?? null,
>             source: enhanced.source ?? null,
              feePayer: enhanced.feePayer ?? null,
            }
  }
  
> let match = 0,
>   mismatch = 0;
  for (const r of results) {
    if (!r.helius) continue;
>   const heliusSwap = r.helius.type === 'SWAP' || r.helius.type === 'SWAP_PARTIAL';
>   if (heliusSwap) match++;
>   else mismatch++;
  }
  
    pumpFixtures: pumpOk,
    probes,
>   helisMatch: match,
>   heliusMismatch: mismatch,
    results,
  };
[0m
[0m$ [0mgit show 2ab477f:package.json | Select-String -Pattern "scripts|lint|typecheck|test|f15" -Context 0,2
>     "backtester"
    ],
>   "scripts": {
      "build": "npm run build --workspaces --if-present",
>     "lint": "eslint . --ext .ts,.tsx --max-warnings 0",
      "format": "prettier --write .",
      "format:check": "prettier --check .",
>     "typecheck": "tsc -b --pretty",
>     "test": "vitest run --passWithNoTests",
>     "test:watch": "vitest",
      "prisma:generate": "prisma generate --schema packages/database/prisma/schema.prisma",
      "prisma:migrate": "prisma migrate dev --schema packages/database/prisma/schema.prisma",
>     "check:secrets": "node infrastructure/scripts/check-secrets.mjs",
      "check:security": "npm run check:secrets && npm audit --audit-level=high"
    },
>     "@typescript-eslint/eslint-plugin": "^7.13.0",
>     "@typescript-eslint/parser": "^7.13.0",
>     "eslint": "^8.57.0",
>     "eslint-config-prettier": "^9.1.0",
      "fast-check": "^3.19.0",
      "prettier": "^3.3.0",
>     "vitest": "^3.2.7"
    },
    "engines": {
[0m
Helius docs confirm the FACT claim (one Pubkey per `mentions`). Now let me check the repo's lint/test wiring and whether scripts are covered.
[0m$ [0mgit show 2ab477f:eslint.config.mjs 2>$null | Select-Object -First 60; git ls-tree -r --name-only 2ab477f | 
Select-String -Pattern "eslint"
.eslintrc.cjs
[0m
[0m$ [0mgit show 2ab477f:f15-fixtures-report.json | Format-Hex | Select-Object -First 4
00 01 02 03 04 05 06 07 08 09 0A 0B 0C 0D 0E 0F

00000000
00001470   69 6F 6E 22 3A 20 22 73 65 6C 6C 22 2C 20 20 20  ion": "sell",   
00001480   20 20 20 20 20 22 6D 69 6E 74 22 3A 20 22 53 6F       "mint": "So
00001490   31 31 31 31 31 31 31 31 31 31 31 31 31 31 31 31  1111111111111111
000014A0   31 31 31 31 31 31 31 31 31 31 31 31 31 31 31 31  1111111111111111
000014B0   31 31 31 31 31 31 31 31 32 22 2C 20 20 20 20 20  111111112",     
000014C0   20 20 20 22 75 73 65 72 22 3A 20 22 45 62 63 54     "user": "EbcT
000014D0   65 66 74 38 59 37 74 35 59 61 57 67 71 69 32 42  eft8Y7t5YaWgqi2B
000014E0   51 43 7A 4E 7A 67 4B 63 6B 77 74 46 33 7A 5A 4B  QCzNzgKckwtF3zZK
000014F0   48 51 64 57 78 68 6A 4B 22 2C 20 20 20 20 20 20  HQdWxhjK",      
00001500   20 20 22 63 6F 75 6E 74 65 72 70 61 72 74 79 22    "counterparty"
00001510   3A 20 22 36 43 53 73 63 38 6A 6D 56 55 52 38 66  : "6CSsc8jmVUR8f
00001520   6B 37 6F 38 6A 77 78 73 51 62 47 66 6F 63 4B 42  k7o8jwxsQbGfocKB
00001530   4E 75 65 56 79 57 79 63 70 37 52 65 59 57 34 22  NueVyWycp7ReYW4"
00001540   20 20 20 20 20 20 7D 2C 20 20 20 20 20 20 22 68        },      "h
00001550   65 6C 69 75 73 22 3A 20 7B 20 20 20 20 20 20 20  elius": {       
00001560   20 22 74 79 70 65 22 3A 20 22 53 57 41 50 22 2C   "type": "SWAP",
00001570   20 20 20 20 20 20 20 20 22 73 6F 75 72 63 65 22          "source"
00001580   3A 20 22 50 55 4D 50 5F 41 4D 4D 22 2C 20 20 20  : "PUMP_AMM",   
00001590   20 20 20 20 20 22 66 65 65 50 61 79 65 72 22 3A       "feePayer":
000015A0   20 22 45 62 63 54 65 66 74 38 59 37 74 35 59 61   "EbcTeft8Y7t5Ya
000015B0   57 67 71 69 32 42 51 43 7A 4E 7A 67 4B 63 6B 77  Wgqi2BQCzNzgKckw
000015C0   74 46 33 7A 5A 4B 48 51 64 57 78 68 6A 4B 22 20  tF3zZKHQdWxhjK" 
000015D0   20 20 20 20 20 7D 20 20 20 20 7D 20 20 5D 7D          }    }  ]}
[0m
[0m$ [0mgit show 2ab477f:data/locks/data-ingestion.lock
19816
1790291576311
[0m
[0m$ [0mgit ls-tree -r --name-only 2ab477f tests packages 2>$null | Select-String -Pattern 
"helius|rpc-provider|integr" ; git ls-tree -r --name-only 2ab477f | Select-String -Pattern "test" | Select-Object 
-First 60
packages/solana/src/helius-provider.ts
packages/solana/src/rpc-provider.ts
.agents/backtest-ml.md
.opencode/agents/backtest-ml.md
backtester/README.md
backtester/fixtures/.gitkeep
backtester/package.json
backtester/reports/.gitkeep
backtester/scenarios/.gitkeep
backtester/src/index.ts
backtester/tsconfig.json
docs/architecture/adr/ADR-004-vitest-fast-check-pytest-hypothesis.md
docs/security/red-team/poc/sprint2-fuzz.poc.test.ts
docs/security/red-team/poc/sprint2-ingestion.poc.test.ts
docs/security/red-team/poc/vitest.poc.config.mjs
ml/pytest.ini
packages/database/src/repositories/audit-log.test.ts
packages/database/src/repositories/observed-events.test.ts
packages/database/src/repositories/system-events.test.ts
packages/pumpfun/src/decode.test.ts
packages/solana/src/backoff.test.ts
services/data-ingestion/src/dedup.test.ts
services/data-ingestion/src/enricher.test.ts
services/data-ingestion/src/instance-lock.test.ts
services/data-ingestion/src/normalizer.test.ts
services/data-ingestion/src/retention.test.ts
services/data-ingestion/src/security-fixes.test.ts
tests/property/dedup.property.test.ts
tests/unit/config.live-guard.test.ts
vitest.config.ts
[0m
## Objective
- Act as code-reviewer agent and adversarially review commit `2ab477f7dc60361db18e2106cd4b9ef48aea977e` (branch `sprint/f1-5-enrichment`), per `.agents/code-reviewer.md` and `AGENTS.md`.
- Output findings with severities and end with the exact line `VERDICT: APROVADO` or `VERDICT: REJEITADO`. Do NOT modify code or commit.

## Important Details
- Reviewer is on `master`; commit under review is on branch `sprint/f1-5-enrichment` ÔÇö all file reads must use `git show 2ab477f:<path>` (working tree is master and differs from the commit).
- Commit diff: 7 files, +214/ÔêÆ18: `data/locks/data-ingestion.lock` (new), `f15-fixtures-report.json` (new, starts with UTF-8 BOM U+FEFF), `packages/pumpfun/scripts/fix-esm-imports.mjs`, `packages/solana/src/helius-provider.ts`, `packages/solana/src/rpc-provider.ts` (maxSupportedTransactionVersion 0ÔåÆ1), `scripts/f15-backfill.mjs` (new), `scripts/f15-fixtures.mjs` (new).
- Helius FACT claim of the commit verified true: Helius docs state `logsSubscribe` `mentions` supports only ONE Pubkey per call; listing additional addresses results in error ÔåÆ per-wallet subscription change is justified (not a finding).
- Review criteria: bugs, errors, incompatibilities, AGENTS.md violations, missing tests; never swallow errors silently (rule 9); no-console is error (use `@ct/logging`); dedup key `signature + instructionIndex + wallet` (rule 6).
- `.gitignore` ignores `data/raw/` and `data/local/` but NOT `data/locks/`.
- Env name `SOLANA_RPC_HTTP` is canonical (packages/config/src/index.ts:36, `z.string().url()`); `INGESTION_WATCH_ADDRESSES` is optional CSV, no wallet-count cap found.
- Cannot modify code or commit in this stage ÔÇö static review. Working tree (master) has pre-existing modified files: `docs/security/red-team/FINDINGS.md`, `SPRINTS.md`, `poc/sprint2-ingestion.poc.test.ts`.

## Work State
### Completed
- Read full commit diff and file trees at `2ab477f` (`git show`), plus: helius-provider.ts, rpc-provider.ts, enricher.ts, enrich-runner.ts, instance-lock.ts, dedup-store.ts, config index, database index.ts, observed-events.ts repo, schema.prisma, pumpfun decode.ts/DecodedSwap, fix-esm-imports.mjs, f15-backfill.mjs, f15-fixtures.mjs, .gitignore, data-ingestion main.ts/index.ts, enricher.test.ts, database exports.
- Confirmed Helius per-wallet subscription restriction via helius-docs search (FACT validated).
- Confirmed **no test exists for `helius-provider.ts`** (packages/solana/src has only `backoff.test.ts`); rpc-provider change also has no test.
- Verified `InstanceLock.acquire()` reads lock file line 1 as PID, throws `CONFIG_ERROR: j├í existe inst├óncia ativa (PID <pid>)` if PID is alive and != current; lock format is `PID\ntimestamp\n`. Committed `data/locks/data-ingestion.lock` contains `19816\n1790291576311` (runtime artifact).
- Verified `f15-fixtures.mjs` match logic: only checks `helius.type === 'SWAP' || 'SWAP_PARTIAL'`; `source`, user/mint/counterparty/amounts never compared; null `helius` rows skipped without counting mismatch; errors silently counted (`probes.errors++`) without logging cause; report row 2 shows `local.user` BwWKÔÇª Ôëá `helius.feePayer` GygjÔÇª (unflagged divergence).
- Verified `f15-fixtures-report.json` begins with BOM ÔåÆ `JSON.parse` fails in Node; committed at repo root; key typo `helisMatch`.
- Verified `fix-esm-imports.mjs` dead ternary (`spec === '.' || spec === '..' ? spec + '/' : spec + '/'` ÔÇö both branches identical).
- Verified enricher/enrich-runner APIs used by scripts exist at `2ab477f` (`runUntilEmpty`, `stats.seen/enriched/undecodable/retried`, `DecodedSwap` fields, database repo exports `markEnriched/markUndecodable/findUnenriched/incrementEnrichAttempt`).

### Active
- Drafting final findings list; verdict not yet issued.
- Still must explicitly read `.agents/code-reviewer.md` and `AGENTS.md` (user explicitly said "leia"; not shown done in transcript) to anchor the verdict criteria.

### Blocked
- None. (Running tests would require checking out `2ab477f`; not done ÔÇö static review only per "NAO modifique codigo".)

## Next Move
1. Read `.agents/code-reviewer.md` and `AGENTS.md` (missing step required by the user prompt).
2. Optionally read `services/data-ingestion/src/instance-lock.test.ts` at `2ab477f` to confirm lock-file semantics before flagging severity.
3. Deliver final response ending with exact `VERDICT: REJEITADO` followed by findings, each tagged CRITICAL/HIGH/MEDIUM/LOW. Tentative findings: (HIGH) runtime lock file committed ÔÇö nondeterministic boot block if PID 19816 alive on any machine; (HIGH) zero tests for the rewired per-wallet WSS subscription path (helius-provider.ts) and rpc-provider change; (HIGH) reconnect path leaks open sockets/partial subscriptions (failed `sendLogsSubscribe` ÔåÆ `openSocket` rejected but socket stays open/`connected=true`; retry creates new socket via `this.ws` overwrite without closing old; no `logsUnsubscribe` ever sent; `subscriptionId` single field overwritten by last sub; `Promise.all` partial success leaves live server-side subs on a "failed" connection); (MEDIUM) fixture "10/10 vs Helius Enhanced" over-claimed ÔÇö match ignores source/user/mint/direction/amounts, null rows skipped, 14 errors silently swallowed, row 2 userÔëáfeePayer demonstrates uncaught divergence; (MEDIUM) BOM in `f15-fixtures-report.json` breaks `JSON.parse` (plus repo-root artifact, `helisMatch` typo); (LOW) dead ternary in fix-esm-imports.mjs and edge case `'./'` ÔåÆ `'.//index.js'`; (LOW) scripts use `console.error` (AGENTS.md no-console, deliberate stdout/stderr split); (LOW) `f15-backfill.mjs` lacks env guard for `SOLANA_RPC_HTTP` (fixtures script guards); (LOW) `f15-fixtures.mjs` no fetch timeout; `TARGET_AMM=2` hardcoded vs commit message "8 PumpSwap" (8 is incidental); `maxSupportedTransactionVersion: 1` is permissive/safe (verdict: LOW/non-issue).

## Relevant Files
- `packages/solana/src/helius-provider.ts`: per-wallet `sendLogsSubscribe`/`sendSingleSubscribe` rewrite ÔÇö core HIGH findings (no tests, socket/subscription leak, single `subscriptionId` field).
- `packages/solana/src/rpc-provider.ts`: `maxSupportedTransactionVersion` 0ÔåÆ1 change at `2ab477f` (working master version still shows 0).
- `data/locks/data-ingestion.lock`: committed runtime lock (PID 19816) ÔÇö boot-block risk; `data/locks/` missing from `.gitignore`.
- `services/data-ingestion/src/instance-lock.ts`: lock acquire/release semantics (CONFIG_ERROR on live PID).
- `services/data-ingestion/src/dedup-store.ts`: dedup key `(signature, instructionIndex, wallet)` ÔÇö absorbs per-wallet duplicate notifications.
- `services/data-ingestion/src/enricher.ts` / `enrich-runner.ts` / `enricher.test.ts`: APIs consumed by backfill/fixtures scripts; tests exist only for enricher here.
- `scripts/f15-fixtures.mjs`: weak Helius comparison, swallowed errors, `TARGET_AMM=2`, no timeout.
- `scripts/f15-backfill.mjs`: no env guard for `SOLANA_RPC_HTTP`.
- `f15-fixtures-report.json`: BOM-prefixed, root-level artifact, `helisMatch` typo, row-2 userÔëáfeePayer divergence.
- `packages/pumpfun/scripts/fix-esm-imports.mjs`: dead ternary, `'.//index.js'` edge case.
- `packages/database/src/index.ts`, `packages/database/src/repositories/observed-events.ts`, `packages/database/prisma/schema.prisma`: repo/schema wiring verified compatible with scripts.
- `packages/config/src/index.ts`: canonical env var names (`SOLANA_RPC_HTTP`, `HELIUS_API_KEY`, `INGESTION_WATCH_ADDRESSES`).
- `.agents/code-reviewer.md`, `AGENTS.md`: required reading, not yet performed.
Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.
[0mÔåÆ [0mRead .agents/code-reviewer.md
[0mÔåÆ [0mRead AGENTS.md
[0m
[0m$ [0mgit show 2ab477f:services/data-ingestion/src/instance-lock.test.ts | Select-Object -First 80
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import { mkdtempSync, rmSync, writeFileSync, existsSync } from 'node:fs';
import { spawn } from 'node:child_process';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { InstanceLock, isProcessAlive } from './instance-lock.js';

let dir: string;
let lockPath: string;

beforeEach(() => {
  dir = mkdtempSync(join(tmpdir(), 'ct-lock-'));
  lockPath = join(dir, 'test.lock');
});
afterEach(() => {
  rmSync(dir, { recursive: true, force: true });
});

describe('InstanceLock', () => {
  it('adquire e libera o lock', () => {
    const lock = new InstanceLock(lockPath);
    lock.acquire();
    expect(existsSync(lockPath)).toBe(true);
    lock.release();
    expect(existsSync(lockPath)).toBe(false);
  });

  it('rejeita segunda aquisi├º├úo quando o PID do lock est├í vivo', () => {
    // Spawna um processo real e vivo para garantir o caminho de rejei├º├úo.
    const child = spawn(process.execPath, ['-e', 'setInterval(() => {}, 1000)'], {
      detached: true,
      stdio: 'ignore',
    });
    try {
      child.unref();
      expect(child.pid).toBeDefined();
      writeFileSync(lockPath, `${child.pid}\n`);
      const lock = new InstanceLock(lockPath);
      expect(() => lock.acquire()).toThrowError(/CONFIG_ERROR/);
    } finally {
      if (child.pid) {
        try {
          process.kill(child.pid);
        } catch {
          /* processo j├í morto ÔÇö ok no teardown */
        }
      }
    }
  });

  it('lock stale (PID morto) ├® substitu├¡do', () => {
    // 4194303 ├® quase certamente inexistente
    writeFileSync(lockPath, '4194303\n');
    const lock = new InstanceLock(lockPath);
    lock.acquire();
    expect(existsSync(lockPath)).toBe(true);
    lock.release();
  });

  it('lock corrompido (n├úo-n├║mero) ├® substitu├¡do', () => {
    writeFileSync(lockPath, 'garbage\n');
    const lock = new InstanceLock(lockPath);
    lock.acquire();
    lock.release();
  });

  it('release n├úo remove lock de outro PID (seguran├ºa contra corrida)', () => {
    const lock = new InstanceLock(lockPath);
    lock.acquire();
    writeFileSync(lockPath, '99999\n'); // outro processo "roubou" o lock
    lock.release();
    expect(existsSync(lockPath)).toBe(true);
  });
});

describe('isProcessAlive', () => {
  it('nosso pr├│prio PID est├í vivo', () => {
    expect(isProcessAlive(process.pid)).toBe(true);
  });
  it('PID inexistente est├í morto', () => {
[0m
[0m$ [0m$c = git show 2ab477f:packages/solana/src/helius-provider.ts | Select-String -Pattern "openSocket|this\.ws|se
ndLogsSubscribe|subscribeWallets|scheduleReconnect|subscriptionId|Promise\.all|connected|lastMessageAt|close\(\)|logsUn
subscribe" | ForEach-Object { "$($_.LineNumber): $($_.Line.Trim())" }; $c
7: * (logsSubscribe / logsUnsubscribe), com filtro `mentions` por endere├ºo.
94: private subscriptionId: number | null = null;
99: private lastMessageAt = 0;
101: private connected = false;
109: this.wsFactory = options.webSocketFactory ?? ((url: string) => new WebSocket(url));
112: this.wsUrl = options.wsUrl;
114: this.wsUrl = `${HELIUS_WSS_BASE}?api-key=${encodeURIComponent(options.apiKey)}`;
117: this.wsUrl = SOLANA_PUBLIC_WSS;
139: return this.openSocket();
142: async subscribeWallets(wallets: string[]): Promise<void> {
145: throw new ClassifiedError('VALIDATION_ERROR', 'wallet inv├ílida em subscribeWallets');
149: if (this.connected) {
150: await this.sendLogsSubscribe();
154: async close(): Promise<void> {
157: const ws = this.ws;
158: this.ws = null;
159: this.connected = false;
160: this.subscriptionId = null;
165: ws.close();
177: private openSocket(): Promise<void> {
179: const ws = this.wsFactory(this.wsUrl);
180: this.ws = ws;
183: this.connected = true;
184: this.lastMessageAt = Date.now();
187: this.logger.info('wss conectado', { urlHost: safeHost(this.wsUrl) });
188: void this.sendLogsSubscribe()
195: if (!this.connected) {
204: this.lastMessageAt = Date.now();
207: this.connected = false;
208: this.subscriptionId = null;
211: this.scheduleReconnect();
219: private async sendLogsSubscribe(): Promise<void> {
220: if (this.wallets.size === 0 || this.ws === null) return;
223: // endereco (Promise.all; se alguma falhar, o connect falha de forma
226: await Promise.all(wallets.map((wallet) => this.sendSingleSubscribe(wallet)));
238: const ws = this.ws;
259: this.lastMessageAt = Date.now();
292: if (typeof parsed.result === 'number') this.subscriptionId = parsed.result;
294: subscriptionId: this.subscriptionId,
348: const ws = this.ws;
350: const silenceMs = Date.now() - this.lastMessageAt;
371: const ws = this.ws;
373: this.ws = null;
380: if (this.reconnectTimer === null) this.scheduleReconnect();
383: private scheduleReconnect(): void {
390: this.openSocket().catch((err: unknown) => {
394: this.scheduleReconnect();
[0m
Now let me pin exact line numbers for the key findings in the changed files:
[0m$ [0m$c = git show 2ab477f:scripts/f15-fixtures.mjs | Select-String -Pattern "TARGET_AMM|MAX_TRIES|helius\.type|SW
AP|errors|matchCount|local\.user|local\.mint|catch|source|fetch\(|sleep|helisMatch|MISMATCH|continue" | ForEach-Object 
{ "$($_.LineNumber): $($_.Line.Trim())" }; $c
10: const TARGET_AMM = 2;
11: const MAX_TRIES = 300;
17: const res = await fetch(`https://api-mainnet.helius-rpc.com/v0/transactions?api-key=${HELIUS_KEY}`, {
28: const res = await fetch(RPC, {
43: `SELECT signature FROM "ObservedEvent" WHERE "receivedAt" > now() - interval '30 minutes' ORDER BY random() LIMIT 
${MAX_TRIES}`,
47: const probes = { decoded: 0, undecodable: 0, errors: 0 };
52: while (i < bigRows.length && (results.length < N || ammOk < TARGET_AMM)) {
70: continue;
73: if (local.kind.startsWith('pumpswap')) ammOk++;
80: mint: local.mint,
81: user: local.user,
87: source: enhanced.source ?? null,
93: } catch (e) {
94: probes.errors++;
99: mismatch = 0;
101: if (!r.helius) continue;
102: const heliusSwap = r.helius.type === 'SWAP' || r.helius.type === 'SWAP_PARTIAL';
103: if (heliusSwap) match++;
104: else mismatch++;
109: pumpswapFixtures: ammOk,
112: helisMatch: match,
113: heliusMismatch: mismatch,
[0m
[0m$ [0m$c = git show 2ab477f:scripts/f15-backfill.mjs | Select-String -Pattern 
"SOLANA_RPC_HTTP|RPC|HELIUS|process\.env|url|undefined|throw|catch" | ForEach-Object { "$($_.LineNumber): 
$($_.Line.Trim())" }; $c
5: import { RpcProvider } from '../packages/solana/dist/index.js';
17: const rpc = new RpcProvider(
18: [{ name: 'primary', url: process.env.SOLANA_RPC_HTTP, tier: 'primary' }],
23: { getTransaction: (sig) => rpc.getTransaction(sig) },
[0m
[0m$ [0m$c = git show 2ab477f:packages/solana/src/helius-provider.ts | Select-String -Pattern 
"sendSingleSubscribe|pendingSubscribes|if \(typeof parsed|result === 'number'|else" -Context 2,6 | Select-Object 
-First 40; $c
if (options.wsUrl !== undefined) {
        this.wsUrl = options.wsUrl;
>     } else if (options.apiKey !== undefined && options.apiKey.length > 0) {
        this.wsUrl = `${HELIUS_WSS_BASE}?api-key=${encodeURIComponent(options.apiKey)}`;
>     } else {
        // ASSUMPTION: fallback no WSS p├║blico Solana ÔÇö mesmo formato de mensagem.
        this.wsUrl = SOLANA_PUBLIC_WSS;
        this.logger.warn('HELIUS_API_KEY ausente ÔÇö usando WSS p├║blico Solana (rate limits menores)');
      }
    }
  
      // audivel e a rotina de reconnect refaz).
      const wallets = [...this.wallets];
>     await Promise.all(wallets.map((wallet) => this.sendSingleSubscribe(wallet)));
    }
  
>   private sendSingleSubscribe(wallet: string): Promise<void> {
      return new Promise((resolve, reject) => {
        const id = this.nextRequestId++;
        const message = JSON.stringify({
          jsonrpc: '2.0',
          id,
          method: 'logsSubscribe',
            return;
          }
>         this.pendingSubscribes.set(id, { resolve, reject });
        });
      });
    }
  
>   private readonly pendingSubscribes = new Map<
      number,
      { resolve: () => void; reject: (e: Error) => void }
    >();
  
    private handleMessage(data: unknown): void {
      this.lastMessageAt = Date.now();
      if (typeof data === 'string') {
        text = data;
>     } else if (Buffer.isBuffer(data)) {
        text = data.toString('utf8');
>     } else if (Array.isArray(data)) {
        text = Buffer.concat(data).toString('utf8');
>     } else {
        this.logger.warn('wss mensagem de tipo inesperado descartada');
        return;
      }
  
      let parsed: JsonRpcNotification;
      try {
  
      // Resposta a logsSubscribe
>     if (parsed.id !== undefined && this.pendingSubscribes.has(Number(parsed.id))) {
>       const pending = this.pendingSubscribes.get(Number(parsed.id));
>       this.pendingSubscribes.delete(Number(parsed.id));
        if (parsed.error) {
          pending?.reject(
            new ClassifiedError(
              'RPC_ERROR',
              `logsSubscribe rejeitado: ${parsed.error.message}`,
            ),
          );
>       } else {
>         if (typeof parsed.result === 'number') this.subscriptionId = parsed.result;
          this.logger.info('logsSubscribe ativo', {
            subscriptionId: this.subscriptionId,
            wallets: this.wallets.size,
          });
          pending?.resolve();
        }
        this.reconnectTimer = null;
      }
>     this.failPendingSubscribes();
    }
  
    /**
     * Rejeita todas as subscri├º├Áes pendentes. Sem isso, um socket fechado
     * antes da confirma├º├úo deixaria promises pendentes para sempre (leak).
     */
>   private failPendingSubscribes(): void {
>     if (this.pendingSubscribes.size === 0) return;
      const err = new ClassifiedError(
        'RPC_ERROR',
        'socket fechado antes da confirma├º├úo de logsSubscribe',
      );
>     for (const { reject } of this.pendingSubscribes.values()) reject(err);
>     this.pendingSubscribes.clear();
    }
  }
  
  /** Extrai host sem expor api-key em logs (seguran├ºa ┬º2). */
  function safeHost(url: string): string {
    try {
[0m
## VEREDITO: REJEITADO

## ACHADOS

**HIGH-1 ÔÇö `packages/solana/src/helius-provider.ts` (arquivo inteiro, reformulado no commit): c├│digo novo sem nenhum teste.**
- Evid├¬ncia: `git ls-tree -r 2ab477f` mostra que `packages/solana/src/` cont├®m apenas `backoff.test.ts`; n├úo existe `helius-provider.test.ts` nem `rpc-provider.test.ts` em nenhum diret├│rio. O commit reescreve exatamente o caminho mais cr├¡tico do F1.5 (subscriptions WSS por wallet) sem nenhum teste unit├írio/property.
- Reproduzir: `npm test` ÔåÆ nenhum caso exercita `subscribeWallets`, `sendLogsSubscribe`, reconnect, partial failure ou timeout.
- Crit├®rio: `.agents/code-reviewer.md` ÔÇö "Aus├¬ncia de testes em c├│digo novo ÔåÆ REJEITADO" + AGENTS.md ┬º4.5/┬º6.
- Sugest├úo: testes com WebSocket fake (wsFactory j├í injet├ível): uma wallet v├ílida ÔåÆ 1├ù `logsSubscribe` com 1 mention; N wallets ÔåÆ N chamadas; sub rejeitada ÔåÆ conex├úo falha e socket anterior ├® fechado; mensagem de erro RPC ÔåÆ promise rejeitada com `RPC_ERROR`; closed-socket antes de ack ÔåÆ `failPendingSubscribes`; reconnect n├úo duplica subscriptions server-side.

**HIGH-2 ÔÇö `packages/solana/src/helius-provider.ts:177-180,188-190,371-394` ÔÇö leak de socket + amplifica├º├úo de subscriptions em falha parcial; sem `logsUnsubscribe` em lugar nenhum.**
- Evid├¬ncia: em `onOpen` (`L183-190`), `connected=true` antes do `sendLogsSubscribe().catch(reject)`. `sendLogsSubscribe` usa `Promise.all` (`L226`): se UMA wallet falha (ex.: address malformatado que passou na valida├º├úo `L145`, que s├│ checa `length === 0`), TODAS as demais subscriptions **j├í confirmadas server-side** ficam vivas, mas `openSocket()` rejeita ÔåÆ `scheduleReconnect` (`L390`) ÔåÆ `openSocket()` cria NOVO `this.ws = ws` (`L180`) **sem fechar o socket anterior**. O socket ├│rf├úo continua aberto, com subscriptions ativas, processando mensagens via `handleMessage`. Cada ciclo de reconnect re-subscribe **todas** as wallets no socket novo (duplica├º├úo), e nenhum `logsUnsubscribe` ├® enviado (`grep logsUnsubscribe` ÔåÆ zero ocorr├¬ncias fora do coment├írio L7). Quando o ├│rf├úo eventualmente fecha (timeout de inatividade Helius ~10min, sem ping do nosso lado), o handler `close` (`L205-211`) seta `connected=false` no socket **saud├ível** e dispara mais um reconnect esp├║rio.
- Reproduzir: configurar `INGESTION_WATCH_ADDRESSES` com 1 endere├ºo inv├ílido de formato (ex.: `abc`) + 9 v├ílidos; observar: connect falha, loop infinito com backoff, socket ├│rf├úo acumulando, volume de notifications duplicado (dedup de DB absorve, mas quota de conex├Áes Helius esgota).
- Impacto concreto: plano Free Helius = **5 conex├Áes simult├óneas**; ap├│s ~5 ciclos de falha, `connection refused` permanente ÔåÆ data-ingestion fora do ar at├® restart. Sem watchdog que detecte (heartbeat pings o socket atual, nunca o ├│rf├úo).
- Sugest├úo: fechar o socket anterior (`oldWs.close()`) antes de recriar; falhar o connect ANTES de deixar subscriptions parciais (ou aceitar partial success com rastreamento por-wallet); enviar `logsUnsubscribe` de ids antigos; validar formado de endere├ºo (zod/PublicKey) em `subscribeWallets`; teto de reconex├Áes com estado terminal + alerta.

**HIGH-3 ÔÇö `data/locks/data-ingestion.lock` (commitado): artefato de runtime no reposit├│rio; boot n├úo-determin├¡stico bloqueado.**
- Evid├¬ncia: arquivo `19816\n1790291576311` commitado. `instance-lock.ts`/`instance-lock.test.ts` (`rejeita segunda aquisi├º├úo quando o PID do lock est├í vivo`): `acquire()` l├¬ PID, e se `isProcessAlive(PID)` ÔåÆ lan├ºa `CONFIG_ERROR: j├í existe inst├óncia ativa (PID 19816)`. `data/locks/` N├âO est├í no `.gitignore` (que ignora `data/raw/` e `data/local/` apenas).
- Reproduzir: em qualquer m├íquina onde exista um processo com PID 19816 (reuso de PID no Windows ├® comum), iniciar o servi├ºo ÔåÆ `CONFIG_ERROR`, recusa a subir. Na m├íquina do commit, se o PID estava vivo no momento do commit, o servi├ºo j├í n├úo sobe at├® o PID morrer.
- Sugest├úo: remover o arquivo do repo, adicionar `data/locks/` ao `.gitignore`, e (defensivo) tratar lock stale por idade, n├úo s├│ por PID morto.

**MEDIUM-1 ÔÇö `scripts/f15-fixtures.mjs:93-94` (e `:70`) ÔÇö erro engolido, sem log.**
- Evid├¬ncia: `catch (e) { probes.errors++; }` ÔÇö descarta o erro sem logar causa; rows com `enhanced` null s├úo pulados (`continue` L70) sem contabilizar mismatch. Viola AGENTS.md ┬º9 (nunca `catch` que s├│ conta) e a classifica├º├úo de erros (`UNKNOWN_ERROR` n├úo ├® observ├ível).
- Reproduzir: rodar com RPC indispon├¡vel ÔÇö `errors` cresce mas o script n├úo diz por qu├¬; relat├│rio "10 / 10 vs Helius Enhanced" gerado mesmo com 14 erros.
- Sugest├úo: logar `e` por linha (via `@ct/logging` ou console com estrutura), e reportar `undecodable`/`errors` com amostras no JSON.

**MEDIUM-2 ÔÇö `scripts/f15-fixtures.mjs:99-104` ÔÇö claim "10/10 vs Helius Enhanced" superestimado: match s├│ compara `helius.type`.**
- Evid├¬ncia: o match (`L102-103`) verifica apenas `helius.type === 'SWAP' || 'SWAP_PARTIAL'`. N├úo compara `source`, `user`, `mint`, `direction`, `counterparty`, valores. Contraevid├¬ncia concreta no pr├│prio `f15-fixtures-report.json` commitado: row 2 tem `local.user=BwWK17cbHxwWBKZkUYvzxLcNQ1YVyaFezduWbtm2de6s` vs `helius.feePayer=Gygj9QQby4j2jryqyqBHvLP7ctv2SaANgh4sCb69BUpA` ÔÇö diverg├¬ncia real n├úo sinalizada, e `helisMatch=10, heliusMismatch=0`.
- Reproduzir: comparar campo a campo rows 1..10 do relat├│rio commitado ÔÇö a diverg├¬ncia acima prova que o "match" ignora atores da transa├º├úo.
- Sugest├úo: comparar `user`(== feePayer), `counterparty`, `mint`, `direction`(buy/sell), e ao menos faixa de valores; tratar diverg├¬ncia como mismatch com `error` detalhado. Relat├│rio n├úo deve afirmar "vs Helius Enhanced" sem essas compara├º├Áes.

**MEDIUM-3 ÔÇö `scripts/f15-fixtures.mjs:10` ÔÇö `TARGET_AMM = 2`, mas commit e relat├│rio reportam 8 PumpSwap; `:17` fetch sem timeout.**
- Evid├¬ncia: `L10 const TARGET_AMM = 2`; relat├│rio commitado `"pumpswapFixtures": 8`. O 8 n├úo ├® alvo, ├® incidental (loop para quando `results.length >= N` OU `ammOk >= TARGET_AMM`; L52). `fetch` (`L17`, `L28`) sem `AbortSignal.timeout` ÔÇö se o RPC travar, o script pendura para sempre (o `MAX_TRIES` s├│ limita as linhas de DB).
- Reproduzir: RPC que nunca responde ÔåÆ script eterno; metas divergentes: commit message "8 PumpSwap + 2 curve" vs c├│digo "2".
- Sugest├úo: tornar `TARGET_AMM`/`N` flags de CLI (ou env+zod), alinhar commit message ao valor real, adicionar timeout em ambos os fetch.

**LOW-1 ÔÇö `packages/pumpfun/scripts/fix-esm-imports.mjs` ÔÇö tern├írio morto e edge case `'./'`.**
- Evid├¬ncia: `spec === '.' || spec === '..' ? spec + '/' : spec + '/'` ÔÇö os dois bra├ºos s├úo id├¬nticos. Para `spec === './'` ÔåÆ resultado `'.//index.js'` (barra dupla).
- Reproduzir: rodar script num pacote com import `from './'`.
- Sugest├úo: remover o tern├írio; tratar `./`/`.`/`..` explicitamente com join can├┤nico.

**LOW-2 ÔÇö `scripts/f15-backfill.mjs:18` ÔÇö `process.env.SOLANA_RPC_HTTP` sem guard.**
- Evid├¬ncia: f15-fixtures.mjs valida `RPC`/`HELIUS_KEY` antes de usar; backfill passa `url: process.env.SOLANA_RPC_HTTP` direto ao `RpcProvider`. Sem env ÔåÆ `url` undefined ÔåÆ falha em runtime com erro n├úo-classificado.
- Reproduzir: `node scripts/f15-backfill.mjs` sem `.env` ÔåÆ erro obscuro de tipo.
- Sugest├úo: mesmo guard do fixtures (exit com mensagem clara se env ausente) + validar via `@ct/config`.

**LOW-3 ÔÇö `scripts/f15-fixtures.mjs:112` ÔÇö typo `helisMatch` no JSON (vs `heliusMismatch`); `f15-fixtures-report.json` commitado na raiz.**
- Evid├¬ncia: L112 `helisMatch: match`. Relat├│rio ├® artefato gerado (runtime) commitado na raiz do repo.
- Sugest├úo: corrigir o nome da chave; mover relat├│rios para `data/fixtures/` ou ignorar no `.gitignore` (commitar s├│ fixtures pequenas ├® defens├ível, mas em caminho padronizado).

**LOW-4 ÔÇö `packages/solana/src/rpc-provider.ts` ÔÇö `maxSupportedTransactionVersion 0ÔåÆ1`.** Mudan├ºa correta quanto ├á dire├º├úo (1 aceita legacy+v0+v1; confirmado nas docs Helius/Solana ÔÇö vers├úo 1 existe no runtime). Requer teste (coberto no HIGH-1). Sem achado funcional.

## TESTES EXECUTADOS (tentativas de quebra)

1. `git ls-tree` em `2ab477f`: invent├írio completo de testes ÔÇö confirmado: zero testes para `helius-provider`/`rpc-provider`; apenas `backoff.test.ts` no pacote solana. ÔåÆ REJEITA (crit├®rio objetivo).
2. Leitura de `instance-lock.test.ts` em `2ab477f`: confirma sem├óntica `isProcessAlive(PID)` ÔåÆ `CONFIG_ERROR` (reproduz o HIGH-3). Teste "lock stale substitu├¡do" mostra que s├│ PID vivo bloqueia ÔÇö exatamente o caso do lock commitado.
3. Hexdump do blob `f15-fixtures-report.json`: conte├║do real lido byte a byte; divergence row 2 (userÔëáfeePayer) verificada; `pumpswapFixtures: 8` vs `TARGET_AMM=2` verificada; chave `helisMatch` confirmada (nota: sem BOM ÔÇö o artefato anterior da leitura era rendering, corrigido aqui).
4. Buscas dirigidas no diff: `logsUnsubscribe` (zero usos), `sendSingleSubscribe`/`Promise.all`/reconnect paths com n├║meros de linha exatos.
5. Docs Helius consultados (MCP): `mentions` = 1 pubkey por chamada ÔåÆ mudan├ºa por-wallet ├® CORRETA (FACT validado); limite de conex├Áes por plano (Free=5) ÔåÆ agrava HIGH-2; m├║ltiplas subscriptions por conex├úo OK.

## O QUE N├âO FOI POSS├ìVEL VERIFICAR

- Se o PID 19816 est├í vivo em alguma m├íquina hoje (n├úo consult├ível) ÔÇö HIGH-3 n├úo depende disso para ser v├ílido: o artefato n├úo deveria existir no repo.
- Comportamento real do WSS Helius ante `mentions` inv├ílido (erro por sub ou por conex├úo) ÔÇö agrava ou atenua HIGH-2; exige teste com backend fake + (opcional) teste de integra├º├úo com conta Dev Helius.
- `f15-fixtures-report.json` s├│ foi lido em blob est├ítico; os 10 rows n├úo foram re-executados contra o RPC (sem network calls por pol├¡tica de review ÔÇö fica como teste a cargo do implementador).
- N├║mero real de wallets monitoradas em produ├º├úo (`INGESTION_WATCH_ADDRESSES` n├úo tem cap no config) ÔÇö se N > limite de subscriptions/conex├úo do plano, agrava HIGH-2.

## Nota de coordena├º├úo

Veredito N├âO commitado (conforme regra 1 das regras duras ÔÇö revisor n├úo commita na branch do produtor). Devolver ao especialista via orchestrator: corrigir HIGH-1/2/3 e re-commitar para nova revis├úo.

