# Review df980637e823da5b804d18efab1a9a92b8e02562

- Branch: sprint/f1-security-fixes-2
- Data: 2026-09-23 20:51:51
- Gates: lint=0 typecheck=0 test=0 (0 = ok)
- Veredito: **APROVADO**

## Saida do code-reviewer

opencode.exe : [0m
No C:\Users\enzo\AppData\Roaming\npm\opencode.ps1:14 caractere:3
+   & "$basedir/node_modules/opencode-ai/bin/opencode.exe"   $args
+   ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : NotSpecified: ([0m:String) [], RemoteException
    + FullyQualifiedErrorId : NativeCommandError
 
> code-reviewer ┬À big-pickle
[0m
[0m$ [0mgit show df980637e823da5b804d18efab1a9a92b8e02562
commit df980637e823da5b804d18efab1a9a92b8e02562
Author: EznRB <EznRB@users.noreply.github.com>
Date:   Wed Sep 23 20:49:02 2026 -0300

    fix(security): elimina byte NUL do teste (fromCharCode) - arquivo 100% UTF-8 texto

diff --git a/services/data-ingestion/src/security-fixes.test.ts b/services/data-ingestion/src/security-fixes.test.ts
index 48b9e33..9c05b75 100644
Binary files a/services/data-ingestion/src/security-fixes.test.ts and 
b/services/data-ingestion/src/security-fixes.test.ts differ
[0m
[0m$ [0mgit log --oneline -5; git status; git branch --show-current
99d0432 review(sprint/f1-security-fixes-2): APROVADO ed0fc2095bbb9c924cef0b3c62dfc63fa600aee1
9ece7b8 docs(handoff): incidente race no merge gate + plano de fechamento do RT-005
676bc50 fix(ops): merge-sprint anti-race (pull antes de verificar + ultimo veredito + state.json rejected)
c1430a4 merge: sprint/f1-security-fixes (todos os commits aprovados em docs/reviews/)
755cbd1 docs(sec): findings RT-004/005/006 + sprint 2 PoCs (red-team)
On branch master
Your branch is ahead of 'origin/master' by 1 commit.
  (use "git push" to publish your local commits)

nothing to commit, working tree clean
master
[0m
[0m$ [0mgit show df980637e823da5b804d18efab1a9a92b8e02562 --stat; git cat-file -p 
df980637e823da5b804d18efab1a9a92b8e02562 | head -20
commit df980637e823da5b804d18efab1a9a92b8e02562
Author: EznRB <EznRB@users.noreply.github.com>
Date:   Wed Sep 23 20:49:02 2026 -0300

    fix(security): elimina byte NUL do teste (fromCharCode) - arquivo 100% UTF-8 texto

 services/data-ingestion/src/security-fixes.test.ts | Bin 9388 -> 9158 bytes
 1 file changed, 0 insertions(+), 0 deletions(-)
head : O termo 'head' n´┐¢o ´┐¢ reconhecido como nome de cmdlet, fun´┐¢´┐¢o, arquivo de script ou programa oper´┐¢vel. 
Verifique 
a grafia do nome ou, se um caminho tiver sido inclu´┐¢do, veja se o caminho est´┐¢ correto e tente novamente.
No linha:1 caractere:118
+ ... ; git cat-file -p df980637e823da5b804d18efab1a9a92b8e02562 | head -20
+                                                                  ~~~~
    + CategoryInfo          : ObjectNotFound: (head:String) [], CommandNotFoundException
    + FullyQualifiedErrorId : CommandNotFoundException
[0m
[0m$ [0mgit log --oneline --all -- services/data-ingestion/src/security-fixes.test.ts | head -20
head : O termo 'head' n´┐¢o ´┐¢ reconhecido como nome de cmdlet, fun´┐¢´┐¢o, arquivo de script ou programa oper´┐¢vel. 
Verifique 
a grafia do nome ou, se um caminho tiver sido inclu´┐¢do, veja se o caminho est´┐¢ correto e tente novamente.
No linha:1 caractere:81
+ ... -all -- services/data-ingestion/src/security-fixes.test.ts | head -20
+                                                                  ~~~~
    + CategoryInfo          : ObjectNotFound: (head:String) [], CommandNotFoundException
    + FullyQualifiedErrorId : CommandNotFoundException
[0m
[0m$ [0mgit log --oneline --all -- services/data-ingestion/src/security-fixes.test.ts | Select-Object -First 20
df98063 fix(security): elimina byte NUL do teste (fromCharCode) - arquivo 100% UTF-8 texto
ed0fc20 fix(security): review 19ffacc - propagacao de erro a waiters (sem cadeia de lideranca), cleanup inFlight com 
self-check, testes dos caminhos de erro, testes reescritos em UTF-8 limpo
ea88b46 fix(security): RT-004 limites de tamanho no schema + RT-006 allowlist base58 na signature
[0m
[0m$ [0mgit show df980637e823da5b804d18efab1a9a92b8e02562:services/data-ingestion/src/security-fixes.test.ts | 
Out-File -Encoding utf8 "$env:TEMP\cur-test.txt"; Get-Item "$env:TEMP\cur-test.txt" | Select-Object Length
Length
------
  9398
[0m
[0m$ [0m$cur = git show df980637e823da5b804d18efab1a9a92b8e02562:services/data-ingestion/src/security-fixes.test.ts; 
$parent = git rev-parse df980637e823da5b804d18efab1a9a92b8e02562^; git show 
"${parent}:services/data-ingestion/src/security-fixes.test.ts" | Out-Null; Write-Output "parent=$parent"
parent=ed0fc2095bbb9c924cef0b3c62dfc63fa600aee1
[0m
[0m$ [0mgit branch --contains df980637e823da5b804d18efab1a9a92b8e02562; git ls-tree 
df980637e823da5b804d18efab1a9a92b8e02562 services/data-ingestion/src/security-fixes.test.ts; git ls-tree 
ed0fc2095bbb9c924cef0b3c62dfc63fa600aee1 services/data-ingestion/src/security-fixes.test.ts
+ sprint/f1-security-fixes-2
100644 blob 9c05b753ae16ffc5a60dec0ba32ba2b38c849cca	services/data-ingestion/src/security-fixes.test.ts
100644 blob 48b9e3379250abb25ab6bc4b9f5dc5d516713ecf	services/data-ingestion/src/security-fixes.test.ts
[0m
[0m$ [0mcmd /c "git cat-file blob c33f437eeee99cbb7e04bf5d6cf9d8dfb5ec19c4 > 
C:\Users\enzo\AppData\Local\Temp\opencode\old-test.bin" 2>$null; Write-Output "---"; cmd /c "git cat-file blob 
8a6f6e4e8e9e9 > C:\Users\enzo\AppData\Local\Temp\opencode\old-test.bin
A cadeia de caracteres n´┐¢o tem o terminador: ".
    + CategoryInfo          : ParserError: (:) [], ParentContainsErrorRecordException
    + FullyQualifiedErrorId : TerminatorExpectedAtEndOfString
[0m
[0m$ [0mcmd /c "git cat-file blob 9c05b753ae16ffc5a60dec0ba32ba2b38c849cca > 
C:\Users\enzo\AppData\Local\Temp\opencode\new-test.bin" ; cmd /c "git cat-file blob 
48b9e3379250abb25ab6bc4b9f5dc5d516713ecf > C:\Users\enzo\AppData\Local\Temp\opencode\old-test.bin" ; Get-Item 
C:\Users\enzo\AppData\Local\Temp\opencode\new-test.bin, C:\Users\enzo\AppData\Local\Temp\opencode\old-test.bin | 
Select-Object Name, Length
Name         Length
----         ------
new-test.bin   9158
old-test.bin   9388
[0m
[0mÔ£ù [0mRead C:/Users/enzo/AppData/Local/Temp/opencode/new-test.bin failed
[91m[1mError: [0mCannot read binary file: C:\Users\enzo\AppData\Local\Temp\opencode\new-test.bin
[0m
[0m$ [0m$b = [System.IO.File]::ReadAllBytes("C:\Users\enzo\AppData\Local\Temp\opencode\new-test.bin"); $nul = @($b | 
Where-Object { $_ -eq 0 }).Count; Write-Output "new-test: total=$($b.Length) nul=$nul"; $b2 = 
[System.IO.File]::ReadAllBytes("C:\Users\enzo\AppData\Local\Temp\opencode\old-test.bin"); $nul2 = @($b2 | Where-Object 
{ $_ -eq 0 }).Count; Write-Output "old-test: total=$($b2.Length) nul=$nul2"
new-test: total=9158 nul=0
old-test: total=9388 nul=1
[0m
[0m$ [0mgit diff --no-index --text "C:\Users\enzo\AppData\Local\Temp\opencode\old-test.bin" 
"C:\Users\enzo\AppData\Local\Temp\opencode\new-test.bin"; Write-Output "EXIT=$LASTEXITCODE"
warning: in the working copy of 'C:\Users\enzo\AppData\Local\Temp\opencode\new-test.bin', LF will be replaced by CRLF 
the next time Git touches it
diff --git "a/C:\\Users\\enzo\\AppData\\Local\\Temp\\opencode\\old-test.bin" 
"b/C:\\Users\\enzo\\AppData\\Local\\Temp\\opencode\\new-test.bin"
index 48b9e33..9c05b75 100644
--- "a/C:\\Users\\enzo\\AppData\\Local\\Temp\\opencode\\old-test.bin"
+++ "b/C:\\Users\\enzo\\AppData\\Local\\Temp\\opencode\\new-test.bin"
@@ -1,236 +1,234 @@
-/**
- * Testes dos fixes de seguranca pos-F1 (sprints f1-security-fixes[-2]).
- * Reproduzem os PoCs do red-team: docs/security/red-team/poc/sprint2-ingestion.poc.test.ts
- *
- * RT-004 - limites de tamanho (DoS memoria/DB)
- * RT-005 - dedup concorrente thread-safe (contadores fieis) + caminhos de erro
- *          (review 19ffacc, achados 2/3/4)
- * RT-006 - signature com allowlist base58 (anti log injection)
- *
- * NOTA: este arquivo e ASCII-only de proposito (achado 6 do review: versao
- * anterior foi commitada com encoding corrompido e o git a tratava como binario).
- */
-import { describe, it, expect } from 'vitest';
-import fc from 'fast-check';
-import { parseRawNotification, boundedJson, SIGNATURE_REGEX } from './schemas.js';
-import { DedupStore, type ObservedEventInsertData } from './dedup-store.js';
-import { dedupKey } from '@ct/types';
-
-const VALID_SIG = '5'.repeat(88); // base58 valido, 88 chars
-const VALID_WALLET = '7'.repeat(44);
-
-function raw(overrides: Record<string, unknown> = {}) {
-  return { signature: VALID_SIG, slot: 123, wallet: VALID_WALLET, ...overrides };
-}
-
-function fakeRepo(latencyMs = 5) {
-  const rows = new Map<string, ObservedEventInsertData>();
-  let createCalls = 0;
-  return {
-    rows,
-    get createCalls() {
-      return createCalls;
-    },
-    async create(args: { data: ObservedEventInsertData }) {
-      createCalls++;
-      const key = dedupKey({
-        signature: args.data.signature,
-        instruction_index: args.data.instructionIndex,
-        wallet: args.data.wallet,
-      });
-      // Latencia antes da checagem: maximiza a janela de corrida entre waiters.
-      await new Promise((r) => setTimeout(r, latencyMs));
-      if (rows.has(key)) {
-        const err = new Error('Unique constraint failed');
-        (err as Error & { code: string }).code = 'P2002';
-        throw err;
-      }
-      rows.set(key, args.data);
-      return {};
-    },
-  };
-}
-
-const record = (sig = VALID_SIG, w = VALID_WALLET, i = 0): ObservedEventInsertData => ({
-  signature: sig,
-  instructionIndex: i,
-  wallet: w,
-  eventType: 'UNKNOWN',
-});
-
-const key1 = (): string => `${VALID_SIG}:0:${VALID_WALLET}`;
-
-describe('RT-004 - limites de tamanho no schema de ingestao', () => {
-  it('signature gigante (10MB) e rejeitada', () => {
-    expect(parseRawNotification(raw({ signature: '5'.repeat(10_000_000) })).success).toBe(false);
-  });
-
-  it('signature valida passa', () => {
-    expect(parseRawNotification(raw()).success).toBe(true);
-  });
-
-  it('wallet gigante e rejeitada', () => {
-    expect(parseRawNotification(raw({ wallet: '7'.repeat(100_000) })).success).toBe(false);
-  });
-
-  it('slot astronomico e rejeitado', () => {
-    expect(parseRawNotification(raw({ slot: Number.MAX_SAFE_INTEGER })).success).toBe(false);
-  });
-
-  it('payload gigante e truncado pela serializacao limitada', () => {
-    const big = { blob: 'x'.repeat(100_000) };
-    const out = boundedJson(big);
-    const parsed = JSON.parse(out) as Record<string, unknown>;
-    expect(parsed['_truncated']).toBe(true);
-    expect(out.length).toBeLessThan(9_000);
-  });
-});
-
-describe('RT-006 - signature com allowlist base58', () => {
-  it('caractere de controle (\\u0000) rejeitado', () => {
-    expect(parseRawNotification(raw({ signature: VALID_SIG.slice(0, 87) + ' ' })).success).toBe(
-      false,
-    );
-  });
-
-  it('RTL override (\\u202E) rejeitado', () => {
-    const sig = VALID_SIG.slice(0, 87) + String.fromCharCode(0x202e);
-    expect(sig.charCodeAt(87)).toBe(0x202e);
-    expect(parseRawNotification(raw({ signature: sig })).success).toBe(false);
-  });
-  it('emoji / multibyte rejeitado', () => {
-    const sig = VALID_SIG.slice(0, 85) + '\uD83D\uDE80';
-    expect(sig.length).toBe(87);
-    expect(parseRawNotification(raw({ signature: sig })).success).toBe(false);
-  });
-
-  it('caracteres fora do alphabet base58 (0, O, I, l) rejeitados', () => {
-    expect(SIGNATURE_REGEX.test('0'.repeat(88))).toBe(false);
-    expect(SIGNATURE_REGEX.test('O'.repeat(88))).toBe(false);
-    expect(SIGNATURE_REGEX.test('I'.repeat(88))).toBe(false);
-    expect(SIGNATURE_REGEX.test('l'.repeat(88))).toBe(false);
-  });
-
-  it('property: toda string aceita e base58 puro de 64-88', () => {
-    fc.assert(
-      fc.property(fc.string({ minLength: 0, maxLength: 200 }), (s) => {
-        const ok = parseRawNotification(raw({ signature: s })).success;
-        if (ok) expect(SIGNATURE_REGEX.test(s)).toBe(true);
-      }),
-    );
-  });
-});
-
-describe('RT-005 - dedup sob concorrencia (contadores fieis)', () => {
-  it('burst de 50 chamadas simultaneas com a mesma key: ingested=1, deduplicated=49', async () => {
-    const repo = fakeRepo();
-    const store = new DedupStore(repo);
-    const outcomes = await Promise.all(
-      Array.from({ length: 50 }, () => store.checkAndPersist(key1(), record())),
-    );
-    expect(repo.rows.size).toBe(1);
-    expect(repo.createCalls).toBe(1);
-    expect(store.counters.ingested).toBe(1);
-    expect(store.counters.deduplicated).toBe(49);
-    expect(store.counters.errors).toBe(0);
-    expect(store.counters.joined).toBe(0);
-    expect(outcomes.filter((o) => o.status === 'new')).toHaveLength(1);
-    expect(outcomes.filter((o) => o.status === 'duplicate')).toHaveLength(49);
-    expect(store.inFlightSize).toBe(0);
-  });
-
-  it('property: bursts arbitrarios concorrentes nunca inflam contadores', async () => {
-    await fc.assert(
-      fc.asyncProperty(fc.integer({ min: 1, max: 30 }), async (burst) => {
-        const repo = fakeRepo();
-        const store = new DedupStore(repo);
-        await Promise.all(
-          Array.from({ length: burst }, () => store.checkAndPersist(key1(), record())),
-        );
-        expect(repo.createCalls).toBe(1);
-        expect(store.counters.ingested).toBe(1);
-        expect(store.counters.deduplicated).toBe(burst - 1);
-        expect(store.counters.errors).toBe(0);
-        expect(store.inFlightSize).toBe(0);
-      }),
-    );
-  });
-
-  it('sequencial continua correto (regressao)', async () => {
-    const repo = fakeRepo();
-    const store = new DedupStore(repo);
-    const r1 = await store.checkAndPersist(key1(), record());
-    const r2 = await store.checkAndPersist(key1(), record());
-    expect(r1.status).toBe('new');
-    expect(r2.status).toBe('duplicate');
-  });
-});
-
-describe('RT-005 - caminhos de erro (review 19ffacc, achados 2/3/4)', () => {
-  it('(a) lider falha + burst: 1 so tentativa de DB, erro propagado a todos, sem cadeia de retries', async () => {
-    let createCalls = 0;
-    const failingRepo = {
-      async create(): Promise<unknown> {
-        createCalls++;
-        await new Promise((r) => setTimeout(r, 10));
-        throw new Error('timeout de rede');
-      },
-    };
-    const store = new DedupStore(failingRepo);
-    const outcomes = await Promise.all(
-      Array.from({ length: 10 }, () => store.checkAndPersist(key1(), record())),
-    );
-    // Sem cadeia de liderancas: UMA unica ida ao DB.
-    expect(createCalls).toBe(1);
-    expect(store.counters.errors).toBe(1);
-    expect(store.counters.joined).toBe(9);
-    expect(store.counters.ingested).toBe(0);
-    expect(store.counters.deduplicated).toBe(0);
-    expect(outcomes.every((o) => o.status === 'error')).toBe(true);
-    expect(store.inFlightSize).toBe(0);
-  });
-
-  it('(b) sync-throw do repo.create nao envenena o inFlight: 2a chamada resolve', async () => {
-    let calls = 0;
-    // Repo com throw SINCRONO (contrato nao garante async) - cenario do achado 3.
-    const syncThrowRepo = {
-      create(_args: { data: ObservedEventInsertData }): Promise<unknown> {
-        calls++;
-        throw new Error('boom sincrono');
-      },
-    };
-    const store = new DedupStore(syncThrowRepo);
-    const r1 = await store.checkAndPersist(key1(), record());
-    expect(r1.status).toBe('error');
-    // Drain de microtasks para o cleanup protegido rodar.
-    await Promise.resolve();
-    await Promise.resolve();
-    expect(store.inFlightSize).toBe(0);
-    // 2a chamada DEVE resolver (nao travar em recursao de join).
-    const r2 = (await Promise.race([
-      store.checkAndPersist(key1(), record()),
-      new Promise((_, rej) => setTimeout(() => rej(new Error('HANG')), 500)),
-    ])) as { status: string };
-    expect(r2.status).toBe('error');
-    expect(calls).toBe(2);
-  });
-
-  it('(c) P2002 no lider com waiters: todos duplicate, 1 remember, 0 errors', async () => {
-    const repo = fakeRepo(10);
-    // Pre-semeia a key no repo: todo insert vira P2002.
-    repo.rows.set(key1(), record());
-    const store = new DedupStore(repo);
-    const outcomes = await Promise.all(
-      Array.from({ length: 8 }, () => store.checkAndPersist(key1(), record())),
-    );
-    expect(outcomes.every((o) => o.status === 'duplicate')).toBe(true);
-    expect(store.counters.deduplicated).toBe(8);
-    expect(store.counters.errors).toBe(0);
-    expect(repo.createCalls).toBe(1); // waiters nunca tocam o DB
-    // `seen` contem a key uma unica vez (Set) ÔÇö remember idempotente.
-    const again = await store.checkAndPersist(key1(), record());
-    expect(again.status).toBe('duplicate'); // agora via cache fast-path
-    expect(repo.createCalls).toBe(1);
-    expect(store.inFlightSize).toBe(0);
-  });
-});
+/**
+ * Testes dos fixes de seguranca pos-F1 (sprints f1-security-fixes[-2]).
+ * Reproduzem os PoCs do red-team: docs/security/red-team/poc/sprint2-ingestion.poc.test.ts
+ *
+ * RT-004 - limites de tamanho (DoS memoria/DB)
+ * RT-005 - dedup concorrente thread-safe (contadores fieis) + caminhos de erro
+ *          (review 19ffacc, achados 2/3/4)
+ * RT-006 - signature com allowlist base58 (anti log injection)
+ *
+ * NOTA: este arquivo e ASCII-only de proposito (achado 6 do review: versao
+ * anterior foi commitada com encoding corrompido e o git a tratava como binario).
+ */
+import { describe, it, expect } from 'vitest';
+import fc from 'fast-check';
+import { parseRawNotification, boundedJson, SIGNATURE_REGEX } from './schemas.js';
+import { DedupStore, type ObservedEventInsertData } from './dedup-store.js';
+import { dedupKey } from '@ct/types';
+
+const VALID_SIG = '5'.repeat(88); // base58 valido, 88 chars
+const VALID_WALLET = '7'.repeat(44);
+
+function raw(overrides: Record<string, unknown> = {}) {
+  return { signature: VALID_SIG, slot: 123, wallet: VALID_WALLET, ...overrides };
+}
+
+function fakeRepo(latencyMs = 5) {
+  const rows = new Map<string, ObservedEventInsertData>();
+  let createCalls = 0;
+  return {
+    rows,
+    get createCalls() {
+      return createCalls;
+    },
+    async create(args: { data: ObservedEventInsertData }) {
+      createCalls++;
+      const key = dedupKey({
+        signature: args.data.signature,
+        instruction_index: args.data.instructionIndex,
+        wallet: args.data.wallet,
+      });
+      // Latencia antes da checagem: maximiza a janela de corrida entre waiters.
+      await new Promise((r) => setTimeout(r, latencyMs));
+      if (rows.has(key)) {
+        const err = new Error('Unique constraint failed');
+        (err as Error & { code: string }).code = 'P2002';
+        throw err;
+      }
+      rows.set(key, args.data);
+      return {};
+    },
+  };
+}
+
+const record = (sig = VALID_SIG, w = VALID_WALLET, i = 0): ObservedEventInsertData => ({
+  signature: sig,
+  instructionIndex: i,
+  wallet: w,
+  eventType: 'UNKNOWN',
+});
+
+const key1 = (): string => `${VALID_SIG}:0:${VALID_WALLET}`;
+
+describe('RT-004 - limites de tamanho no schema de ingestao', () => {
+  it('signature gigante (10MB) e rejeitada', () => {
+    expect(parseRawNotification(raw({ signature: '5'.repeat(10_000_000) })).success).toBe(false);
+  });
+
+  it('signature valida passa', () => {
+    expect(parseRawNotification(raw()).success).toBe(true);
+  });
+
+  it('wallet gigante e rejeitada', () => {
+    expect(parseRawNotification(raw({ wallet: '7'.repeat(100_000) })).success).toBe(false);
+  });
+
+  it('slot astronomico e rejeitado', () => {
+    expect(parseRawNotification(raw({ slot: Number.MAX_SAFE_INTEGER })).success).toBe(false);
+  });
+
+  it('payload gigante e truncado pela serializacao limitada', () => {
+    const big = { blob: 'x'.repeat(100_000) };
+    const out = boundedJson(big);
+    const parsed = JSON.parse(out) as Record<string, unknown>;
+    expect(parsed['_truncated']).toBe(true);
+    expect(out.length).toBeLessThan(9_000);
+  });
+});
+
+describe('RT-006 - signature com allowlist base58', () => {
+  it('caractere de controle (\\u0000) rejeitado', () => {
+    expect(parseRawNotification(raw({ signature: VALID_SIG.slice(0, 87) + String.fromCharCode(0) 
})).success).toBe(false);
+  });
+
+  it('RTL override (\\u202E) rejeitado', () => {
+    const sig = VALID_SIG.slice(0, 87) + String.fromCharCode(0x202e);
+    expect(sig.charCodeAt(87)).toBe(0x202e);
+    expect(parseRawNotification(raw({ signature: sig })).success).toBe(false);
+  });
+  it('emoji / multibyte rejeitado', () => {
+    const sig = VALID_SIG.slice(0, 85) + '\uD83D\uDE80';
+    expect(sig.length).toBe(87);
+    expect(parseRawNotification(raw({ signature: sig })).success).toBe(false);
+  });
+
+  it('caracteres fora do alphabet base58 (0, O, I, l) rejeitados', () => {
+    expect(SIGNATURE_REGEX.test('0'.repeat(88))).toBe(false);
+    expect(SIGNATURE_REGEX.test('O'.repeat(88))).toBe(false);
+    expect(SIGNATURE_REGEX.test('I'.repeat(88))).toBe(false);
+    expect(SIGNATURE_REGEX.test('l'.repeat(88))).toBe(false);
+  });
+
+  it('property: toda string aceita e base58 puro de 64-88', () => {
+    fc.assert(
+      fc.property(fc.string({ minLength: 0, maxLength: 200 }), (s) => {
+        const ok = parseRawNotification(raw({ signature: s })).success;
+        if (ok) expect(SIGNATURE_REGEX.test(s)).toBe(true);
+      }),
+    );
+  });
+});
+
+describe('RT-005 - dedup sob concorrencia (contadores fieis)', () => {
+  it('burst de 50 chamadas simultaneas com a mesma key: ingested=1, deduplicated=49', async () => {
+    const repo = fakeRepo();
+    const store = new DedupStore(repo);
+    const outcomes = await Promise.all(
+      Array.from({ length: 50 }, () => store.checkAndPersist(key1(), record())),
+    );
+    expect(repo.rows.size).toBe(1);
+    expect(repo.createCalls).toBe(1);
+    expect(store.counters.ingested).toBe(1);
+    expect(store.counters.deduplicated).toBe(49);
+    expect(store.counters.errors).toBe(0);
+    expect(store.counters.joined).toBe(0);
+    expect(outcomes.filter((o) => o.status === 'new')).toHaveLength(1);
+    expect(outcomes.filter((o) => o.status === 'duplicate')).toHaveLength(49);
+    expect(store.inFlightSize).toBe(0);
+  });
+
+  it('property: bursts arbitrarios concorrentes nunca inflam contadores', async () => {
+    await fc.assert(
+      fc.asyncProperty(fc.integer({ min: 1, max: 30 }), async (burst) => {
+        const repo = fakeRepo();
+        const store = new DedupStore(repo);
+        await Promise.all(
+          Array.from({ length: burst }, () => store.checkAndPersist(key1(), record())),
+        );
+        expect(repo.createCalls).toBe(1);
+        expect(store.counters.ingested).toBe(1);
+        expect(store.counters.deduplicated).toBe(burst - 1);
+        expect(store.counters.errors).toBe(0);
+        expect(store.inFlightSize).toBe(0);
+      }),
+    );
+  });
+
+  it('sequencial continua correto (regressao)', async () => {
+    const repo = fakeRepo();
+    const store = new DedupStore(repo);
+    const r1 = await store.checkAndPersist(key1(), record());
+    const r2 = await store.checkAndPersist(key1(), record());
+    expect(r1.status).toBe('new');
+    expect(r2.status).toBe('duplicate');
+  });
+});
+
+describe('RT-005 - caminhos de erro (review 19ffacc, achados 2/3/4)', () => {
+  it('(a) lider falha + burst: 1 so tentativa de DB, erro propagado a todos, sem cadeia de retries', async () => {
+    let createCalls = 0;
+    const failingRepo = {
+      async create(): Promise<unknown> {
+        createCalls++;
+        await new Promise((r) => setTimeout(r, 10));
+        throw new Error('timeout de rede');
+      },
+    };
+    const store = new DedupStore(failingRepo);
+    const outcomes = await Promise.all(
+      Array.from({ length: 10 }, () => store.checkAndPersist(key1(), record())),
+    );
+    // Sem cadeia de liderancas: UMA unica ida ao DB.
+    expect(createCalls).toBe(1);
+    expect(store.counters.errors).toBe(1);
+    expect(store.counters.joined).toBe(9);
+    expect(store.counters.ingested).toBe(0);
+    expect(store.counters.deduplicated).toBe(0);
+    expect(outcomes.every((o) => o.status === 'error')).toBe(true);
+    expect(store.inFlightSize).toBe(0);
+  });
+
+  it('(b) sync-throw do repo.create nao envenena o inFlight: 2a chamada resolve', async () => {
+    let calls = 0;
+    // Repo com throw SINCRONO (contrato nao garante async) - cenario do achado 3.
+    const syncThrowRepo = {
+      create(_args: { data: ObservedEventInsertData }): Promise<unknown> {
+        calls++;
+        throw new Error('boom sincrono');
+      },
+    };
+    const store = new DedupStore(syncThrowRepo);
+    const r1 = await store.checkAndPersist(key1(), record());
+    expect(r1.status).toBe('error');
+    // Drain de microtasks para o cleanup protegido rodar.
+    await Promise.resolve();
+    await Promise.resolve();
+    expect(store.inFlightSize).toBe(0);
+    // 2a chamada DEVE resolver (nao travar em recursao de join).
+    const r2 = (await Promise.race([
+      store.checkAndPersist(key1(), record()),
+      new Promise((_, rej) => setTimeout(() => rej(new Error('HANG')), 500)),
+    ])) as { status: string };
+    expect(r2.status).toBe('error');
+    expect(calls).toBe(2);
+  });
+
+  it('(c) P2002 no lider com waiters: todos duplicate, 1 remember, 0 errors', async () => {
+    const repo = fakeRepo(10);
+    // Pre-semeia a key no repo: todo insert vira P2002.
+    repo.rows.set(key1(), record());
+    const store = new DedupStore(repo);
+    const outcomes = await Promise.all(
+      Array.from({ length: 8 }, () => store.checkAndPersist(key1(), record())),
+    );
+    expect(outcomes.every((o) => o.status === 'duplicate')).toBe(true);
+    expect(store.counters.deduplicated).toBe(8);
+    expect(store.counters.errors).toBe(0);
+    expect(repo.createCalls).toBe(1); // waiters nunca tocam o DB
+    // `seen` contem a key uma unica vez (Set) ÔÇö remember idempotente.
+    const again = await store.checkAndPersist(key1(), record());
+    expect(again.status).toBe('duplicate'); // agora via cache fast-path
+    expect(repo.createCalls).toBe(1);
+    expect(store.inFlightSize).toBe(0);
+  });
+});
EXIT=1
[0m
[0m$ [0m$b = [System.IO.File]::ReadAllBytes("C:\Users\enzo\AppData\Local\Temp\opencode\new-test.bin"); $nonAscii = 
@($b | Where-Object { $_ -ge 0x80 }).Count; Write-Output "new-test: total=$($b.Length) nonAscii=$nonAscii"; $hex = 
[BitConverter]::ToString($b[0..40]); Write-Output "first41: $hex"
new-test: total=9158 nonAscii=3
first41: 2F-2A-2A-0A-20-2A-20-54-65-73-74-65-73-20-64-6F-73-20-66-69-78-65-73-20-64-65-20-73-65-67-75-72-61-6E-63-61-20
-70-6F-73-2D
[0m
[0m$ [0m$b = [System.IO.File]::ReadAllBytes("C:\Users\enzo\AppData\Local\Temp\opencode\new-test.bin"); $cr = @($b | 
Where-Object { $_ -eq 0x0D }).Count; $lf = @($b | Where-Object { $_ -eq 0x0A }).Count; Write-Output "CR=$cr LF=$lf"; 
$idx = [System.Array]::FindAll($b, [Predicate[byte]]{ param($x) $x -eq 0x00 }) | Measure-Object; Write-Output "NUL 
bytes: $($idx.Count)"
CR=0 LF=234
NUL bytes: 0
[0m
[0m$ [0m$b = [System.IO.File]::ReadAllBytes("C:\Users\enzo\AppData\Local\Temp\opencode\new-test.bin"); for ($i=0; $i 
-lt $b.Length; $i++) { if ($b[$i] -ge 0x80) { $start = [Math]::Max(0, $i-8); $len = [Math]::Min(24, $b.Length - 
$start); $hex = [BitConverter]::ToString($b[$start..($start+$len-1)]); $ascii = -join ($b[$start..($start+$len-1)] | 
ForEach-Object { if ($_ -ge 32 -and $_ -lt 127) { [char]$_ } else { '.' } }); Write-Output "offset=$i 
byte=0x$('{0:X2}' -f $b[$i]) ctx=[$ascii] hex=$hex" } }
offset=8906 byte=0xE2 ctx=[z (Set) ... remember ide] 
hex=7A-20-28-53-65-74-29-20-E2-80-94-20-72-65-6D-65-6D-62-65-72-20-69-64-65
offset=8907 byte=0x80 ctx=[ (Set) ... remember idem] 
hex=20-28-53-65-74-29-20-E2-80-94-20-72-65-6D-65-6D-62-65-72-20-69-64-65-6D
offset=8908 byte=0x94 ctx=[(Set) ... remember idemp] 
hex=28-53-65-74-29-20-E2-80-94-20-72-65-6D-65-6D-62-65-72-20-69-64-65-6D-70
[0m
O diff ├® um ├║nico arquivo de teste. Agora vou validar o restante: o c├│digo sob teste (`schemas.ts`, `dedup-store.ts`), os scripts da branch e tentar executar os testes.
[0mÔ£▒ [0mGlob "services/data-ingestion/src/*.ts"[90m 10 matches[0m
[0m
[0m$ [0mgit branch -a; git log --oneline sprint/f1-security-fixes-2 --not master | Select-Object -First 10
* master
+ sprint/f1-postgres-live
+ sprint/f1-security-fixes
+ sprint/f1-security-fixes-2
  remotes/origin/HEAD -> origin/master
  remotes/origin/master
df98063 fix(security): elimina byte NUL do teste (fromCharCode) - arquivo 100% UTF-8 texto
ed0fc20 fix(security): review 19ffacc - propagacao de erro a waiters (sem cadeia de lideranca), cleanup inFlight com 
self-check, testes dos caminhos de erro, testes reescritos em UTF-8 limpo
[0m
[0m$ [0mgit show df980637e823da5b804d18efab1a9a92b8e02562:services/data-ingestion/src/schemas.ts
/**
 * Schemas zod ÔÇö valida├º├úo de TUDO que entra/sai do pipeline (AGENTS.md ┬º6:
 * dados on-chain e externos s├úo hostis).
 *
 * Regras:
 * - Campos ausentes ÔåÆ aplicado default null no normalizador (nunca inventar).
 * - Dados malformados ÔåÆ safeParse falha ÔåÆ erro VALIDATION_ERROR, nunca crash.
 * - LIMITES EXPL├ìCITOS (RT-004/RT-006): strings com .max() e allowlist base58
 *   para signature ÔÇö prote├º├úo contra DoS de mem├│ria/DB e log injection.
 */

import { z } from 'zod';

/**
 * Base58 estrito (alphabet Solana/Bitcoin, sem 0, O, I, l).
 * Signature Solana: 87ÔÇô88 chars (permitimos 64ÔÇô88 para toler├óncia a variantes).
 */
export const BASE58_REGEX = /^[1-9A-HJ-NP-Za-km-z]+$/;
export const SIGNATURE_REGEX = /^[1-9A-HJ-NP-Za-km-z]{64,88}$/;
export const WALLET_REGEX = /^[1-9A-HJ-NP-Za-km-z]{32,44}$/;

/**
 * Notifica├º├úo crua recebida do provider (wire).
 * `wallet` pode ser desconhecida neste est├ígio (UNKNOWN) ÔÇö o enriquecimento ├® F1.5.
 *
 * RT-004: todos os campos string possuem .max(). Campos num├®ricos com teto.
 * RT-006: signature validada contra allowlist base58 (bloqueia \u0000, RTL
 * override, emoji ÔÇö vetores de log injection via dedupKey/logs).
 */
export const rawNotificationSchema = z
  .object({
    signature: z.string().min(1).max(128).regex(SIGNATURE_REGEX, 'signature n├úo ├® base58 v├ílida'),
    slot: z.coerce.number().int().nonnegative().max(10_000_000_000),
    instruction_index: z.coerce.number().int().nonnegative().max(1024).default(0),
    // wallet pode ser 'UNKNOWN' nesta fase (logsNotification n├úo a traz); por isso
    // aceitamos o literal 'UNKNOWN' OU base58 32ÔÇô44. Verificado no enriquecimento (F1.5).
    wallet: z
      .string()
      .min(1)
      .max(64)
      .refine((w) => w === 'UNKNOWN' || WALLET_REGEX.test(w), 'wallet inv├ílida')
      .default('UNKNOWN'),
    block_time: z.coerce.number().int().positive().max(4_102_444_800).nullish(), // at├® 2100-01-01
    // payload: tamanho controlado na camada de serializa├º├úo (JSON.stringify com limite)
    // ÔÇö ver normalizer.ts. Aqui apenas marcado como desconhecido/hostil.
    payload: z.unknown().optional(),
  })
  .strict();

export type RawNotification = z.infer<typeof rawNotificationSchema>;

/** Evento normalizado de sa├¡da ÔÇö espelha NormalizedEvent de @ct/types. */
export const normalizedEventSchema = z
  .object({
    event_id: z.string().uuid(),
    correlation_id: z.string().min(1).max(64),
    source: z.string().min(1).max(64),
    wallet: z.string().min(1).max(64),
    signature: z.string().min(1).max(128).regex(SIGNATURE_REGEX),
    instruction_index: z.number().int().nonnegative().max(1024),
    slot: z.number().int().nonnegative().max(10_000_000_000),
    block_time: z.number().int().positive().max(4_102_444_800).nullable(),
    detected_at: z.number().int().positive(),
    processed_at: z.number().int().positive().nullable(),
    token_mint: z.string().min(1).max(64),
    action: z.enum(['BUY', 'SELL', 'TRANSFER', 'CREATE', 'UNKNOWN']),
    sol_amount: z.number().finite().nullable(),
    token_amount: z.number().finite().nullable(),
    price: z.number().finite().nullable(),
    market_cap: z.number().finite().nullable(),
    liquidity: z.number().finite().nullable(),
    latency_ms: z.number().nullable(),
  })
  .strict();

export function parseRawNotification(input: unknown) {
  return rawNotificationSchema.safeParse(input);
}

export function parseNormalizedEvent(input: unknown) {
  return normalizedEventSchema.safeParse(input);
}

/**
 * Serializa payload hostil com teto de tamanho (RT-004 defesa em profundidade).
 * Acima do limite ÔåÆ trunca para objeto marcador, NUNCA persiste o bruto gigante.
 */
export function boundedJson(value: unknown, maxBytes = 8_192): string {
  let text: string;
  try {
    text = JSON.stringify(value) ?? 'null';
  } catch {
    return JSON.stringify({ _unserializable: true });
  }
  if (text.length > maxBytes) {
    return JSON.stringify({
      _truncated: true,
      _originalBytes: text.length,
      preview: text.slice(0, 512),
    });
  }
  return text;
}
[0m
[0m$ [0mTest-Path services/data-ingestion/node_modules; Test-Path node_modules; Get-Content 
services/data-ingestion/package.json -TotalCount 40
True
True
{
  "name": "@ct/service-data-ingestion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@ct/config": "*",
    "@ct/database": "*",
    "@ct/logging": "*",
    "@ct/solana": "*",
    "@ct/types": "*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "fast-check": "^3.19.0",
    "typescript": "^5.4.5",
    "vitest": "^3.2.7"
  }
}
[0m
[0m$ [0mgit show ed0fc20 --stat | Select-String "\.ts|\.md|\.json" | ForEach-Object { $_.Line }
services/data-ingestion/src/dedup-store.ts         |  44 ++++++++++++++++-----
 services/data-ingestion/src/security-fixes.test.ts | Bin 5687 -> 9388 bytes
[0m
[0m$ [0m$m = git rev-parse master:services/data-ingestion/src/schemas.ts; $d = git rev-parse 
df98063:services/data-ingestion/src/schemas.ts; $m2 = git rev-parse master:services/data-ingestion/src/dedup-store.ts; 
$d2 = git rev-parse df98063:services/data-ingestion/src/dedup-store.ts; Write-Output "schemas master=$m df98063=$d"; 
Write-Output "dedup master=$m2 df98063=$d2"
schemas master=f894b91fd95f361863bdcbb9e95a36d275259533 df98063=f894b91fd95f361863bdcbb9e95a36d275259533
dedup master=16523b3715a9a7767a114693986dd9845d0e0d24 df98063=b160be6904c6e42735ee62f69be9f50f5c242535
[0m
[0m$ [0mGet-Content package.json -TotalCount 60
{
  "name": "solana-copytrade-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Solana Smart-Money Intelligence + Pump.fun Copy Trading Engine (PAPER-first)",
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*",
    "backtester"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "lint": "eslint . --ext .ts,.tsx --max-warnings 0",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc -b --pretty",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "prisma:generate": "prisma generate --schema packages/database/prisma/schema.prisma",
    "prisma:migrate": "prisma migrate dev --schema packages/database/prisma/schema.prisma",
    "check:secrets": "node infrastructure/scripts/check-secrets.mjs",
    "check:security": "npm run check:secrets && npm audit --audit-level=high"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@typescript-eslint/eslint-plugin": "^7.13.0",
    "@typescript-eslint/parser": "^7.13.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "fast-check": "^3.19.0",
    "prettier": "^3.3.0",
    "typescript": "^5.4.5",
    "vitest": "^3.2.7"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
[0m
[0m$ [0mgit status --porcelain; cmd /c "git show 
df980637e823da5b804d18efab1a9a92b8e02562:services/data-ingestion/src/security-fixes.test.ts > 
services\data-ingestion\src\security-fixes.test.ts" ; cmd /c "git show 
df980637e823da5b804d18efab1a9a92b8e02562:services/data-ingestion/src/dedup-store.ts > 
services\data-ingestion\src\dedup-store.ts"; git status --porcelain
M services/data-ingestion/src/dedup-store.ts
 M services/data-ingestion/src/security-fixes.test.ts
[0m
Os arquivos da branch foram copiados ao working tree (estado verificado via `git status`). Agora vou rodar o teste alvo e o typecheck:
[0m$ [0mnpx vitest run services/data-ingestion/src/security-fixes.test.ts 2>&1 | Select-Object -Last 40
[1m[46m RUN [49m[22m [36mv3.2.7 [39m[90mC:/Users/enzo/Desktop/copytrade 2[39m

 [32mÔ£ô[39m services/data-ingestion/src/security-fixes.test.ts [2m([22m[2m16 tests[22m[2m)[22m[33m 
1605[2mms[22m[39m
   [33m[2mÔ£ô[22m[39m RT-005 - dedup sob concorrencia (contadores fieis)[2m > [22mproperty: bursts arbitrarios 
concorrentes nunca inflam contadores [33m 1532[2mms[22m[39m

[2m Test Files [22m [1m[32m1 passed[39m[22m[90m (1)[39m
[2m      Tests [22m [1m[32m16 passed[39m[22m[90m (16)[39m
[2m   Start at [22m 20:50:47
[2m   Duration [22m 2.22s[2m (transform 61ms, setup 0ms, collect 257ms, tests 1.61s, environment 0ms, prepare 
113ms)[22m
[0m
16 testes passam. Agora vou verificar typecheck, lint e o tratamento de fim de linha/git attributes:
[0m$ [0mnpx eslint services/data-ingestion/src/security-fixes.test.ts --max-warnings 0 2>&1 | Select-Object -Last 
15; Write-Output "ESLINT_EXIT=$LASTEXITCODE"
ESLINT_EXIT=0
[0m
[0m$ [0mnpx tsc -b services/data-ingestion 2>&1 | Select-Object -Last 15; Write-Output "TSC_EXIT=$LASTEXITCODE"
TSC_EXIT=0
[0m
[0m$ [0mTest-Path .gitattributes; if (Test-Path .gitattributes) { Get-Content .gitattributes }; git config 
core.autocrlf
False
true
[0m
[0m$ [0m$b2 = [System.IO.File]::ReadAllBytes("C:\Users\enzo\AppData\Local\Temp\opencode\old-test.bin"); $cr2 = @($b2 
| Where-Object { $_ -eq 0x0D }).Count; Write-Output "old-test CR=$cr2"; npx vitest run 
services/data-ingestion/src/dedup.test.ts 2>&1 | Select-Object -Last 8
old-test CR=236

 [32mÔ£ô[39m services/data-ingestion/src/dedup.test.ts [2m([22m[2m3 tests[22m[2m)[22m[32m 17[2mms[22m[39m

[2m Test Files [22m [1m[32m1 passed[39m[22m[90m (1)[39m
[2m      Tests [22m [1m[32m3 passed[39m[22m[90m (3)[39m
[2m   Start at [22m 20:51:10
[2m   Duration [22m 568ms[2m (transform 48ms, setup 0ms, collect 217ms, tests 17ms, environment 0ms, prepare 
110ms)[22m
[0m
[0m$ [0m$b = [System.IO.File]::ReadAllBytes("C:\Users\enzo\AppData\Local\Temp\opencode\old-test.bin"); $em = 0; for 
($i=0; $i -lt $b.Length; $i++) { if ($b[$i] -ge 0x80) { $em++ } }; Write-Output "old-test nonAscii=$em"; $nulIdx = 
@(); for ($i=0; $i -lt $b.Length; $i++) { if ($b[$i] -eq 0) { $nulIdx += $i } }; Write-Output "old-test NUL positions: 
$($nulIdx -join ',')"
old-test nonAscii=3
old-test NUL positions: 3360
[0m
[0m$ [0mgit diff ea88b46 df98063 --stat -- services/data-ingestion/src/security-fixes.test.ts; git diff ea88b46 
df98063 --numstat -- services/data-ingestion/src/security-fixes.test.ts
services/data-ingestion/src/security-fixes.test.ts | Bin 5687 -> 9158 bytes
 1 file changed, 0 insertions(+), 0 deletions(-)
-	-	services/data-ingestion/src/security-fixes.test.ts
[0m
Vou fazer mais 2 execu├º├Áes do teste de seguran├ºa para checar flakiness (property tests + corrida com timers) e depois restaurar o working tree:
[0m$ [0m$env:VITEST_SEED="12345"; npx vitest run services/data-ingestion/src/security-fixes.test.ts 2>&1 | 
Select-String "Tests |failed|passed"; Remove-Item Env:VITEST_SEED; $env:VITEST_SEED="98765"; npx vitest run 
services/data-ingestion/src/security-fixes.test.ts 2>&1 | Select-String "Tests |failed|passed"
[2m Test Files [22m [1m[32m1 passed[39m[22m[90m (1)[39m
[2m      Tests [22m [1m[32m16 passed[39m[22m[90m (16)[39m
[2m   Duration [22m 2.22s[2m (transform 57ms, setup 0ms, collect 251ms, tests 1.62s, environment 0ms, prepare 
113ms)[22m
[2m Test Files [22m [1m[32m1 passed[39m[22m[90m (1)[39m
[2m      Tests [22m [1m[32m16 passed[39m[22m[90m (16)[39m
[2m   Duration [22m 2.22s[2m (transform 58ms, setup 0ms, collect 248ms, tests 1.63s, environment 0ms, prepare 
119ms)[22m
[0m
Est├ível em 3 execu├º├Áes (seeds distintas). Restaurando o working tree:
[0m$ [0mgit checkout -- services/data-ingestion/src/security-fixes.test.ts 
services/data-ingestion/src/dedup-store.ts; git status --porcelain; Write-Output "CLEAN=$($(git status 
--porcelain).Count -eq 0)"
CLEAN=True
[0m
# Relat├│rio de Revis├úo Adversarial ÔÇö `df980637e823da5b804d18efab1a9a92b8e02562`

**Escopo:** 1 arquivo ÔÇö `services/data-ingestion/src/security-fixes.test.ts` (remover byte NUL embutido no source; troca do literal `\u0000` por `String.fromCharCode(0)`; re-grava├º├úo CRLFÔåÆLF).

## VEREDITO: APROVADO

## ACHADOS

1. **LOW ÔÇö `services/data-ingestion/src/security-fixes.test.ts:215` (coment├írio "`seen` contem a key uma unica vez (Set) ÔÇö remember idempotente.")**
   - **Evid├¬ncia:** an├ílise bin├íria do blob novo (9c05b75): 0 bytes NUL, mas 3 bytes n├úo-ASCII em `E2 80 94` = em-dash U+2014 em UTF-8. A nota de cabe├ºalho do arquivo afirma "este arquivo e ASCII-only de proposito", o que agora ├® falso.
   - **Como reproduzir:** `git show df98063:services/data-ingestion/src/security-fixes.test.ts | find /c ""` + busca por bytes ÔëÑ0x80 ÔÇö h├í 3.
   - **Impacto:** nenhum funcional (coment├írio, UTF-8 v├ílido, git trata como texto ÔÇö objetivo do commit atingido). O em-dash j├í existia no parent (ed0fc20) e n├úo foi introduzido por este commit; por├®m o commit reafirma a nota imprecisa.
   - **Sugest├úo:** trocar `ÔÇö` por `--` para honrar a nota, ou corrigir a nota ("arquivo ASCII quase puro / UTF-8 limpo").

2. **LOW ÔÇö teste de propriedade RT-006 ("property: toda string aceita e base58 puro") ├® tautol├│gico**
   - **Evid├¬ncia:** `security-fixes.test.ts` importa `SIGNATURE_REGEX` do pr├│prio m├│dulo sob teste (`./schemas.js`). A premissa `if (ok) expect(SIGNATURE_REGEX.test(s)).toBe(true)` ├® verdadeira por constru├º├úo ÔÇö `rawNotificationSchema` valida `signature` com exatamente esse regex. O teste nunca falha a menos que o zod mude o comportamento de `.regex()`; n├úo detectaria regress├úo que alterasse schema e regex juntos de forma errada (ex.: permitir `\u0000` em ambos).
   - **Como reproduzir:** mutar `SIGNATURE_REGEX` e o schema juntos para aceitar `\u0000` ÔÇö o teste continua verde.
   - **Impacto:** cobertura de sanidade/consist├¬ncia, n├úo independente. N├úo bloqueante; os outros testes RT-006 (NUL, RTL, emoji, 0/O/I/l) s├úo diretos e efetivos.
   - **Sugest├úo:** adicionar property inversa com alphabet pr├│prio no teste (`/^[1-9A-HJ-NP-Za-km-z]{64,88}$/` hardcoded) ou contra-exemplos gerados fora do alphabet.

3. **Observa├º├úo (n├úo-bloqueante)** ÔÇö o diff deste commit aparecer├í como `Bin 9388 -> 9158` no hist├│rico porque o blob antigo continha NUL (verificado: 1 NUL na posi├º├úo 3360). Cosm├®tico; diffs futuros ser├úo texto.

## TESTES EXECUTADOS (tentativas de quebra)

- `npx vitest run services/data-ingestion/src/security-fixes.test.ts` ÔåÆ **16/16 passam**, repetido com 3 seeds distintas (`default`, `VITEST_SEED=12345`, `VITEST_SEED=98765`) para flakiness do property test e do burst concorrente com timers reais ÔÇö est├ível.
- `npx vitest run services/data-ingestion/src/dedup.test.ts` (3 testes existentes) com `dedup-store.ts` da branch ÔåÆ **passa** (compatibilidade retida).
- `npx tsc -b services/data-ingestion` ÔåÆ exit 0.
- `npx eslint services/data-ingestion/src/security-fixes.test.ts --max-warnings 0` ÔåÆ exit 0.
- An├ílise bin├íria old/new: novo blob = 9158 bytes, NUL=0, CR=0/LF=234 (autocrlf=true normaliza), UTF-8 v├ílido; antigo (parent ed0fc20) = 9388 bytes, NUL=1.
- Confirma├º├úo sem├óntica: `VALID_SIG.slice(0,87) + String.fromCharCode(0)` produz exatamente o mesmo runtime (88 code units) que o literal antigo; o teste rejeita NUL via `SIGNATURE_REGEX` (n├úo casa o alphabet) ÔÇö o teste permanece efetivo (falharia se o fix RT-006 fosse revertido).
- Check de regras AGENTS.md: sem mudan├ºa em c├│digo de produ├º├úo; sem toque em `TRADING_MODE`/`LIVE_TRADING_ENABLED`/`MAX_*`; sem secrets; nenhum teste removido; sem erros engolidos.

## O QUE N├âO FOI POSS├ìVEL VERIFICAR

- CI remoto (Security Gate / `check:security`) ÔÇö apenas execu├º├úo local.
- Comportamento em m├íquina com `core.autocrlf=false` ou checkout em Linux (convers├úo CRLF ├® aplicada pelo git local; o blob ├® LF puro).

Working tree restaurado e limpo (`git status` vazio) ÔÇö nenhuma modifica├º├úo ou commit realizado.

VERDICT: APROVADO

