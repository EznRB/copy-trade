# Review 20d93cfec10044434fc0a1966d5093e1da427b05

- Branch: sprint/f1-5-enrichment
- Data: 2026-09-25 02:40:49
- Gates: lint=1 typecheck=1 test=1 (0 = ok)
- Veredito: **REJEITADO**

## Saida do code-reviewer

opencode.exe : [0m
No C:\Users\enzo\AppData\Roaming\npm\opencode.ps1:14 caractere:3
+   & "$basedir/node_modules/opencode-ai/bin/opencode.exe"   $args
+   ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : NotSpecified: ([0m:String) [], RemoteException
    + FullyQualifiedErrorId : NativeCommandError
 
> code-reviewer ┬À big-pickle
[0m
Vou come├ºar lendo os arquivos normativos e depois o commit em quest├úo.
[0m$ [0mgit show 20d93cfec10044434fc0a1966d5093e1da427b05 --stat
commit 20d93cfec10044434fc0a1966d5093e1da427b05
Author: EznRB <EznRB@users.noreply.github.com>
Date:   Fri Sep 25 02:18:39 2026 -0300

    feat(f1.5): helius-provider per-wallet subscriptions + anti-leak de sockets + maxSupportedTransactionVersion=1 + 
testes
    
    - 1 logsSubscribe por endereco (plano Helius free so suporta 1); mapa subscriptionByWallet
    - anti-orfao: fecha socket anterior antes de recriar; falha de connect fecha o socket
    - logsUnsubscribe de todas as subs no close(); subscriptionId removido (campo obsoleto)
    - validacao base58 estrita em subscribeWallets; reconnect storm com teto maxReconnects (estado terminal)
    - consecutiveReconnects reseta em connect()/onOpen (review cc64b8d HIGH)
    - helius-provider.test.ts + rpc-provider.test.ts: subs por wallet, rejeicao RPC, reconnect, storm, v1
    
    Resolve: review 2ab477f (HIGH-1 testes, HIGH-2 leak/orfao/subscriptions, HIGH cc64b8d reset).

 packages/solana/src/helius-provider.test.ts | 256 ++++++++++++++++++++++++++++
 packages/solana/src/helius-provider.ts      | 119 ++++++++++---
 packages/solana/src/rpc-provider.test.ts    |  61 +++++++
 packages/solana/src/rpc-provider.ts         |   6 +-
 4 files changed, 417 insertions(+), 25 deletions(-)
[0m
[0m$ [0mgit log --oneline -5 20d93cfec10044434fc0a1966d5093e1da427b05
20d93cf feat(f1.5): helius-provider per-wallet subscriptions + anti-leak de sockets + maxSupportedTransactionVersion=1 
+ testes
3d6c9db feat(f1.5): schema ObservedEvent com enrichment + migration + repositories com guarda TOCTOU + testes
7d7e3d1 feat(f1.5): decoder pump.fun/PumpSwap via codama da IDL oficial (pump-public-docs @81091419)
3e51d3e review(sprint/f1-5-enrichment): REJEITADO (gates falharam) cfca4f89323cbfb68e0dcfa8aafe23a39e69301f
b7364bc review(sprint/f1-5-enrichment): REJEITADO (gates falharam) 0167a0d89fe0f21d6aa83d51e4738a1f4c5e96ae
[0m
[0m$ [0mgit show 20d93cfec10044434fc0a1966d5093e1da427b05 -- packages/solana/src/helius-provider.ts
commit 20d93cfec10044434fc0a1966d5093e1da427b05
Author: EznRB <EznRB@users.noreply.github.com>
Date:   Fri Sep 25 02:18:39 2026 -0300

    feat(f1.5): helius-provider per-wallet subscriptions + anti-leak de sockets + maxSupportedTransactionVersion=1 + 
testes
    
    - 1 logsSubscribe por endereco (plano Helius free so suporta 1); mapa subscriptionByWallet
    - anti-orfao: fecha socket anterior antes de recriar; falha de connect fecha o socket
    - logsUnsubscribe de todas as subs no close(); subscriptionId removido (campo obsoleto)
    - validacao base58 estrita em subscribeWallets; reconnect storm com teto maxReconnects (estado terminal)
    - consecutiveReconnects reseta em connect()/onOpen (review cc64b8d HIGH)
    - helius-provider.test.ts + rpc-provider.test.ts: subs por wallet, rejeicao RPC, reconnect, storm, v1
    
    Resolve: review 2ab477f (HIGH-1 testes, HIGH-2 leak/orfao/subscriptions, HIGH cc64b8d reset).

diff --git a/packages/solana/src/helius-provider.ts b/packages/solana/src/helius-provider.ts
index d5919db..e177986 100644
--- a/packages/solana/src/helius-provider.ts
+++ b/packages/solana/src/helius-provider.ts
@@ -50,6 +50,8 @@ export interface HeliusProviderOptions {
   backoff?: { baseMs?: number; maxMs?: number; jitterRatio?: number; random?: () => number };
   /** F├íbrica de WebSocket injet├ível (testes). */
   webSocketFactory?: (url: string) => WebSocket;
+  /** Teto de reconex├Áes consecutivas antes de estado terminal (default 50). */
+  maxReconnects?: number;
 }
 
 // FACT (docs Helius, verificado em 2026-09): WSS correto ├® mainnet.helius-rpc.com
@@ -91,7 +93,11 @@ export class HeliusProvider implements BlockchainDataProvider {
   private readonly wallets = new Set<string>();
 
   private ws: WebSocket | null = null;
-  private subscriptionId: number | null = null;
+  /** Subscriptions por wallet no socket ATUAL (review 2ab477f HIGH-2: sem mapa = leak). */
+  private readonly subscriptionByWallet = new Map<string, number>();
+  /** Contagem de reconexoes consecutivas ÔÇö teto evita reconnect storm infinita. */
+  private consecutiveReconnects = 0;
+  private readonly maxReconnects: number;
   private nextRequestId = 1;
   private reconnectBackoff: ExponentialBackoff;
   private reconnectTimer: ReturnType<typeof setTimeout> | null = null;
@@ -106,6 +112,7 @@ export class HeliusProvider implements BlockchainDataProvider {
     this.pingIntervalMs = options.pingIntervalMs ?? 30_000;
     this.silenceTimeoutMs = options.silenceTimeoutMs ?? 60_000;
     this.backoffOpts = options.backoff;
+    this.maxReconnects = options.maxReconnects ?? 50;
     this.wsFactory = options.webSocketFactory ?? ((url: string) => new WebSocket(url));
     this.reconnectBackoff = new ExponentialBackoff(this.backoffOpts);
     if (options.wsUrl !== undefined) {
@@ -136,12 +143,16 @@ export class HeliusProvider implements BlockchainDataProvider {
 
   connect(): Promise<void> {
     this.closed = false;
+    this.consecutiveReconnects = 0; // reset explicito no boot
     return this.openSocket();
   }
 
   async subscribeWallets(wallets: string[]): Promise<void> {
+    // Base58 estrito (32-44): formato errado ├® VALIDATION_ERROR na borda,
+    // nunca chega ao wire (review: "abc" passava com a checagem antiga).
+    const B58 = /^[1-9A-HJ-NP-Za-km-z]{32,44}$/;
     for (const w of wallets) {
-      if (typeof w !== 'string' || w.length === 0) {
+      if (typeof w !== 'string' || !B58.test(w)) {
         throw new ClassifiedError('VALIDATION_ERROR', 'wallet inv├ílida em subscribeWallets');
       }
       this.wallets.add(w);
@@ -154,10 +165,12 @@ export class HeliusProvider implements BlockchainDataProvider {
   async close(): Promise<void> {
     this.closed = true;
     this.clearTimers();
+    // logsUnsubscribe de todas as subscriptions ativas (review HIGH-2).
+    this.sendAllUnsubscribes();
     const ws = this.ws;
     this.ws = null;
     this.connected = false;
-    this.subscriptionId = null;
+    this.subscriptionByWallet.clear();
     if (ws) {
       await new Promise<void>((resolve) => {
         ws.once('close', () => resolve());
@@ -176,18 +189,40 @@ export class HeliusProvider implements BlockchainDataProvider {
 
   private openSocket(): Promise<void> {
     return new Promise((resolve, reject) => {
+      // Anti-├│rf├úo (review HIGH-2): fecha socket anterior ANTES de criar o novo.
+      const oldWs = this.ws;
+      if (oldWs) {
+        try {
+          oldWs.removeAllListeners();
+          oldWs.close();
+        } catch {
+          /* socket j├í morto */
+        }
+      }
       const ws = this.wsFactory(this.wsUrl);
       this.ws = ws;
 
       const onOpen = (): void => {
-        this.connected = true;
         this.lastMessageAt = Date.now();
         this.reconnectBackoff.reset();
-        this.startHeartbeat();
+        this.consecutiveReconnects = 0; // sucesso reseta a serie de falhas
         this.logger.info('wss conectado', { urlHost: safeHost(this.wsUrl) });
+        // connected s├│ ├® P├ÜBLICO ap├│s todas as subscriptions confirmadas; falha
+        // parcial n├úo deixa socket aberto com subs vivas (fecha e rejeita).
         void this.sendLogsSubscribe()
-          .then(() => resolve())
-          .catch((err: unknown) => reject(err));
+          .then(() => {
+            this.connected = true;
+            this.startHeartbeat();
+            resolve();
+          })
+          .catch((err: unknown) => {
+            try {
+              ws.close();
+            } catch {
+              /* noop */
+            }
+            reject(err);
+          });
       };
 
       ws.once('open', onOpen);
@@ -205,7 +240,7 @@ export class HeliusProvider implements BlockchainDataProvider {
       });
       ws.on('close', (code: number, reason: Buffer) => {
         this.connected = false;
-        this.subscriptionId = null;
+        this.subscriptionByWallet.clear();
         this.clearTimers();
         this.logger.warn('wss fechado', { code, reason: reason.toString() });
         this.scheduleReconnect();
@@ -216,18 +251,41 @@ export class HeliusProvider implements BlockchainDataProvider {
     });
   }
 
-  private sendLogsSubscribe(): Promise<void> {
-    if (this.wallets.size === 0 || this.ws === null) return Promise.resolve();
+  /** Logs de unsubscribe (fire-and-forget) ÔÇö chamado antes de fechar socket. */
+  private sendAllUnsubscribes(): void {
+    const ws = this.ws;
+    if (!ws) return;
+    for (const subId of this.subscriptionByWallet.values()) {
+      try {
+        ws.send(
+          JSON.stringify({ jsonrpc: '2.0', id: this.nextRequestId++, method: 'logsUnsubscribe', params: [subId] }),
+        );
+      } catch (err) {
+        this.logger.warn('logsUnsubscribe falhou', {
+          error: err instanceof Error ? err.message : String(err),
+        });
+      }
+    }
+  }
+
+  private async sendLogsSubscribe(): Promise<void> {
+    if (this.wallets.size === 0 || this.ws === null) return;
+    // FACT (Helius, observado 2026-09-24): logsSubscribe aceita apenas 1
+    // endereco em `mentions` neste plano. Resolver: uma subscription por
+    // endereco (Promise.all; se alguma falhar, o connect falha de forma
+    // audivel e a rotina de reconnect refaz).
+    const wallets = [...this.wallets];
+    await Promise.all(wallets.map((wallet) => this.sendSingleSubscribe(wallet)));
+  }
+
+  private sendSingleSubscribe(wallet: string): Promise<void> {
     return new Promise((resolve, reject) => {
       const id = this.nextRequestId++;
       const message = JSON.stringify({
         jsonrpc: '2.0',
         id,
         method: 'logsSubscribe',
-        params: [
-          { mentions: [...this.wallets] },
-          { commitment: this.commitment },
-        ],
+        params: [{ mentions: [wallet] }, { commitment: this.commitment }],
       });
       const ws = this.ws;
       if (!ws) {
@@ -239,16 +297,14 @@ export class HeliusProvider implements BlockchainDataProvider {
           reject(new ClassifiedError('RPC_ERROR', `logsSubscribe falhou: ${err.message}`, err));
           return;
         }
-        // Confirma├º├úo chega como resposta JSON-RPC com result = subscription id;
-        // resolu├º├úo ocorre em handleMessage via pending map simplificado:
-        this.pendingSubscribes.set(id, { resolve, reject });
+        this.pendingSubscribes.set(id, { resolve, reject, wallet });
       });
     });
   }
 
   private readonly pendingSubscribes = new Map<
     number,
-    { resolve: () => void; reject: (e: Error) => void }
+    { resolve: () => void; reject: (e: Error) => void; wallet?: string }
   >();
 
   private handleMessage(data: unknown): void {
@@ -285,10 +341,14 @@ export class HeliusProvider implements BlockchainDataProvider {
           ),
         );
       } else {
-        if (typeof parsed.result === 'number') this.subscriptionId = parsed.result;
+        const subId = typeof parsed.result === 'number' ? parsed.result : null;
+        if (subId !== null && pending?.wallet) {
+          this.subscriptionByWallet.set(pending.wallet, subId);
+        }
         this.logger.info('logsSubscribe ativo', {
-          subscriptionId: this.subscriptionId,
-          wallets: this.wallets.size,
+          wallet: pending?.wallet,
+          subscriptionId: subId,
+          activeSubscriptions: this.subscriptionByWallet.size,
         });
         pending?.resolve();
       }
@@ -378,8 +438,23 @@ export class HeliusProvider implements BlockchainDataProvider {
 
   private scheduleReconnect(): void {
     if (this.closed || this.reconnectTimer !== null) return;
+    // Teto de reconnect storm (review HIGH-2): passar do limite vira estado
+    // terminal ÔÇö log├ível pela camada superior; n├úo gera loop infinito.
+    if (this.consecutiveReconnects >= this.maxReconnects) {
+      this.logger.error('wss reconnects esgotados ÔÇö estado terminal (restart manual necess├írio)', {
+        errorClass: 'RPC_ERROR',
+        maxReconnects: this.maxReconnects,
+      });
+      this.closed = true;
+      return;
+    }
+    this.consecutiveReconnects++;
     const delay = this.reconnectBackoff.nextDelay();
-    this.logger.info('wss reconnect agendado', { delayMs: delay });
+    this.logger.info('wss reconnect agendado', {
+      delayMs: delay,
+      attempt: this.consecutiveReconnects,
+      maxReconnects: this.maxReconnects,
+    });
     this.reconnectTimer = setTimeout(() => {
       this.reconnectTimer = null;
       if (this.closed) return;
[0m
Agora vou ler o arquivo completo do helius-provider.ts para entender o contexto integral, e o diff dos testes.
[0mÔåÆ [0mRead packages/solana/src/helius-provider.ts
[0m
[0m$ [0mgit show 20d93cfec10044434fc0a1966d5093e1da427b05 -- packages/solana/src/rpc-provider.ts 
packages/solana/src/rpc-provider.test.ts
commit 20d93cfec10044434fc0a1966d5093e1da427b05
Author: EznRB <EznRB@users.noreply.github.com>
Date:   Fri Sep 25 02:18:39 2026 -0300

    feat(f1.5): helius-provider per-wallet subscriptions + anti-leak de sockets + maxSupportedTransactionVersion=1 + 
testes
    
    - 1 logsSubscribe por endereco (plano Helius free so suporta 1); mapa subscriptionByWallet
    - anti-orfao: fecha socket anterior antes de recriar; falha de connect fecha o socket
    - logsUnsubscribe de todas as subs no close(); subscriptionId removido (campo obsoleto)
    - validacao base58 estrita em subscribeWallets; reconnect storm com teto maxReconnects (estado terminal)
    - consecutiveReconnects reseta em connect()/onOpen (review cc64b8d HIGH)
    - helius-provider.test.ts + rpc-provider.test.ts: subs por wallet, rejeicao RPC, reconnect, storm, v1
    
    Resolve: review 2ab477f (HIGH-1 testes, HIGH-2 leak/orfao/subscriptions, HIGH cc64b8d reset).

diff --git a/packages/solana/src/rpc-provider.test.ts b/packages/solana/src/rpc-provider.test.ts
new file mode 100644
index 0000000..5e7c1d6
--- /dev/null
+++ b/packages/solana/src/rpc-provider.test.ts
@@ -0,0 +1,61 @@
+/**
+ * Testes do RpcProvider (review 2ab477f HIGH-1): param v1 + failover + erro.
+ * ASCII-only; sem rede.
+ */
+import { describe, it, expect } from 'vitest';
+import { RpcProvider, ClassifiedError } from './rpc-provider.js';
+
+const silent = { debug: () => {}, info: () => {}, warn: () => {}, error: () => {} };
+
+const FAST = { baseMs: 1, maxMs: 2, jitterRatio: 0 } as const;
+
+function ep(name: string, tier: 'primary' | 'secondary' | 'tertiary') {
+  return { name, url: `https://${name}.example`, tier };
+}
+
+describe('RpcProvider', () => {
+  it('getTransaction envia maxSupportedTransactionVersion=1', async () => {
+    const seen: string[] = [];
+    const fetchFn = (async (_u: string | URL | Request, init?: RequestInit) => {
+      seen.push(String(JSON.parse(String(init?.body)).params?.[1]?.maxSupportedTransactionVersion));
+      return new Response(JSON.stringify({ jsonrpc: '2.0', id: 1, result: null }), {
+        status: 200,
+        headers: { 'content-type': 'application/json' },
+      });
+    }) as unknown as typeof fetch;
+    const rpc = new RpcProvider([ep('p', 'primary')], { logger: silent, fetchFn });
+    await rpc.getTransaction('5'.repeat(88));
+    expect(seen).toEqual(['1']);
+  });
+
+  it('HTTP 500 no primario classifica como RPC_ERROR', async () => {
+    const fetchFn = (async () => new Response('down', { status: 500 })) as unknown as typeof fetch;
+    const rpc = new RpcProvider([ep('p', 'primary')], {
+      logger: silent,
+      fetchFn,
+      maxAttempts: 2,
+      backoff: FAST,
+      requestTimeoutMs: 500,
+    });
+    await expect(rpc.getSlot()).rejects.toBeInstanceOf(ClassifiedError);
+  });
+
+  it('failover: primario morto, secundario responde', async () => {
+    const fetchFn = (async (u: string | URL | Request) => {
+      if (String(u).includes('p1')) return new Response('down', { status: 500 });
+      return new Response(JSON.stringify({ jsonrpc: '2.0', id: 1, result: 123 }), {
+        status: 200,
+        headers: { 'content-type': 'application/json' },
+      });
+    }) as unknown as typeof fetch;
+    const rpc = new RpcProvider([ep('p1', 'primary'), ep('s1', 'secondary')], {
+      logger: silent,
+      fetchFn,
+      failoverThreshold: 1,
+      maxAttempts: 4,
+      backoff: FAST,
+      requestTimeoutMs: 500,
+    });
+    expect(await rpc.getSlot()).toBe(123);
+  });
+});
diff --git a/packages/solana/src/rpc-provider.ts b/packages/solana/src/rpc-provider.ts
index 42ca1f2..5cb5334 100644
--- a/packages/solana/src/rpc-provider.ts
+++ b/packages/solana/src/rpc-provider.ts
@@ -215,13 +215,13 @@ export class RpcProvider {
   }
 
   /**
-   * getTransaction com encoding jsonParsed e maxSupportedTransactionVersion: 0
-   * (FACT: requerido para transa├º├Áes versionadas/v0 com address lookup tables).
+   * getTransaction com encoding jsonParsed e maxSupportedTransactionVersion: 1
+   * (FACT 2026-09-24: versao 1 introduzida pelo runtime; 0 insuficiente). Requerido para transa├º├Áes versionadas/v0 
com address lookup tables).
    */
   async getTransaction(signature: string): Promise<unknown | null> {
     return this.call<unknown | null>('getTransaction', [
       signature,
-      { encoding: 'jsonParsed', maxSupportedTransactionVersion: 0 },
+      { encoding: 'jsonParsed', maxSupportedTransactionVersion: 1 },
     ]);
   }
[0m
[0m$ [0mgit show 20d93cf:packages/solana/src/helius-provider.ts > 
C:\Users\enzo\AppData\Local\Temp\opencode\helius-new.ts; Get-Content 
C:\Users\enzo\AppData\Local\Temp\opencode\helius-new.ts | Measure-Object -Line
Lines Words Characters Property
----- ----- ---------- --------
  479
[0m
O arquivo no working tree atual est├í na vers├úo antiga ÔÇö preciso ler o conte├║do do commit em si. Vou fazer v├írias verifica├º├Áes em paralelo.
[0m$ [0mgit status; git branch -a; git log --oneline -3
On branch master
Your branch is ahead of 'origin/master' by 11 commits.
  (use "git push" to publish your local commits)

nothing to commit, working tree clean
  backup/pre-squash-f1-5
* master
+ sprint/f1-5-enrichment
+ sprint/f1-postgres-live
+ sprint/f1-security-fixes
+ sprint/f1-security-fixes-2
  remotes/origin/HEAD -> origin/master
  remotes/origin/dependabot/github_actions/actions/checkout-7
  remotes/origin/dependabot/github_actions/actions/setup-node-7
  remotes/origin/dependabot/npm_and_yarn/fast-check-4.10.2
  remotes/origin/dependabot/npm_and_yarn/prisma-7.10.0
  remotes/origin/dependabot/npm_and_yarn/prisma/client-7.10.0
  remotes/origin/dependabot/npm_and_yarn/protocolo-critico-1885cac2e2
  remotes/origin/dependabot/npm_and_yarn/tooling-3930011c01
  remotes/origin/dependabot/npm_and_yarn/types/node-26.6.2
  remotes/origin/dependabot/npm_and_yarn/zod-4.6.5
  remotes/origin/master
  remotes/origin/sprint/f1-5-enrichment
b5bb013 review(sprint/f1-5-enrichment): REJEITADO (gates falharam) 3d6c9db383b0f064b92cc1321171854b0a36b2fe
b2a2048 review(sprint/f1-5-enrichment): REJEITADO (gates falharam) 7d7e3d129fb025dadbf3e48478297be86d18850d
cc09b02 review(sprint/f1-5-enrichment): REJEITADO d46525dda3e8b2edaa46d09a3947c56230b38d4b
[0m
[0m$ [0mGet-ChildItem docs/reviews -Name 2>$null | Select-Object -First 30; Get-Content docs/reviews/state.json 
2>$null | Select-Object -First 20
0167a0d89fe0f21d6aa83d51e4738a1f4c5e96ae.md
06d452b47eeb2a57f9a7503c92db95e9850cc944.md
071140969c2b27580f35e7ebf23714b406c200c0.md
0b7f55e5d8810bf2638c221a55592621bdf36c3c.md
19ffacc3726223dab70613484bba5432cd289d9c.md
2ab477f7dc60361db18e2106cd4b9ef48aea977e.md
38cdaac0b30e0ddf53dab697b7131e4a3499d55c.md
3d6c9db383b0f064b92cc1321171854b0a36b2fe.md
5299d250602fd81b9ac1849c026ec11955411feb.md
538ed8f1f82de7dce8c9e842a416d702bfa720fa.md
5c84a7419243edc6a3eeb9bfe7725bd320689c8e.md
609a1a65fb7495e8351d676d5d49a4ef76a054d2.md
6cd436b9bc7582a41c42b1ae40805b3cec015936.md
6d984e2fba17bc9e75cd9a89b9e6bd9dd104229f.md
799d04834d147cbdcc4ad8e46c4b8499d8ad1b00.md
7d7e3d129fb025dadbf3e48478297be86d18850d.md
7edc05c736368be87306896333ea6ed3bf65af5a.md
812ef1a7f376b01de7f7dbc21ffae9f645e1edc3.md
88152bbfafe2705932d909bd7c67571241f4b248.md
887e5160abe34afa8f947d9d3d028f8f978f1a08.md
afe614cf46169461a019b3a9599f58177ae83e0d.md
c438ef1c0c9483258896afaaf976f7e67a3404e9.md
cbd49d4fa82a2f97e8e9b134239f9ab282539ac0.md
cc64b8d9db54422332e4016b2f86a8d332ee389a.md
ccd5313e042c9e6ed45480dd91f955fb43ce6624.md
cd4468ae7f1ff2d39cdc249c4188a690144bba44.md
cfca4f89323cbfb68e0dcfa8aafe23a39e69301f.md
d016b8b22f89cd2350ee5dacdbc9ed0a06525f9c.md
d46525dda3e8b2edaa46d09a3947c56230b38d4b.md
dddb30b148daf08293bdf90097f010bb40322218.md
{
    "reviewed":  [
                     "887e5160abe34afa8f947d9d3d028f8f978f1a08",
                     "6d984e2fba17bc9e75cd9a89b9e6bd9dd104229f",
                     "0b7f55e5d8810bf2638c221a55592621bdf36c3c",
                     "609a1a65fb7495e8351d676d5d49a4ef76a054d2",
                     "ffc094cb0de340613451df935feeebd62c1e2cfc",
                     "7edc05c736368be87306896333ea6ed3bf65af5a",
                     "071140969c2b27580f35e7ebf23714b406c200c0",
                     "06d452b47eeb2a57f9a7503c92db95e9850cc944",
                     "ea88b4649a2b109ebf0b963b92f289dcc957feb5",
                     "ed0fc2095bbb9c924cef0b3c62dfc63fa600aee1",
                     "df980637e823da5b804d18efab1a9a92b8e02562",
                     "538ed8f1f82de7dce8c9e842a416d702bfa720fa",
                     "ccd5313e042c9e6ed45480dd91f955fb43ce6624",
                     "5c84a7419243edc6a3eeb9bfe7725bd320689c8e",
                     "38cdaac0b30e0ddf53dab697b7131e4a3499d55c",
                     "afe614cf46169461a019b3a9599f58177ae83e0d"
                 ],
    "rejected":  [
[0m
[0mÔ£ù [0mRead C:/Users/enzo/AppData/Local/Temp/opencode/helius-new.ts failed
[91m[1mError: [0mCannot read binary file: C:\Users\enzo\AppData\Local\Temp\opencode\helius-new.ts
[0m
[0m$ [0mgit show 20d93cf:packages/solana/src/helius-provider.test.ts > 
C:\Users\enzo\AppData\Local\Temp\opencode\helius-test-new.ts; Get-Content 
C:\Users\enzo\AppData\Local\Temp\opencode\helius-test-new.ts | Measure-Object -Line
Lines Words Characters Property
----- ----- ---------- --------
  242
[0m
Encoding problem with the redirect. Vou ler direto via `git show`:
[0m$ [0mgit show 20d93cf:packages/solana/src/helius-provider.ts
/**
 * HeliusProvider ÔÇö stream de transa├º├Áes via WebSocket.
 *
 * FACT (documenta├º├úo Helius Enhanced Websockets): endpoint
 *   wss://mainnet.helius-rpc.com/?api-key=<HELIUS_API_KEY>
 * aceita os mesmos m├®todos de subscription do WSS padr├úo da Solana
 * (logsSubscribe / logsUnsubscribe), com filtro `mentions` por endere├ºo.
 * Fallback: sem API key, conecta no WSS padr├úo da Solana
 * (wss://api.mainnet-beta.solana.com) ÔÇö o formato das mensagens ├® o mesmo
 * (JSON-RPC 2.0, notifica├º├úo `logsNotification`).
 *
 * Refer├¬ncia HTTP Helius (para uso futuro do rpc-provider):
 *   https://mainnet.helius-rpc.com/?api-key=<KEY>
 *
 * Features:
 * - logsSubscribe com { mentions: wallets } e commitment configur├ível.
 * - Reconnect com backoff exponencial + jitter (cap 30s).
 * - Heartbeat: ping a cada 30s; se nenhuma mensagem por 60s ÔåÆ reconnect for├ºado.
 * - Emite eventos crus RawTransactionNotification { signature, slot, value }.
 *   Nenhuma interpreta├º├úo/normaliza├º├úo aqui (responsabilidade da camada superior).
 */

import { WebSocket } from 'ws';
import { ExponentialBackoff } from './backoff.js';
import { ClassifiedError, type LoggerLike } from './rpc-provider.js';
import type { BlockchainDataProvider, EventCallback, ObservedChainEvent } from './chain-event.js';

/** Payload bruto de uma notifica├º├úo logsNotification (campos crus, sem interpreta├º├úo). */
export interface RawTransactionNotification {
  signature: string;
  slot: number;
  /** `value` cru do result da notifica├º├úo (err, logs, etc.) ÔÇö dados hostis, validar downstream. */
  value: unknown;
}

export type RawEventCallback = (notification: RawTransactionNotification) => void;

export interface HeliusProviderOptions {
  logger: LoggerLike;
  /** API key Helius. Se ausente, usa WSS p├║blico da Solana. */
  apiKey?: string;
  /** Override de URL WSS (ex.: devnet). Tem preced├¬ncia sobre apiKey. */
  wsUrl?: string;
  commitment?: 'processed' | 'confirmed' | 'finalized';
  /** Intervalo de ping em ms. Default 30_000. */
  pingIntervalMs?: number;
  /** Sil├¬ncio m├íximo antes de for├ºar reconnect, em ms. Default 60_000. */
  silenceTimeoutMs?: number;
  /** Backoff de reconnect injet├ível (testes). */
  backoff?: { baseMs?: number; maxMs?: number; jitterRatio?: number; random?: () => number };
  /** F├íbrica de WebSocket injet├ível (testes). */
  webSocketFactory?: (url: string) => WebSocket;
  /** Teto de reconex├Áes consecutivas antes de estado terminal (default 50). */
  maxReconnects?: number;
}

// FACT (docs Helius, verificado em 2026-09): WSS correto ├® mainnet.helius-rpc.com
// ("LaserStream WebSocket"). wss://atlas-mainnet... retorna HTTP 403.
const HELIUS_WSS_BASE = 'wss://mainnet.helius-rpc.com';
const SOLANA_PUBLIC_WSS = 'wss://api.mainnet-beta.solana.com';

interface LogsNotificationValue {
  signature?: unknown;
  err?: unknown;
  logs?: unknown;
}

interface JsonRpcNotification {
  jsonrpc?: string;
  method?: string;
  params?: {
    result?: {
      context?: { slot?: unknown };
      value?: LogsNotificationValue;
    };
    subscription?: unknown;
  };
  id?: number | string;
  result?: unknown;
  error?: { code: number; message: string };
}

export class HeliusProvider implements BlockchainDataProvider {
  private readonly logger: LoggerLike;
  private readonly wsUrl: string;
  private readonly commitment: 'processed' | 'confirmed' | 'finalized';
  private readonly pingIntervalMs: number;
  private readonly silenceTimeoutMs: number;
  private readonly backoffOpts: HeliusProviderOptions['backoff'];
  private readonly wsFactory: (url: string) => WebSocket;
  private readonly rawListeners: RawEventCallback[] = [];
  private readonly eventListeners: EventCallback[] = [];
  private readonly wallets = new Set<string>();

  private ws: WebSocket | null = null;
  /** Subscriptions por wallet no socket ATUAL (review 2ab477f HIGH-2: sem mapa = leak). */
  private readonly subscriptionByWallet = new Map<string, number>();
  /** Contagem de reconexoes consecutivas ÔÇö teto evita reconnect storm infinita. */
  private consecutiveReconnects = 0;
  private readonly maxReconnects: number;
  private nextRequestId = 1;
  private reconnectBackoff: ExponentialBackoff;
  private reconnectTimer: ReturnType<typeof setTimeout> | null = null;
  private pingTimer: ReturnType<typeof setInterval> | null = null;
  private lastMessageAt = 0;
  private closed = false;
  private connected = false;

  constructor(options: HeliusProviderOptions) {
    this.logger = options.logger;
    this.commitment = options.commitment ?? 'confirmed';
    this.pingIntervalMs = options.pingIntervalMs ?? 30_000;
    this.silenceTimeoutMs = options.silenceTimeoutMs ?? 60_000;
    this.backoffOpts = options.backoff;
    this.maxReconnects = options.maxReconnects ?? 50;
    this.wsFactory = options.webSocketFactory ?? ((url: string) => new WebSocket(url));
    this.reconnectBackoff = new ExponentialBackoff(this.backoffOpts);
    if (options.wsUrl !== undefined) {
      this.wsUrl = options.wsUrl;
    } else if (options.apiKey !== undefined && options.apiKey.length > 0) {
      this.wsUrl = `${HELIUS_WSS_BASE}?api-key=${encodeURIComponent(options.apiKey)}`;
    } else {
      // ASSUMPTION: fallback no WSS p├║blico Solana ÔÇö mesmo formato de mensagem.
      this.wsUrl = SOLANA_PUBLIC_WSS;
      this.logger.warn('HELIUS_API_KEY ausente ÔÇö usando WSS p├║blico Solana (rate limits menores)');
    }
  }

  onRawEvent(cb: RawEventCallback): void {
    this.rawListeners.push(cb);
  }

  /**
   * Adapta├º├úo para a interface BlockchainDataProvider: emite ObservedChainEvent
   * com os campos conhecidos (signature/slot) e desconhecidos como neutros
   * (instructionIndex 0, wallet ''): logsNotification n├úo identifica a wallet
   * espec├¡fica mencionada ÔÇö a resolu├º├úo ├® responsabilidade da camada de
   * normaliza├º├úo downstream (ver .agents/solana-ingestion.md ┬º2).
   */
  onEvent(cb: EventCallback): void {
    this.eventListeners.push(cb);
  }

  connect(): Promise<void> {
    this.closed = false;
    this.consecutiveReconnects = 0; // reset explicito no boot
    return this.openSocket();
  }

  async subscribeWallets(wallets: string[]): Promise<void> {
    // Base58 estrito (32-44): formato errado ├® VALIDATION_ERROR na borda,
    // nunca chega ao wire (review: "abc" passava com a checagem antiga).
    const B58 = /^[1-9A-HJ-NP-Za-km-z]{32,44}$/;
    for (const w of wallets) {
      if (typeof w !== 'string' || !B58.test(w)) {
        throw new ClassifiedError('VALIDATION_ERROR', 'wallet inv├ílida em subscribeWallets');
      }
      this.wallets.add(w);
    }
    if (this.connected) {
      await this.sendLogsSubscribe();
    }
  }

  async close(): Promise<void> {
    this.closed = true;
    this.clearTimers();
    // logsUnsubscribe de todas as subscriptions ativas (review HIGH-2).
    this.sendAllUnsubscribes();
    const ws = this.ws;
    this.ws = null;
    this.connected = false;
    this.subscriptionByWallet.clear();
    if (ws) {
      await new Promise<void>((resolve) => {
        ws.once('close', () => resolve());
        try {
          ws.close();
        } catch {
          resolve();
        }
        // N├úo esperar indefinidamente o close handshake.
        setTimeout(resolve, 2_000).unref();
      });
    }
  }

  // ---------------------------------------------------------------- internals

  private openSocket(): Promise<void> {
    return new Promise((resolve, reject) => {
      // Anti-├│rf├úo (review HIGH-2): fecha socket anterior ANTES de criar o novo.
      const oldWs = this.ws;
      if (oldWs) {
        try {
          oldWs.removeAllListeners();
          oldWs.close();
        } catch {
          /* socket j├í morto */
        }
      }
      const ws = this.wsFactory(this.wsUrl);
      this.ws = ws;

      const onOpen = (): void => {
        this.lastMessageAt = Date.now();
        this.reconnectBackoff.reset();
        this.consecutiveReconnects = 0; // sucesso reseta a serie de falhas
        this.logger.info('wss conectado', { urlHost: safeHost(this.wsUrl) });
        // connected s├│ ├® P├ÜBLICO ap├│s todas as subscriptions confirmadas; falha
        // parcial n├úo deixa socket aberto com subs vivas (fecha e rejeita).
        void this.sendLogsSubscribe()
          .then(() => {
            this.connected = true;
            this.startHeartbeat();
            resolve();
          })
          .catch((err: unknown) => {
            try {
              ws.close();
            } catch {
              /* noop */
            }
            reject(err);
          });
      };

      ws.once('open', onOpen);
      ws.once('error', (err) => {
        if (!this.connected) {
          reject(
            new ClassifiedError('RPC_ERROR', `wss falha ao conectar: ${err.message}`, err),
          );
        }
      });

      ws.on('message', (data: unknown) => this.handleMessage(data));
      ws.on('pong', () => {
        this.lastMessageAt = Date.now();
      });
      ws.on('close', (code: number, reason: Buffer) => {
        this.connected = false;
        this.subscriptionByWallet.clear();
        this.clearTimers();
        this.logger.warn('wss fechado', { code, reason: reason.toString() });
        this.scheduleReconnect();
      });
      ws.on('error', (err: Error) => {
        this.logger.error('wss erro', { error: err.message });
      });
    });
  }

  /** Logs de unsubscribe (fire-and-forget) ÔÇö chamado antes de fechar socket. */
  private sendAllUnsubscribes(): void {
    const ws = this.ws;
    if (!ws) return;
    for (const subId of this.subscriptionByWallet.values()) {
      try {
        ws.send(
          JSON.stringify({ jsonrpc: '2.0', id: this.nextRequestId++, method: 'logsUnsubscribe', params: [subId] }),
        );
      } catch (err) {
        this.logger.warn('logsUnsubscribe falhou', {
          error: err instanceof Error ? err.message : String(err),
        });
      }
    }
  }

  private async sendLogsSubscribe(): Promise<void> {
    if (this.wallets.size === 0 || this.ws === null) return;
    // FACT (Helius, observado 2026-09-24): logsSubscribe aceita apenas 1
    // endereco em `mentions` neste plano. Resolver: uma subscription por
    // endereco (Promise.all; se alguma falhar, o connect falha de forma
    // audivel e a rotina de reconnect refaz).
    const wallets = [...this.wallets];
    await Promise.all(wallets.map((wallet) => this.sendSingleSubscribe(wallet)));
  }

  private sendSingleSubscribe(wallet: string): Promise<void> {
    return new Promise((resolve, reject) => {
      const id = this.nextRequestId++;
      const message = JSON.stringify({
        jsonrpc: '2.0',
        id,
        method: 'logsSubscribe',
        params: [{ mentions: [wallet] }, { commitment: this.commitment }],
      });
      const ws = this.ws;
      if (!ws) {
        resolve();
        return;
      }
      ws.send(message, (err?: Error) => {
        if (err) {
          reject(new ClassifiedError('RPC_ERROR', `logsSubscribe falhou: ${err.message}`, err));
          return;
        }
        this.pendingSubscribes.set(id, { resolve, reject, wallet });
      });
    });
  }

  private readonly pendingSubscribes = new Map<
    number,
    { resolve: () => void; reject: (e: Error) => void; wallet?: string }
  >();

  private handleMessage(data: unknown): void {
    this.lastMessageAt = Date.now();
    let text: string;
    if (typeof data === 'string') {
      text = data;
    } else if (Buffer.isBuffer(data)) {
      text = data.toString('utf8');
    } else if (Array.isArray(data)) {
      text = Buffer.concat(data).toString('utf8');
    } else {
      this.logger.warn('wss mensagem de tipo inesperado descartada');
      return;
    }

    let parsed: JsonRpcNotification;
    try {
      parsed = JSON.parse(text) as JsonRpcNotification;
    } catch {
      this.logger.warn('wss mensagem n├úo-JSON descartada');
      return;
    }

    // Resposta a logsSubscribe
    if (parsed.id !== undefined && this.pendingSubscribes.has(Number(parsed.id))) {
      const pending = this.pendingSubscribes.get(Number(parsed.id));
      this.pendingSubscribes.delete(Number(parsed.id));
      if (parsed.error) {
        pending?.reject(
          new ClassifiedError(
            'RPC_ERROR',
            `logsSubscribe rejeitado: ${parsed.error.message}`,
          ),
        );
      } else {
        const subId = typeof parsed.result === 'number' ? parsed.result : null;
        if (subId !== null && pending?.wallet) {
          this.subscriptionByWallet.set(pending.wallet, subId);
        }
        this.logger.info('logsSubscribe ativo', {
          wallet: pending?.wallet,
          subscriptionId: subId,
          activeSubscriptions: this.subscriptionByWallet.size,
        });
        pending?.resolve();
      }
      return;
    }

    // Notifica├º├úo de logs
    if (parsed.method === 'logsNotification') {
      const slot = Number(parsed.params?.result?.context?.slot ?? 0);
      const value = parsed.params?.result?.value;
      const signature = typeof value?.signature === 'string' ? value.signature : null;
      if (signature === null) {
        // Dados hostis/malformados: nunca inferir ÔÇö m├®trica/log e descarte.
        this.logger.warn('logsNotification malformada descartada', { slot });
        return;
      }
      const notification: RawTransactionNotification = {
        signature,
        slot: Number.isFinite(slot) ? slot : 0,
        value,
      };
      const observed: ObservedChainEvent = {
        signature,
        instructionIndex: 0,
        wallet: '',
        slot: BigInt(notification.slot),
        payload: value,
      };
      for (const cb of this.eventListeners) {
        try {
          cb(observed);
        } catch (err) {
          this.logger.error('listener onEvent lan├ºou exce├º├úo', {
            error: err instanceof Error ? err.message : String(err),
          });
        }
      }
      for (const cb of this.rawListeners) {
        try {
          cb(notification);
        } catch (err) {
          this.logger.error('listener onRawEvent lan├ºou exce├º├úo', {
            error: err instanceof Error ? err.message : String(err),
          });
        }
      }
    }
  }

  private startHeartbeat(): void {
    this.clearTimers();
    this.pingTimer = setInterval(() => {
      const ws = this.ws;
      if (ws === null) return;
      const silenceMs = Date.now() - this.lastMessageAt;
      if (silenceMs > this.silenceTimeoutMs) {
        this.logger.warn('wss sil├¬ncio excedeu limite ÔÇö for├ºando reconnect', {
          silenceMs,
          silenceTimeoutMs: this.silenceTimeoutMs,
        });
        this.forceReconnect();
        return;
      }
      try {
        ws.ping();
      } catch (err) {
        this.logger.warn('wss ping falhou', {
          error: err instanceof Error ? err.message : String(err),
        });
      }
    }, this.pingIntervalMs);
    this.pingTimer.unref?.();
  }

  private forceReconnect(): void {
    const ws = this.ws;
    if (ws !== null) {
      this.ws = null;
      try {
        ws.terminate();
      } catch {
        // noop ÔÇö reconnect j├í agendado pelo handler de close
      }
    }
    if (this.reconnectTimer === null) this.scheduleReconnect();
  }

  private scheduleReconnect(): void {
    if (this.closed || this.reconnectTimer !== null) return;
    // Teto de reconnect storm (review HIGH-2): passar do limite vira estado
    // terminal ÔÇö log├ível pela camada superior; n├úo gera loop infinito.
    if (this.consecutiveReconnects >= this.maxReconnects) {
      this.logger.error('wss reconnects esgotados ÔÇö estado terminal (restart manual necess├írio)', {
        errorClass: 'RPC_ERROR',
        maxReconnects: this.maxReconnects,
      });
      this.closed = true;
      return;
    }
    this.consecutiveReconnects++;
    const delay = this.reconnectBackoff.nextDelay();
    this.logger.info('wss reconnect agendado', {
      delayMs: delay,
      attempt: this.consecutiveReconnects,
      maxReconnects: this.maxReconnects,
    });
    this.reconnectTimer = setTimeout(() => {
      this.reconnectTimer = null;
      if (this.closed) return;
      this.openSocket().catch((err: unknown) => {
        this.logger.error('wss reconnect falhou', {
          error: err instanceof Error ? err.message : String(err),
        });
        this.scheduleReconnect();
      });
    }, delay);
    this.reconnectTimer.unref?.();
  }

  private clearTimers(): void {
    if (this.pingTimer !== null) {
      clearInterval(this.pingTimer);
      this.pingTimer = null;
    }
    if (this.reconnectTimer !== null) {
      clearTimeout(this.reconnectTimer);
      this.reconnectTimer = null;
    }
    this.failPendingSubscribes();
  }

  /**
   * Rejeita todas as subscri├º├Áes pendentes. Sem isso, um socket fechado
   * antes da confirma├º├úo deixaria promises pendentes para sempre (leak).
   */
  private failPendingSubscribes(): void {
    if (this.pendingSubscribes.size === 0) return;
    const err = new ClassifiedError(
      'RPC_ERROR',
      'socket fechado antes da confirma├º├úo de logsSubscribe',
    );
    for (const { reject } of this.pendingSubscribes.values()) reject(err);
    this.pendingSubscribes.clear();
  }
}

/** Extrai host sem expor api-key em logs (seguran├ºa ┬º2). */
function safeHost(url: string): string {
  try {
    return new URL(url).host;
  } catch {
    return 'unknown';
  }
}

/** Refer├¬ncia HTTP documentada (Helius), exportada para configura├º├úo do RpcProvider. */
export const HELIUS_HTTP_BASE = 'https://mainnet.helius-rpc.com';

export function buildHeliusHttpUrl(apiKey: string): string {
  return `${HELIUS_HTTP_BASE}/?api-key=${encodeURIComponent(apiKey)}`;
}

export function buildHeliusWsUrl(apiKey: string): string {
  return `${HELIUS_WSS_BASE}?api-key=${encodeURIComponent(apiKey)}`;
}
[0m
Agora o arquivo de testes novo e os reviews anteriores citados como refer├¬ncia:
[0m$ [0mgit show 20d93cf:packages/solana/src/helius-provider.test.ts
/**
 * Testes do HeliusProvider (review 2ab477f, HIGH-1/HIGH-2). ASCII-only.
 * WebSocket fake injetavel; sem rede.
 */
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { EventEmitter } from 'node:events';
import { HeliusProvider, type RawTransactionNotification } from './helius-provider.js';

class FakeWs extends EventEmitter {
  static instances: FakeWs[] = [];
  static reset(): void { FakeWs.instances = []; }
  sent: Array<Record<string, unknown>> = [];
  closed = 0;
  url: string;

  constructor(url: string) {
    super();
    this.url = url;
    FakeWs.instances.push(this);
    queueMicrotask(() => this.emit('open'));
  }

  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  send(data: any, cb?: (err?: Error) => void) {
    this.sent.push(JSON.parse(String(data)));
    cb?.();
  }
  ping(): void {}
  close(): void {
    this.closed++;
    queueMicrotask(() => this.emit('close', 1006, Buffer.from('')));
  }
  terminate(): void {}
}

const W = (c: string) => c.repeat(44); // base58 valido

function providerWithFake(): { provider: HeliusProvider; logs: string[] } {
  const logs: string[] = [];
  const logger = {
    debug: (m: string) => logs.push(`d:${m}`),
    info: (m: string) => logs.push(`i:${m}`),
    warn: (m: string) => logs.push(`w:${m}`),
    error: (m: string) => logs.push(`e:${m}`),
  };
  const p = new HeliusProvider({
    logger,
    wsUrl: 'wss://fake.local',
    backoff: { baseMs: 1, maxMs: 2, jitterRatio: 0 },
    maxReconnects: 50,
    webSocketFactory: (url) => new FakeWs(url) as unknown as import('ws').WebSocket,
  });
  return { provider: p, logs };
}

/** Confirma a subscricao atual pendente com sucesso. */
function confirmSubs(ws: FakeWs): void {
  const pending = ws.sent.filter((m) => m['method'] === 'logsSubscribe');
  for (const m of pending) {
    ws.emit('message', JSON.stringify({ jsonrpc: '2.0', id: m['id'], result: 4242 }));
  }
}

describe('HeliusProvider - subscriptions por wallet', () => {
  beforeEach(() => { FakeWs.reset(); });
  it('N wallets validas geram N logsSubscribe (1 mention cada)', async () => {
    const { provider } = providerWithFake();
    await provider.subscribeWallets([W('A'), W('B')]);
    const p = provider.connect();
    await new Promise((r) => setTimeout(r, 10));
    const ws = FakeWs.instances[0]!;
    confirmSubs(ws);
    await p;
    const subs = ws.sent.filter((m) => m['method'] === 'logsSubscribe');
    expect(subs).toHaveLength(2);
    expect((subs[0]!['params'] as unknown[])[0]).toEqual({ mentions: [W('A')] });
    await provider.close();
  });

  it('rejeita endereco mal-formado na borda (VALIDATION_ERROR)', async () => {
    const { provider } = providerWithFake();
    try {
      await provider.subscribeWallets(['abc']);
      throw new Error('nao deveria passar');
    } catch (err) {
      expect((err as { errorClass?: string }).errorClass).toBe('VALIDATION_ERROR');
    }
  });

  it('sub rejeitada pela RPC: promise falha com RPC_ERROR e socket e fechado', async () => {
    const { provider } = providerWithFake();
    await provider.subscribeWallets([W('A')]);
    const p = provider.connect();
    const bad = providerWithFake();
    void bad;
    await new Promise((r) => setTimeout(r, 10));
    const ws = FakeWs.instances[0]!;
    const sub = ws.sent.find((m) => m['method'] === 'logsSubscribe');
    ws.emit(
      'message',
      JSON.stringify({ jsonrpc: '2.0', id: sub!['id'], error: { code: -32602, message: 'bad' } }),
    );
    await expect(p).rejects.toThrowError(/logsSubscribe rejeitado/);
    // Socket do connect falho foi fechado explicitamente (anti-orfao).
    expect(ws.closed).toBeGreaterThanOrEqual(1);
  });

  it('close() envia logsUnsubscribe para cada sub ativa', async () => {
    const { provider } = providerWithFake();
    await provider.subscribeWallets([W('A'), W('B')]);
    const p = provider.connect();
    await new Promise((r) => setTimeout(r, 10));
    const ws = FakeWs.instances[0]!;
    confirmSubs(ws);
    await p;
    await provider.close();
    const unsubs = ws.sent.filter((m) => m['method'] === 'logsUnsubscribe');
    expect(unsubs).toHaveLength(2);
  });

  it('reconnect nao acumula sockets (sempre 1 vivo) nem duplica subs', async () => {
    const { provider } = providerWithFake();
    await provider.subscribeWallets([W('A')]);
    const p = provider.connect();
    await new Promise((r) => setTimeout(r, 10));
    confirmSubs(FakeWs.instances[0]!);
    await p;
    // Simula queda e reconnect
    FakeWs.instances[0]!.emit('close', 1006, Buffer.from('net'));
    await new Promise((r) => setTimeout(r, 30));
    expect(FakeWs.instances.length).toBe(2);
    confirmSubs(FakeWs.instances[1]!);
    await new Promise((r) => setTimeout(r, 10));
    const subs2 = FakeWs.instances[1]!.sent.filter((m) => m['method'] === 'logsSubscribe');
    expect(subs2).toHaveLength(1); // re-subscribe UMA vez por wallet no socket novo
    await provider.close();
  });

  it('eventos de logsNotification chegam ao onRawEvent com assinatura valida', async () => {
    const { provider } = providerWithFake();
    const got: RawTransactionNotification[] = [];
    provider.onRawEvent((n) => got.push(n));
    await provider.subscribeWallets([W('A')]);
    const p = provider.connect();
    await new Promise((r) => setTimeout(r, 10));
    const ws = FakeWs.instances[0]!;
    confirmSubs(ws);
    await p;
    ws.emit(
      'message',
      JSON.stringify({
        jsonrpc: '2.0',
        method: 'logsNotification',
        params: { result: { context: { slot: 9 }, value: { signature: W('5',) + 'x'.repeat(44) } }, subscription: 4242 
},
      }),
    );
    expect(got).toHaveLength(1);
    expect(got[0]!.signature.length).toBeGreaterThan(0);
    await provider.close();
  });
});

describe('HeliusProvider - reset da serie de falhas', () => {
  it('N falhas + sucesso + nova falha: NAO entra em estado terminal (serie resetou)', async () => {
    const logs: string[] = [];
    const logger = {
      debug: (m: string) => logs.push(m),
      info: (m: string) => logs.push(m),
      warn: (m: string) => logs.push(m),
      error: (m: string) => logs.push(m),
    };
    let mode: 'fail' | 'ok' = 'fail';
    const p = new HeliusProvider({
      logger,
      wsUrl: 'wss://fake.local',
      backoff: { baseMs: 1, maxMs: 2, jitterRatio: 0 },
      maxReconnects: 3,
      webSocketFactory: (_url: string) => {
        void _url;
        class W extends EventEmitter {
          constructor() {
            super();
            queueMicrotask(() => {
              if (mode === 'fail') this.emit('error', new Error('down'));
              else this.emit('open');
            });
          }
          send(_d: unknown): void {
            void _d;
          }
          ping(): void {}
          close(): void {
            this.emit('close', 1006, Buffer.from(''));
          }
        }
        return new W() as unknown as import('ws').WebSocket;
      },
    });
    await expect(p.connect()).rejects.toThrowError(); // falha 1
    await expect(p.connect()).rejects.toThrowError(); // falha 2
    // sucesso: agora abre (sem subscriptions -> resolve direto)
    mode = 'ok';
    await expect(p.connect()).resolves.toBeUndefined();
    await p.close();
    // nova falha apos sucesso: nao pode entrar em terminal imediatamente
    mode = 'fail';
    await expect(p.connect()).rejects.toThrowError();
    expect(logs.some((l) => l.includes('estado terminal'))).toBe(false);
  });
});

describe('HeliusProvider - reconnect storm', () => {
  it('apos maxReconnects falhas consecutivas vira estado terminal (sem loop)', async () => {
    vi.useFakeTimers();
    try {
      const logs: string[] = [];
      const logger = {
        debug: (m: string) => logs.push(m),
        info: (m: string) => logs.push(m),
        warn: (m: string) => logs.push(m),
        error: (m: string) => logs.push(m),
      };
      let failEvery = true;
      const p = new HeliusProvider({
        logger,
        wsUrl: 'wss://fake.local',
        backoff: { baseMs: 1, maxMs: 2, jitterRatio: 0 },
        maxReconnects: 3,
        webSocketFactory: (_url: string) => {
          void _url;
          class FailingWs extends EventEmitter {
            constructor() {
              super();
              queueMicrotask(() => {
                if (failEvery) this.emit('error', new Error('down'));
                this.emit('close', 1006, Buffer.from(''));
              });
            }
            send(): void {}
            ping(): void {}
            close(): void {
              this.emit('close', 1006, Buffer.from(''));
            }
          }
          return new FailingWs() as unknown as import('ws').WebSocket;
        },
      });
      await expect(p.connect()).rejects.toThrowError(/falha ao conectar|logsSubscribe rejeitado/);
      failEvery = false;
      for (let i = 0; i < 20; i++) await vi.advanceTimersByTimeAsync(5);
      expect(logs.some((l) => l.includes('estado terminal'))).toBe(true);
    } finally {
      vi.useRealTimers();
    }
  });
});
[0m
[0m$ [0mGet-Content docs/reviews/2ab477f7dc60361db18e2106cd4b9ef48aea977e.md
...output truncated...

Full output saved to: C:\Users\enzo\.local\share\opencode\tool-output\tool_0d7114c3a0019yfFAYxw90P9ej

00000500   69 67 6E 61 74 75 72 65 22 3A 20 22 32 7A 61 47  ignature": "2zaG
00000510   4E 5A 39 58 43 33 45 73 43 70 41 38 53 4A 33 37  NZ9XC3EsCpA8SJ37
00000520   7A 4A 48 39 6D 70 63 44 64 4A 37 77 75 4B 37 34  zJH9mpcDdJ7wuK74
00000530   69 62 36 32 4E 59 71 74 6D 73 53 6A 4D 47 79 79  ib62NYqtmsSjMGyy
00000540   36 6B 63 59 79 41 67 4E 36 32 46 57 71 45 51 44  6kcYyAgN62FWqEQD
00000550   42 57 79 33 63 5A 73 4A 69 7A 4E 6D 76 7A 57 50  BWy3cZsJizNmvzWP
00000560   69 6F 75 57 22 2C 20 20 20 20 20 20 22 6C 6F 63  iouW",      "loc
00000570   61 6C 22 3A 20 7B 20 20 20 20 20 20 20 20 22 6B  al": {        "k
00000580   69 6E 64 22 3A 20 22 70 75 6D 70 73 77 61 70 5F  ind": "pumpswap_
00000590   62 75 79 22 2C 20 20 20 20 20 20 20 20 22 64 69  buy",        "di
000005A0   72 65 63 74 69 6F 6E 22 3A 20 22 62 75 79 22 2C  rection": "buy",
000005B0   20 20 20 20 20 20 20 20 22 6D 69 6E 74 22 3A 20          "mint": 
000005C0   22 42 67 37 35 47 61 65 54 4E 37 75 56 71 74 55  "Bg75GaeTN7uVqtU
000005D0   45 73 71 75 68 36 31 68 77 45 4E 65 61 41 38 54  Esquh61hwENeaA8T
000005E0   44 35 4B 38 48 77 58 34 4C 66 6F 6D 6F 22 2C 20  D5K8HwX4Lfomo", 
000005F0   20 20 20 20 20 20 20 22 75 73 65 72 22 3A 20 22         "user": "
00000600   34 31 6E 38 67 79 6F 56 41 75 44 6A 5A 64 55 65  41n8gyoVAuDjZdUe
00000610   48 52 47 72 50 73 63 47 32 44 7A 4A 61 77 44 48  HRGrPscG2DzJawDH
00000620   70 61 31 59 36 56 4C 56 34 56 71 4A 22 2C 20 20  pa1Y6VLV4VqJ",  
00000630   20 20 20 20 20 20 22 63 6F 75 6E 74 65 72 70 61        "counterpa
00000640   72 74 79 22 3A 20 22 45 4A 58 45 62 64 73 58 46  rty": "EJXEbdsXF
00000650   4A 39 41 46 45 75 31 45 71 4E 48 79 63 66 41 45  J9AFEu1EqNHycfAE
00000660   6F 42 4E 37 42 46 74 66 4C 56 57 78 34 44 73 45  oBN7BFtfLVWx4DsE
00000670   57 55 47 22 20 20 20 20 20 20 7D 2C 20 20 20 20  WUG"      },    
00000680   20 20 22 68 65 6C 69 75 73 22 3A 20 7B 20 20 20    "helius": {   
00000690   20 20 20 20 20 22 74 79 70 65 22 3A 20 22 53 57       "type": "SW
000006A0   41 50 22 2C 20 20 20 20 20 20 20 20 22 73 6F 75  AP",        "sou
000006B0   72 63 65 22 3A 20 22 50 55 4D 50 5F 41 4D 4D 22  rce": "PUMP_AMM"
000006C0   2C 20 20 20 20 20 20 20 20 22 66 65 65 50 61 79  ,        "feePay
000006D0   65 72 22 3A 20 22 34 31 6E 38 67 79 6F 56 41 75  er": "41n8gyoVAu
000006E0   44 6A 5A 64 55 65 48 52 47 72 50 73 63 47 32 44  DjZdUeHRGrPscG2D
000006F0   7A 4A 61 77 44 48 70 61 31 59 36 56 4C 56 34 56  zJawDHpa1Y6VLV4V
00000700   71 4A 22 20 20 20 20 20 20 7D 20 20 20 20 7D 2C  qJ"      }    },
00000710   20 20 20 20 7B 20 20 20 20 20 20 22 73 69 67 6E      {      "sign
00000720   61 74 75 72 65 22 3A 20 22 4A 6D 55 37 6E 68 6B  ature": "JmU7nhk
00000730   48 36 35 4D 68 77 38 44 41 6A 53 53 63 51 50 56  H65Mhw8DAjSScQPV
00000740   74 35 48 44 73 4C 32 79 43 31 4D 6E 74 73 61 62  t5HDsL2yC1Mntsab
00000750   7A 46 66 39 47 57 36 41 54 4D 63 56 53 57 44 43  zFf9GW6ATMcVSWDC
00000760   78 44 4C 74 57 61 42 61 67 61 56 6F 47 76 69 67  xDLtWaBagaVoGvig
00000770   6D 6D 55 52 70 72 38 50 37 75 58 75 4C 61 4C 5A  mmURpr8P7uXuLaLZ
00000780   22 2C 20 20 20 20 20 20 22 6C 6F 63 61 6C 22 3A  ",      "local":
00000790   20 7B 20 20 20 20 20 20 20 20 22 6B 69 6E 64 22   {        "kind"
000007A0   3A 20 22 70 75 6D 70 73 77 61 70 5F 73 65 6C 6C  : "pumpswap_sell
000007B0   22 2C 20 20 20 20 20 20 20 20 22 64 69 72 65 63  ",        "direc
000007C0   74 69 6F 6E 22 3A 20 22 73 65 6C 6C 22 2C 20 20  tion": "sell",  
000007D0   20 20 20 20 20 20 22 6D 69 6E 74 22 3A 20 22 42        "mint": "B
000007E0   67 37 35 47 61 65 54 4E 37 75 56 71 74 55 45 73  g75GaeTN7uVqtUEs
000007F0   71 75 68 36 31 68 77 45 4E 65 61 41 38 54 44 35  quh61hwENeaA8TD5
00000800   4B 38 48 77 58 34 4C 66 6F 6D 6F 22 2C 20 20 20  K8HwX4Lfomo",   
00000810   20 20 20 20 20 22 75 73 65 72 22 3A 20 22 38 4A       "user": "8J
00000820   6A 56 4E 79 6A 36 35 4E 79 68 43 75 69 74 32 6E  jVNyj65NyhCuit2n
00000830   34 78 41 50 48 61 4D 4A 48 76 4A 73 51 61 72 67  4xAPHaMJHvJsQarg
00000840   32 33 77 46 72 68 61 77 41 63 22 2C 20 20 20 20  23wFrhawAc",    
00000850   20 20 20 20 22 63 6F 75 6E 74 65 72 70 61 72 74      "counterpart
00000860   79 22 3A 20 22 45 4A 58 45 62 64 73 58 46 4A 39  y": "EJXEbdsXFJ9
00000870   41 46 45 75 31 45 71 4E 48 79 63 66 41 45 6F 42  AFEu1EqNHycfAEoB
00000880   4E 37 42 46 74 66 4C 56 57 78 34 44 73 45 57 55  N7BFtfLVWx4DsEWU
00000890   47 22 20 20 20 20 20 20 7D 2C 20 20 20 20 20 20  G"      },      
000008A0   22 68 65 6C 69 75 73 22 3A 20 7B 20 20 20 20 20  "helius": {     
000008B0   20 20 20 22 74 79 70 65 22 3A 20 22 53 57 41 50     "type": "SWAP
000008C0   22 2C 20 20 20 20 20 20 20 20 22 73 6F 75 72 63  ",        "sourc
000008D0   65 22 3A 20 22 50 55 4D 50 5F 41 4D 4D 22 2C 20  e": "PUMP_AMM", 
000008E0   20 20 20 20 20 20 20 22 66 65 65 50 61 79 65 72         "feePayer
000008F0   22 3A 20 22 38 4A 6A 56 4E 79 6A 36 35 4E 79 68  ": "8JjVNyj65Nyh
00000900   43 75 69 74 32 6E 34 78 41 50 48 61 4D 4A 48 76  Cuit2n4xAPHaMJHv
00000910   4A 73 51 61 72 67 32 33 77 46 72 68 61 77 41 63  JsQarg23wFrhawAc
00000920   22 20 20 20 20 20 20 7D 20 20 20 20 7D 2C 20 20  "      }    },  
00000930   20 20 7B 20 20 20 20 20 20 22 73 69 67 6E 61 74    {      "signat
00000940   75 72 65 22 3A 20 22 33 43 56 71 44 71 37 52 67  ure": "3CVqDq7Rg
00000950   31 57 38 68 45 57 4E 71 50 74 41 6E 6E 45 61 58  1W8hEWNqPtAnnEaX
00000960   32 52 51 4B 35 7A 79 4C 37 50 48 59 34 55 70 35  2RQK5zyL7PHY4Up5
00000970   51 37 67 46 4B 74 71 64 76 72 48 55 62 66 4B 6A  Q7gFKtqdvrHUbfKj
00000980   63 47 74 43 74 57 47 41 76 43 4A 53 61 6A 32 65  cGtCtWGAvCJSaj2e
00000990   6E 59 4D 73 6F 32 6A 4A 37 74 35 67 77 6E 34 22  nYMso2jJ7t5gwn4"
000009A0   2C 20 20 20 20 20 20 22 6C 6F 63 61 6C 22 3A 20  ,      "local": 
000009B0   7B 20 20 20 20 20 20 20 20 22 6B 69 6E 64 22 3A  {        "kind":
000009C0   20 22 70 75 6D 70 73 77 61 70 5F 62 75 79 22 2C   "pumpswap_buy",
000009D0   20 20 20 20 20 20 20 20 22 64 69 72 65 63 74 69          "directi
000009E0   6F 6E 22 3A 20 22 62 75 79 22 2C 20 20 20 20 20  on": "buy",     
000009F0   20 20 20 22 6D 69 6E 74 22 3A 20 22 42 67 37 35     "mint": "Bg75
00000A00   47 61 65 54 4E 37 75 56 71 74 55 45 73 71 75 68  GaeTN7uVqtUEsquh
00000A10   36 31 68 77 45 4E 65 61 41 38 54 44 35 4B 38 48  61hwENeaA8TD5K8H
00000A20   77 58 34 4C 66 6F 6D 6F 22 2C 20 20 20 20 20 20  wX4Lfomo",      
00000A30   20 20 22 75 73 65 72 22 3A 20 22 34 31 6E 38 67    "user": "41n8g
00000A40   79 6F 56 41 75 44 6A 5A 64 55 65 48 52 47 72 50  yoVAuDjZdUeHRGrP
00000A50   73 63 47 32 44 7A 4A 61 77 44 48 70 61 31 59 36  scG2DzJawDHpa1Y6
00000A60   56 4C 56 34 56 71 4A 22 2C 20 20 20 20 20 20 20  VLV4VqJ",       
00000A70   20 22 63 6F 75 6E 74 65 72 70 61 72 74 79 22 3A   "counterparty":
00000A80   20 22 45 4A 58 45 62 64 73 58 46 4A 39 41 46 45   "EJXEbdsXFJ9AFE
00000A90   75 31 45 71 4E 48 79 63 66 41 45 6F 42 4E 37 42  u1EqNHycfAEoBN7B
00000AA0   46 74 66 4C 56 57 78 34 44 73 45 57 55 47 22 20  FtfLVWx4DsEWUG" 
00000AB0   20 20 20 20 20 7D 2C 20 20 20 20 20 20 22 68 65       },      "he
00000AC0   6C 69 75 73 22 3A 20 7B 20 20 20 20 20 20 20 20  lius": {        
00000AD0   22 74 79 70 65 22 3A 20 22 53 57 41 50 22 2C 20  "type": "SWAP", 
00000AE0   20 20 20 20 20 20 20 22 73 6F 75 72 63 65 22 3A         "source":
00000AF0   20 22 50 55 4D 50 5F 41 4D 4D 22 2C 20 20 20 20   "PUMP_AMM",    
00000B00   20 20 20 20 22 66 65 65 50 61 79 65 72 22 3A 20      "feePayer": 
00000B10   22 34 31 6E 38 67 79 6F 56 41 75 44 6A 5A 64 55  "41n8gyoVAuDjZdU
00000B20   65 48 52 47 72 50 73 63 47 32 44 7A 4A 61 77 44  eHRGrPscG2DzJawD
00000B30   48 70 61 31 59 36 56 4C 56 34 56 71 4A 22 20 20  Hpa1Y6VLV4VqJ"  
00000B40   20 20 20 20 7D 20 20 20 20 7D 2C 20 20 20 20 7B      }    },    {
00000B50   20 20 20 20 20 20 22 73 69 67 6E 61 74 75 72 65        "signature
00000B60   22 3A 20 22 34 50 48 32 73 4C 51 77 31 46 4D 67  ": "4PH2sLQw1FMg
00000B70   59 55 76 54 70 6F 52 77 38 78 69 6D 6F 38 50 4C  YUvTpoRw8ximo8PL
00000B80   32 74 56 62 4C 78 79 66 78 70 64 6A 50 4D 7A 58  2tVbLxyfxpdjPMzX
00000B90   53 39 6D 42 41 66 74 4A 44 42 61 6A 56 70 46 75  S9mBAftJDBajVpFu
00000BA0   4E 54 31 57 78 31 53 63 39 42 31 63 42 34 48 70  NT1Wx1Sc9B1cB4Hp
00000BB0   78 4A 6E 75 33 61 31 4C 65 5A 64 65 22 2C 20 20  xJnu3a1LeZde",  
00000BC0   20 20 20 20 22 6C 6F 63 61 6C 22 3A 20 7B 20 20      "local": {  
00000BD0   20 20 20 20 20 20 22 6B 69 6E 64 22 3A 20 22 70        "kind": "p
00000BE0   75 6D 70 73 77 61 70 5F 62 75 79 22 2C 20 20 20  umpswap_buy",   
00000BF0   20 20 20 20 20 22 64 69 72 65 63 74 69 6F 6E 22       "direction"
00000C00   3A 20 22 62 75 79 22 2C 20 20 20 20 20 20 20 20  : "buy",        
00000C10   22 6D 69 6E 74 22 3A 20 22 73 70 43 50 4E 41 41  "mint": "spCPNAA
00000C20   6D 68 76 58 61 6B 63 45 6B 77 45 76 77 4C 6E 71  mhvXakcEkwEvwLnq
00000C30   54 63 71 4A 4B 71 62 56 52 77 76 33 74 45 73 63  TcqJKqbVRwv3tEsc
00000C40   70 75 6D 70 22 2C 20 20 20 20 20 20 20 20 22 75  pump",        "u
00000C50   73 65 72 22 3A 20 22 32 56 70 4E 37 47 68 55 4B  ser": "2VpN7GhUK
00000C60   56 54 56 53 66 78 59 4B 35 37 56 31 55 4E 4A 6B  VTVSfxYK57V1UNJk
00000C70   4D 68 59 56 6D 55 71 70 39 34 6A 56 64 73 75 46  MhYVmUqp94jVdsuF
00000C80   6D 50 57 22 2C 20 20 20 20 20 20 20 20 22 63 6F  mPW",        "co
00000C90   75 6E 74 65 72 70 61 72 74 79 22 3A 20 22 4A 39  unterparty": "J9
00000CA0   51 43 6B 32 4C 37 38 76 47 36 65 76 63 43 44 6D  QCk2L78vG6evcCDm
00000CB0   6D 59 68 56 44 39 51 4C 6E 4C 4D 7A 32 73 33 4D  mYhVD9QLnLMz2s3M
00000CC0   36 62 55 61 32 6D 57 45 36 55 22 20 20 20 20 20  6bUa2mWE6U"     
00000CD0   20 7D 2C 20 20 20 20 20 20 22 68 65 6C 69 75 73   },      "helius
00000CE0   22 3A 20 7B 20 20 20 20 20 20 20 20 22 74 79 70  ": {        "typ
00000CF0   65 22 3A 20 22 53 57 41 50 22 2C 20 20 20 20 20  e": "SWAP",     
00000D00   20 20 20 22 73 6F 75 72 63 65 22 3A 20 22 50 55     "source": "PU
00000D10   4D 50 5F 41 4D 4D 22 2C 20 20 20 20 20 20 20 20  MP_AMM",        
00000D20   22 66 65 65 50 61 79 65 72 22 3A 20 22 32 56 70  "feePayer": "2Vp
00000D30   4E 37 47 68 55 4B 56 54 56 53 66 78 59 4B 35 37  N7GhUKVTVSfxYK57
00000D40   56 31 55 4E 4A 6B 4D 68 59 56 6D 55 71 70 39 34  V1UNJkMhYVmUqp94
00000D50   6A 56 64 73 75 46 6D 50 57 22 20 20 20 20 20 20  jVdsuFmPW"      
00000D60   7D 20 20 20 20 7D 2C 20 20 20 20 7B 20 20 20 20  }    },    {    
00000D70   20 20 22 73 69 67 6E 61 74 75 72 65 22 3A 20 22    "signature": "
00000D80   32 54 6A 52 79 4E 55 54 38 50 4C 57 59 75 34 35  2TjRyNUT8PLWYu45
00000D90   66 4B 67 56 42 54 4D 58 48 56 64 45 35 76 50 34  fKgVBTMXHVdE5vP4
00000DA0   33 32 43 69 79 36 4A 39 64 57 50 50 6D 51 75 39  32Ciy6J9dWPPmQu9
00000DB0   6E 61 70 38 43 35 70 31 42 79 6D 53 64 77 37 59  nap8C5p1BymSdw7Y
00000DC0   59 48 31 44 62 46 46 38 67 76 7A 42 4C 36 4B 50  YH1DbFF8gvzBL6KP
00000DD0   42 56 6E 4E 4B 55 6D 43 22 2C 20 20 20 20 20 20  BVnNKUmC",      
00000DE0   22 6C 6F 63 61 6C 22 3A 20 7B 20 20 20 20 20 20  "local": {      
00000DF0   20 20 22 6B 69 6E 64 22 3A 20 22 70 75 6D 70 73    "kind": "pumps
00000E00   77 61 70 5F 73 65 6C 6C 22 2C 20 20 20 20 20 20  wap_sell",      
00000E10   20 20 22 64 69 72 65 63 74 69 6F 6E 22 3A 20 22    "direction": "
00000E20   73 65 6C 6C 22 2C 20 20 20 20 20 20 20 20 22 6D  sell",        "m
00000E30   69 6E 74 22 3A 20 22 53 6F 31 31 31 31 31 31 31  int": "So1111111
00000E40   31 31 31 31 31 31 31 31 31 31 31 31 31 31 31 31  1111111111111111
00000E50   31 31 31 31 31 31 31 31 31 31 31 31 31 31 31 31  1111111111111111
00000E60   31 32 22 2C 20 20 20 20 20 20 20 20 22 75 73 65  12",        "use
00000E70   72 22 3A 20 22 44 35 55 66 6E 4E 46 6E 4B 72 55  r": "D5UfnNFnKrU
00000E80   74 38 48 62 79 38 72 64 77 4C 59 35 50 7A 33 67  t8Hby8rdwLY5Pz3g
00000E90   46 6E 5A 4D 52 62 37 4D 68 54 41 39 42 34 46 79  FnZMRb7MhTA9B4Fy
00000EA0   62 22 2C 20 20 20 20 20 20 20 20 22 63 6F 75 6E  b",        "coun
00000EB0   74 65 72 70 61 72 74 79 22 3A 20 22 41 6A 58 4C  terparty": "AjXL
00000EC0   4E 71 6F 59 6B 56 4A 50 42 59 77 51 48 75 6A 50  NqoYkVJPBYwQHujP
00000ED0   74 58 6D 46 4C 6A 57 6F 47 64 4E 44 6B 69 78 6A  tXmFLjWoGdNDkixj
00000EE0   6F 4D 7A 38 75 50 44 5A 22 20 20 20 20 20 20 7D  oMz8uPDZ"      }
00000EF0   2C 20 20 20 20 20 20 22 68 65 6C 69 75 73 22 3A  ,      "helius":
00000F00   20 7B 20 20 20 20 20 20 20 20 22 74 79 70 65 22   {        "type"
00000F10   3A 20 22 53 57 41 50 22 2C 20 20 20 20 20 20 20  : "SWAP",       
00000F20   20 22 73 6F 75 72 63 65 22 3A 20 22 50 55 4D 50   "source": "PUMP
00000F30   5F 41 4D 4D 22 2C 20 20 20 20 20 20 20 20 22 66  _AMM",        "f
00000F40   65 65 50 61 79 65 72 22 3A 20 22 44 35 55 66 6E  eePayer": "D5Ufn
00000F50   4E 46 6E 4B 72 55 74 38 48 62 79 38 72 64 77 4C  NFnKrUt8Hby8rdwL
00000F60   59 35 50 7A 33 67 46 6E 5A 4D 52 62 37 4D 68 54  Y5Pz3gFnZMRb7MhT
00000F70   41 39 42 34 46 79 62 22 20 20 20 20 20 20 7D 20  A9B4Fyb"      } 
00000F80   20 20 20 7D 2C 20 20 20 20 7B 20 20 20 20 20 20     },    {      
00000F90   22 73 69 67 6E 61 74 75 72 65 22 3A 20 22 32 36  "signature": "26
00000FA0   71 74 6D 74 4C 53 44 6A 45 64 48 61 31 65 36 6D  qtmtLSDjEdHa1e6m
00000FB0   48 73 42 5A 64 38 43 38 45 79 75 37 76 61 53 41  HsBZd8C8Eyu7vaSA
00000FC0   61 47 57 33 66 44 77 77 73 72 6F 32 4C 70 6F 76  aGW3fDwwsro2Lpov
00000FD0   53 36 33 57 41 4B 41 77 59 77 61 52 59 79 69 6D  S63WAKAwYwaRYyim
00000FE0   4C 44 31 61 61 63 70 35 33 53 41 77 59 47 39 31  LD1aacp53SAwYG91
00000FF0   4E 65 58 79 7A 63 22 2C 20 20 20 20 20 20 22 6C  NeXyzc",      "l
00001000   6F 63 61 6C 22 3A 20 7B 20 20 20 20 20 20 20 20  ocal": {        
00001010   22 6B 69 6E 64 22 3A 20 22 70 75 6D 70 5F 73 65  "kind": "pump_se
00001020   6C 6C 22 2C 20 20 20 20 20 20 20 20 22 64 69 72  ll",        "dir
00001030   65 63 74 69 6F 6E 22 3A 20 22 73 65 6C 6C 22 2C  ection": "sell",
00001040   20 20 20 20 20 20 20 20 22 6D 69 6E 74 22 3A 20          "mint": 
00001050   22 41 55 57 66 78 70 72 46 63 4A 37 74 6A 6E 51  "AUWfxprFcJ7tjnQ
00001060   7A 76 65 75 50 35 6E 65 72 4C 5A 36 65 32 6E 6E  zveuP5nerLZ6e2nn
00001070   72 42 7A 64 51 42 72 4C 67 70 75 6D 70 22 2C 20  rBzdQBrLgpump", 
00001080   20 20 20 20 20 20 20 22 75 73 65 72 22 3A 20 22         "user": "
00001090   33 72 44 39 43 7A 6D 4C 47 66 79 61 41 67 46 62  3rD9CzmLGfyaAgFb
000010A0   54 38 47 4D 36 6D 4A 69 33 4C 59 6F 63 65 47 69  T8GM6mJi3LYoceGi
000010B0   32 4A 4C 31 45 5A 65 63 44 68 47 73 22 2C 20 20  2JL1EZecDhGs",  
000010C0   20 20 20 20 20 20 22 63 6F 75 6E 74 65 72 70 61        "counterpa
000010D0   72 74 79 22 3A 20 22 38 39 4C 78 6F 75 75 44 5A  rty": "89LxouuDZ
000010E0   50 31 6D 47 39 32 69 65 6B 34 67 58 5A 46 50 6D  P1mG92iek4gXZFPm
000010F0   32 38 69 43 34 59 63 43 53 4D 5A 67 5A 6B 70 53  28iC4YcCSMZgZkpS
00001100   67 7A 57 22 20 20 20 20 20 20 7D 2C 20 20 20 20  gzW"      },    
00001110   20 20 22 68 65 6C 69 75 73 22 3A 20 7B 20 20 20    "helius": {   
00001120   20 20 20 20 20 22 74 79 70 65 22 3A 20 22 53 57       "type": "SW
00001130   41 50 22 2C 20 20 20 20 20 20 20 20 22 73 6F 75  AP",        "sou
00001140   72 63 65 22 3A 20 22 50 55 4D 50 5F 46 55 4E 22  rce": "PUMP_FUN"
00001150   2C 20 20 20 20 20 20 20 20 22 66 65 65 50 61 79  ,        "feePay
00001160   65 72 22 3A 20 22 33 72 44 39 43 7A 6D 4C 47 66  er": "3rD9CzmLGf
00001170   79 61 41 67 46 62 54 38 47 4D 36 6D 4A 69 33 4C  yaAgFbT8GM6mJi3L
00001180   59 6F 63 65 47 69 32 4A 4C 31 45 5A 65 63 44 68  YoceGi2JL1EZecDh
00001190   47 73 22 20 20 20 20 20 20 7D 20 20 20 20 7D 2C  Gs"      }    },
000011A0   20 20 20 20 7B 20 20 20 20 20 20 22 73 69 67 6E      {      "sign
000011B0   61 74 75 72 65 22 3A 20 22 62 72 57 53 35 67 61  ature": "brWS5ga
000011C0   4D 73 70 61 48 56 6E 68 37 75 73 54 34 6D 57 64  MspaHVnh7usT4mWd
000011D0   72 6D 36 41 6F 43 73 69 56 78 38 59 67 42 79 6D  rm6AoCsiVx8YgBym
000011E0   4B 36 36 6E 41 43 4B 6D 6D 48 38 7A 4C 4E 4A 57  K66nACKmmH8zLNJW
000011F0   31 4C 77 74 6F 5A 78 73 72 79 65 4B 38 75 32 53  1LwtoZxsryeK8u2S
00001200   34 47 6D 58 44 68 65 43 38 62 75 34 76 6A 76 5A  4GmXDheC8bu4vjvZ
00001210   22 2C 20 20 20 20 20 20 22 6C 6F 63 61 6C 22 3A  ",      "local":
00001220   20 7B 20 20 20 20 20 20 20 20 22 6B 69 6E 64 22   {        "kind"
00001230   3A 20 22 70 75 6D 70 73 77 61 70 5F 73 65 6C 6C  : "pumpswap_sell
00001240   22 2C 20 20 20 20 20 20 20 20 22 64 69 72 65 63  ",        "direc
00001250   74 69 6F 6E 22 3A 20 22 73 65 6C 6C 22 2C 20 20  tion": "sell",  
00001260   20 20 20 20 20 20 22 6D 69 6E 74 22 3A 20 22 42        "mint": "B
00001270   67 37 35 47 61 65 54 4E 37 75 56 71 74 55 45 73  g75GaeTN7uVqtUEs
00001280   71 75 68 36 31 68 77 45 4E 65 61 41 38 54 44 35  quh61hwENeaA8TD5
00001290   4B 38 48 77 58 34 4C 66 6F 6D 6F 22 2C 20 20 20  K8HwX4Lfomo",   
000012A0   20 20 20 20 20 22 75 73 65 72 22 3A 20 22 38 4A       "user": "8J
000012B0   6A 56 4E 79 6A 36 35 4E 79 68 43 75 69 74 32 6E  jVNyj65NyhCuit2n
000012C0   34 78 41 50 48 61 4D 4A 48 76 4A 73 51 61 72 67  4xAPHaMJHvJsQarg
000012D0   32 33 77 46 72 68 61 77 41 63 22 2C 20 20 20 20  23wFrhawAc",    
000012E0   20 20 20 20 22 63 6F 75 6E 74 65 72 70 61 72 74      "counterpart
000012F0   79 22 3A 20 22 45 4A 58 45 62 64 73 58 46 4A 39  y": "EJXEbdsXFJ9
00001300   41 46 45 75 31 45 71 4E 48 79 63 66 41 45 6F 42  AFEu1EqNHycfAEoB
00001310   4E 37 42 46 74 66 4C 56 57 78 34 44 73 45 57 55  N7BFtfLVWx4DsEWU
00001320   47 22 20 20 20 20 20 20 7D 2C 20 20 20 20 20 20  G"      },      
00001330   22 68 65 6C 69 75 73 22 3A 20 7B 20 20 20 20 20  "helius": {     
00001340   20 20 20 22 74 79 70 65 22 3A 20 22 53 57 41 50     "type": "SWAP
00001350   22 2C 20 20 20 20 20 20 20 20 22 73 6F 75 72 63  ",        "sourc
00001360   65 22 3A 20 22 50 55 4D 50 5F 41 4D 4D 22 2C 20  e": "PUMP_AMM", 
00001370   20 20 20 20 20 20 20 22 66 65 65 50 61 79 65 72         "feePayer
00001380   22 3A 20 22 38 4A 6A 56 4E 79 6A 36 35 4E 79 68  ": "8JjVNyj65Nyh
00001390   43 75 69 74 32 6E 34 78 41 50 48 61 4D 4A 48 76  Cuit2n4xAPHaMJHv
000013A0   4A 73 51 61 72 67 32 33 77 46 72 68 61 77 41 63  JsQarg23wFrhawAc
000013B0   22 20 20 20 20 20 20 7D 20 20 20 20 7D 2C 20 20  "      }    },  
000013C0   20 20 7B 20 20 20 20 20 20 22 73 69 67 6E 61 74    {      "signat
000013D0   75 72 65 22 3A 20 22 32 55 4A 43 52 73 44 57 52  ure": "2UJCRsDWR
000013E0   70 31 69 4A 52 56 64 53 38 75 72 62 79 68 4C 53  p1iJRVdS8urbyhLS
000013F0   68 41 62 37 56 77 4D 6D 70 77 43 73 59 6B 77 33  hAb7VwMmpwCsYkw3
00001400   57 59 43 4C 75 41 70 48 37 6E 34 68 4B 47 58 63  WYCLuApH7n4hKGXc
00001410   39 7A 39 45 39 55 73 75 34 50 41 53 47 71 4D 71  9z9E9Usu4PASGqMq
00001420   44 77 6B 33 46 76 51 4B 43 4D 58 66 66 52 6A 22  Dwk3FvQKCMXffRj"
00001430   2C 20 20 20 20 20 20 22 6C 6F 63 61 6C 22 3A 20  ,      "local": 
00001440   7B 20 20 20 20 20 20 20 20 22 6B 69 6E 64 22 3A  {        "kind":
00001450   20 22 70 75 6D 70 73 77 61 70 5F 73 65 6C 6C 22   "pumpswap_sell"
00001460   2C 20 20 20 20 20 20 20 20 22 64 69 72 65 63 74  ,        "direct
00001470   69 6F 6E 22 3A 20 22 73 65 6C 6C 22 2C 20 20 20  ion": "sell",   
00001480   20 20 20 20 20 22 6D 69 6E 74 22 3A 20 22 53 6F       "mint": "So
00001490   31 31 31 31 31 31 31 31 31 31 31 31 31 31 31 31  1111111111111111
000014A0   31 31 31 31 31 31 31 31 31 31 31 31 31 31 31 31  1111111111111111
000014B0   31 31 31 31 31 31 31 31 32 22 2C 20 20 20 20 20  111111112",     
000014C0   20 20 20 22 75 73 65 72 22 3A 20 22 45 62 63 54     "user": "EbcT
000014D0   65 66 74 38 59 37 74 35 59 61 57 67 71 69 32 42  eft8Y7t5YaWgqi2B
000014E0   51 43 7A 4E 7A 67 4B 63 6B 77 74 46 33 7A 5A 4B  QCzNzgKckwtF3zZK
000014F0   48 51 64 57 78 68 6A 4B 22 2C 20 20 20 20 20 20  HQdWxhjK",      
00001500   20 20 22 63 6F 75 6E 74 65 72 70 61 72 74 79 22    "counterparty"
00001510   3A 20 22 36 43 53 73 63 38 6A 6D 56 55 52 38 66  : "6CSsc8jmVUR8f
00001520   6B 37 6F 38 6A 77 78 73 51 62 47 66 6F 63 4B 42  k7o8jwxsQbGfocKB
00001530   4E 75 65 56 79 57 79 63 70 37 52 65 59 57 34 22  NueVyWycp7ReYW4"
00001540   20 20 20 20 20 20 7D 2C 20 20 20 20 20 20 22 68        },      "h
00001550   65 6C 69 75 73 22 3A 20 7B 20 20 20 20 20 20 20  elius": {       
00001560   20 22 74 79 70 65 22 3A 20 22 53 57 41 50 22 2C   "type": "SWAP",
00001570   20 20 20 20 20 20 20 20 22 73 6F 75 72 63 65 22          "source"
00001580   3A 20 22 50 55 4D 50 5F 41 4D 4D 22 2C 20 20 20  : "PUMP_AMM",   
00001590   20 20 20 20 20 22 66 65 65 50 61 79 65 72 22 3A       "feePayer":
000015A0   20 22 45 62 63 54 65 66 74 38 59 37 74 35 59 61   "EbcTeft8Y7t5Ya
000015B0   57 67 71 69 32 42 51 43 7A 4E 7A 67 4B 63 6B 77  Wgqi2BQCzNzgKckw
000015C0   74 46 33 7A 5A 4B 48 51 64 57 78 68 6A 4B 22 20  tF3zZKHQdWxhjK" 
000015D0   20 20 20 20 20 7D 20 20 20 20 7D 20 20 5D 7D          }    }  ]}
[0m
[0m$ [0mgit show 2ab477f:data/locks/data-ingestion.lock
19816
1790291576311
[0m
[0m$ [0mgit ls-tree -r --name-only 2ab477f tests packages 2>$null | Select-String -Pattern 
"helius|rpc-provider|integr" ; git ls-tree -r --name-only 2ab477f | Select-String -Pattern "test" | Select-Object 
-First 60
packages/solana/src/helius-provider.ts
packages/solana/src/rpc-provider.ts
.agents/backtest-ml.md
.opencode/agents/backtest-ml.md
backtester/README.md
backtester/fixtures/.gitkeep
backtester/package.json
backtester/reports/.gitkeep
backtester/scenarios/.gitkeep
backtester/src/index.ts
backtester/tsconfig.json
docs/architecture/adr/ADR-004-vitest-fast-check-pytest-hypothesis.md
docs/security/red-team/poc/sprint2-fuzz.poc.test.ts
docs/security/red-team/poc/sprint2-ingestion.poc.test.ts
docs/security/red-team/poc/vitest.poc.config.mjs
ml/pytest.ini
packages/database/src/repositories/audit-log.test.ts
packages/database/src/repositories/observed-events.test.ts
packages/database/src/repositories/system-events.test.ts
packages/pumpfun/src/decode.test.ts
packages/solana/src/backoff.test.ts
services/data-ingestion/src/dedup.test.ts
services/data-ingestion/src/enricher.test.ts
services/data-ingestion/src/instance-lock.test.ts
services/data-ingestion/src/normalizer.test.ts
services/data-ingestion/src/retention.test.ts
services/data-ingestion/src/security-fixes.test.ts
tests/property/dedup.property.test.ts
tests/unit/config.live-guard.test.ts
vitest.config.ts
[0m
## Objective
- Act as code-reviewer agent and adversarially review commit `2ab477f7dc60361db18e2106cd4b9ef48aea977e` (branch 
`sprint/f1-5-enrichment`), per `.agents/code-reviewer.md` and `AGENTS.md`.
- Output findings with severities and end with the exact line `VERDICT: APROVADO` or `VERDICT: REJEITADO`. Do NOT 
modify code or commit.

## Important Details
- Reviewer is on `master`; commit under review is on branch `sprint/f1-5-enrichment` ÔÇö all file reads must use `git 
show 2ab477f:<path>` (working tree is master and differs from the commit).
- Commit diff: 7 files, +214/ÔêÆ18: `data/locks/data-ingestion.lock` (new), `f15-fixtures-report.json` (new, starts 
with UTF-8 BOM U+FEFF), `packages/pumpfun/scripts/fix-esm-imports.mjs`, `packages/solana/src/helius-provider.ts`, 
`packages/solana/src/rpc-provider.ts` (maxSupportedTransactionVersion 0ÔåÆ1), `scripts/f15-backfill.mjs` (new), 
`scripts/f15-fixtures.mjs` (new).
- Helius FACT claim of the commit verified true: Helius docs state `logsSubscribe` `mentions` supports only ONE Pubkey 
per call; listing additional addresses results in error ÔåÆ per-wallet subscription change is justified (not a 
finding).
- Review criteria: bugs, errors, incompatibilities, AGENTS.md violations, missing tests; never swallow errors silently 
(rule 9); no-console is error (use `@ct/logging`); dedup key `signature + instructionIndex + wallet` (rule 6).
- `.gitignore` ignores `data/raw/` and `data/local/` but NOT `data/locks/`.
- Env name `SOLANA_RPC_HTTP` is canonical (packages/config/src/index.ts:36, `z.string().url()`); 
`INGESTION_WATCH_ADDRESSES` is optional CSV, no wallet-count cap found.
- Cannot modify code or commit in this stage ÔÇö static review. Working tree (master) has pre-existing modified files: 
`docs/security/red-team/FINDINGS.md`, `SPRINTS.md`, `poc/sprint2-ingestion.poc.test.ts`.

## Work State
### Completed
- Read full commit diff and file trees at `2ab477f` (`git show`), plus: helius-provider.ts, rpc-provider.ts, 
enricher.ts, enrich-runner.ts, instance-lock.ts, dedup-store.ts, config index, database index.ts, observed-events.ts 
repo, schema.prisma, pumpfun decode.ts/DecodedSwap, fix-esm-imports.mjs, f15-backfill.mjs, f15-fixtures.mjs, 
.gitignore, data-ingestion main.ts/index.ts, enricher.test.ts, database exports.
- Confirmed Helius per-wallet subscription restriction via helius-docs search (FACT validated).
- Confirmed **no test exists for `helius-provider.ts`** (packages/solana/src has only `backoff.test.ts`); rpc-provider 
change also has no test.
- Verified `InstanceLock.acquire()` reads lock file line 1 as PID, throws `CONFIG_ERROR: j├í existe inst├óncia ativa 
(PID <pid>)` if PID is alive and != current; lock format is `PID\ntimestamp\n`. Committed 
`data/locks/data-ingestion.lock` contains `19816\n1790291576311` (runtime artifact).
- Verified `f15-fixtures.mjs` match logic: only checks `helius.type === 'SWAP' || 'SWAP_PARTIAL'`; `source`, 
user/mint/counterparty/amounts never compared; null `helius` rows skipped without counting mismatch; errors silently 
counted (`probes.errors++`) without logging cause; report row 2 shows `local.user` BwWKÔÇª Ôëá `helius.feePayer` 
GygjÔÇª (unflagged divergence).
- Verified `f15-fixtures-report.json` begins with BOM ÔåÆ `JSON.parse` fails in Node; committed at repo root; key typo 
`helisMatch`.
- Verified `fix-esm-imports.mjs` dead ternary (`spec === '.' || spec === '..' ? spec + '/' : spec + '/'` ÔÇö both 
branches identical).
- Verified enricher/enrich-runner APIs used by scripts exist at `2ab477f` (`runUntilEmpty`, 
`stats.seen/enriched/undecodable/retried`, `DecodedSwap` fields, database repo exports 
`markEnriched/markUndecodable/findUnenriched/incrementEnrichAttempt`).

### Active
- Drafting final findings list; verdict not yet issued.
- Still must explicitly read `.agents/code-reviewer.md` and `AGENTS.md` (user explicitly said "leia"; not shown done 
in transcript) to anchor the verdict criteria.

### Blocked
- None. (Running tests would require checking out `2ab477f`; not done ÔÇö static review only per "NAO modifique 
codigo".)

## Next Move
1. Read `.agents/code-reviewer.md` and `AGENTS.md` (missing step required by the user prompt).
2. Optionally read `services/data-ingestion/src/instance-lock.test.ts` at `2ab477f` to confirm lock-file semantics 
before flagging severity.
3. Deliver final response ending with exact `VERDICT: REJEITADO` followed by findings, each tagged 
CRITICAL/HIGH/MEDIUM/LOW. Tentative findings: (HIGH) runtime lock file committed ÔÇö nondeterministic boot block if 
PID 19816 alive on any machine; (HIGH) zero tests for the rewired per-wallet WSS subscription path 
(helius-provider.ts) and rpc-provider change; (HIGH) reconnect path leaks open sockets/partial subscriptions (failed 
`sendLogsSubscribe` ÔåÆ `openSocket` rejected but socket stays open/`connected=true`; retry creates new socket via 
`this.ws` overwrite without closing old; no `logsUnsubscribe` ever sent; `subscriptionId` single field overwritten by 
last sub; `Promise.all` partial success leaves live server-side subs on a "failed" connection); (MEDIUM) fixture 
"10/10 vs Helius Enhanced" over-claimed ÔÇö match ignores source/user/mint/direction/amounts, null rows skipped, 14 
errors silently swallowed, row 2 userÔëáfeePayer demonstrates uncaught divergence; (MEDIUM) BOM in 
`f15-fixtures-report.json` breaks `JSON.parse` (plus repo-root artifact, `helisMatch` typo); (LOW) dead ternary in 
fix-esm-imports.mjs and edge case `'./'` ÔåÆ `'.//index.js'`; (LOW) scripts use `console.error` (AGENTS.md no-console, 
deliberate stdout/stderr split); (LOW) `f15-backfill.mjs` lacks env guard for `SOLANA_RPC_HTTP` (fixtures script 
guards); (LOW) `f15-fixtures.mjs` no fetch timeout; `TARGET_AMM=2` hardcoded vs commit message "8 PumpSwap" (8 is 
incidental); `maxSupportedTransactionVersion: 1` is permissive/safe (verdict: LOW/non-issue).

## Relevant Files
- `packages/solana/src/helius-provider.ts`: per-wallet `sendLogsSubscribe`/`sendSingleSubscribe` rewrite ÔÇö core HIGH 
findings (no tests, socket/subscription leak, single `subscriptionId` field).
- `packages/solana/src/rpc-provider.ts`: `maxSupportedTransactionVersion` 0ÔåÆ1 change at `2ab477f` (working master 
version still shows 0).
- `data/locks/data-ingestion.lock`: committed runtime lock (PID 19816) ÔÇö boot-block risk; `data/locks/` missing from 
`.gitignore`.
- `services/data-ingestion/src/instance-lock.ts`: lock acquire/release semantics (CONFIG_ERROR on live PID).
- `services/data-ingestion/src/dedup-store.ts`: dedup key `(signature, instructionIndex, wallet)` ÔÇö absorbs 
per-wallet duplicate notifications.
- `services/data-ingestion/src/enricher.ts` / `enrich-runner.ts` / `enricher.test.ts`: APIs consumed by 
backfill/fixtures scripts; tests exist only for enricher here.
- `scripts/f15-fixtures.mjs`: weak Helius comparison, swallowed errors, `TARGET_AMM=2`, no timeout.
- `scripts/f15-backfill.mjs`: no env guard for `SOLANA_RPC_HTTP`.
- `f15-fixtures-report.json`: BOM-prefixed, root-level artifact, `helisMatch` typo, row-2 userÔëáfeePayer divergence.
- `packages/pumpfun/scripts/fix-esm-imports.mjs`: dead ternary, `'.//index.js'` edge case.
- `packages/database/src/index.ts`, `packages/database/src/repositories/observed-events.ts`, 
`packages/database/prisma/schema.prisma`: repo/schema wiring verified compatible with scripts.
- `packages/config/src/index.ts`: canonical env var names (`SOLANA_RPC_HTTP`, `HELIUS_API_KEY`, 
`INGESTION_WATCH_ADDRESSES`).
- `.agents/code-reviewer.md`, `AGENTS.md`: required reading, not yet performed.
Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.
[0mÔåÆ [0mRead .agents/code-reviewer.md
[0mÔåÆ [0mRead AGENTS.md
[0m
[0m$ [0mgit show 2ab477f:services/data-ingestion/src/instance-lock.test.ts | Select-Object -First 80
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import { mkdtempSync, rmSync, writeFileSync, existsSync } from 'node:fs';
import { spawn } from 'node:child_process';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { InstanceLock, isProcessAlive } from './instance-lock.js';

let dir: string;
let lockPath: string;

beforeEach(() => {
  dir = mkdtempSync(join(tmpdir(), 'ct-lock-'));
  lockPath = join(dir, 'test.lock');
});
afterEach(() => {
  rmSync(dir, { recursive: true, force: true });
});

describe('InstanceLock', () => {
  it('adquire e libera o lock', () => {
    const lock = new InstanceLock(lockPath);
    lock.acquire();
    expect(existsSync(lockPath)).toBe(true);
    lock.release();
    expect(existsSync(lockPath)).toBe(false);
  });

  it('rejeita segunda aquisi├º├úo quando o PID do lock est├í vivo', () => {
    // Spawna um processo real e vivo para garantir o caminho de rejei├º├úo.
    const child = spawn(process.execPath, ['-e', 'setInterval(() => {}, 1000)'], {
      detached: true,
      stdio: 'ignore',
    });
    try {
      child.unref();
      expect(child.pid).toBeDefined();
      writeFileSync(lockPath, `${child.pid}\n`);
      const lock = new InstanceLock(lockPath);
      expect(() => lock.acquire()).toThrowError(/CONFIG_ERROR/);
    } finally {
      if (child.pid) {
        try {
          process.kill(child.pid);
        } catch {
          /* processo j├í morto ÔÇö ok no teardown */
        }
      }
    }
  });

  it('lock stale (PID morto) ├® substitu├¡do', () => {
    // 4194303 ├® quase certamente inexistente
    writeFileSync(lockPath, '4194303\n');
    const lock = new InstanceLock(lockPath);
    lock.acquire();
    expect(existsSync(lockPath)).toBe(true);
    lock.release();
  });

  it('lock corrompido (n├úo-n├║mero) ├® substitu├¡do', () => {
    writeFileSync(lockPath, 'garbage\n');
    const lock = new InstanceLock(lockPath);
    lock.acquire();
    lock.release();
  });

  it('release n├úo remove lock de outro PID (seguran├ºa contra corrida)', () => {
    const lock = new InstanceLock(lockPath);
    lock.acquire();
    writeFileSync(lockPath, '99999\n'); // outro processo "roubou" o lock
    lock.release();
    expect(existsSync(lockPath)).toBe(true);
  });
});

describe('isProcessAlive', () => {
  it('nosso pr├│prio PID est├í vivo', () => {
    expect(isProcessAlive(process.pid)).toBe(true);
  });
  it('PID inexistente est├í morto', () => {
[0m
[0m$ [0m$c = git show 2ab477f:packages/solana/src/helius-provider.ts | Select-String -Pattern "openSocket|this\.ws|se
ndLogsSubscribe|subscribeWallets|scheduleReconnect|subscriptionId|Promise\.all|connected|lastMessageAt|close\(\)|logsUn
subscribe" | ForEach-Object { "$($_.LineNumber): $($_.Line.Trim())" }; $c
7: * (logsSubscribe / logsUnsubscribe), com filtro `mentions` por endere├ºo.
94: private subscriptionId: number | null = null;
99: private lastMessageAt = 0;
101: private connected = false;
109: this.wsFactory = options.webSocketFactory ?? ((url: string) => new WebSocket(url));
112: this.wsUrl = options.wsUrl;
114: this.wsUrl = `${HELIUS_WSS_BASE}?api-key=${encodeURIComponent(options.apiKey)}`;
117: this.wsUrl = SOLANA_PUBLIC_WSS;
139: return this.openSocket();
142: async subscribeWallets(wallets: string[]): Promise<void> {
145: throw new ClassifiedError('VALIDATION_ERROR', 'wallet inv├ílida em subscribeWallets');
149: if (this.connected) {
150: await this.sendLogsSubscribe();
154: async close(): Promise<void> {
157: const ws = this.ws;
158: this.ws = null;
159: this.connected = false;
160: this.subscriptionId = null;
165: ws.close();
177: private openSocket(): Promise<void> {
179: const ws = this.wsFactory(this.wsUrl);
180: this.ws = ws;
183: this.connected = true;
184: this.lastMessageAt = Date.now();
187: this.logger.info('wss conectado', { urlHost: safeHost(this.wsUrl) });
188: void this.sendLogsSubscribe()
195: if (!this.connected) {
204: this.lastMessageAt = Date.now();
207: this.connected = false;
208: this.subscriptionId = null;
211: this.scheduleReconnect();
219: private async sendLogsSubscribe(): Promise<void> {
220: if (this.wallets.size === 0 || this.ws === null) return;
223: // endereco (Promise.all; se alguma falhar, o connect falha de forma
226: await Promise.all(wallets.map((wallet) => this.sendSingleSubscribe(wallet)));
238: const ws = this.ws;
259: this.lastMessageAt = Date.now();
292: if (typeof parsed.result === 'number') this.subscriptionId = parsed.result;
294: subscriptionId: this.subscriptionId,
348: const ws = this.ws;
350: const silenceMs = Date.now() - this.lastMessageAt;
371: const ws = this.ws;
373: this.ws = null;
380: if (this.reconnectTimer === null) this.scheduleReconnect();
383: private scheduleReconnect(): void {
390: this.openSocket().catch((err: unknown) => {
394: this.scheduleReconnect();
[0m
Now let me pin exact line numbers for the key findings in the changed files:
[0m$ [0m$c = git show 2ab477f:scripts/f15-fixtures.mjs | Select-String -Pattern "TARGET_AMM|MAX_TRIES|helius\.type|SW
AP|errors|matchCount|local\.user|local\.mint|catch|source|fetch\(|sleep|helisMatch|MISMATCH|continue" | ForEach-Object 
{ "$($_.LineNumber): $($_.Line.Trim())" }; $c
10: const TARGET_AMM = 2;
11: const MAX_TRIES = 300;
17: const res = await fetch(`https://api-mainnet.helius-rpc.com/v0/transactions?api-key=${HELIUS_KEY}`, {
28: const res = await fetch(RPC, {
43: `SELECT signature FROM "ObservedEvent" WHERE "receivedAt" > now() - interval '30 minutes' ORDER BY random() LIMIT 
${MAX_TRIES}`,
47: const probes = { decoded: 0, undecodable: 0, errors: 0 };
52: while (i < bigRows.length && (results.length < N || ammOk < TARGET_AMM)) {
70: continue;
73: if (local.kind.startsWith('pumpswap')) ammOk++;
80: mint: local.mint,
81: user: local.user,
87: source: enhanced.source ?? null,
93: } catch (e) {
94: probes.errors++;
99: mismatch = 0;
101: if (!r.helius) continue;
102: const heliusSwap = r.helius.type === 'SWAP' || r.helius.type === 'SWAP_PARTIAL';
103: if (heliusSwap) match++;
104: else mismatch++;
109: pumpswapFixtures: ammOk,
112: helisMatch: match,
113: heliusMismatch: mismatch,
[0m
[0m$ [0m$c = git show 2ab477f:scripts/f15-backfill.mjs | Select-String -Pattern 
"SOLANA_RPC_HTTP|RPC|HELIUS|process\.env|url|undefined|throw|catch" | ForEach-Object { "$($_.LineNumber): 
$($_.Line.Trim())" }; $c
5: import { RpcProvider } from '../packages/solana/dist/index.js';
17: const rpc = new RpcProvider(
18: [{ name: 'primary', url: process.env.SOLANA_RPC_HTTP, tier: 'primary' }],
23: { getTransaction: (sig) => rpc.getTransaction(sig) },
[0m
[0m$ [0m$c = git show 2ab477f:packages/solana/src/helius-provider.ts | Select-String -Pattern 
"sendSingleSubscribe|pendingSubscribes|if \(typeof parsed|result === 'number'|else" -Context 2,6 | Select-Object 
-First 40; $c
if (options.wsUrl !== undefined) {
        this.wsUrl = options.wsUrl;
>     } else if (options.apiKey !== undefined && options.apiKey.length > 0) {
        this.wsUrl = `${HELIUS_WSS_BASE}?api-key=${encodeURIComponent(options.apiKey)}`;
>     } else {
        // ASSUMPTION: fallback no WSS p├║blico Solana ÔÇö mesmo formato de mensagem.
        this.wsUrl = SOLANA_PUBLIC_WSS;
        this.logger.warn('HELIUS_API_KEY ausente ÔÇö usando WSS p├║blico Solana (rate limits menores)');
      }
    }
  
      // audivel e a rotina de reconnect refaz).
      const wallets = [...this.wallets];
>     await Promise.all(wallets.map((wallet) => this.sendSingleSubscribe(wallet)));
    }
  
>   private sendSingleSubscribe(wallet: string): Promise<void> {
      return new Promise((resolve, reject) => {
        const id = this.nextRequestId++;
        const message = JSON.stringify({
          jsonrpc: '2.0',
          id,
          method: 'logsSubscribe',
            return;
          }
>         this.pendingSubscribes.set(id, { resolve, reject });
        });
      });
    }
  
>   private readonly pendingSubscribes = new Map<
      number,
      { resolve: () => void; reject: (e: Error) => void }
    >();
  
    private handleMessage(data: unknown): void {
      this.lastMessageAt = Date.now();
      if (typeof data === 'string') {
        text = data;
>     } else if (Buffer.isBuffer(data)) {
        text = data.toString('utf8');
>     } else if (Array.isArray(data)) {
        text = Buffer.concat(data).toString('utf8');
>     } else {
        this.logger.warn('wss mensagem de tipo inesperado descartada');
        return;
      }
  
      let parsed: JsonRpcNotification;
      try {
  
      // Resposta a logsSubscribe
>     if (parsed.id !== undefined && this.pendingSubscribes.has(Number(parsed.id))) {
>       const pending = this.pendingSubscribes.get(Number(parsed.id));
>       this.pendingSubscribes.delete(Number(parsed.id));
        if (parsed.error) {
          pending?.reject(
            new ClassifiedError(
              'RPC_ERROR',
              `logsSubscribe rejeitado: ${parsed.error.message}`,
            ),
          );
>       } else {
>         if (typeof parsed.result === 'number') this.subscriptionId = parsed.result;
          this.logger.info('logsSubscribe ativo', {
            subscriptionId: this.subscriptionId,
            wallets: this.wallets.size,
          });
          pending?.resolve();
        }
        this.reconnectTimer = null;
      }
>     this.failPendingSubscribes();
    }
  
    /**
     * Rejeita todas as subscri├º├Áes pendentes. Sem isso, um socket fechado
     * antes da confirma├º├úo deixaria promises pendentes para sempre (leak).
     */
>   private failPendingSubscribes(): void {
>     if (this.pendingSubscribes.size === 0) return;
      const err = new ClassifiedError(
        'RPC_ERROR',
        'socket fechado antes da confirma├º├úo de logsSubscribe',
      );
>     for (const { reject } of this.pendingSubscribes.values()) reject(err);
>     this.pendingSubscribes.clear();
    }
  }
  
  /** Extrai host sem expor api-key em logs (seguran├ºa ┬º2). */
  function safeHost(url: string): string {
    try {
[0m
## VEREDITO: REJEITADO

## ACHADOS

**HIGH-1 ÔÇö `packages/solana/src/helius-provider.ts` (arquivo inteiro, reformulado no commit): c├│digo novo sem 
nenhum teste.**
- Evid├¬ncia: `git ls-tree -r 2ab477f` mostra que `packages/solana/src/` cont├®m apenas `backoff.test.ts`; n├úo existe 
`helius-provider.test.ts` nem `rpc-provider.test.ts` em nenhum diret├│rio. O commit reescreve exatamente o caminho 
mais cr├¡tico do F1.5 (subscriptions WSS por wallet) sem nenhum teste unit├írio/property.
- Reproduzir: `npm test` ÔåÆ nenhum caso exercita `subscribeWallets`, `sendLogsSubscribe`, reconnect, partial failure 
ou timeout.
- Crit├®rio: `.agents/code-reviewer.md` ÔÇö "Aus├¬ncia de testes em c├│digo novo ÔåÆ REJEITADO" + AGENTS.md ┬º4.5/┬º6.
- Sugest├úo: testes com WebSocket fake (wsFactory j├í injet├ível): uma wallet v├ílida ÔåÆ 1├ù `logsSubscribe` com 1 
mention; N wallets ÔåÆ N chamadas; sub rejeitada ÔåÆ conex├úo falha e socket anterior ├® fechado; mensagem de erro RPC 
ÔåÆ promise rejeitada com `RPC_ERROR`; closed-socket antes de ack ÔåÆ `failPendingSubscribes`; reconnect n├úo duplica 
subscriptions server-side.

**HIGH-2 ÔÇö `packages/solana/src/helius-provider.ts:177-180,188-190,371-394` ÔÇö leak de socket + amplifica├º├úo de 
subscriptions em falha parcial; sem `logsUnsubscribe` em lugar nenhum.**
- Evid├¬ncia: em `onOpen` (`L183-190`), `connected=true` antes do `sendLogsSubscribe().catch(reject)`. 
`sendLogsSubscribe` usa `Promise.all` (`L226`): se UMA wallet falha (ex.: address malformatado que passou na 
valida├º├úo `L145`, que s├│ checa `length === 0`), TODAS as demais subscriptions **j├í confirmadas server-side** ficam 
vivas, mas `openSocket()` rejeita ÔåÆ `scheduleReconnect` (`L390`) ÔåÆ `openSocket()` cria NOVO `this.ws = ws` 
(`L180`) **sem fechar o socket anterior**. O socket ├│rf├úo continua aberto, com subscriptions ativas, processando 
mensagens via `handleMessage`. Cada ciclo de reconnect re-subscribe **todas** as wallets no socket novo 
(duplica├º├úo), e nenhum `logsUnsubscribe` ├® enviado (`grep logsUnsubscribe` ÔåÆ zero ocorr├¬ncias fora do 
coment├írio L7). Quando o ├│rf├úo eventualmente fecha (timeout de inatividade Helius ~10min, sem ping do nosso lado), 
o handler `close` (`L205-211`) seta `connected=false` no socket **saud├ível** e dispara mais um reconnect esp├║rio.
- Reproduzir: configurar `INGESTION_WATCH_ADDRESSES` com 1 endere├ºo inv├ílido de formato (ex.: `abc`) + 9 v├ílidos; 
observar: connect falha, loop infinito com backoff, socket ├│rf├úo acumulando, volume de notifications duplicado 
(dedup de DB absorve, mas quota de conex├Áes Helius esgota).
- Impacto concreto: plano Free Helius = **5 conex├Áes simult├óneas**; ap├│s ~5 ciclos de falha, `connection refused` 
permanente ÔåÆ data-ingestion fora do ar at├® restart. Sem watchdog que detecte (heartbeat pings o socket atual, nunca 
o ├│rf├úo).
- Sugest├úo: fechar o socket anterior (`oldWs.close()`) antes de recriar; falhar o connect ANTES de deixar 
subscriptions parciais (ou aceitar partial success com rastreamento por-wallet); enviar `logsUnsubscribe` de ids 
antigos; validar formado de endere├ºo (zod/PublicKey) em `subscribeWallets`; teto de reconex├Áes com estado terminal + 
alerta.

**HIGH-3 ÔÇö `data/locks/data-ingestion.lock` (commitado): artefato de runtime no reposit├│rio; boot 
n├úo-determin├¡stico bloqueado.**
- Evid├¬ncia: arquivo `19816\n1790291576311` commitado. `instance-lock.ts`/`instance-lock.test.ts` (`rejeita segunda 
aquisi├º├úo quando o PID do lock est├í vivo`): `acquire()` l├¬ PID, e se `isProcessAlive(PID)` ÔåÆ lan├ºa 
`CONFIG_ERROR: j├í existe inst├óncia ativa (PID 19816)`. `data/locks/` N├âO est├í no `.gitignore` (que ignora 
`data/raw/` e `data/local/` apenas).
- Reproduzir: em qualquer m├íquina onde exista um processo com PID 19816 (reuso de PID no Windows ├® comum), iniciar o 
servi├ºo ÔåÆ `CONFIG_ERROR`, recusa a subir. Na m├íquina do commit, se o PID estava vivo no momento do commit, o 
servi├ºo j├í n├úo sobe at├® o PID morrer.
- Sugest├úo: remover o arquivo do repo, adicionar `data/locks/` ao `.gitignore`, e (defensivo) tratar lock stale por 
idade, n├úo s├│ por PID morto.

**MEDIUM-1 ÔÇö `scripts/f15-fixtures.mjs:93-94` (e `:70`) ÔÇö erro engolido, sem log.**
- Evid├¬ncia: `catch (e) { probes.errors++; }` ÔÇö descarta o erro sem logar causa; rows com `enhanced` null s├úo 
pulados (`continue` L70) sem contabilizar mismatch. Viola AGENTS.md ┬º9 (nunca `catch` que s├│ conta) e a 
classifica├º├úo de erros (`UNKNOWN_ERROR` n├úo ├® observ├ível).
- Reproduzir: rodar com RPC indispon├¡vel ÔÇö `errors` cresce mas o script n├úo diz por qu├¬; relat├│rio "10 / 10 vs 
Helius Enhanced" gerado mesmo com 14 erros.
- Sugest├úo: logar `e` por linha (via `@ct/logging` ou console com estrutura), e reportar `undecodable`/`errors` com 
amostras no JSON.

**MEDIUM-2 ÔÇö `scripts/f15-fixtures.mjs:99-104` ÔÇö claim "10/10 vs Helius Enhanced" superestimado: match s├│ compara 
`helius.type`.**
- Evid├¬ncia: o match (`L102-103`) verifica apenas `helius.type === 'SWAP' || 'SWAP_PARTIAL'`. N├úo compara `source`, 
`user`, `mint`, `direction`, `counterparty`, valores. Contraevid├¬ncia concreta no pr├│prio `f15-fixtures-report.json` 
commitado: row 2 tem `local.user=BwWK17cbHxwWBKZkUYvzxLcNQ1YVyaFezduWbtm2de6s` vs 
`helius.feePayer=Gygj9QQby4j2jryqyqBHvLP7ctv2SaANgh4sCb69BUpA` ÔÇö diverg├¬ncia real n├úo sinalizada, e 
`helisMatch=10, heliusMismatch=0`.
- Reproduzir: comparar campo a campo rows 1..10 do relat├│rio commitado ÔÇö a diverg├¬ncia acima prova que o "match" 
ignora atores da transa├º├úo.
- Sugest├úo: comparar `user`(== feePayer), `counterparty`, `mint`, `direction`(buy/sell), e ao menos faixa de valores; 
tratar diverg├¬ncia como mismatch com `error` detalhado. Relat├│rio n├úo deve afirmar "vs Helius Enhanced" sem essas 
compara├º├Áes.

**MEDIUM-3 ÔÇö `scripts/f15-fixtures.mjs:10` ÔÇö `TARGET_AMM = 2`, mas commit e relat├│rio reportam 8 PumpSwap; `:17` 
fetch sem timeout.**
- Evid├¬ncia: `L10 const TARGET_AMM = 2`; relat├│rio commitado `"pumpswapFixtures": 8`. O 8 n├úo ├® alvo, ├® 
incidental (loop para quando `results.length >= N` OU `ammOk >= TARGET_AMM`; L52). `fetch` (`L17`, `L28`) sem 
`AbortSignal.timeout` ÔÇö se o RPC travar, o script pendura para sempre (o `MAX_TRIES` s├│ limita as linhas de DB).
- Reproduzir: RPC que nunca responde ÔåÆ script eterno; metas divergentes: commit message "8 PumpSwap + 2 curve" vs 
c├│digo "2".
- Sugest├úo: tornar `TARGET_AMM`/`N` flags de CLI (ou env+zod), alinhar commit message ao valor real, adicionar 
timeout em ambos os fetch.

**LOW-1 ÔÇö `packages/pumpfun/scripts/fix-esm-imports.mjs` ÔÇö tern├írio morto e edge case `'./'`.**
- Evid├¬ncia: `spec === '.' || spec === '..' ? spec + '/' : spec + '/'` ÔÇö os dois bra├ºos s├úo id├¬nticos. Para 
`spec === './'` ÔåÆ resultado `'.//index.js'` (barra dupla).
- Reproduzir: rodar script num pacote com import `from './'`.
- Sugest├úo: remover o tern├írio; tratar `./`/`.`/`..` explicitamente com join can├┤nico.

**LOW-2 ÔÇö `scripts/f15-backfill.mjs:18` ÔÇö `process.env.SOLANA_RPC_HTTP` sem guard.**
- Evid├¬ncia: f15-fixtures.mjs valida `RPC`/`HELIUS_KEY` antes de usar; backfill passa `url: 
process.env.SOLANA_RPC_HTTP` direto ao `RpcProvider`. Sem env ÔåÆ `url` undefined ÔåÆ falha em runtime com erro 
n├úo-classificado.
- Reproduzir: `node scripts/f15-backfill.mjs` sem `.env` ÔåÆ erro obscuro de tipo.
- Sugest├úo: mesmo guard do fixtures (exit com mensagem clara se env ausente) + validar via `@ct/config`.

**LOW-3 ÔÇö `scripts/f15-fixtures.mjs:112` ÔÇö typo `helisMatch` no JSON (vs `heliusMismatch`); 
`f15-fixtures-report.json` commitado na raiz.**
- Evid├¬ncia: L112 `helisMatch: match`. Relat├│rio ├® artefato gerado (runtime) commitado na raiz do repo.
- Sugest├úo: corrigir o nome da chave; mover relat├│rios para `data/fixtures/` ou ignorar no `.gitignore` (commitar 
s├│ fixtures pequenas ├® defens├ível, mas em caminho padronizado).

**LOW-4 ÔÇö `packages/solana/src/rpc-provider.ts` ÔÇö `maxSupportedTransactionVersion 0ÔåÆ1`.** Mudan├ºa correta 
quanto ├á dire├º├úo (1 aceita legacy+v0+v1; confirmado nas docs Helius/Solana ÔÇö vers├úo 1 existe no runtime). Requer 
teste (coberto no HIGH-1). Sem achado funcional.

## TESTES EXECUTADOS (tentativas de quebra)

1. `git ls-tree` em `2ab477f`: invent├írio completo de testes ÔÇö confirmado: zero testes para 
`helius-provider`/`rpc-provider`; apenas `backoff.test.ts` no pacote solana. ÔåÆ REJEITA (crit├®rio objetivo).
2. Leitura de `instance-lock.test.ts` em `2ab477f`: confirma sem├óntica `isProcessAlive(PID)` ÔåÆ `CONFIG_ERROR` 
(reproduz o HIGH-3). Teste "lock stale substitu├¡do" mostra que s├│ PID vivo bloqueia ÔÇö exatamente o caso do lock 
commitado.
3. Hexdump do blob `f15-fixtures-report.json`: conte├║do real lido byte a byte; divergence row 2 (userÔëáfeePayer) 
verificada; `pumpswapFixtures: 8` vs `TARGET_AMM=2` verificada; chave `helisMatch` confirmada (nota: sem BOM ÔÇö o 
artefato anterior da leitura era rendering, corrigido aqui).
4. Buscas dirigidas no diff: `logsUnsubscribe` (zero usos), `sendSingleSubscribe`/`Promise.all`/reconnect paths com 
n├║meros de linha exatos.
5. Docs Helius consultados (MCP): `mentions` = 1 pubkey por chamada ÔåÆ mudan├ºa por-wallet ├® CORRETA (FACT 
validado); limite de conex├Áes por plano (Free=5) ÔåÆ agrava HIGH-2; m├║ltiplas subscriptions por conex├úo OK.

## O QUE N├âO FOI POSS├ìVEL VERIFICAR

- Se o PID 19816 est├í vivo em alguma m├íquina hoje (n├úo consult├ível) ÔÇö HIGH-3 n├úo depende disso para ser 
v├ílido: o artefato n├úo deveria existir no repo.
- Comportamento real do WSS Helius ante `mentions` inv├ílido (erro por sub ou por conex├úo) ÔÇö agrava ou atenua 
HIGH-2; exige teste com backend fake + (opcional) teste de integra├º├úo com conta Dev Helius.
- `f15-fixtures-report.json` s├│ foi lido em blob est├ítico; os 10 rows n├úo foram re-executados contra o RPC (sem 
network calls por pol├¡tica de review ÔÇö fica como teste a cargo do implementador).
- N├║mero real de wallets monitoradas em produ├º├úo (`INGESTION_WATCH_ADDRESSES` n├úo tem cap no config) ÔÇö se N > 
limite de subscriptions/conex├úo do plano, agrava HIGH-2.

## Nota de coordena├º├úo

Veredito N├âO commitado (conforme regra 1 das regras duras ÔÇö revisor n├úo commita na branch do produtor). Devolver 
ao especialista via orchestrator: corrigir HIGH-1/2/3 e re-commitar para nova revis├úo.
[0m
[0m$ [0mGet-Content docs/reviews/cc64b8d9db54422332e4016b2f86a8d332ee389a.md
...output truncated...

Full output saved to: C:\Users\enzo\.local\share\opencode\tool-output\tool_0d7114cfb001OgVrMGeU31GceE

anti-orfao de sockets + teto maxReconnects + HIGH-2 leak resolvido
fd6a5a6 fix(review): testes para markEnriched/markUndecodable/findUnenriched/incrementEnrichAttempt + guarda TOCTOU 
(updateMany where enrichedAt null) + schema.prisma ASCII puro
5299d25 docs(f1.5): roadmap/handoff/ADR-025 + artefatos de evidencia (fixtures + backfill 10k = 19.1%)
38cdaac chore: lock files fora do repo
2ab477f feat(f1.5): fixtures reais mainnet 10/10 vs Helius Enhanced (8 PumpSwap + 2 curve) + logsSubscribe 1-endereco 
+ getTransaction v1
[0m
[0m$ [0mgit show cc64b8d9db54422332e4016b2f86a8d332ee389a -- packages/solana/src/helius-provider.ts
commit cc64b8d9db54422332e4016b2f86a8d332ee389a
Author: EznRB <EznRB@users.noreply.github.com>
Date:   Fri Sep 25 00:46:17 2026 -0300

    fix(review): HIGH-1 testes helius-provider (per-wallet subs, base58, unsubscribe, reconnect storm) + anti-orfao de 
sockets + teto maxReconnects + HIGH-2 leak resolvido

diff --git a/packages/solana/src/helius-provider.ts b/packages/solana/src/helius-provider.ts
index 40e395b..93fb859 100644
--- a/packages/solana/src/helius-provider.ts
+++ b/packages/solana/src/helius-provider.ts
@@ -50,6 +50,8 @@ export interface HeliusProviderOptions {
   backoff?: { baseMs?: number; maxMs?: number; jitterRatio?: number; random?: () => number };
   /** F├íbrica de WebSocket injet├ível (testes). */
   webSocketFactory?: (url: string) => WebSocket;
+  /** Teto de reconex├Áes consecutivas antes de estado terminal (default 50). */
+  maxReconnects?: number;
 }
 
 // FACT (docs Helius, verificado em 2026-09): WSS correto ├® mainnet.helius-rpc.com
@@ -91,7 +93,11 @@ export class HeliusProvider implements BlockchainDataProvider {
   private readonly wallets = new Set<string>();
 
   private ws: WebSocket | null = null;
-  private subscriptionId: number | null = null;
+  /** Subscriptions por wallet no socket ATUAL (review 2ab477f HIGH-2: sem mapa = leak). */
+  private readonly subscriptionByWallet = new Map<string, number>();
+  /** Contagem de reconexoes consecutivas ÔÇö teto evita reconnect storm infinita. */
+  private consecutiveReconnects = 0;
+  private readonly maxReconnects: number;
   private nextRequestId = 1;
   private reconnectBackoff: ExponentialBackoff;
   private reconnectTimer: ReturnType<typeof setTimeout> | null = null;
@@ -106,6 +112,7 @@ export class HeliusProvider implements BlockchainDataProvider {
     this.pingIntervalMs = options.pingIntervalMs ?? 30_000;
     this.silenceTimeoutMs = options.silenceTimeoutMs ?? 60_000;
     this.backoffOpts = options.backoff;
+    this.maxReconnects = options.maxReconnects ?? 50;
     this.wsFactory = options.webSocketFactory ?? ((url: string) => new WebSocket(url));
     this.reconnectBackoff = new ExponentialBackoff(this.backoffOpts);
     if (options.wsUrl !== undefined) {
@@ -140,8 +147,11 @@ export class HeliusProvider implements BlockchainDataProvider {
   }
 
   async subscribeWallets(wallets: string[]): Promise<void> {
+    // Base58 estrito (32-44): formato errado ├® VALIDATION_ERROR na borda,
+    // nunca chega ao wire (review: "abc" passava com a checagem antiga).
+    const B58 = /^[1-9A-HJ-NP-Za-km-z]{32,44}$/;
     for (const w of wallets) {
-      if (typeof w !== 'string' || w.length === 0) {
+      if (typeof w !== 'string' || !B58.test(w)) {
         throw new ClassifiedError('VALIDATION_ERROR', 'wallet inv├ílida em subscribeWallets');
       }
       this.wallets.add(w);
@@ -154,10 +164,12 @@ export class HeliusProvider implements BlockchainDataProvider {
   async close(): Promise<void> {
     this.closed = true;
     this.clearTimers();
+    // logsUnsubscribe de todas as subscriptions ativas (review HIGH-2).
+    this.sendAllUnsubscribes();
     const ws = this.ws;
     this.ws = null;
     this.connected = false;
-    this.subscriptionId = null;
+    this.subscriptionByWallet.clear();
     if (ws) {
       await new Promise<void>((resolve) => {
         ws.once('close', () => resolve());
@@ -176,18 +188,39 @@ export class HeliusProvider implements BlockchainDataProvider {
 
   private openSocket(): Promise<void> {
     return new Promise((resolve, reject) => {
+      // Anti-├│rf├úo (review HIGH-2): fecha socket anterior ANTES de criar o novo.
+      const oldWs = this.ws;
+      if (oldWs) {
+        try {
+          oldWs.removeAllListeners();
+          oldWs.close();
+        } catch {
+          /* socket j├í morto */
+        }
+      }
       const ws = this.wsFactory(this.wsUrl);
       this.ws = ws;
 
       const onOpen = (): void => {
-        this.connected = true;
         this.lastMessageAt = Date.now();
         this.reconnectBackoff.reset();
-        this.startHeartbeat();
         this.logger.info('wss conectado', { urlHost: safeHost(this.wsUrl) });
+        // connected s├│ ├® P├ÜBLICO ap├│s todas as subscriptions confirmadas; falha
+        // parcial n├úo deixa socket aberto com subs vivas (fecha e rejeita).
         void this.sendLogsSubscribe()
-          .then(() => resolve())
-          .catch((err: unknown) => reject(err));
+          .then(() => {
+            this.connected = true;
+            this.startHeartbeat();
+            resolve();
+          })
+          .catch((err: unknown) => {
+            try {
+              ws.close();
+            } catch {
+              /* noop */
+            }
+            reject(err);
+          });
       };
 
       ws.once('open', onOpen);
@@ -205,7 +238,7 @@ export class HeliusProvider implements BlockchainDataProvider {
       });
       ws.on('close', (code: number, reason: Buffer) => {
         this.connected = false;
-        this.subscriptionId = null;
+        this.subscriptionByWallet.clear();
         this.clearTimers();
         this.logger.warn('wss fechado', { code, reason: reason.toString() });
         this.scheduleReconnect();
@@ -216,6 +249,23 @@ export class HeliusProvider implements BlockchainDataProvider {
     });
   }
 
+  /** Logs de unsubscribe (fire-and-forget) ÔÇö chamado antes de fechar socket. */
+  private sendAllUnsubscribes(): void {
+    const ws = this.ws;
+    if (!ws) return;
+    for (const subId of this.subscriptionByWallet.values()) {
+      try {
+        ws.send(
+          JSON.stringify({ jsonrpc: '2.0', id: this.nextRequestId++, method: 'logsUnsubscribe', params: [subId] }),
+        );
+      } catch (err) {
+        this.logger.warn('logsUnsubscribe falhou', {
+          error: err instanceof Error ? err.message : String(err),
+        });
+      }
+    }
+  }
+
   private async sendLogsSubscribe(): Promise<void> {
     if (this.wallets.size === 0 || this.ws === null) return;
     // FACT (Helius, observado 2026-09-24): logsSubscribe aceita apenas 1
@@ -245,14 +295,14 @@ export class HeliusProvider implements BlockchainDataProvider {
           reject(new ClassifiedError('RPC_ERROR', `logsSubscribe falhou: ${err.message}`, err));
           return;
         }
-        this.pendingSubscribes.set(id, { resolve, reject });
+        this.pendingSubscribes.set(id, { resolve, reject, wallet });
       });
     });
   }
 
   private readonly pendingSubscribes = new Map<
     number,
-    { resolve: () => void; reject: (e: Error) => void }
+    { resolve: () => void; reject: (e: Error) => void; wallet?: string }
   >();
 
   private handleMessage(data: unknown): void {
@@ -289,10 +339,14 @@ export class HeliusProvider implements BlockchainDataProvider {
           ),
         );
       } else {
-        if (typeof parsed.result === 'number') this.subscriptionId = parsed.result;
+        const subId = typeof parsed.result === 'number' ? parsed.result : null;
+        if (subId !== null && pending?.wallet) {
+          this.subscriptionByWallet.set(pending.wallet, subId);
+        }
         this.logger.info('logsSubscribe ativo', {
-          subscriptionId: this.subscriptionId,
-          wallets: this.wallets.size,
+          wallet: pending?.wallet,
+          subscriptionId: subId,
+          activeSubscriptions: this.subscriptionByWallet.size,
         });
         pending?.resolve();
       }
@@ -382,8 +436,23 @@ export class HeliusProvider implements BlockchainDataProvider {
 
   private scheduleReconnect(): void {
     if (this.closed || this.reconnectTimer !== null) return;
+    // Teto de reconnect storm (review HIGH-2): passar do limite vira estado
+    // terminal ÔÇö log├ível pela camada superior; n├úo gera loop infinito.
+    if (this.consecutiveReconnects >= this.maxReconnects) {
+      this.logger.error('wss reconnects esgotados ÔÇö estado terminal (restart manual necess├írio)', {
+        errorClass: 'RPC_ERROR',
+        maxReconnects: this.maxReconnects,
+      });
+      this.closed = true;
+      return;
+    }
+    this.consecutiveReconnects++;
     const delay = this.reconnectBackoff.nextDelay();
-    this.logger.info('wss reconnect agendado', { delayMs: delay });
+    this.logger.info('wss reconnect agendado', {
+      delayMs: delay,
+      attempt: this.consecutiveReconnects,
+      maxReconnects: this.maxReconnects,
+    });
     this.reconnectTimer = setTimeout(() => {
       this.reconnectTimer = null;
       if (this.closed) return;
[0m
[0m$ [0mgit show cc64b8d9db54422332e4016b2f86a8d332ee389a -- packages/solana/src/helius-provider.test.ts
commit cc64b8d9db54422332e4016b2f86a8d332ee389a
Author: EznRB <EznRB@users.noreply.github.com>
Date:   Fri Sep 25 00:46:17 2026 -0300

    fix(review): HIGH-1 testes helius-provider (per-wallet subs, base58, unsubscribe, reconnect storm) + anti-orfao de 
sockets + teto maxReconnects + HIGH-2 leak resolvido

diff --git a/packages/solana/src/helius-provider.test.ts b/packages/solana/src/helius-provider.test.ts
new file mode 100644
index 0000000..a2ea248
--- /dev/null
+++ b/packages/solana/src/helius-provider.test.ts
@@ -0,0 +1,207 @@
+/**
+ * Testes do HeliusProvider (review 2ab477f, HIGH-1/HIGH-2). ASCII-only.
+ * WebSocket fake injetavel; sem rede.
+ */
+import { describe, it, expect, vi, beforeEach } from 'vitest';
+import { EventEmitter } from 'node:events';
+import { HeliusProvider, type RawTransactionNotification } from './helius-provider.js';
+
+class FakeWs extends EventEmitter {
+  static instances: FakeWs[] = [];
+  static reset(): void { FakeWs.instances = []; }
+  sent: Array<Record<string, unknown>> = [];
+  closed = 0;
+  url: string;
+
+  constructor(url: string) {
+    super();
+    this.url = url;
+    FakeWs.instances.push(this);
+    queueMicrotask(() => this.emit('open'));
+  }
+
+  // eslint-disable-next-line @typescript-eslint/no-explicit-any
+  send(data: any, cb?: (err?: Error) => void) {
+    this.sent.push(JSON.parse(String(data)));
+    cb?.();
+  }
+  ping(): void {}
+  close(): void {
+    this.closed++;
+    queueMicrotask(() => this.emit('close', 1006, Buffer.from('')));
+  }
+  terminate(): void {}
+}
+
+const W = (c: string) => c.repeat(44); // base58 valido
+
+function providerWithFake(): { provider: HeliusProvider; logs: string[] } {
+  const logs: string[] = [];
+  const logger = {
+    debug: (m: string) => logs.push(`d:${m}`),
+    info: (m: string) => logs.push(`i:${m}`),
+    warn: (m: string) => logs.push(`w:${m}`),
+    error: (m: string) => logs.push(`e:${m}`),
+  };
+  const p = new HeliusProvider({
+    logger,
+    wsUrl: 'wss://fake.local',
+    backoff: { baseMs: 1, maxMs: 2, jitterRatio: 0 },
+    maxReconnects: 50,
+    webSocketFactory: (url) => new FakeWs(url) as unknown as import('ws').WebSocket,
+  });
+  return { provider: p, logs };
+}
+
+/** Confirma a subscricao atual pendente com sucesso. */
+function confirmSubs(ws: FakeWs): void {
+  const pending = ws.sent.filter((m) => m['method'] === 'logsSubscribe');
+  for (const m of pending) {
+    ws.emit('message', JSON.stringify({ jsonrpc: '2.0', id: m['id'], result: 4242 }));
+  }
+}
+
+describe('HeliusProvider - subscriptions por wallet', () => {
+  beforeEach(() => { FakeWs.reset(); });
+  it('N wallets validas geram N logsSubscribe (1 mention cada)', async () => {
+    const { provider } = providerWithFake();
+    await provider.subscribeWallets([W('A'), W('B')]);
+    const p = provider.connect();
+    await new Promise((r) => setTimeout(r, 10));
+    const ws = FakeWs.instances[0]!;
+    confirmSubs(ws);
+    await p;
+    const subs = ws.sent.filter((m) => m['method'] === 'logsSubscribe');
+    expect(subs).toHaveLength(2);
+    expect((subs[0]!['params'] as unknown[])[0]).toEqual({ mentions: [W('A')] });
+    await provider.close();
+  });
+
+  it('rejeita endereco mal-formado na borda (VALIDATION_ERROR)', async () => {
+    const { provider } = providerWithFake();
+    try {
+      await provider.subscribeWallets(['abc']);
+      throw new Error('nao deveria passar');
+    } catch (err) {
+      expect((err as { errorClass?: string }).errorClass).toBe('VALIDATION_ERROR');
+    }
+  });
+
+  it('sub rejeitada pela RPC: promise falha com RPC_ERROR e socket e fechado', async () => {
+    const { provider } = providerWithFake();
+    await provider.subscribeWallets([W('A')]);
+    const p = provider.connect();
+    const bad = providerWithFake();
+    void bad;
+    await new Promise((r) => setTimeout(r, 10));
+    const ws = FakeWs.instances[0]!;
+    const sub = ws.sent.find((m) => m['method'] === 'logsSubscribe');
+    ws.emit(
+      'message',
+      JSON.stringify({ jsonrpc: '2.0', id: sub!['id'], error: { code: -32602, message: 'bad' } }),
+    );
+    await expect(p).rejects.toThrowError(/logsSubscribe rejeitado/);
+    // Socket do connect falho foi fechado explicitamente (anti-orfao).
+    expect(ws.closed).toBeGreaterThanOrEqual(1);
+  });
+
+  it('close() envia logsUnsubscribe para cada sub ativa', async () => {
+    const { provider } = providerWithFake();
+    await provider.subscribeWallets([W('A'), W('B')]);
+    const p = provider.connect();
+    await new Promise((r) => setTimeout(r, 10));
+    const ws = FakeWs.instances[0]!;
+    confirmSubs(ws);
+    await p;
+    await provider.close();
+    const unsubs = ws.sent.filter((m) => m['method'] === 'logsUnsubscribe');
+    expect(unsubs).toHaveLength(2);
+  });
+
+  it('reconnect nao acumula sockets (sempre 1 vivo) nem duplica subs', async () => {
+    const { provider } = providerWithFake();
+    await provider.subscribeWallets([W('A')]);
+    const p = provider.connect();
+    await new Promise((r) => setTimeout(r, 10));
+    confirmSubs(FakeWs.instances[0]!);
+    await p;
+    // Simula queda e reconnect
+    FakeWs.instances[0]!.emit('close', 1006, Buffer.from('net'));
+    await new Promise((r) => setTimeout(r, 30));
+    expect(FakeWs.instances.length).toBe(2);
+    confirmSubs(FakeWs.instances[1]!);
+    await new Promise((r) => setTimeout(r, 10));
+    const subs2 = FakeWs.instances[1]!.sent.filter((m) => m['method'] === 'logsSubscribe');
+    expect(subs2).toHaveLength(1); // re-subscribe UMA vez por wallet no socket novo
+    await provider.close();
+  });
+
+  it('eventos de logsNotification chegam ao onRawEvent com assinatura valida', async () => {
+    const { provider } = providerWithFake();
+    const got: RawTransactionNotification[] = [];
+    provider.onRawEvent((n) => got.push(n));
+    await provider.subscribeWallets([W('A')]);
+    const p = provider.connect();
+    await new Promise((r) => setTimeout(r, 10));
+    const ws = FakeWs.instances[0]!;
+    confirmSubs(ws);
+    await p;
+    ws.emit(
+      'message',
+      JSON.stringify({
+        jsonrpc: '2.0',
+        method: 'logsNotification',
+        params: { result: { context: { slot: 9 }, value: { signature: W('5',) + 'x'.repeat(44) } }, subscription: 
4242 },
+      }),
+    );
+    expect(got).toHaveLength(1);
+    expect(got[0]!.signature.length).toBeGreaterThan(0);
+    await provider.close();
+  });
+});
+
+describe('HeliusProvider - reconnect storm', () => {
+  it('apos maxReconnects falhas consecutivas vira estado terminal (sem loop)', async () => {
+    vi.useFakeTimers();
+    try {
+      const logs: string[] = [];
+      const logger = {
+        debug: (m: string) => logs.push(m),
+        info: (m: string) => logs.push(m),
+        warn: (m: string) => logs.push(m),
+        error: (m: string) => logs.push(m),
+      };
+      let failEvery = true;
+      const p = new HeliusProvider({
+        logger,
+        wsUrl: 'wss://fake.local',
+        backoff: { baseMs: 1, maxMs: 2, jitterRatio: 0 },
+        maxReconnects: 3,
+        webSocketFactory: (_url: string) => {
+          void _url;
+          class FailingWs extends EventEmitter {
+            constructor() {
+              super();
+              queueMicrotask(() => {
+                if (failEvery) this.emit('error', new Error('down'));
+                this.emit('close', 1006, Buffer.from(''));
+              });
+            }
+            send(): void {}
+            ping(): void {}
+            close(): void {
+              this.emit('close', 1006, Buffer.from(''));
+            }
+          }
+          return new FailingWs() as unknown as import('ws').WebSocket;
+        },
+      });
+      await expect(p.connect()).rejects.toThrowError(/falha ao conectar|logsSubscribe rejeitado/);
+      failEvery = false;
+      for (let i = 0; i < 20; i++) await vi.advanceTimersByTimeAsync(5);
+      expect(logs.some((l) => l.includes('estado terminal'))).toBe(true);
+    } finally {
+      vi.useRealTimers();
+    }
+  });
+});
[0m
[0m$ [0mgit show cc64b8d9db54422332e4016b2f86a8d332ee389a -- packages/solana/src/rpc-provider.test.ts
commit cc64b8d9db54422332e4016b2f86a8d332ee389a
Author: EznRB <EznRB@users.noreply.github.com>
Date:   Fri Sep 25 00:46:17 2026 -0300

    fix(review): HIGH-1 testes helius-provider (per-wallet subs, base58, unsubscribe, reconnect storm) + anti-orfao de 
sockets + teto maxReconnects + HIGH-2 leak resolvido

diff --git a/packages/solana/src/rpc-provider.test.ts b/packages/solana/src/rpc-provider.test.ts
new file mode 100644
index 0000000..5e7c1d6
--- /dev/null
+++ b/packages/solana/src/rpc-provider.test.ts
@@ -0,0 +1,61 @@
+/**
+ * Testes do RpcProvider (review 2ab477f HIGH-1): param v1 + failover + erro.
+ * ASCII-only; sem rede.
+ */
+import { describe, it, expect } from 'vitest';
+import { RpcProvider, ClassifiedError } from './rpc-provider.js';
+
+const silent = { debug: () => {}, info: () => {}, warn: () => {}, error: () => {} };
+
+const FAST = { baseMs: 1, maxMs: 2, jitterRatio: 0 } as const;
+
+function ep(name: string, tier: 'primary' | 'secondary' | 'tertiary') {
+  return { name, url: `https://${name}.example`, tier };
+}
+
+describe('RpcProvider', () => {
+  it('getTransaction envia maxSupportedTransactionVersion=1', async () => {
+    const seen: string[] = [];
+    const fetchFn = (async (_u: string | URL | Request, init?: RequestInit) => {
+      seen.push(String(JSON.parse(String(init?.body)).params?.[1]?.maxSupportedTransactionVersion));
+      return new Response(JSON.stringify({ jsonrpc: '2.0', id: 1, result: null }), {
+        status: 200,
+        headers: { 'content-type': 'application/json' },
+      });
+    }) as unknown as typeof fetch;
+    const rpc = new RpcProvider([ep('p', 'primary')], { logger: silent, fetchFn });
+    await rpc.getTransaction('5'.repeat(88));
+    expect(seen).toEqual(['1']);
+  });
+
+  it('HTTP 500 no primario classifica como RPC_ERROR', async () => {
+    const fetchFn = (async () => new Response('down', { status: 500 })) as unknown as typeof fetch;
+    const rpc = new RpcProvider([ep('p', 'primary')], {
+      logger: silent,
+      fetchFn,
+      maxAttempts: 2,
+      backoff: FAST,
+      requestTimeoutMs: 500,
+    });
+    await expect(rpc.getSlot()).rejects.toBeInstanceOf(ClassifiedError);
+  });
+
+  it('failover: primario morto, secundario responde', async () => {
+    const fetchFn = (async (u: string | URL | Request) => {
+      if (String(u).includes('p1')) return new Response('down', { status: 500 });
+      return new Response(JSON.stringify({ jsonrpc: '2.0', id: 1, result: 123 }), {
+        status: 200,
+        headers: { 'content-type': 'application/json' },
+      });
+    }) as unknown as typeof fetch;
+    const rpc = new RpcProvider([ep('p1', 'primary'), ep('s1', 'secondary')], {
+      logger: silent,
+      fetchFn,
+      failoverThreshold: 1,
+      maxAttempts: 4,
+      backoff: FAST,
+      requestTimeoutMs: 500,
+    });
+    expect(await rpc.getSlot()).toBe(123);
+  });
+});
[0m
[0mÔåÆ [0mRead packages/solana/src/helius-provider.ts
[0m
[0m$ [0mgit branch --show-current; git status --short | Select-Object -First 20; git log --oneline -3
master
768175e review(sprint/f1-5-enrichment): REJEITADO (gates falharam) fd6a5a6feae30ada70e1a0dec0d1ab0b99cc8e5e
f92985a fix(review): afe614cf tambem tinha veredito final APROVADO (state sincronizado)
b09c20f fix(review): state.json reflete veredito final APROVADO do 38cdaac (arquivo = autoridade)
[0m
The working tree doesn't match the commit's content ÔÇö let me check the current branch state.
[0m$ [0mgit show cc64b8d9db54422332e4016b2f86a8d332ee389a:packages/solana/src/helius-provider.ts | Select-String 
-Pattern "subscriptionByWallet|maxReconnects|B58|sendAllUnsubscribes|consecutiveReconnects" | ForEach-Object { 
$_.LineNumber.ToString() + ": " + $_.Line }
54:   maxReconnects?: number;
97:   private readonly subscriptionByWallet = new Map<string, number>();
99:   private consecutiveReconnects = 0;
100:   private readonly maxReconnects: number;
115:     this.maxReconnects = options.maxReconnects ?? 50;
152:     const B58 = /^[1-9A-HJ-NP-Za-km-z]{32,44}$/;
154:       if (typeof w !== 'string' || !B58.test(w)) {
168:     this.sendAllUnsubscribes();
172:     this.subscriptionByWallet.clear();
241:         this.subscriptionByWallet.clear();
253:   private sendAllUnsubscribes(): void {
256:     for (const subId of this.subscriptionByWallet.values()) {
344:           this.subscriptionByWallet.set(pending.wallet, subId);
349:           activeSubscriptions: this.subscriptionByWallet.size,
441:     if (this.consecutiveReconnects >= this.maxReconnects) {
444:         maxReconnects: this.maxReconnects,
449:     this.consecutiveReconnects++;
453:       attempt: this.consecutiveReconnects,
454:       maxReconnects: this.maxReconnects,
[0m
The working tree is on master; the commit is on the sprint branch. I'll review the committed version in full.
[0m$ [0mgit show cc64b8d9db54422332e4016b2f86a8d332ee389a:packages/solana/src/helius-provider.ts > 
"$env:TEMP\opencode\helius-provider-cc64b8d.ts"; (Get-Content "$env:TEMP\opencode\helius-provider-cc64b8d.ts").Count
514
[0m
[0mÔ£ù [0mRead C:/Users/enzo/AppData/Local/Temp/opencode/helius-provider-cc64b8d.ts failed
[91m[1mError: [0mCannot read binary file: C:\Users\enzo\AppData\Local\Temp\opencode\helius-provider-cc64b8d.ts
[0m
[0m$ [0mgit show cc64b8d9db54422332e4016b2f86a8d332ee389a:packages/solana/src/helius-provider.ts | Out-File 
-FilePath "$env:TEMP\opencode\helius-provider-cc64b8d.ts" -Encoding utf8; git show 
cc64b8d9db54422332e4016b2f86a8d332ee389a:packages/solana/src/helius-provider.ts | Select-Object -First 1
/**
[0m
[0mÔåÆ [0mRead C:/Users/enzo/AppData/Local/Temp/opencode/helius-provider-cc64b8d.ts
[0m
[0m$ [0mgit show cc64b8d9db54422332e4016b2f86a8d332ee389a:packages/solana/src/helius-provider.ts | & 
"$env:SYSTEMROOT\System32\WindowsPowerShell\v1.0\powershell.exe" -NoProfile -Command "$input | Set-Content -Path 
$env:TEMP\opencode\hp-raw.bin -Encoding Byte -NoNewline" 2>$null; $bytes = 
[System.IO.File]::ReadAllBytes("$env:TEMP\opencode\hp-raw.bin"); $bad = 0; $i = 0; while ($i -lt $bytes.Length) { $b = 
$bytes[$i]; if ($b -lt 0x80) { $i++ } elseif (($b -band 0xE0) -eq 0xC0) { if ($i+1 -ge $bytes.Length -or ($bytes[$i+1] 
-band 0xC0) -ne 0x80) { $bad++; $i++ } else { $i += 2 } } elseif (($b -band 0xF0) -eq 0xE0) { if ($i+2 -ge 
$bytes.Length -or ($bytes[$i+1] -band 0xC0) -ne 0x80 -or ($bytes[$i+2] -band 0xC0) -ne 0x80) { $bad++; $i++ } else { 
$i += 3 } } elseif (($b -band 0xF8) -eq 0xF0) { if ($i+3 -ge $bytes.Length -or ($bytes[$i+1] -band 0xC0) -ne 0x80 -or 
($bytes[$i+2] -band 0xC0) -ne 0x80 -or ($bytes[$i+3] -band 0xC0) -ne 0x80) { $bad++; $i++ } else { $i += 4 } } else { 
$bad++; $i++ } }; "invalid UTF-8 sequences: $bad"; $nonAscii = ($bytes | Where-Object { $_ -ge 0x80 }).Count; 
"non-ASCII bytes: $nonAscii"
Exce´┐¢´┐¢o ao chamar "ReadAllBytes" com "1" argumento(s): "N´┐¢o foi poss´┐¢vel localizar o arquivo 
'C:\Users\enzo\AppData\Local\Temp\opencode\hp-raw.bin'."
invalid UTF-8 sequences: 0
No linha:1 caractere:273
+ ... e" 2>$null; $bytes = [System.IO.File]::ReadAllBytes("$env:TEMP\openco ...
+                 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : NotSpecified: (:) [], MethodInvocationException
    + FullyQualifiedErrorId : FileNotFoundException
 
non-ASCII bytes: 0
[0m
Now let me check supporting files, encoding of the committed files, and try running the tests.
[0m$ [0mgit show cc64b8d9db54422332e4016b2f86a8d332ee389a:packages/solana/src/helius-provider.test.ts | & 
"$env:SYSTEMROOT\System32\WindowsPowerShell\v1.0\powershell.exe" -NoProfile -Command "$input | Set-Content -Path 
$env:TEMP\opencode\hp-test-raw.bin -Encoding Byte -NoNewline" 2>$null; $bytes = 
[System.IO.File]::ReadAllBytes("$env:TEMP\opencode\hp-test-raw.bin"); $bad = 0; $i = 0; while ($i -lt $bytes.Length) { 
$b = $bytes[$i]; if ($b -lt 0x80) { $i++ } elseif (($b -band 0xE0) -eq 0xC0) { if ($i+1 -ge $bytes.Length -or 
($bytes[$i+1] -band 0xC0) -ne 0x80) { $bad++; $i++ } else { $i += 2 } } elseif (($b -band 0xF0) -eq 0xE0) { if ($i+2 
-ge $bytes.Length -or ($bytes[$i+1] -band 0xC0) -ne 0x80 -or ($bytes[$i+2] -band 0xC0) -ne 0x80) { $bad++; $i++ } else 
{ $i += 3 } } elseif (($b -band 0xF8) -eq 0xF0) { if ($i+3 -ge $bytes.Length -or ($bytes[$i+1] -band 0xC0) -ne 0x80 
-or ($bytes[$i+2] -band 0xC0) -ne 0x80 -or ($bytes[$i+3] -band 0xC0) -ne 0x80) { $bad++; $i++ } else { $i += 4 } } 
else { $bad++; $i++ } }; "invalid UTF-8 sequences: $bad"; $nonAscii = ($bytes | Where-Object { $_ -ge 0x80 }).Count; 
"non-ASCII bytes: $nonAscii"
Exce´┐¢´┐¢o ao chamar "ReadAllBytes" com "1" argumento(s): "N´┐¢o foi poss´┐¢vel localizar o arquivo 
invalid UTF-8 sequences: 0
non-ASCII bytes: 0
'C:\Users\enzo\AppData\Local\Temp\opencode\hp-test-raw.bin'."
No linha:1 caractere:283
+ ... e" 2>$null; $bytes = [System.IO.File]::ReadAllBytes("$env:TEMP\openco ...
+                 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : NotSpecified: (:) [], MethodInvocationException
    + FullyQualifiedErrorId : FileNotFoundException
[0m
[0m$ [0mcmd /c "git show cc64b8d9db54422332e4016b2f86a8d332ee389a:packages/solana/src/helius-provider.ts > 
%TEMP%\opencode\hp-raw.ts"; $bytes = [System.IO.File]::ReadAllBytes("$env:TEMP\opencode\hp-raw.ts"); $bad = 0; $i = 0; 
while ($i -lt $bytes.Length) { $b = $bytes[$i]; if ($b -lt 0x80) { $i++ } elseif (($b -band 0xE0) -eq 0xC0) { if ($i+1 
-ge $bytes.Length -or ($bytes[$i+1] -band 0xC0) -ne 0x80) { $bad++; $i++ } else { $i += 2 } } elseif (($b -band 0xF0) 
-eq 0xE0) { if ($i+2 -ge $bytes.Length -or ($bytes[$i+1] -band 0xC0) -ne 0x80 -or ($bytes[$i+2] -band 0xC0) -ne 0x80) 
{ $bad++; $i++ } else { $i += 3 } } elseif (($b -band 0xF8) -eq 0xF0) { if ($i+3 -ge $bytes.Length -or ($bytes[$i+1] 
-band 0xC0) -ne 0x80 -or ($bytes[$i+2] -band 0xC0) -ne 0x80 -or ($bytes[$i+3] -band 0xC0) -ne 0x80) { $bad++; $i++ } 
else { $i += 4 } } else { $bad++; $i++ } }; "invalid UTF-8: $bad"; $nonAscii = ($bytes | Where-Object { $_ -ge 0x80 
}).Count; "non-ASCII bytes: $nonAscii"; $hasBOM = ($bytes[0] -eq 0xEF -and $bytes[1] -eq 0xBB -and $bytes[2] -eq 
0xBF); "UTF-8 BOM: $hasBOM"
invalid UTF-8: 0
non-ASCII bytes: 210
UTF-8 BOM: False
[0m
[0m$ [0mcmd /c "git show cc64b8d9db54422332e4016b2f86a8d332ee389a:packages/solana/src/helius-provider.test.ts > 
%TEMP%\opencode\hp-test-raw.ts"; $bytes = [System.IO.File]::ReadAllBytes("$env:TEMP\opencode\hp-test-raw.ts"); $bad = 
0; $i = 0; while ($i -lt $bytes.Length) { $b = $bytes[$i]; if ($b -lt 0x80) { $i++ } elseif (($b -band 0xE0) -eq 0xC0) 
{ if ($i+1 -ge $bytes.Length -or ($bytes[$i+1] -band 0xC0) -ne 0x80) { $bad++; $i++ } else { $i += 2 } } elseif (($b 
-band 0xF0) -eq 0xE0) { if ($i+2 -ge $bytes.Length -or ($bytes[$i+1] -band 0xC0) -ne 0x80 -or ($bytes[$i+2] -band 
0xC0) -ne 0x80) { $bad++; $i++ } else { $i += 3 } } elseif (($b -band 0xF8) -eq 0xF0) { if ($i+3 -ge $bytes.Length -or 
($bytes[$i+1] -band 0xC0) -ne 0x80 -or ($bytes[$i+2] -band 0xC0) -ne 0x80 -or ($bytes[$i+3] -band 0xC0) -ne 0x80) { 
$bad++; $i++ } else { $i += 4 } } else { $bad++; $i++ } }; "invalid UTF-8: $bad"; $nonAscii = ($bytes | Where-Object { 
$_ -ge 0x80 }).Count; "non-ASCII bytes: $nonAscii"; $hasBOM = ($bytes[0] -eq 0xEF -and $bytes[1] -eq 0xBB -and 
$bytes[2] -eq 0xBF); "UTF-8 BOM: $hasBOM"
invalid UTF-8: 0
non-ASCII bytes: 0
UTF-8 BOM: False
[0m
[0mÔåÆ [0mRead packages/solana/package.json
[0m
[0m$ [0mcmd /c "git show cc64b8d9db54422332e4016b2f86a8d332ee389a:packages/solana/src/rpc-provider.test.ts > 
%TEMP%\opencode\rpc-test-raw.ts"; $bytes = [System.IO.File]::ReadAllBytes("$env:TEMP\opencode\rpc-test-raw.ts"); 
$nonAscii = ($bytes | Where-Object { $_ -ge 0x80 }).Count; "rpc-provider.test.ts non-ASCII bytes: $nonAscii"
rpc-provider.test.ts non-ASCII bytes: 0
[0m
[0m$ [0mGet-Content package.json -Raw; Get-ChildItem vitest.config.* -ErrorAction SilentlyContinue | Select-Object 
-ExpandProperty Name
{
  "name": "solana-copytrade-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Solana Smart-Money Intelligence + Pump.fun Copy Trading Engine (PAPER-first)",
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*",
    "backtester"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "lint": "eslint . --ext .ts,.tsx --max-warnings 0",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc -b --pretty",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "prisma:generate": "prisma generate --schema packages/database/prisma/schema.prisma",
    "prisma:migrate": "prisma migrate dev --schema packages/database/prisma/schema.prisma",
    "check:secrets": "node infrastructure/scripts/check-secrets.mjs",
    "check:security": "npm run check:secrets && npm audit --audit-level=high"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@typescript-eslint/eslint-plugin": "^7.13.0",
    "@typescript-eslint/parser": "^7.13.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "fast-check": "^3.19.0",
    "prettier": "^3.3.0",
    "typescript": "^5.4.5",
    "vitest": "^3.2.7"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}

vitest.config.ts
[0m
[0mÔåÆ [0mRead packages/solana/src/rpc-provider.ts[90m [limit=120][0m
Let me verify the test harness and check rpc-provider source to validate the new test assertions.
[0m
[0m$ [0mTest-Path node_modules; Test-Path packages\solana\src\helius-provider.test.ts; Test-Path 
packages\solana\src\rpc-provider.test.ts
True
False
False
[0m
[0m$ [0mgit show cc64b8d9db54422332e4016b2f86a8d332ee389a:packages/solana/src/rpc-provider.ts | Select-String 
-Pattern "getTransaction|maxSupportedTransactionVersion|getSlot" -Context 2,6 | ForEach-Object { $_.ToString() }
}
  
>   /** getSlot ÔÇö FACT: m├®todo padr├úo do JSON-RPC Solana. */
>   async getSlot(commitment: 'processed' | 'confirmed' | 'finalized' = 'confirmed'): Promise<number> {
>     return this.call<number>('getSlot', [{ commitment }]);
    }
  
    /**
     * getSignaturesForAddress com pagina├º├úo autom├ítica (`before`).
     * Retorna at├® `limit` assinaturas (default 1000, m├íximo da API por p├ígina).
     */
  
    /**
>    * getTransaction com encoding jsonParsed e maxSupportedTransactionVersion: 1
     * (FACT 2026-09-24: versao 1 introduzida pelo runtime; 0 insuficiente). Requerido para transa├º├Áes 
versionadas/v0 com address lookup tables).
     */
>   async getTransaction(signature: string): Promise<unknown | null> {
>     return this.call<unknown | null>('getTransaction', [
        signature,
>       { encoding: 'jsonParsed', maxSupportedTransactionVersion: 1 },
      ]);
    }
  
    /**
     * N├║cleo: tenta a chamada no endpoint ativo; ap├│s `failoverThreshold`
     * falhas consecutivas, faz failover para o pr├│ximo endpoint e continua.
[0m
[0mÔåÆ [0mRead packages/solana/src/rpc-provider.ts[90m [offset=140, limit=140][0m
The rpc tests assert branch behavior consistent with the committed source. Now let me actually run the new tests in an 
isolated temp worktree (no commits, no repo mutation beyond a removable worktree registration) to get concrete 
evidence.
[0m
[0m$ [0mgit worktree add --detach "$env:TEMP\opencode\wt-cc64b8d" cc64b8d9db54422332e4016b2f86a8d332ee389a 2>&1 | 
Select-Object -Last 3
git : Preparing worktree (detached HEAD cc64b8d)
No linha:1 caractere:1
+ git worktree add --detach "$env:TEMP\opencode\wt-cc64b8d" cc64b8d9db5 ...
+ ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : NotSpecified: (Preparing workt...d HEAD cc64b8d):String) [], RemoteException
    + FullyQualifiedErrorId : NativeCommandError
 
HEAD is now at cc64b8d fix(review): HIGH-1 testes helius-provider (per-wallet subs, base58, unsubscribe, reconnect 
storm) + anti-orfao de sockets + teto maxReconnects + HIGH-2 leak resolvido
[0m
[0m$ [0mcmd /c "mklink /J %TEMP%\opencode\wt-cc64b8d\node_modules C:\Users\enzo\Desktop\copytrade 2\node_modules" 
2>&1; Test-Path "$env:TEMP\opencode\wt-cc64b8d\node_modules\.bin\vitest.cmd"
cmd : A sintaxe do comando est´┐¢ incorreta.
No linha:1 caractere:1
+ cmd /c "mklink /J %TEMP%\opencode\wt-cc64b8d\node_modules C:\Users\en ...
+ ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : NotSpecified: (A sintaxe do comando est´┐¢ incorreta.:String) [], RemoteException
    + FullyQualifiedErrorId : NativeCommandError
 
False
[0m
[0m$ [0mcmd /c "mklink /J `"C:\Users\enzo\AppData\Local\Temp\opencode\wt-cc64b8d\node_modules`" 
`"C:\Users\enzo\Desktop\copytrade 2\node_modules`""
Jun´┐¢´┐¢o criada para C:\Users\enzo\AppData\Local\Temp\opencode\wt-cc64b8d\node_modules <<===>> 
C:\Users\enzo\Desktop\copytrade 2\node_modules
[0m
[0m$ [0mnpx vitest run packages/solana/src/helius-provider.test.ts packages/solana/src/rpc-provider.test.ts 2>&1 | 
Select-Object -Last 40
node.exe : npm warn exec The following package was not found and will be installed: vitest@4.1.11
No linha:1 caractere:1
+ & "C:\Program Files\nodejs/node.exe" "C:\Program Files\nodejs/node_mo ...
+ ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : NotSpecified: (npm warn exec T...: vitest@4.1.11:String) [], RemoteException
    + FullyQualifiedErrorId : NativeCommandError
 
vitest.config.ts (1:383) [33m[UNRESOLVED_IMPORT] [0mCould not resolve 'vitest/config' in vitest.config.ts
 - Module not found, treating it as an external dependency in vitest.config.ts at 383..398
[31mfailed to load config from C:\Users\enzo\AppData\Local\Temp\opencode\wt-cc64b8d\vitest.config.ts[39m
ÔÄ»ÔÄ»ÔÄ»ÔÄ»ÔÄ»ÔÄ»ÔÄ» Startup Error ÔÄ»ÔÄ»ÔÄ»ÔÄ»ÔÄ»ÔÄ»ÔÄ»ÔÄ»
Error: Cannot find module 'vitest/config'
Require stack:
- C:\Users\enzo\AppData\Local\Temp\opencode\wt-cc64b8d\vitest.config.ts
- C:\Users\enzo\AppData\Local\npm-cache\_npx\69c381f8ad94b576\node_modules\vite\dist\node\chunks\node.js
    at Module._resolveFilename (node:internal/modules/cjs/loader:1455:15)
    at defaultResolveImpl (node:internal/modules/cjs/loader:1065:19)
    at resolveForCJSWithHooks (node:internal/modules/cjs/loader:1070:22)
    at Module._load (node:internal/modules/cjs/loader:1241:25)
    at wrapModuleLoad (node:internal/modules/cjs/loader:255:19)
    at Module.require (node:internal/modules/cjs/loader:1555:12)
    at require (node:internal/modules/helpers:190:16)
    at Object.<anonymous> (C:\Users\enzo\AppData\Local\Temp\opencode\wt-cc64b8d\vitest.config.ts:1:33)
    at Module._compile (node:internal/modules/cjs/loader:1811:14)
    at _require.extensions.<computed> [as .js] (file:///C:/Users/enzo/AppData/Local/npm-cache/_npx/69c381f8ad94b576/nod
e_modules/vite/dist/node/chunks/node.js:37874:42) {
  code: 'MODULE_NOT_FOUND',
  requireStack: [
    'C:\\Users\\enzo\\AppData\\Local\\Temp\\opencode\\wt-cc64b8d\\vitest.config.ts',
    
'C:\\Users\\enzo\\AppData\\Local\\npm-cache\\_npx\\69c381f8ad94b576\\node_modules\\vite\\dist\\node\\chunks\\node.js'
  ]
}
[0m
[0m$ [0m$local = "C:\Users\enzo\Desktop\copytrade 2\node_modules\.bin\vitest.cmd"; if (Test-Path $local) { & $local 
run packages/solana/src/helius-provider.test.ts packages/solana/src/rpc-provider.test.ts 2>&1 | Select-Object -Last 45 
} else { "local vitest not found" }
local vitest not found
[0m
[0m$ [0mTest-Path node_modules\vitest\package.json; Test-Path node_modules\.bin\vitest.cmd; Get-ChildItem 
node_modules\.bin\vitest* 2>$null | Select-Object -ExpandProperty Name
False
False
[0m
[0m$ [0mGet-ChildItem -Path node_modules -Filter "vitest" -Directory -Recurse -Depth 2 -ErrorAction SilentlyContinue 
| Select-Object -First 5 -ExpandProperty FullName; Get-ChildItem -Path . -Filter "vitest.config.ts" -Recurse -Depth 2 
-ErrorAction SilentlyContinue | Select-Object -ExpandProperty FullName
C:\Users\enzo\Desktop\copytrade 2\vitest.config.ts
[0m
[0m$ [0m(Get-ChildItem node_modules -Directory -ErrorAction SilentlyContinue).Count; Test-Path 
node_modules\typescript; Test-Path node_modules\ws; Test-Path node_modules\@ct
0
False
False
False
[0m
node_modules is empty ÔÇö no dependency install exists, so the suite can't be executed locally. Cleaning up the temp 
worktree:
[0m$ [0mcd "C:\Users\enzo\Desktop\copytrade 2"; git worktree remove --force "$env:TEMP\opencode\wt-cc64b8d" 2>&1; 
git worktree prune; git worktree list
C:/Users/enzo/Desktop/copytrade 2                      768175e [master]
C:/Users/enzo/Desktop/copytrade-wt-f1-5-enrichment     cc64b8d [sprint/f1-5-enrichment]
C:/Users/enzo/Desktop/copytrade-wt-f1-postgres-live    cbd49d4 [sprint/f1-postgres-live]
C:/Users/enzo/Desktop/copytrade-wt-f1-security-fixes   19ffacc [sprint/f1-security-fixes]
C:/Users/enzo/Desktop/copytrade-wt-f1-security-fixes-2 df98063 [sprint/f1-security-fixes-2]
[0m
[0m$ [0m$before = (Get-ChildItem "C:\Users\enzo\Desktop\copytrade-wt-f1-5-enrichment" -Force -ErrorAction 
SilentlyContinue).Count; "wt-f1-5-enrichment entries: $before"; Test-Path 
"C:\Users\enzo\Desktop\copytrade-wt-f1-5-enrichment\packages\solana\src\helius-provider.ts"
wt-f1-5-enrichment entries: 35
True
[0m
The producer's worktree exists but has no node_modules either. Final verification of my key finding ÔÇö grep the 
committed blob for any reset of the reconnect counter:
[0m$ [0mgit show cc64b8d9db54422332e4016b2f86a8d332ee389a:packages/solana/src/helius-provider.ts | Select-String 
-Pattern "consecutiveReconnects|maxReconnects|reconnectBackoff.reset" | ForEach-Object { $_.Line.Trim() }; "---"; git 
show cc64b8d9db54422332e4016b2f86a8d332ee389a:packages/solana/src/helius-provider.ts | Select-String -Pattern 
"subscription\$|params\?\.subscription" -CaseSensitive | ForEach-Object { $_.Line.Trim() }
maxReconnects?: number;
private consecutiveReconnects = 0;
private readonly maxReconnects: number;
this.maxReconnects = options.maxReconnects ?? 50;
this.reconnectBackoff.reset();
if (this.consecutiveReconnects >= this.maxReconnects) {
maxReconnects: this.maxReconnects,
this.consecutiveReconnects++;
attempt: this.consecutiveReconnects,
maxReconnects: this.maxReconnects,
---
[0m
I have complete evidence. The committed blob confirms no reset of `consecutiveReconnects` anywhere, and 
`handleMessage` never gates on `params.subscription`. Cleaning up the temp file and delivering the verdict.

---

## VEREDITO: REJEITADO

Commit revisado: `cc64b8d` (sprint/f1-5-enrichment) ÔÇö "fix(review): HIGH-1 testes helius-provider (... per-wallet 
subs, base58, unsubscribe, reconnect storm) + anti-orfao de sockets + teto maxReconnects + HIGH-2 leak resolvido"

---

## ACHADOS

### A1 ÔÇö HIGH ÔÇö `consecutiveReconnects` NUNCA ├® resetado ÔåÆ teto de "storm" vira teto de reconnects do tempo de 
vida; outage permanente sem recupera├º├úo
**Arquivo:** `packages/solana/src/helius-provider.ts:437-467` (checagem na 441, incremento na 449; `onOpen` 204-224 
n├úo reseta)

**Evid├¬ncia:** o contador ├® declarado (`= 0`, linha 99), incrementado em `scheduleReconnect` (449) e comparado (441) 
ÔÇö e **nunca** zerado. `onOpen` chama `this.reconnectBackoff.reset()` (206) mas n├úo o contador. `connect()` 
(144-147) reseta `closed` mas n├úo o contador. `close()` tampouco. Confirmei por grep no blob do commit: nenhuma 
ocorr├¬ncia de reset.

**Como reproduzir:** provider com `maxReconnects: 3`. Ciclo: conecta ÔåÆ blip de rede ÔåÆ close ÔåÆ reconnect 
**bem-sucedido** (sess├úo longa) ÔåÆ blip ÔåÆ ... Ao terceiro evento de reconnect (mesmo com sess├Áes saud├íveis entre 
eles, ou seja, N├âO consecutivos), o pr├│ximo `close` dispara: `3 >= 3` ÔåÆ `closed = true` ÔåÆ `logger.error('... 
estado terminal ...')` ÔåÆ **stream morto para sempre**. Chamar `connect()` de novo reseta `closed` mas o contador 
fica em 3 ÔåÆ no primeiro close subsequente volta a terminal. ├Ünica sa├¡da: nova inst├óncia (equivale a restart 
manual ÔÇö sem observabilidade de quem restarta). Com default 50, um daemon de ingestion 24/7 com rede inst├ível 
atinge terminal silenciosamente (s├│ um log entre o ru├¡do) ap├│s 50 blips no total.

**Sugest├úo:** zerar `consecutiveReconnects` no `onOpen` junto do `reconnectBackoff.reset()`, e tamb├®m no 
`connect()`. O teste novo (`reconnect storm`) s├│ cobre o caminho de falhas consecutivas; n├úo cobre reset ap├│s 
sucesso. Adicionar teste: N falhas ÔåÆ sucesso ÔåÆ nova falha ÔåÆ n├úo deve entrar em terminal antes de N reconnects 
*consecutivos*.

---

### A2 ÔÇö MEDIUM ÔÇö `subscribeWallets` incremental re-subscribe de TODAS as wallets ÔåÆ subscriptions duplicadas no 
servidor; map guarda s├│ o ├║ltimo subId ÔåÆ unsubscribe deixa ├│rf├ú; notifica├º├Áes sem gate por subscription id
**Arquivo:** `packages/solana/src/helius-provider.ts:159-161` (re-send do set inteiro), `269-277` (`sendLogsSubscribe` 
itera `[...this.wallets]`), `342-345` (sobrescreve subId por wallet), `308-354` (handleMessage nunca valida 
`params.subscription`)

**Evid├¬ncia:** `subscribeWallets([B])` com `connected=true` chama `sendLogsSubscribe()`, que re-envia `logsSubscribe` 
para o set inteiro (A e B). O servidor mant├®m a sub antiga de A **e** a nova ÔåÆ `subscriptionByWallet` fica com A 
ÔåÆ subId novo (o antigo ├® sobreescrito); `sendAllUnsubscribes` (256-259) s├│ cobre o subId novo ÔåÆ subId antigo de 
A fica ├│rf├úo no servidor, entregando `logsNotification` duplicadas. O `handleMessage` aceita qualquer 
`logsNotification` sem conferir `params.subscription`, ent├úo cada evento de A emite **duas vezes** `onRawEvent` 
(mitigado s├│ por dedup downstream `signature+instructionIndex+wallet`, com wallet=''/instructionIndex=0 ÔåÆ dedup 
colapsa para signature). Viola├º├úo latente da regra de idempot├¬ncia (nunca processar a mesma opera├º├úo duas vezes) 
na borda do pipeline.

**Reproduzir:** 2 wallets; `subscribeWallets([A,B])`; `connect()`; depois `await subscribeWallets([C])`; inspecionar 
frames `logsSubscribe` enviados ÔÇö A sofre dupla sub.

**Sugest├úo:** em `sendLogsSubscribe`, enviar apenas as wallets **novas** (diff entre set e map), e filtrar 
notifica├º├Áes por `subscription` no handleMessage; manter lista de subIds por wallet (n├úo s├│ o ├║ltimo).

---

### A3 ÔÇö MEDIUM ÔÇö race window: wallet adicionada durante `connect()` em voo nunca ├® subscrita (silencioso)
**Arquivo:** `packages/solana/src/helius-provider.ts:149-162` vs `204-224`

**Evid├¬ncia:** `connected` s├│ ├® `true` ap├│s **todas** as confirma├º├Áes de subscribe (212). Se 
`subscribeWallets([B])` for chamado no intervalo entre `onOpen` (envio das subs de A) e a confirma├º├úo, `connected` 
ainda ├® `false` ÔåÆ B ├® adicionado ao set mas nenhum envio ocorre; quando as confirma├º├Áes de A chegam, 
`connected=true` e **nada re-dispara** o subscribe de B ÔåÆ B fica sem sub at├® o pr├│ximo reconnect, sem erro nem 
log. A mudan├ºa "connected ap├│s confirma├º├Áes" (para corrigir dupla execu├º├úo) introduziu essa perda silenciosa.

**Sugest├úo:** no `.then()` do `sendLogsSubscribe` do onOpen, comparar `wallets.size` com o que foi enviado e 
re-enviar faltantes; ou mover o envio para ap├│s `connected=true` com guarda de dedup por wallet.

---

### A4 ÔÇö LOW ÔÇö ap├│s `connect()` rejeitado, loop de reconnect continua em background; estado terminal n├úo ├® 
exposto programaticamente
**Arquivo:** `packages/solana/src/helius-provider.ts:216-223`, `437-467`

**Evid├¬ncia:** no catch do onOpen, `ws.close()` ÔåÆ o handler de `close` (239-244) dispara `scheduleReconnect()` ÔÇö 
rejei├º├úo de `connect()` n├úo aborta o background; o provider fica tentando reconectar (at├® maxReconnects) sem que o 
chamador saiba. O estado terminal s├│ vira `logger.error` com `errorClass: 'RPC_ERROR'` como **campo de log** 
(442-445) ÔÇö nenhuma promise/evento/status consult├ível; camada superior precisa parsear log para detectar. Vai 
contra a prioridade OBSERVABILIDADE.

**Sugest├úo:** expor estado (ex.: getter `isTerminal()`/evento) e, no catch do onOpen, decidir explicitamente se 
rejei├º├úo de connect = abortar reconnect (ou documentar o comportamento).

---

### A5 ÔÇö LOW ÔÇö `openSocket` com `removeAllListeners()` no socket antigo: connect() concorrente deixa pendings da 
primeira chamada presos para sempre
**Arquivo:** `packages/solana/src/helius-provider.ts:191-200`

**Evid├¬ncia:** se `connect()` for chamado duas vezes sem guarda (sem `reconnectTimer`/`connected` check), o segundo 
`openSocket` faz `oldWs.removeAllListeners()` + `close()` no primeiro socket ÔåÆ o close handler do primeiro (que 
rejeitaria os pendings via `clearTimers ÔåÆ failPendingSubscribes`) foi removido ÔåÆ `pendingSubscribes` do primeiro 
connect ficam pendentes para sempre e a primeira promise nunca settle (leak introduzido por este mesmo commit, que 
adicionou o removeAllListeners). Caso de uso real: retry do chamador ap├│s timeout sem `close()`.

**Sugest├úo:** guarda reentrante em `connect()` (se `this.ws !== null && this.connected` ÔåÆ retornar promise 
existente ou rejeitar com VALIDATION_ERROR).

---

### A6 ÔÇö LOW ÔÇö regex base58 aceita strings que decodificam para <32 bytes
**Arquivo:** `packages/solana/src/helius-provider.ts:152`

**Evid├¬ncia:** `/^[1-9A-HJ-NP-Za-km-z]{32,44}$/` aceita `'1'.repeat(32)` (32 bytes de zeros ÔåÆ pubkey v├ílida, ok) 
mas tamb├®m `'1'.repeat(32)` s├│ cobre o caso zero; pior: aceita qualquer 32-char base58 que decodifica para ~23 bytes 
ÔÇö n├úo ├® pubkey v├ílida. A checagem ├® heur├¡stica na borda e o RPC rejeita depois (tratamento existe), ent├úo n├úo 
├® falha de seguran├ºa ÔÇö mas o coment├írio "Base58 estrito" superestima o que o regex garante.

**Sugest├úo:** validar com `new PublicKey()` ou ao menos comprimento decodificado de 32 bytes; adicionar teste de 
caso-limite.

---

## TESTES EXECUTADOS (tentativas de quebra)

1. **An├ílise de reconex├úo bem-sucedida com contador alto** (tentativa de contraexemplo para "consecutivo"): trace 
manual ÔÇö blipÔåÆreconnect sucessoÔåÆblipÔåÆ... ÔåÆ 3┬║ close com `maxReconnects:3` ÔåÆ terminal. **Bug confirmado 
est├íticamente (A1).**
2. **subscribeWallets incremental** ÔÇö trace dos frames `logsSubscribe` no socket: re-sub de A quando C ├® 
adicionado. Duplica├º├úo confirmada (A2).
3. **Race subscribeWallets durante connect()** ÔÇö janela `connected=false` com subs em voo: wallet perdida (A3).
4. **Execu├º├úo da su├¡te:** **n├úo execut├ível** ÔÇö `node_modules` vazio no repo (0 pacotes; sem vitest local); 
tentei worktree tempor├írio em `%TEMP%` (detached HEAD em cc64b8d, junction de node_modules) ÔÇö sem depend├¬ncias 
instaladas, `vitest` n├úo resolve. `mklink` e worktree foram removidos (nenhum commit/ref criado). Valida├º├úo dos 
testes (FakeWs, confirmSubs, storm com fake timers, rpc-provider fetchFn) foi feita por rastreamento est├ítico: o 
fluxo dos testes ├® internamente consistente e deve passar quando as depend├¬ncias existirem ÔÇö exceto que **n├úo h├í 
teste para reset do contador** (A1) nem para re-sub duplicado (A2), exatamente onde est├úo os bugs.
5. **Verifica├º├úo de encoding:** `helius-provider.test.ts` e `rpc-provider.test.ts` 100% ASCII (bate com o cabe├ºalho 
"ASCII-only"); `helius-provider.ts` UTF-8 v├ílido, sem BOM, sem mojibake.

## O QUE N├âO FOI POSS├ìVEL VERIFICAR

- Execu├º├úo real de `npm run lint`, `npm run typecheck`, `npm test` no SHA (sem `node_modules` no ambiente; `vitest` 
indispon├¡vel) ÔÇö verifica├º├úo fica est├ítica.
- Comportamento do `ws` real (`readyState` em `send` durante CONNECTING; flush de `logsUnsubscribe` antes do close) 
ÔÇö s├│ o fake foi analis├ível.
- Se a camada superior (normaliza├º├úo/ingestion) realmente dedupa por signature antes de persistir ÔÇö mitiga 
parcialmente A2, n├úo foi auditada neste commit.

---

VERDICT: REJEITADO

**Achados resumidos:** A1 HIGH (contador de reconnect nunca resetado ÔåÆ terminal permanente ap├│s reconnects 
n├úo-consecutivos; sem recupera├º├úo via `connect()`; sem teste) ┬À A2 MEDIUM (re-sub duplicado no subscribe 
incremental + unsubscribe ├│rf├úo + notifica├º├Áes sem gate por subscription id ÔåÆ eventos duplicados) ┬À A3 MEDIUM 
(race `subscribeWallets` durante `connect()` ÔåÆ wallet nunca subscrita, silenciosa) ┬À A4 LOW (reconnect em 
background ap├│s `connect()` rejeitado; terminal sem exposi├º├úo program├ítica) ┬À A5 LOW (connect concorrente ÔåÆ 
pendings presos, leak introduzido pelo removeAllListeners) ┬À A6 LOW (regex base58 heur├¡stica aceita n├úo-pubkeys).

**Condi├º├úo para aprova├º├úo:** zerar `consecutiveReconnects` no `onOpen`/`connect()` + teste que comprove reset 
ap├│s sucesso (A1); decidir tratamento do re-sub incremental e filtrar notifica├º├Áes por subscription id (A2); fechar 
a janela do A3; rodar lint/typecheck/test com depend├¬ncias instaladas e anexar evid├¬ncia.
[0m
A revis├úo anterior (cc64b8d) ├® contexto valioso. Agora preciso verificar: (1) estado atual da branch e worktree do produtor, (2) se os gates podem ser executados, (3) consumidores de `subscribeWallets` (impacto A2/A3), (4) rpc-provider.ts completo.
[0mÔ£▒ [0mGrep "subscribeWallets"[90m 4 matches[0m
[0m
[0m$ [0mgit worktree list; git log sprint/f1-5-enrichment --oneline -8; git merge-base --is-ancestor cc64b8d 
20d93cf; echo "cc64b8d ancestor of 20d93cf: $LASTEXITCODE"
C:/Users/enzo/Desktop/copytrade 2                      b5bb013 [master]
C:/Users/enzo/Desktop/copytrade-wt-f1-5-enrichment     73eac5e [sprint/f1-5-enrichment]
C:/Users/enzo/Desktop/copytrade-wt-f1-postgres-live    cbd49d4 [sprint/f1-postgres-live]
C:/Users/enzo/Desktop/copytrade-wt-f1-security-fixes   19ffacc [sprint/f1-security-fixes]
C:/Users/enzo/Desktop/copytrade-wt-f1-security-fixes-2 df98063 [sprint/f1-security-fixes-2]
73eac5e chore(ops): check-secrets isenta docs/ (relatorios com dados on-chain publicos)
38cf505 feat(f1.5): enricher+runner+wiring, retention opt-in, lock por idade, scripts de gate, docs
20d93cf feat(f1.5): helius-provider per-wallet subscriptions + anti-leak de sockets + maxSupportedTransactionVersion=1 
+ testes
3d6c9db feat(f1.5): schema ObservedEvent com enrichment + migration + repositories com guarda TOCTOU + testes
7d7e3d1 feat(f1.5): decoder pump.fun/PumpSwap via codama da IDL oficial (pump-public-docs @81091419)
3e51d3e review(sprint/f1-5-enrichment): REJEITADO (gates falharam) cfca4f89323cbfb68e0dcfa8aafe23a39e69301f
b7364bc review(sprint/f1-5-enrichment): REJEITADO (gates falharam) 0167a0d89fe0f21d6aa83d51e4738a1f4c5e96ae
10f6011 review(sprint/f1-5-enrichment): REJEITADO (gates falharam) 812ef1a7f376b01de7f7dbc21ffae9f645e1edc3
cc64b8d ancestor of 20d93cf: 1
[0m
## Objective
- Revisar adversarialmente o commit `20d93cfec10044434fc0a1966d5093e1da427b05` (branch `sprint/f1-5-enrichment`) como code-reviewer, seguindo `.agents/code-reviewer.md` e `AGENTS.md`.
- Produzir veredito final com UMA das linhas exatas `VERDICT: APROVADO` ou `VERDICT: REJEITADO` + lista de achados com severidade CRITICAL/HIGH/MEDIUM/LOW. Sem modificar c├│digo, sem commit.

## Important Details
- Instru├º├Áes do usu├írio: rodar `git show 20d93cf...`; crit├®rios: bugs, erros, incompatibilidades, viola├º├Áes do AGENTS.md, testes faltantes; n├úo modificar c├│digo; n├úo commitar; resposta em portugu├¬s.
- **Ainda N├âO foram lidos** `.agents/code-reviewer.md` nem `AGENTS.md` (exigidos pelo prompt).
- O working tree est├í na branch `master` com a vers├úo ANTIGA de `helius-provider.ts` (cont├®m `subscriptionId`, valida├º├úo `w.length === 0`). Para revisar o commit ├® obrigat├│rio usar `git show 20d93cf:<path>`; a ferramenta `read` no working tree engana.
- Redirecionar `git show` para arquivo temp via PowerShell `>` gera UTF-16 ÔåÆ `read` falha ("Cannot read binary file"). Usar sa├¡da direta do `git show` no bash. AGENTS.md ┬º8.3.6 pro├¡be Set-Content/Out-File.
- Escopo do commit (stat): 4 arquivos, +417/ÔêÆ25. `helius-provider.ts` (+119), `helius-provider.test.ts` (+256), `rpc-provider.ts` (+6), `rpc-provider.test.ts` (+61).
- Mudan├ºas principais: mapa `subscriptionByWallet` (1 sub/wallet), `maxReconnects` (default 50, estado terminal `closed=true`), reset de `consecutiveReconnects` em `connect()`, valida├º├úo base58 estrita `/^[1-9A-HJ-NP-Za-km-z]{32,44}$/` com `ClassifiedError('VALIDATION_ERROR', ...)`, anti-├│rf├úo (fecha socket anterior antes de recriar), `sendAllUnsubscribes` no `close()`, remo├º├úo do campo `subscriptionId`, `maxSupportedTransactionVersion=1` no RPC.
- Commit message afirma resolver: review `2ab477f` (HIGH-1 testes, HIGH-2 leak/├│rf├úo/subscriptions) e review `cc64b8d` (reset de reconex├Áes).
- Git log do branch de 20d93cf: `20d93cf ÔåÆ 3d6c9db ÔåÆ 7d7e3d1 ÔåÆ 3e51d3e (review REJEITADO cfca4f8) ÔåÆ b7364bc (review REJEITADO 0167a0d)`. Master local: `b5bb013`/`b2a2048`/`cc09b02` = reviews "REJEITADO (gates falharam)" de 3d6c9db, 7d7e3d1, d46525d.
- `docs/reviews/` cont├®m os .md de reviews anteriores + `state.json` (array "reviewed"); `Get-Content` nesses .md retornou hexdump truncado (usar `git show` para ler).

## Work State
### Completed
- `git show --stat`, `git log -5` de 20d93cf e `git status`/`git branch -a` (master limpa, ahead 11; branch `sprint/f1-5-enrichment` existe).
- Diff completo de `helius-provider.ts` (novo arquivo = 479 linhas; corpo lido, meio truncado).
- Diff de `rpc-provider.ts` + in├¡cio de `rpc-provider.test.ts` (teste verifica `params[1].maxSupportedTransactionVersion===1` via fetch fake; `RpcProvider` tem failover por tier primary/secondary/tertiary e `ClassifiedError`).
- Leitura inicial de `helius-provider.test.ts` (242 linhas no commit): `FakeWs extends EventEmitter` (emite `open` via queueMicrotask; `close()` incrementa `closed` e emite `close` 1006; `send` grava JSON parseado), helper `W = c => c.repeat(44)` (base58 v├ílido), `providerWithFake()` (logger com logs array, backoff baseMs:1/maxMs:2/jitter 0, maxReconnects:50), `confirmSubs()` (responde result 4242 p/ logsSubscribe pendentes); describe "subscriptions por wallet" com beforeEach (resto truncado).
- Confirmado que working tree tem vers├úo antiga (linhas 94, 144, 160, 208 com `subscriptionId`/`w.length===0`).
- Hip├│teses de achados j├í identificadas para verifica├º├úo:
  - Re-subscri├º├úo duplicada: `subscribeWallets` chamado repetido com socket vivo re-envia `logsSubscribe` para TODAS as wallets; `subscriptionByWallet.set` sobrescreve o ID antigo ÔåÆ sub ├│rf├ú no servidor (nunca unsubscribed at├® fechar socket) + entrega duplicada. MEDIUM prov├ível.
  - Duplo `connect()`: anti-├│rf├úo faz `oldWs.removeAllListeners()` (remove handlers 'open'/'error' do socket antigo) ÔåÆ a promise do primeiro `connect()` nunca settle. LOW/MEDIUM.
  - `sendSingleSubscribe` com `this.ws === null` faz `resolve()` silencioso (estado inconsistente; j├í existia no c├│digo antigo).
  - Handler `close` N├âO faz `this.ws = null` (s├│ `connected=false`, `subscriptionByWallet.clear()`, `clearTimers()`, `scheduleReconnect()`); anti-├│rf├úo no `openSocket` seguinte lida com isso ÔÇö comportamento intencional mas a apontar.
  - `maxReconnects`: estado "terminal" (`closed=true`) ├® reset├ível por `connect()`; subir teto consome timers unref'd.
  - Base58 `{32,44}` aceita comprimentos abaixo do real (pubkeys = 43-44) ÔÇö toler├ível.
  - `maxSupportedTransactionVersion=1` ├® correto (v0/versioned txs com lookup tables).

### Active
- Falta ler integralmente: `.agents/code-reviewer.md`, `AGENTS.md`, corpo truncado do novo `helius-provider.ts`, testes completos e `rpc-provider.ts`.
- Falta verificar reviews citados (`2ab477f`, `cc64b8d`) via `git show` (Get-Content est├í retornando hexdump truncado).
- Falta verificar consumidores reais de `subscribeWallets` (padr├úo de chamada incremental/duplicada) para confirmar/refutar o achado de sub duplicada.
- Falta rodar gates (lint/typecheck/test segundo AGENTS.md) ÔÇö working tree ├® master (c├│digo antigo), ainda n├úo foi achado worktree da branch para testar o commit.

### Blocked
- Leitura de `docs/reviews/2ab477f...md` e `cc64b8d...md` via `Get-Content` ÔåÆ hexdump/truncado; retry pendente via `git show <sha>:<path>`.
- Gates do commit n├úo podem ser executados no working tree atual (master = vers├úo antiga); nenhum worktree (`../copytrade-wt-*`) verificado ainda.

## Next Move
1. Ler `.agents/code-reviewer.md` e `AGENTS.md` (obrigat├│rio pelo prompt).
2. Ler na ├¡ntegra via `git show 20d93cf:<path>` (sem redirect temp): `helius-provider.ts`, `helius-provider.test.ts`, `rpc-provider.test.ts`, `rpc-provider.ts`.
3. Ler `docs/reviews/2ab477f7dc60361db18e2106cd4b9ef48aea977e.md` e `docs/reviews/cc64b8d9db54422332e4016b2f86a8d332ee389a.md` via `git show` (autor dos reviews) para validar as claims do commit message.
4. Grep por chamadas a `subscribeWallets` no repo para testar o cen├írio de re-subscri├º├úo duplicada.
5. Verificar se existe worktree da branch (`git worktree list`) para rodar gates; sen├úo, decidir por an├ílise est├ítica e listar "testes faltantes".
6. Redigir resposta final em portugu├¬s: achados com severidade + linha final exata `VERDICT: ...`.

## Relevant Files
- `C:\Users\enzo\Desktop\copytrade 2\.agents\code-reviewer.md`: instru├º├Áes do reviewer (ainda n├úo lido).
- `C:\Users\enzo\Desktop\copytrade 2\AGENTS.md`: regras/gates do repo (ainda n├úo lido).
- `packages/solana/src/helius-provider.ts`: arquivo principal sob revis├úo (nova vers├úo, 479 linhas, no commit).
- `packages/solana/src/helius-provider.test.ts`: testes novos (+256 no stat; 242 via git show).
- `packages/solana/src/rpc-provider.ts`: mudan├ºa `maxSupportedTransactionVersion` 0ÔåÆ1 (+6).
- `packages/solana/src/rpc-provider.test.ts`: testes novos do RPC (+61).
- `packages/solana/src/chain-event.ts`: interfaces `BlockchainDataProvider`/`ObservedChainEvent` usadas pelo provider.
- `packages/solana/src/backoff.ts`: `ExponentialBackoff` usado no reconnect.
- `docs/reviews/2ab477f7dc60361db18e2106cd4b9ef48aea977e.md`: base das claims HIGH-1/HIGH-2 do commit.
- `docs/reviews/cc64b8d9db54422332e4016b2f86a8d332ee389a.md`: review do reset de `consecutiveReconnects`.
- `docs/reviews/state.json`: ledger de reviews (cont├®m o commit atual? n├úo verificado).
Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.
[0mÔåÆ [0mRead services/data-ingestion/src/index.ts
Contexto atualizado: o branch `sprint/f1-5-enrichment` tem HEAD em `73eac5e` (2 commits ├á frente de `20d93cf`), e `cc64b8d` **n├úo** ├® ancestral de `20d93cf` ÔÇö o fix de reset de `consecutiveReconnects` foi portado via conte├║do, n├úo via merge. O ├║nico consumidor de `subscribeWallets` ├® `services/data-ingestion/src/index.ts:72` ÔÇö isso afeta a severidade dos achados A2/A3.

Vou verificar o consumidor, o ambiente de gates e os reviews citados:
[0m
[0m$ [0mTest-Path "C:\Users\enzo\Desktop\copytrade-wt-f1-5-enrichment\node_modules"; Test-Path 
"C:\Users\enzo\Desktop\copytrade-wt-f1-5-enrichment\packages\solana\node_modules"; git show 
2ab477f7dc60361db18e2106cd4b9ef48aea977e:docs/reviews/2ab477f7dc60361db18e2106cd4b9ef48aea977e.md 2>$null | 
Select-Object -First 5; git cat-file -t 2ab477f7dc60361db18e2106cd4b9ef48aea977e; git cat-file -t 
cc64b8d9db54422332e4016b2f86a8d332ee389a
True
True
commit
commit
[0m
[93m[1m! [0mpermission requested: external_directory (C:\Users\enzo\Desktop\copytrade-wt-f1-5-enrichment\*); 
auto-rejecting
[0mÔ£ù [0mgit diff --stat 20d93cf..73eac5e -- packages/solana; echo "---reviews em 2ab477f---"; git ls-tree -r 
--name-only 2ab477f7dc60361db18e2106cd4b9ef48aea977e docs/reviews 2>$null; echo "---package.json scripts---"; 
Get-Content "C:\Users\enzo\Desktop\copytrade-wt-f1-5-enrichment\package.json" | Select-String -Pattern 
'"(lint|typecheck|test)[^"]*"\s*:' ; echo "---root node_modules---"; Test-Path "C:\Users\enzo\Desktop\copytrade 
2\node_modules" failed
[91m[1mError: [0mThe user rejected permission to use this specific tool call.
`subscribeWallets` ├® chamado **uma ├║nica vez, no boot, depois de `connect()`** ÔÇö isso atenua A2 (sub duplicada) e A3 (race), mas preciso confirmar se os commits posteriores do branch (`38cf505`, `73eac5e`) tocaram os arquivos sob revis├úo, e se consigo rodar os gates no worktree do produtor sem modific├í-lo. Tamb├®m vou ler os reviews citados e os arquivos completos:

