# Review 73eac5e926950176d7d18fbfb997c9906ef1b2c3

- Branch: sprint/f1-5-enrichment
- Data: 2026-09-25 02:54:00
- Gates: lint=1 typecheck=1 test=1 (0 = ok)
- Veredito: **REJEITADO**

## Saida do code-reviewer

opencode.exe : [0m
No C:\Users\enzo\AppData\Roaming\npm\opencode.ps1:14 caractere:3
+   & "$basedir/node_modules/opencode-ai/bin/opencode.exe"   $args
+   ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : NotSpecified: ([0m:String) [], RemoteException
    + FullyQualifiedErrorId : NativeCommandError
 
> code-reviewer ┬À big-pickle
[0m
[0m$ [0mgit log --oneline -5 73eac5e926950176d7d18fbfb997c9906ef1b2c3; git status --short | Select-Object -First 5
73eac5e chore(ops): check-secrets isenta docs/ (relatorios com dados on-chain publicos)
38cf505 feat(f1.5): enricher+runner+wiring, retention opt-in, lock por idade, scripts de gate, docs
20d93cf feat(f1.5): helius-provider per-wallet subscriptions + anti-leak de sockets + maxSupportedTransactionVersion=1 
+ testes
3d6c9db feat(f1.5): schema ObservedEvent com enrichment + migration + repositories com guarda TOCTOU + testes
7d7e3d1 feat(f1.5): decoder pump.fun/PumpSwap via codama da IDL oficial (pump-public-docs @81091419)
[0m
[0m$ [0mgit show 73eac5e926950176d7d18fbfb997c9906ef1b2c3 --stat
commit 73eac5e926950176d7d18fbfb997c9906ef1b2c3
Author: EznRB <EznRB@users.noreply.github.com>
Date:   Fri Sep 25 02:24:07 2026 -0300

    chore(ops): check-secrets isenta docs/ (relatorios com dados on-chain publicos)

 infrastructure/scripts/check-secrets.mjs | 5 ++++-
 1 file changed, 4 insertions(+), 1 deletion(-)
[0m
[0m$ [0mgit show 73eac5e926950176d7d18fbfb997c9906ef1b2c3
commit 73eac5e926950176d7d18fbfb997c9906ef1b2c3
Author: EznRB <EznRB@users.noreply.github.com>
Date:   Fri Sep 25 02:24:07 2026 -0300

    chore(ops): check-secrets isenta docs/ (relatorios com dados on-chain publicos)

diff --git a/infrastructure/scripts/check-secrets.mjs b/infrastructure/scripts/check-secrets.mjs
index 619095f..ec1da99 100644
--- a/infrastructure/scripts/check-secrets.mjs
+++ b/infrastructure/scripts/check-secrets.mjs
@@ -47,6 +47,8 @@ function walk(dir) {
     const isResearchData =
       relForExempt.startsWith("docs/research/") && relForExempt.endsWith(".json");
     if (isResearchData) continue;
+    if (relForExempt.startsWith('docs/')) continue;
+
     const ext = extname(entry).toLowerCase();
     const isEnv = entry.startsWith(".env") && entry !== ".env.example";
     if (!isEnv && !textExtensions.has(ext)) continue;
@@ -55,7 +57,8 @@ function walk(dir) {
     lines.forEach((line, i) => {
       const trimmed = line.trim();
       if (trimmed.startsWith("#") || trimmed.startsWith("//")) return;
-      const phraseExempt = phraseExemptFiles.has(entry) || ext === ".md";
+      const isDocs = relForExempt.startsWith('docs/');
+      const phraseExempt = phraseExemptFiles.has(entry) || ext === '.md';
       for (const p of patterns) {
         if (phraseExempt && p.name !== "base58 longo (possivel key/secret)") continue;
         if (!p.regex.test(line)) continue;
[0m
[0mÔåÆ [0mRead infrastructure/scripts/check-secrets.mjs
[0mÔ£▒ [0mGrep "relForExempt"[90m in infrastructure ┬À 0 matches[0m
[0m
[0m$ [0mgit show 73eac5e~1:infrastructure/scripts/check-secrets.mjs
// check-secrets.mjs
// Varredura de padr├Áes suspeitos de secrets (private key, seed phrase, base58 longo).
// Cross-platform (Windows/Linux CI). Exit 1 se encontrar qualquer suspeita.
// Substitui o antigo check-secrets.ps1 (que n├úo rodava no CI Ubuntu).
import { readdirSync, readFileSync, statSync } from "node:fs";
import { join, relative, extname, dirname } from "node:path";
import { fileURLToPath } from "node:url";

const repoRoot = join(dirname(fileURLToPath(import.meta.url)), "..", "..");
console.log(`Varrendo: ${repoRoot}`);

const excludeDirs = new Set(["node_modules", ".git", "dist", ".next", "out", "coverage"]);
const skipFiles = new Set(["package-lock.json"]);
const allowExtensions = [".example", ".sample", ".template"];
const textExtensions = new Set([
  ".ts", ".tsx", ".js", ".mjs", ".cjs", ".json", ".jsonc",
  ".py", ".md", ".yml", ".yaml", ".toml", ".ini", ".cfg",
  ".env", ".txt", ".sql", ".prisma", ".ps1", ".sh",
]);

const patterns = [
  { name: "private key literal", regex: /PRIVATE KEY|private[_ -]?key\s*[:=]|BEGIN [A-Z ]*PRIVATE KEY/i },
  { name: "seed phrase", regex: /seed phrase|mnemonic|secret recovery phrase/i },
  { name: "base58 longo (possivel key/secret)", regex: /\b[1-9A-HJ-NP-Za-km-z]{64,88}\b/ },
];

// Arquivos isentos de padr├Áes de FRASE (pol├¡ticas/documenta├º├úo mencionam os termos).
// A varredura de base58 continua ativa neles.
const phraseExemptFiles = new Set(["check-secrets.mjs"]);

const policyComment = /never|proibido|forbidden|NUNCA|poli(cy|tica)|exemplo sem valor/;

const findings = [];

function walk(dir) {
  for (const entry of readdirSync(dir)) {
    const full = join(dir, entry);
    if (excludeDirs.has(entry)) continue;
    const stats = statSync(full);
    if (stats.isDirectory()) { walk(full); continue; }

    if (allowExtensions.some((e) => entry.endsWith(e))) continue;
    if (skipFiles.has(entry)) continue;
    // Relatorios de evidencia (docs/research/**/*.json) contem dados on-chain
    // publicos (signatures/base58) por definicao ÔÇö nao sao secrets.
    const relForExempt = relative(repoRoot, full).replace(/\\/g, "/");
    const isResearchData =
      relForExempt.startsWith("docs/research/") && relForExempt.endsWith(".json");
    if (isResearchData) continue;
    const ext = extname(entry).toLowerCase();
    const isEnv = entry.startsWith(".env") && entry !== ".env.example";
    if (!isEnv && !textExtensions.has(ext)) continue;

    const lines = readFileSync(full, "utf8").split(/\r?\n/);
    lines.forEach((line, i) => {
      const trimmed = line.trim();
      if (trimmed.startsWith("#") || trimmed.startsWith("//")) return;
      const phraseExempt = phraseExemptFiles.has(entry) || ext === ".md";
      for (const p of patterns) {
        if (phraseExempt && p.name !== "base58 longo (possivel key/secret)") continue;
        if (!p.regex.test(line)) continue;
        if (policyComment.test(line)) continue;
        findings.push({
          file: relative(repoRoot, full),
          line: i + 1,
          pattern: p.name,
          excerpt: trimmed.slice(0, 80),
        });
      }
    });
  }
}

walk(repoRoot);

console.log("");
if (findings.length > 0) {
  console.error(`[FALHA] Encontradas ${findings.length} ocorr├¬ncia(s) suspeita(s):`);
  for (const f of findings) console.error(`  ${f.file}:${f.line}  [${f.pattern}]  ${f.excerpt}`);
  console.error("\nA├º├úo: remova o conte├║do real, mova para o ambiente/secret manager e, se j├í foi commitado, 
revogue/rotacione.");
  process.exit(1);
}

console.log("[OK] Nenhum padr├úo suspeito encontrado.");
[0m
[0mÔåÆ [0mRead infrastructure/scripts/check-secrets.mjs[90m [offset=35, limit=35][0m
[0m
[0m$ [0mrg -n "relForExempt|isDocs|docs/" "infrastructure/scripts/check-secrets.mjs"
rg : O termo 'rg' n´┐¢o ´┐¢ reconhecido como nome de cmdlet, fun´┐¢´┐¢o, arquivo de script ou programa oper´┐¢vel. 
Verifique a 
grafia do nome ou, se um caminho tiver sido inclu´┐¢do, veja se o caminho est´┐¢ correto e tente novamente.
No linha:1 caractere:1
+ rg -n "relForExempt|isDocs|docs/" "infrastructure/scripts/check-secre ...
+ ~~
    + CategoryInfo          : ObjectNotFound: (rg:String) [], CommandNotFoundException
    + FullyQualifiedErrorId : CommandNotFoundException
[0m
[0m$ [0mgit branch --show-current; git status --short | Out-String -Width 200
master
[0m
The working-tree file differs from the commit ÔÇö I must review the exact committed content. Let me pull the file as committed and check the branch state.
[0m$ [0mgit show 73eac5e926950176d7d18fbfb997c9906ef1b2c3:infrastructure/scripts/check-secrets.mjs | Select-Object 
-First 78
// check-secrets.mjs
// Varredura de padr├Áes suspeitos de secrets (private key, seed phrase, base58 longo).
// Cross-platform (Windows/Linux CI). Exit 1 se encontrar qualquer suspeita.
// Substitui o antigo check-secrets.ps1 (que n├úo rodava no CI Ubuntu).
import { readdirSync, readFileSync, statSync } from "node:fs";
import { join, relative, extname, dirname } from "node:path";
import { fileURLToPath } from "node:url";

const repoRoot = join(dirname(fileURLToPath(import.meta.url)), "..", "..");
console.log(`Varrendo: ${repoRoot}`);

const excludeDirs = new Set(["node_modules", ".git", "dist", ".next", "out", "coverage"]);
const skipFiles = new Set(["package-lock.json"]);
const allowExtensions = [".example", ".sample", ".template"];
const textExtensions = new Set([
  ".ts", ".tsx", ".js", ".mjs", ".cjs", ".json", ".jsonc",
  ".py", ".md", ".yml", ".yaml", ".toml", ".ini", ".cfg",
  ".env", ".txt", ".sql", ".prisma", ".ps1", ".sh",
]);

const patterns = [
  { name: "private key literal", regex: /PRIVATE KEY|private[_ -]?key\s*[:=]|BEGIN [A-Z ]*PRIVATE KEY/i },
  { name: "seed phrase", regex: /seed phrase|mnemonic|secret recovery phrase/i },
  { name: "base58 longo (possivel key/secret)", regex: /\b[1-9A-HJ-NP-Za-km-z]{64,88}\b/ },
];

// Arquivos isentos de padr├Áes de FRASE (pol├¡ticas/documenta├º├úo mencionam os termos).
// A varredura de base58 continua ativa neles.
const phraseExemptFiles = new Set(["check-secrets.mjs"]);

const policyComment = /never|proibido|forbidden|NUNCA|poli(cy|tica)|exemplo sem valor/;

const findings = [];

function walk(dir) {
  for (const entry of readdirSync(dir)) {
    const full = join(dir, entry);
    if (excludeDirs.has(entry)) continue;
    const stats = statSync(full);
    if (stats.isDirectory()) { walk(full); continue; }

    if (allowExtensions.some((e) => entry.endsWith(e))) continue;
    if (skipFiles.has(entry)) continue;
    // Relatorios de evidencia (docs/research/**/*.json) contem dados on-chain
    // publicos (signatures/base58) por definicao ÔÇö nao sao secrets.
    const relForExempt = relative(repoRoot, full).replace(/\\/g, "/");
    const isResearchData =
      relForExempt.startsWith("docs/research/") && relForExempt.endsWith(".json");
    if (isResearchData) continue;
    if (relForExempt.startsWith('docs/')) continue;

    const ext = extname(entry).toLowerCase();
    const isEnv = entry.startsWith(".env") && entry !== ".env.example";
    if (!isEnv && !textExtensions.has(ext)) continue;

    const lines = readFileSync(full, "utf8").split(/\r?\n/);
    lines.forEach((line, i) => {
      const trimmed = line.trim();
      if (trimmed.startsWith("#") || trimmed.startsWith("//")) return;
      const isDocs = relForExempt.startsWith('docs/');
      const phraseExempt = phraseExemptFiles.has(entry) || ext === '.md';
      for (const p of patterns) {
        if (phraseExempt && p.name !== "base58 longo (possivel key/secret)") continue;
        if (!p.regex.test(line)) continue;
        if (policyComment.test(line)) continue;
        findings.push({
          file: relative(repoRoot, full),
          line: i + 1,
          pattern: p.name,
          excerpt: trimmed.slice(0, 80),
        });
      }
    });
  }
}

walk(repoRoot);
[0m
Now I have the exact committed content. Two red flags are visible: a blanket `docs/` skip plus a dead `isDocs` variable. Let me verify the CI wiring, existing tests for this script, and what's actually under `docs/` on the sprint branch.
[0m$ [0mgit ls-tree -r 73eac5e926950176d7d18fbfb997c9906ef1b2c3 --name-only | Select-String -Pattern 
"check-secrets|github|workflow" | Out-String -Width 200
.github/dependabot.yml
.github/workflows/ci.yml
.github/workflows/ecosystem-monitor.yml
.github/workflows/hygiene.yml
infrastructure/scripts/check-secrets.mjs
[0m
[0m$ [0mgit ls-tree -r 73eac5e926950176d7d18fbfb997c9906ef1b2c3 --name-only | Select-String -Pattern "^docs/" | 
Out-String -Width 200
docs/architecture/adr/ADR-001-npm-workspaces-como-gerenciador-de-monorepo.md
docs/architecture/adr/ADR-002-postgresql-local-via-prisma-sem-acoplamento-ao-sup.md
docs/architecture/adr/ADR-003-dev-local-nativo-docker-apenas-para-o-servidor-fut.md
docs/architecture/adr/ADR-004-vitest-fast-check-pytest-hypothesis.md
docs/architecture/adr/ADR-005-zod-para-validacao-de-config-e-payloads.md
docs/architecture/adr/ADR-006-guard-live-em-falha-fechada-no-carregamento-de-con.md
docs/architecture/adr/ADR-007-sem-redis-na-fase-inicial.md
docs/architecture/adr/ADR-008-llm-fora-do-caminho-critico.md
docs/architecture/adr/ADR-009-perfis-de-agentes-versionados-no-repo.md
docs/architecture/adr/ADR-010-observedevent-wallet-como-address-string-sem-fk.md
docs/architecture/adr/ADR-011-stack-de-dados-solana-solana-kit-helius-sdk-yellow.md
docs/architecture/adr/ADR-012-pump-fun-idl-vendored-nenhum-sdk-npm-de-terceiros.md
docs/architecture/adr/ADR-013-jupiter-dexscreener-birdeye-rugcheck-via-clientes-.md
docs/architecture/adr/ADR-014-mcp-servers-no-ambiente-de-dev-opencode-json.md
docs/architecture/adr/ADR-015-stack-quantitativa-e-repos-de-referencia-avaliacao.md
docs/architecture/adr/ADR-016-pump-fun-tem-sdk-docs-oficiais-revisao-parcial-do-.md
docs/architecture/adr/ADR-017-aprendizados-arquiteturais-do-cutupdev-solana-copy.md
docs/architecture/adr/ADR-018-chainstacklabs-pumpfun-bonkfun-bot-como-referencia.md
docs/architecture/adr/ADR-019-camada-de-streaming-vixen-shredstream-laserstream-.md
docs/architecture/adr/ADR-020-baseline-consolidado-de-repositorios-2026-09-24-ve.md
docs/architecture/adr/ADR-021-endpoint-wss-helius-mainnet-helius-rpc-com-laserst.md
docs/architecture/adr/ADR-022-schema-de-env-sem-zod-strict.md
docs/architecture/adr/ADR-023-fixture-de-smoke-f1-programa-pump-fun-como-alvo-de.md
docs/architecture/adr/ADR-024-risco-emergente-retencao-de-dados-e-single-instanc.md
docs/architecture/adr/ADR-025-fatos-runtime-f15-decoder-enrichment.md
docs/architecture/architecture.md
docs/architecture/decision-log.md
docs/handoff.md
docs/operations/chat-prompts.md
docs/operations/database-setup.md
docs/operations/phase-pipeline.md
docs/operations/runbook.md
docs/plan-de-acao.md
docs/research/experiments/TEMPLATE.md
docs/research/f15/README.md
docs/research/f15/backfill-report.json
docs/research/f15/fixtures-report.json
docs/reviews/0167a0d89fe0f21d6aa83d51e4738a1f4c5e96ae.md
docs/reviews/06d452b47eeb2a57f9a7503c92db95e9850cc944.md
docs/reviews/071140969c2b27580f35e7ebf23714b406c200c0.md
docs/reviews/0b7f55e5d8810bf2638c221a55592621bdf36c3c.md
docs/reviews/19ffacc3726223dab70613484bba5432cd289d9c.md
docs/reviews/2ab477f7dc60361db18e2106cd4b9ef48aea977e.md
docs/reviews/38cdaac0b30e0ddf53dab697b7131e4a3499d55c.md
docs/reviews/5299d250602fd81b9ac1849c026ec11955411feb.md
docs/reviews/538ed8f1f82de7dce8c9e842a416d702bfa720fa.md
docs/reviews/5c84a7419243edc6a3eeb9bfe7725bd320689c8e.md
docs/reviews/609a1a65fb7495e8351d676d5d49a4ef76a054d2.md
docs/reviews/6cd436b9bc7582a41c42b1ae40805b3cec015936.md
docs/reviews/6d984e2fba17bc9e75cd9a89b9e6bd9dd104229f.md
docs/reviews/799d04834d147cbdcc4ad8e46c4b8499d8ad1b00.md
docs/reviews/7edc05c736368be87306896333ea6ed3bf65af5a.md
docs/reviews/812ef1a7f376b01de7f7dbc21ffae9f645e1edc3.md
docs/reviews/88152bbfafe2705932d909bd7c67571241f4b248.md
docs/reviews/887e5160abe34afa8f947d9d3d028f8f978f1a08.md
docs/reviews/README.md
docs/reviews/afe614cf46169461a019b3a9599f58177ae83e0d.md
docs/reviews/c438ef1c0c9483258896afaaf976f7e67a3404e9.md
docs/reviews/cbd49d4fa82a2f97e8e9b134239f9ab282539ac0.md
docs/reviews/cc64b8d9db54422332e4016b2f86a8d332ee389a.md
docs/reviews/ccd5313e042c9e6ed45480dd91f955fb43ce6624.md
docs/reviews/cd4468ae7f1ff2d39cdc249c4188a690144bba44.md
docs/reviews/cfca4f89323cbfb68e0dcfa8aafe23a39e69301f.md
docs/reviews/dddb30b148daf08293bdf90097f010bb40322218.md
docs/reviews/df980637e823da5b804d18efab1a9a92b8e02562.md
docs/reviews/ea88b4649a2b109ebf0b963b92f289dcc957feb5.md
docs/reviews/ed0fc2095bbb9c924cef0b3c62dfc63fa600aee1.md
docs/reviews/fd6a5a6feae30ada70e1a0dec0d1ab0b99cc8e5e.md
docs/reviews/ffc094cb0de340613451df935feeebd62c1e2cfc.md
docs/reviews/state.json
docs/roadmap.md
docs/security/red-team/FINDINGS.md
docs/security/red-team/README.md
docs/security/red-team/SPRINTS.md
docs/security/red-team/poc/sprint2-fuzz.poc.test.ts
docs/security/red-team/poc/sprint2-ingestion.poc.test.ts
docs/security/red-team/poc/vitest.poc.config.mjs
docs/security/red-team/threat-model.md
docs/security/secret-policy.md
[0m
[0m$ [0m$wf = git show 73eac5e926950176d7d18fbfb997c9906ef1b2c3:.github/workflows/ci.yml; $wf | Select-String 
-Pattern "secrets|security|check" -Context 2,2 | Out-String -Width 200
pull_request:
    schedule:
>     # Auditoria de seguran├ºa semanal (segunda 03:00 UTC) ÔÇö roda check:security completo
      - cron: "0 3 * * 1"
  
      runs-on: ubuntu-latest
      steps:
>       - name: Checkout
>         uses: actions/checkout@v4
  
        - name: Setup Node.js 20
          run: npm ci
  
>       - name: Secrets scan
>         run: npm run check:secrets
  
>   security:
>     name: Security Gate
      runs-on: ubuntu-latest
      steps:
>       - name: Checkout
>         uses: actions/checkout@v4
  
        - name: Setup Node.js 20
          run: npm ci
  
>       - name: Security check (secrets + dependency audit)
>         run: npm run check:security
[0m
[0m$ [0mgit ls-tree -r 73eac5e926950176d7d18fbfb997c9906ef1b2c3 --name-only | Select-String -Pattern "test|spec" | 
Select-String -Pattern "secret|lock|infra" | Out-String -Width 200
services/data-ingestion/src/instance-lock.test.ts
[0m
[0m$ [0m$pkg = git show 73eac5e926950176d7d18fbfb997c9906ef1b2c3:package.json; $pkg | Select-String -Pattern 
"check:secrets|check:security|check-secrets" | Out-String -Width 200
"check:secrets": "node infrastructure/scripts/check-secrets.mjs",
    "check:security": "npm run check:secrets && npm audit --audit-level=high"
[0m
[0m$ [0mgit ls-tree -r 73eac5e926950176d7d18fbfb997c9906ef1b2c3 --name-only | Select-String -Pattern 
"fixtures|\.env" | Out-String -Width 200
.env.example
backtester/fixtures/.gitkeep
docs/research/f15/fixtures-report.json
scripts/f15-fixtures.mjs
[0m
CI wiring confirmed: `check:security` (which includes `check:secrets`) is the mandatory Security Gate on every push/PR, and there is **no test** for this script anywhere. Now let me empirically break-test the committed version vs. the parent to prove the behavior change concretely.
[0m$ [0m$base = "C:\Users\enzo\AppData\Local\Temp\opencode\cs-repro"; New-Item -ItemType Directory -Force -Path 
"$base\infrastructure\scripts", "$base\docs", "$base\src" | Out-Null; git show 
73eac5e926950176d7d18fbfb997c9906ef1b2c3:infrastructure/scripts/check-secrets.mjs | Set-Content -Encoding utf8 
"$base\infrastructure\scripts\check-secrets-new.mjs"; git show 
73eac5e926950176d7d18fbfb997c9906ef1b2c3~1:infrastructure/scripts/check-secrets.mjs | Set-Content -Encoding utf8 
"$base\infrastructure\scripts\check-secrets-old.mjs"; '# KEY MATERIAL (fake, for scan test)' | Set-Content -Encoding 
utf8 "$base\docs\leak.env"; 'PRIVATE_KEY=5ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz1234567890XX' | 
Add-Content -Encoding utf8 "$base\docs\leak.env"; 'otimizacao de docs' | Set-Content -Encoding utf8 "$base\docs\x.md"; 
'some long base58-ish token: 8uQU2Vky7vXyVXqRkL9N5HjW3mZbCdEfGgHhIiJjKkLlMmNnOoPpQqRrSsTtUuVvWwXxYyZz1234567890AbCdEf' 
| Add-Content -Encoding utf8 "$base\docs\x.md"; 'console.log(1)' | Set-Content -Encoding utf8 "$base\src\index.ts"; 
Get-ChildItem -Recurse "$base" | Select-Object FullName | Out-String -Width 200
FullName                                                                                       
--------                                                                                       
C:\Users\enzo\AppData\Local\Temp\opencode\cs-repro\docs                                        
C:\Users\enzo\AppData\Local\Temp\opencode\cs-repro\infrastructure                              
C:\Users\enzo\AppData\Local\Temp\opencode\cs-repro\src                                         
C:\Users\enzo\AppData\Local\Temp\opencode\cs-repro\docs\leak.env                               
C:\Users\enzo\AppData\Local\Temp\opencode\cs-repro\docs\x.md                                   
C:\Users\enzo\AppData\Local\Temp\opencode\cs-repro\infrastructure\scripts                      
C:\Users\enzo\AppData\Local\Temp\opencode\cs-repro\infrastructure\scripts\check-secrets-new.mjs
C:\Users\enzo\AppData\Local\Temp\opencode\cs-repro\infrastructure\scripts\check-secrets-old.mjs
C:\Users\enzo\AppData\Local\Temp\opencode\cs-repro\src\index.ts
[0m
[0m$ [0m$base = "C:\Users\enzo\AppData\Local\Temp\opencode\cs-repro"; Write-Output "=== OLD (parent 73eac5e~1) ==="; 
node "$base\infrastructure\scripts\check-secrets-old.mjs"; Write-Output "OLD exit code: $LASTEXITCODE"; Write-Output 
"=== NEW (commit 73eac5e) ==="; node "$base\infrastructure\scripts\check-secrets-new.mjs"; Write-Output "NEW exit 
code: $LASTEXITCODE"
=== OLD (parent 73eac5e~1) ===
Varrendo: C:\Users\enzo\AppData\Local\Temp\opencode\cs-repro
[FALHA] Encontradas 5 ocorrÔö£┬¼ncia(s) suspeita(s):
  docs\leak.env:2  [private key literal]  PRIVATE_KEY=5ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz1234567890XX
  infrastructure\scripts\check-secrets-new.mjs:22  [private key literal]  { name: "private key literal", regex: 
/PRIVATE KEY|private[_ -]?key\s*[:=]|BEGIN
  infrastructure\scripts\check-secrets-new.mjs:23  [seed phrase]  { name: "seed phrase", regex: /seed 
phrase|mnemonic|secret recovery phrase/i },
  infrastructure\scripts\check-secrets-old.mjs:22  [private key literal]  { name: "private key literal", regex: 
/PRIVATE KEY|private[_ -]?key\s*[:=]|BEGIN
  infrastructure\scripts\check-secrets-old.mjs:23  [seed phrase]  { name: "seed phrase", regex: /seed 
phrase|mnemonic|secret recovery phrase/i },

AÔö£┬║Ôö£├║o: remova o conteÔö£Ôòædo real, mova para o ambiente/secret manager e, se jÔö£├¡ foi commitado, 
revogue/rotacione.

OLD exit code: 1
=== NEW (commit 73eac5e) ===
Varrendo: C:\Users\enzo\AppData\Local\Temp\opencode\cs-repro
[FALHA] Encontradas 4 ocorrÔö£┬¼ncia(s) suspeita(s):
  infrastructure\scripts\check-secrets-new.mjs:22  [private key literal]  { name: "private key literal", regex: 
/PRIVATE KEY|private[_ -]?key\s*[:=]|BEGIN
  infrastructure\scripts\check-secrets-new.mjs:23  [seed phrase]  { name: "seed phrase", regex: /seed 
phrase|mnemonic|secret recovery phrase/i },
  infrastructure\scripts\check-secrets-old.mjs:22  [private key literal]  { name: "private key literal", regex: 
/PRIVATE KEY|private[_ -]?key\s*[:=]|BEGIN
  infrastructure\scripts\check-secrets-old.mjs:23  [seed phrase]  { name: "seed phrase", regex: /seed 
phrase|mnemonic|secret recovery phrase/i },

AÔö£┬║Ôö£├║o: remova o conteÔö£Ôòædo real, mova para o ambiente/secret manager e, se jÔö£├¡ foi commitado, 
revogue/rotacione.

NEW exit code: 1
[0m
The empirical break is confirmed: with the **NEW** version, `docs/leak.env` containing a `PRIVATE_KEY=` is silently ignored (only a repro artifactÔÇörenamed script files self-matching the regexÔÇökept exit 1). Let me redo with the real filename to show the gate passing cleanly:
[0m$ [0m$base = "C:\Users\enzo\AppData\Local\Temp\opencode\cs-repro"; Copy-Item 
"$base\infrastructure\scripts\check-secrets-new.mjs" "$base\infrastructure\scripts\check-secrets.mjs"; Remove-Item 
"$base\infrastructure\scripts\check-secrets-new.mjs", "$base\infrastructure\scripts\check-secrets-old.mjs"; node 
"$base\infrastructure\scripts\check-secrets.mjs"; Write-Output "NEW(clean) exit code: $LASTEXITCODE"
Varrendo: C:\Users\enzo\AppData\Local\Temp\opencode\cs-repro

[OK] Nenhum padrÔö£├║o suspeito encontrado.
NEW(clean) exit code: 0
[0m
Empirical proof complete. Cleaning up the repro and closing the review.
[0m$ [0mRemove-Item -Recurse -Force "C:\Users\enzo\AppData\Local\Temp\opencode\cs-repro"; Write-Output "cleaned"
cleaned
[0m
---

## VEREDITO: REJEITADO

Commit auditado: `73eac5e` ÔÇö "chore(ops): check-secrets isenta docs/ (relatorios com dados on-chain publicos)" (branch `sprint/f1-5-enrichment`)

Escopo: 1 arquivo, `infrastructure/scripts/check-secrets.mjs` (+4/ÔêÆ1). N├úo ├® um commit cosm├®tico: altera o comportamento de um **controle de seguran├ºa aut├┤nomo que roda em todo push/PR** (`.github/workflows/ci.yml` job **Security Gate** ÔåÆ `npm run check:security` ÔåÆ `check:secrets`; AGENTS.md ┬º8.1). Revisei o conte├║do exato do commit via `git show 73eac5e:...` (o working tree est├í em `master` e difere da branch ÔÇö n├úo usei o arquivo local como fonte).

## ACHADOS

**1. HIGH ÔÇö `infrastructure/scripts/check-secrets.mjs:50` ÔÇö isen├º├úo em branco de TODO `docs/` desativa a varredura de secrets na pasta inteira (incluindo arquivos que n├úo s├úo relat├│rios).**
- Evid├¬ncia (reproduzido empiricamente, repo tempor├írio com o conte├║do exato do commit): criei `docs/leak.env` com `PRIVATE_KEY=5ABC...`. Vers├úo do parent (`73eac5e~1`): **exit 1**, `docs\leak.env:2 [private key literal]`. Vers├úo do commit: **exit 0**, `[OK] Nenhum padr├úo suspeito encontrado.`
- O `continue` na linha 50 pula **qualquer** arquivo sob `docs/` ÔÇö `.md`, `.json`, `.ts`, `.env`, `.pem`, `.txt` ÔÇö sem exce├º├úo. Na pr├ítica, `docs/security/red-team/poc/*.test.ts`, `docs/**/*.env`, ADRs e `docs/reviews/*.md` ficam invis├¡veis ao gate. Um secret real commitado ali n├úo gera nenhum sinal e o CI continua verde.
- Contradiz a pol├¡tica documentada no pr├│prio arquivo (linhas 27ÔÇô28): "Arquivos isentos de padr├Áes de FRASE... **A varredura de base58 continua ativa neles**." O commit remove essa garantia para docs/ inteiro. A mensagem do commit alega escopo "relatorios", mas a implementa├º├úo ├® toda a pasta.
- O gatilho leg├¡timo (signatures/serials base58 64ÔÇô88 em relat├│rios) j├í tinha solu├º├úo escopada no pr├│prio arquivo (`isResearchData` + `phraseExemptFiles` + pol├¡tica frase-exempt/base58-ativa). Sugest├úo: isentar apenas relat├│rios (ex.: `.md`/`.json` sob `docs/research|docs/reviews`, ou allowlist expl├¡cita por arquivo) e **manter base58 ativo** no restante de `docs/` ÔÇö n├úo pular a pasta inteira.

**2. MEDIUM ÔÇö `infrastructure/scripts/check-secrets.mjs:60` ÔÇö vari├ível `isDocs` morta e inalcan├º├ível.**
- `const isDocs = relForExempt.startsWith('docs/');` ├® calculada dentro do `forEach` mas **nunca usada**; e ├® logicamente inalcan├º├ível ÔÇö qualquer arquivo `docs/` j├í deu `continue` na linha 50 antes de chegar aqui.
- Evid├¬ncia de inten├º├úo abandonada: o autor come├ºou a escrever a isen├º├úo escopada (usar `isDocs` na composi├º├úo de `phraseExempt`, mantendo base58 ativo) e n├úo terminou; o resultado ├® c├│digo incoerente que induz mantenedores ao erro. Remover, ou usar de fato: `phraseExempt = ... || (isDocs && p.name !== "base58...")` e eliminar o skip da linha 50.

**3. MEDIUM ÔÇö `infrastructure/scripts/check-secrets.mjs:47ÔÇô49` ÔÇö ramo `isResearchData` agora ├® c├│digo morto.**
- A regra escopada original (`docs/research/**/*.json`) foi subsumida pela regra gen├®rica da linha 50 e nunca mais executa. C├│digo morto que esconde a inten├º├úo original. Consolidar em uma ├║nica regra nomeada e testada.

**4. MEDIUM ÔÇö Zero testes para mudan├ºa em controle de seguran├ºa.**
- N├úo existe nenhum teste para `check-secrets.mjs` (varri a ├írvore do commit: nenhum arquivo de teste/fixture referencia o script; `phraseExemptFiles` cobre s├│ o pr├│prio script).
- O commit altera o comportamento do gate do CI (payload `PRIVATE_KEY` sob `docs/` agora passa) **sem nenhuma verifica├º├úo mec├ónica**. AGENTS.md ┬º2.9 e o crit├®rio de decis├úo deste revisor ("Aus├¬ncia de testes em c├│digo novo ÔåÆ REJEITADO", "Suspeita n├úo confirm├ível ÔåÆ exige teste") aplicam diretamente.

**5. LOW ÔÇö Mensagem do commit Ôëá implementa├º├úo.** O texto diz "isenta docs/ (relatorios com dados on-chain publicos)", mas o efeito ├® isentar a pasta inteira ÔÇö incluindo `.env`, `.ts`, PoCs do red-team. Severidade amarrada ao #1; registrado para o autor dimensionar o escopo correto.

## TESTES EXECUTADOS (tentativas de quebra)

1. **Repro emp├¡rico old-vs-new** com ├írvore sint├®tica id├¬ntica ├á real: `docs/leak.env` (`PRIVATE_KEY=5ABC...`) + base58 longo em `docs/x.md` ÔåÆ parent: exit 1, flag em `docs\leak.env:2`; commit: exit 0, sil├¬ncio total. Prova direta da regress├úo.
2. **Wiring de CI**: confirmei que `check:secrets` roda como "Secrets scan" e dentro de `check:security` no job Security Gate (todo push/PR + schedule semanal) ÔÇö o enfraquecimento ├® de um gate aut├┤nomo obrigat├│rio.
3. **Corretude da regra**: `relForExempt` usa `relative(...).replace(/\\/g,"/")` ÔÇö normaliza├º├úo Windows ok; `startsWith('docs/')` n├úo casa `docs2/` (correto); sem off-by-one adicional.
4. **├ürvore do commit**: nenhum `.env` real sob `docs/` presente no commit (nenhum vazamento j├í existente detect├ível por ls-tree).

## O QUE N├âO FOI POSS├ìVEL VERIFICAR

- N├úo executei o workflow real do GitHub Actions (sem runner local); a equival├¬ncia assumida ├® `npm run check:secrets` ÔåÆ `node infrastructure/scripts/check-secrets.mjs`, confirmada no `package.json`.
- N├úo h├í como provar aus├¬ncia futura de secrets sob `docs/` ÔÇö exatamente o ponto do achado #1.
- N├úo testei o comportamento em CI Ubuntu/Linux (repro local foi Windows); a l├│gica ├® cross-platform por constru├º├úo (`node:path`), mas o CI real n├úo foi observado.

## RECOMENDA├ç├âO AO PRODUTOR

- Escopar a isen├º├úo aos relat├│rios (`.md`/`.json` em `docs/research|docs/reviews` ou allowlist por arquivo), **mantendo a varredura de base58 ativa** no restante de `docs/` (inclusive PoCs do red-team e `.env` acidentais).
- Remover `isDocs` e o ramo `isResearchData` morto; consolidar numa regra ├║nica e nomeada.
- Adicionar teste unit├írio para `check-secrets.mjs` (pelo menos: base58 ativo em docs, frase isenta em .md, `.env` fora de docs pego, `.env` dentro de docs pego) antes de re-enviar.

---

VERDICT: REJEITADO

