// @ts-nocheck
/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from '@solana/kit';

export const SYNC_USER_VOLUME_ACCUMULATOR_EVENT_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([197, 122, 167, 124, 116, 81, 91, 255]);

export function getSyncUserVolumeAccumulatorEventEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SYNC_USER_VOLUME_ACCUMULATOR_EVENT_EVENT_DISCRIMINATOR,
  );
}

export type SyncUserVolumeAccumulatorEventEvent = {
  user: Address;
  totalClaimedTokensBefore: bigint;
  totalClaimedTokensAfter: bigint;
  timestamp: bigint;
};

export type SyncUserVolumeAccumulatorEventEventArgs = {
  user: Address;
  totalClaimedTokensBefore: number | bigint;
  totalClaimedTokensAfter: number | bigint;
  timestamp: number | bigint;
};

/** Gets the encoder for {@link SyncUserVolumeAccumulatorEventEventArgs} event data. */
export function getSyncUserVolumeAccumulatorEventEventEncoder(): FixedSizeEncoder<SyncUserVolumeAccumulatorEventEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ['user', getAddressEncoder()],
      ['totalClaimedTokensBefore', getU64Encoder()],
      ['totalClaimedTokensAfter', getU64Encoder()],
      ['timestamp', getI64Encoder()],
    ]),
    [getConstantEncoder(SYNC_USER_VOLUME_ACCUMULATOR_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link SyncUserVolumeAccumulatorEventEvent} event data. */
export function getSyncUserVolumeAccumulatorEventEventDecoder(): FixedSizeDecoder<SyncUserVolumeAccumulatorEventEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ['user', getAddressDecoder()],
      ['totalClaimedTokensBefore', getU64Decoder()],
      ['totalClaimedTokensAfter', getU64Decoder()],
      ['timestamp', getI64Decoder()],
    ]),
    [getConstantDecoder(SYNC_USER_VOLUME_ACCUMULATOR_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link SyncUserVolumeAccumulatorEventEvent} event data. */
export function getSyncUserVolumeAccumulatorEventEventCodec(): FixedSizeCodec<
  SyncUserVolumeAccumulatorEventEventArgs,
  SyncUserVolumeAccumulatorEventEvent
> {
  return combineCodec(
    getSyncUserVolumeAccumulatorEventEventEncoder(),
    getSyncUserVolumeAccumulatorEventEventDecoder(),
  );
}

/** Parses the provided bytes into {@link SyncUserVolumeAccumulatorEventEvent} event data. */
export function parseSyncUserVolumeAccumulatorEventEvent(
  data: ReadonlyUint8Array | Uint8Array,
): SyncUserVolumeAccumulatorEventEvent {
  if (containsBytes(data, SYNC_USER_VOLUME_ACCUMULATOR_EVENT_EVENT_DISCRIMINATOR, 0)) {
    return getSyncUserVolumeAccumulatorEventEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "SyncUserVolumeAccumulatorEventEvent" event discriminators.',
  );
}
