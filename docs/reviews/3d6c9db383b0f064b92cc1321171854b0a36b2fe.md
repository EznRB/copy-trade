# Review 3d6c9db383b0f064b92cc1321171854b0a36b2fe

- Branch: sprint/f1-5-enrichment
- Data: 2026-09-25 02:35:36
- Gates: lint=1 typecheck=1 test=1 (0 = ok)
- Veredito: **REJEITADO (gates falharam)**

## Saida do code-reviewer

opencode.exe : [0m
No C:\Users\enzo\AppData\Roaming\npm\opencode.ps1:14 caractere:3
+   & "$basedir/node_modules/opencode-ai/bin/opencode.exe"   $args
+   ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : NotSpecified: ([0m:String) [], RemoteException
    + FullyQualifiedErrorId : NativeCommandError
 
> code-reviewer ┬À big-pickle
[0m
[0m$ [0mgit show 3d6c9db383b0f064b92cc1321171854b0a36b2fe --stat
commit 3d6c9db383b0f064b92cc1321171854b0a36b2fe
Author: EznRB <EznRB@users.noreply.github.com>
Date:   Fri Sep 25 02:17:51 2026 -0300

    feat(f1.5): schema ObservedEvent com enrichment + migration + repositories com guarda TOCTOU + testes
    
    - colunas nullable direction/amountSol/tokenAmount/mint/counterparty/enrichSource/enrichedAt/enrichAttempts + 
@@index
    - migration offline (20260924142844_f15_enrichment) sem BOM
    - repositories: markEnriched/markUndecodable/findUnenriched/incrementEnrichAttempt
    - TOCTOU: updateMany WHERE enrichedAt IS NULL (idempotencia por construcao)
    - observed-events.enrich.test.ts: 9 testes com fakes (sem prisma singleton)
    - schema.prisma sanitizado ASCII (remove U+FFFD/CJK)
    
    Resolve achados: review 88152bbf (HIGH-1 cobertura repos, MEDIUM TOCTOU, MEDIUM encoding), 5299d25*.

 .../20260924142844_f15_enrichment/migration.sql    |  14 ++
 packages/database/prisma/schema.prisma             |  33 ++--
 packages/database/src/index.ts                     |   7 +
 .../repositories/observed-events.enrich.test.ts    | 194 +++++++++++++++++++++
 .../database/src/repositories/observed-events.ts   | 141 +++++++++++++++
 5 files changed, 379 insertions(+), 10 deletions(-)
[0m
[0m$ [0mgit log --oneline -15 3d6c9db383b0f064b92cc1321171854b0a36b2fe
3d6c9db feat(f1.5): schema ObservedEvent com enrichment + migration + repositories com guarda TOCTOU + testes
7d7e3d1 feat(f1.5): decoder pump.fun/PumpSwap via codama da IDL oficial (pump-public-docs @81091419)
3e51d3e review(sprint/f1-5-enrichment): REJEITADO (gates falharam) cfca4f89323cbfb68e0dcfa8aafe23a39e69301f
b7364bc review(sprint/f1-5-enrichment): REJEITADO (gates falharam) 0167a0d89fe0f21d6aa83d51e4738a1f4c5e96ae
10f6011 review(sprint/f1-5-enrichment): REJEITADO (gates falharam) 812ef1a7f376b01de7f7dbc21ffae9f645e1edc3
1e1d208 review(sprint/f1-5-enrichment): REJEITADO (gates falharam) 799d04834d147cbdcc4ad8e46c4b8499d8ad1b00
d119914 review(sprint/f1-5-enrichment): REJEITADO (gates falharam) 6cd436b9bc7582a41c42b1ae40805b3cec015936
829323d review(sprint/f1-5-enrichment): REJEITADO cc64b8d9db54422332e4016b2f86a8d332ee389a
768175e review(sprint/f1-5-enrichment): REJEITADO (gates falharam) fd6a5a6feae30ada70e1a0dec0d1ab0b99cc8e5e
f92985a fix(review): afe614cf tambem tinha veredito final APROVADO (state sincronizado)
b09c20f fix(review): state.json reflete veredito final APROVADO do 38cdaac (arquivo = autoridade)
161a486 docs(sec): SEC fecha RT-004/005/006 (FIXED-VERIFIED, re-teste 17/17 PoCs)
8736882 review(sprint/f1-5-enrichment): REJEITADO 5299d250602fd81b9ac1849c026ec11955411feb
3b9e273 review(sprint/f1-5-enrichment): REJEITADO (gates falharam) 38cdaac0b30e0ddf53dab697b7131e4a3499d55c
82e2a52 review(sprint/f1-5-enrichment): REJEITADO 2ab477f7dc60361db18e2106cd4b9ef48aea977e
[0m
[0m$ [0mgit show 3d6c9db383b0f064b92cc1321171854b0a36b2fe --format=fuller --no-ext-diff
commit 3d6c9db383b0f064b92cc1321171854b0a36b2fe
Author:     EznRB <EznRB@users.noreply.github.com>
AuthorDate: Fri Sep 25 02:17:51 2026 -0300
Commit:     EznRB <EznRB@users.noreply.github.com>
CommitDate: Fri Sep 25 02:21:27 2026 -0300

    feat(f1.5): schema ObservedEvent com enrichment + migration + repositories com guarda TOCTOU + testes
    
    - colunas nullable direction/amountSol/tokenAmount/mint/counterparty/enrichSource/enrichedAt/enrichAttempts + 
@@index
    - migration offline (20260924142844_f15_enrichment) sem BOM
    - repositories: markEnriched/markUndecodable/findUnenriched/incrementEnrichAttempt
    - TOCTOU: updateMany WHERE enrichedAt IS NULL (idempotencia por construcao)
    - observed-events.enrich.test.ts: 9 testes com fakes (sem prisma singleton)
    - schema.prisma sanitizado ASCII (remove U+FFFD/CJK)
    
    Resolve achados: review 88152bbf (HIGH-1 cobertura repos, MEDIUM TOCTOU, MEDIUM encoding), 5299d25*.

diff --git a/packages/database/prisma/migrations/20260924142844_f15_enrichment/migration.sql 
b/packages/database/prisma/migrations/20260924142844_f15_enrichment/migration.sql
new file mode 100644
index 0000000..d1a2430
--- /dev/null
+++ b/packages/database/prisma/migrations/20260924142844_f15_enrichment/migration.sql
@@ -0,0 +1,14 @@
+-- F1.5 enrichment: colunas nullable em "ObservedEvent".
+-- Eventos antigos e nao decodificaveis permanecem NULL/UNKNOWN (nunca inferidos).
+ALTER TABLE "ObservedEvent"
+  ADD COLUMN "direction" TEXT,
+  ADD COLUMN "amountSol" DECIMAL(65,30),
+  ADD COLUMN "tokenAmount" DECIMAL(65,30),
+  ADD COLUMN "mint" TEXT,
+  ADD COLUMN "counterparty" TEXT,
+  ADD COLUMN "enrichSource" TEXT,
+  ADD COLUMN "enrichedAt" TIMESTAMP(3),
+  ADD COLUMN "enrichAttempts" INTEGER NOT NULL DEFAULT 0;
+
+CREATE INDEX "ObservedEvent_mint_idx" ON "ObservedEvent"("mint");
+CREATE INDEX "ObservedEvent_eventType_idx" ON "ObservedEvent"("eventType");
diff --git a/packages/database/prisma/schema.prisma b/packages/database/prisma/schema.prisma
index 87679b0..ee0b14e 100644
--- a/packages/database/prisma/schema.prisma
+++ b/packages/database/prisma/schema.prisma
@@ -1,5 +1,5 @@
-// @ct/database ÔÇö Prisma schema v1 (Fases 1ÔÇô9)
-// Fonte de verdade para persist├¬ncia. Nunca adicionar secrets aqui (somente env refs).
+// @ct/database ? Prisma schema v1 (Fases 1?9)
+// Fonte de verdade para persist?ncia. Nunca adicionar secrets aqui (somente env refs).
 
 datasource db {
   provider = "postgresql"
@@ -25,7 +25,7 @@ model Wallet {
   metrics        WalletMetric[]
   trades         WalletTrade[]
   clusterMembers WalletClusterMember[]
-  // ObservedEvent usa `wallet` como address (String), n├úo FK ÔÇö sem relation aqui.
+  // ObservedEvent usa `wallet` como address (String), n?o FK ? sem relation aqui.
 }
 
 model WalletMetric {
@@ -130,7 +130,7 @@ model Creator {
 // ---------------------------------------------------------------------------
 
 model ObservedEvent {
-  id               String   @id @default(cuid())
+  id               String    @id @default(cuid())
   signature        String
   instructionIndex Int
   wallet           String
@@ -138,14 +138,27 @@ model ObservedEvent {
   slot             BigInt?
   blockTime        DateTime?
   payload          Json?
-  receivedAt       DateTime @default(now())
-
-  // Chave de dedup (idempot├¬ncia ÔÇö AGENTS.md regra 6)
+  receivedAt       DateTime  @default(now())
+
+  // Enrichment F1.5 (decoder pump.fun/PumpSwap) ? tudo nullable:
+  // eventos antigos e nao decodificaveis ficam null/UNKNOWN, nunca inferidos.
+  direction      String? // 'buy' | 'sell'
+  amountSol      Decimal?
+  tokenAmount    Decimal?
+  mint           String?
+  counterparty   String? // bonding curve (? curve) ou pool PumpSwap
+  enrichSource   String? // ex.: 'pump-decoder-v1'
+  enrichedAt     DateTime?
+  enrichAttempts Int      @default(0)
+
+  // Chave de dedup (idempotencia - AGENTS.md regra 6)
   @@unique([signature, instructionIndex, wallet])
+  @@index([mint])
+  @@index([eventType])
 }
 
 // ---------------------------------------------------------------------------
-// Signals (Fases 4ÔÇô7)
+// Signals (Fases 4?7)
 // ---------------------------------------------------------------------------
 
 model Signal {
@@ -176,7 +189,7 @@ model SignalFeature {
 }
 
 // ---------------------------------------------------------------------------
-// Execution (Fases 9ÔÇô10) ÔÇö nunca habilitar LIVE sem aprova├º├úo humana
+// Execution (Fases 9?10) ? nunca habilitar LIVE sem aprova??o humana
 // ---------------------------------------------------------------------------
 
 model Order {
@@ -233,7 +246,7 @@ model ExecutionMetric {
 }
 
 // ---------------------------------------------------------------------------
-// Backtest / ML (Fases 5ÔÇô7)
+// Backtest / ML (Fases 5?7)
 // ---------------------------------------------------------------------------
 
 model BacktestRun {
diff --git a/packages/database/src/index.ts b/packages/database/src/index.ts
index ad87218..eb4e6de 100644
--- a/packages/database/src/index.ts
+++ b/packages/database/src/index.ts
@@ -26,9 +26,16 @@ export type * from '@prisma/client';
 export {
   insertObservedEvent,
   existsObservedEvent,
+  markEnriched,
+  markUndecodable,
+  findUnenriched,
+  incrementEnrichAttempt,
   type InsertResult,
   type ObservedEventInsertData,
   type ObservedEventDelegate,
+  type EnrichmentData,
+  type UnenrichedRow,
+  type ObservedEventEnrichDelegate,
 } from './repositories/observed-events.js';
 export {
   recordSystemEvent,
diff --git a/packages/database/src/repositories/observed-events.enrich.test.ts 
b/packages/database/src/repositories/observed-events.enrich.test.ts
new file mode 100644
index 0000000..6fa4180
--- /dev/null
+++ b/packages/database/src/repositories/observed-events.enrich.test.ts
@@ -0,0 +1,194 @@
+/**
+ * Testes dos metodos de enrichment do ObservedEvent (review 88152bbf/5299d25):
+ * markEnriched, markUndecodable, findUnenriched, incrementEnrichAttempt.
+ * Fakes em memoria; nunca importa o singleton prisma.
+ */
+import { describe, it, expect } from 'vitest';
+import {
+  markEnriched,
+  markUndecodable,
+  findUnenriched,
+  incrementEnrichAttempt,
+  type UnenrichedRow,
+} from './observed-events.js';
+import { RepositoryError } from './errors.js';
+
+interface Row {
+  id: string;
+  signature: string;
+  instructionIndex: number;
+  wallet: string;
+  enrichedAt: Date | null;
+  enrichAttempts: number;
+  receivedAt: Date;
+  direction?: string | null;
+  mint?: string | null;
+}
+
+/** Delegate fake com update guardado (TOCTOU), updateMany, findMany ordenado. */
+function fakeDelegate(initial: Row[]) {
+  const rows = [...initial];
+  return {
+    rows,
+    async update(args: { where: { id: string }; data: Record<string, unknown> }) {
+      const r = rows.find((x) => x.id === args.where.id);
+      if (!r) throw new Error('not found');
+      Object.assign(r, args.data);
+      return r;
+    },
+    async updateMany(args: { where: Record<string, unknown>; data: Record<string, unknown> }) {
+      const { id, enrichedAt } = args.where as { id?: string; enrichedAt?: Date | null };
+      let count = 0;
+      for (const r of rows) {
+        if (id !== undefined && r.id !== id) continue;
+        if ('enrichedAt' in args.where) {
+          if (enrichedAt === null) {
+            if (r.enrichedAt !== null) continue;
+          }
+        }
+        Object.assign(r, args.data);
+        count++;
+      }
+      return { count };
+    },
+    async findMany(args: {
+      where: Record<string, unknown>;
+      orderBy: Record<string, unknown>;
+      take: number;
+      select: Record<string, boolean>;
+    }): Promise<UnenrichedRow[]> {
+      const out = rows
+        .filter((r) => {
+          const w = args.where as { enrichedAt?: null; enrichAttempts?: { lt?: number } };
+          if (w.enrichedAt === null && r.enrichedAt !== null) return false;
+          if (w.enrichAttempts && r.enrichAttempts >= (w.enrichAttempts.lt ?? Infinity))
+            return false;
+          return true;
+        })
+        .sort((a, b) => a.receivedAt.getTime() - b.receivedAt.getTime())
+        .slice(0, args.take)
+        .map((r) => ({
+          id: r.id,
+          signature: r.signature,
+          instructionIndex: r.instructionIndex,
+          wallet: r.wallet,
+        }));
+      return out;
+    },
+    async updateManyInc(args: { where: { id: string }; data: Record<string, unknown> }) {
+      const r = rows.find((x) => x.id === args.where.id);
+      if (!r) return { count: 0 };
+      const inc = (args.data['enrichAttempts'] as { increment: number }).increment;
+      r.enrichAttempts += inc;
+      return { count: 1 };
+    },
+  };
+}
+
+const row = (id: string, over: Partial<Row> = {}): Row => ({
+  id,
+  signature: id,
+  instructionIndex: 0,
+  wallet: 'W',
+  enrichedAt: null,
+  enrichAttempts: 0,
+  receivedAt: new Date('2026-01-01T00:00:00Z'),
+  ...over,
+});
+
+describe('markEnriched', () => {
+  it('persiste todos os campos + enrichedAt', async () => {
+    const d = fakeDelegate([row('a')]);
+    await markEnriched({ observedEvent: d }, {
+      id: 'a',
+      direction: 'buy',
+      amountSol: '0.5',
+      tokenAmount: '1000',
+      mint: 'M'.repeat(44),
+      counterparty: 'C'.repeat(44),
+      enrichSource: 'pump-decoder-v1:pump_buy',
+    });
+    expect(d.rows[0]!.direction).toBe('buy');
+    expect(d.rows[0]!.enrichedAt).toBeInstanceOf(Date);
+  });
+
+  it('TOCTOU: escrita guardada nao sobrescreve evento ja enriquecido', async () => {
+    const d = fakeDelegate([row('a', { enrichedAt: new Date() })]);
+    await expect(
+      markEnriched({ observedEvent: d }, {
+        id: 'a',
+        direction: 'sell',
+        amountSol: '1',
+        tokenAmount: '1',
+        mint: 'M'.repeat(44),
+        counterparty: 'C'.repeat(44),
+        enrichSource: 'pump-decoder-v1:pump_sell',
+      }),
+    ).rejects.toThrowError(RepositoryError);
+  });
+
+  it('delegate sem updateMany cai no update simples (compat)', async () => {
+    const d = fakeDelegate([row('a')]);
+    const noUM = { ...d } as Omit<typeof d, 'updateMany'>;
+    delete (noUM as Record<string, unknown>).updateMany;
+    await markEnriched({ observedEvent: noUM }, {
+      id: 'a',
+      direction: 'buy',
+      amountSol: null,
+      tokenAmount: '10',
+      mint: 'M'.repeat(44),
+      counterparty: 'C'.repeat(44),
+      enrichSource: 'pump-decoder-v1:pump_buy',
+    });
+    expect(d.rows[0]!.enrichedAt).toBeInstanceOf(Date);
+  });
+});
+
+describe('markUndecodable', () => {
+  it('marca com reason truncada a 128 chars', async () => {
+    const d = fakeDelegate([row('a')]);
+    const long = 'x'.repeat(500);
+    await markUndecodable({ observedEvent: d }, 'a', long);
+    const src = (d.rows[0] as unknown as Record<string, unknown>)['enrichSource'] as string;
+    expect(src.startsWith('undecodable:')).toBe(true);
+    expect(src.length).toBeLessThanOrEqual(128);
+    expect(d.rows[0]!.enrichedAt).toBeInstanceOf(Date);
+  });
+});
+
+describe('findUnenriched', () => {
+  it('filtra enrichedAt null e enrichAttempts < max, ordena por receivedAt asc, respeita take', async () => {
+    const d = fakeDelegate([
+      row('old', { receivedAt: new Date('2026-01-01') }),
+      row('new', { receivedAt: new Date('2026-02-01') }),
+      row('enriched', { enrichedAt: new Date() }),
+      row('maxed', { enrichAttempts: 5 }),
+    ]);
+    const out = await findUnenriched({ observedEvent: d }, { limit: 10, maxAttempts: 5 });
+    expect(out.map((r) => r.id)).toEqual(['old', 'new']);
+    const one = await findUnenriched({ observedEvent: d }, { limit: 1, maxAttempts: 5 });
+    expect(one.map((r) => r.id)).toEqual(['old']);
+  });
+});
+
+describe('incrementEnrichAttempt', () => {
+  it('incrementa via updateMany', async () => {
+    const d = fakeDelegate([row('a')]);
+    // usa o updateMany de incremento do prisma (fake aproxima shape do Prisma)
+    const shaped = {
+      ...d,
+      updateMany: d.updateManyInc.bind(d),
+    };
+    await incrementEnrichAttempt({ observedEvent: shaped }, 'a');
+    expect(d.rows[0]!.enrichAttempts).toBe(1);
+  });
+
+  it('delegate sem updateMany lanca RepositoryError', async () => {
+    const d = fakeDelegate([row('a')]);
+    const noUM = { ...d } as Omit<typeof d, 'updateMany'>;
+    delete (noUM as Record<string, unknown>).updateMany;
+    await expect(incrementEnrichAttempt({ observedEvent: noUM }, 'a')).rejects.toBeInstanceOf(
+      RepositoryError,
+    );
+  });
+});
diff --git a/packages/database/src/repositories/observed-events.ts 
b/packages/database/src/repositories/observed-events.ts
index 1ebaab6..878cbdd 100644
--- a/packages/database/src/repositories/observed-events.ts
+++ b/packages/database/src/repositories/observed-events.ts
@@ -76,3 +76,144 @@ export async function existsObservedEvent(
     );
   }
 }
+
+// ---------------------------------------------------------------------------
+// F1.5 ÔÇö Enrichment (decoder pump.fun/PumpSwap)
+// ---------------------------------------------------------------------------
+
+export interface EnrichmentData {
+  id: string;
+  direction: 'buy' | 'sell';
+  amountSol: string | number | null;
+  tokenAmount: string | number | null;
+  mint: string;
+  counterparty: string;
+  enrichSource: string;
+}
+
+export interface UnenrichedRow {
+  id: string;
+  signature: string;
+  instructionIndex: number;
+  wallet: string;
+}
+
+export interface ObservedEventEnrichDelegate {
+  update(args: { where: { id: string }; data: Record<string, unknown> }): Promise<unknown>;
+  findMany(args: {
+    where: Record<string, unknown>;
+    orderBy: Record<string, unknown>;
+    take: number;
+    select: Record<string, boolean>;
+  }): Promise<UnenrichedRow[]>;
+  updateMany?(args: { where: Record<string, unknown>; data: Record<string, unknown> }): Promise<unknown>;
+}
+
+/**
+ * Marca um evento como enriquecido com os campos decodificados.
+ * TOCTOU-safe (review 88152bbf): guarda `enrichedAt: null` no WHERE ÔÇö
+ * se outro worker enriqueceu antes, o update n├úo afeta linha alguma e
+ * reportamos que n├úo foi gravado (falso positivo de escrita eliminado).
+ */
+export async function markEnriched(
+  client: { observedEvent: ObservedEventEnrichDelegate },
+  data: EnrichmentData,
+): Promise<void> {
+  // Fallback deterministico quando o delegate nao tem updateMany com guarda.
+  const supportsGuardedWrite = typeof client.observedEvent.updateMany === 'function';
+  const payload = {
+    direction: data.direction,
+    amountSol: data.amountSol,
+    tokenAmount: data.tokenAmount,
+    mint: data.mint,
+    counterparty: data.counterparty,
+    enrichSource: data.enrichSource,
+    enrichedAt: new Date(),
+  };
+  try {
+    if (supportsGuardedWrite && client.observedEvent.updateMany) {
+      const res = (await client.observedEvent.updateMany({
+        where: { id: data.id, enrichedAt: null },
+        data: payload,
+      })) as { count?: number } | undefined;
+      if (res && typeof res.count === 'number' && res.count === 0) {
+        throw new RepositoryError(
+          'DATABASE_ERROR',
+          `markEnriched ignorado: ObservedEvent ${data.id} ja estava enriquecido (TOCTOU)`,
+          new Error('guarda enrichedAt IS NULL falhou'),
+        );
+      }
+      return;
+    }
+    await client.observedEvent.update({
+      where: { id: data.id },
+      data: payload,
+    });
+  } catch (err) {
+    if (err instanceof RepositoryError) throw err;
+    throw new RepositoryError('DATABASE_ERROR', `Falha ao enriquecer ObservedEvent ${data.id}`, err);
+  }
+}
+
+/** Marca o evento como processado sem decodificar (UNKNOWN expl├¡cito). */
+export async function markUndecodable(
+  client: { observedEvent: ObservedEventEnrichDelegate },
+  id: string,
+  reason: string,
+): Promise<void> {
+  try {
+    const guarded = typeof client.observedEvent.updateMany === 'function';
+    const payload = {
+      enrichSource: `undecodable:${reason}`.slice(0, 128),
+      enrichedAt: new Date(),
+    };
+    if (guarded && client.observedEvent.updateMany) {
+      await client.observedEvent.updateMany({
+        where: { id, enrichedAt: null },
+        data: payload,
+      });
+      return;
+    }
+    await client.observedEvent.update({
+      where: { id },
+      data: payload,
+    });
+  } catch (err) {
+    throw new RepositoryError('DATABASE_ERROR', `Falha ao marcar undecodable ${id}`, err);
+  }
+}
+
+/** Busca lote de eventos ainda n├úo enriquecidos para processar (backfill/enricher). */
+export async function findUnenriched(
+  client: { observedEvent: ObservedEventEnrichDelegate },
+  opts: { limit: number; maxAttempts: number },
+): Promise<UnenrichedRow[]> {
+  try {
+    return await client.observedEvent.findMany({
+      where: { enrichedAt: null, enrichAttempts: { lt: opts.maxAttempts } },
+      orderBy: { receivedAt: 'asc' },
+      take: opts.limit,
+      select: { id: true, signature: true, instructionIndex: true, wallet: true },
+    });
+  } catch (err) {
+    throw new RepositoryError('DATABASE_ERROR', 'Falha ao buscar eventos n├úo enriquecidos', err);
+  }
+}
+
+/** Incrementa o contador de tentativas de enrichment (evita retry infinito). */
+export async function incrementEnrichAttempt(
+  client: { observedEvent: ObservedEventEnrichDelegate },
+  id: string,
+): Promise<void> {
+  if (!client.observedEvent.updateMany) {
+    throw new RepositoryError('DATABASE_ERROR', 'delegate sem updateMany', new Error('unsupported'));
+  }
+  try {
+    await client.observedEvent.updateMany({
+      where: { id },
+      data: { enrichAttempts: { increment: 1 } },
+    });
+  } catch (err) {
+    throw new RepositoryError('DATABASE_ERROR', `Falha ao incrementar enrichAttempts ${id}`, err);
+  }
+}
[0m
[0mÔåÆ [0mRead packages/database/prisma/schema.prisma
[0m
[0m$ [0mgit log --oneline -5 master; git branch --show-current; git status --short | Select-Object -First 20
b2a2048 review(sprint/f1-5-enrichment): REJEITADO (gates falharam) 7d7e3d129fb025dadbf3e48478297be86d18850d
cc09b02 review(sprint/f1-5-enrichment): REJEITADO d46525dda3e8b2edaa46d09a3947c56230b38d4b
5646293 review(sprint/f1-5-enrichment): REJEITADO d016b8b22f89cd2350ee5dacdbc9ed0a06525f9c
3e51d3e review(sprint/f1-5-enrichment): REJEITADO (gates falharam) cfca4f89323cbfb68e0dcfa8aafe23a39e69301f
b7364bc review(sprint/f1-5-enrichment): REJEITADO (gates falharam) 0167a0d89fe0f21d6aa83d51e4738a1f4c5e96ae
master
[0m
[0m$ [0mGet-ChildItem -Recurse packages/database/prisma/migrations | Select-Object FullName
FullName                                                                                             
--------                                                                                             
C:\Users\enzo\Desktop\copytrade 2\packages\database\prisma\migrations\000000000000_init              
C:\Users\enzo\Desktop\copytrade 2\packages\database\prisma\migrations\migration_lock.toml            
C:\Users\enzo\Desktop\copytrade 2\packages\database\prisma\migrations\000000000000_init\migration.sql
[0m
[0m$ [0mgit show 3d6c9db:packages/database/prisma/schema.prisma
// @ct/database ? Prisma schema v1 (Fases 1?9)
// Fonte de verdade para persist?ncia. Nunca adicionar secrets aqui (somente env refs).

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

// ---------------------------------------------------------------------------
// Wallets (Fase 2)
// ---------------------------------------------------------------------------

model Wallet {
  id             String   @id @default(cuid())
  address        String   @unique
  label          String?
  classification String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  metrics        WalletMetric[]
  trades         WalletTrade[]
  clusterMembers WalletClusterMember[]
  // ObservedEvent usa `wallet` como address (String), n?o FK ? sem relation aqui.
}

model WalletMetric {
  id               String   @id @default(cuid())
  walletId         String
  wallet           Wallet   @relation(fields: [walletId], references: [id])
  window           String // ex.: "7d", "30d"
  tradeCount       Int      @default(0)
  winRate          Float?
  grossPnlLamports BigInt   @default(0)
  netPnlLamports   BigInt   @default(0)
  avgHoldSeconds   Int?
  computedAt       DateTime @default(now())

  @@index([walletId, window])
}

model WalletTrade {
  id                 String   @id @default(cuid())
  walletId           String
  wallet             Wallet   @relation(fields: [walletId], references: [id])
  mint               String
  side               String // "buy" | "sell"
  signature          String
  instructionIndex   Int
  amountInLamports   BigInt?
  amountOutLamports  BigInt?
  executedAt         DateTime

  @@unique([signature, instructionIndex, walletId])
}

model WalletCluster {
  id          String   @id @default(cuid())
  name        String?
  method      String // ex.: "funding_source", "heuristic"
  createdAt   DateTime @default(now())

  members     WalletClusterMember[]
}

model WalletClusterMember {
  id        String        @id @default(cuid())
  clusterId String
  cluster   WalletCluster @relation(fields: [clusterId], references: [id])
  walletId  String
  wallet    Wallet        @relation(fields: [walletId], references: [id])

  @@unique([clusterId, walletId])
}

// ---------------------------------------------------------------------------
// Tokens (Fase 3)
// ---------------------------------------------------------------------------

model Token {
  id        String   @id @default(cuid())
  mint      String   @unique
  symbol    String?
  name      String?
  creatorId String?
  creator   Creator?  @relation(fields: [creatorId], references: [id])
  createdAt DateTime @default(now())

  metrics    TokenMetric[]
  riskEvents TokenRiskEvent[]
}

model TokenMetric {
  id               String   @id @default(cuid())
  tokenId          String
  token            Token    @relation(fields: [tokenId], references: [id])
  liquidityLamports BigInt?
  holderCount      Int?
  topHolderPct     Float?
  computedAt       DateTime @default(now())

  @@index([tokenId, computedAt])
}

model TokenRiskEvent {
  id        String   @id @default(cuid())
  tokenId   String
  token     Token    @relation(fields: [tokenId], references: [id])
  kind      String // ex.: "bundle", "dev_dump", "honeypot_suspect"
  severity  String // "info" | "warn" | "critical"
  details   Json?
  detectedAt DateTime @default(now())
}

model Creator {
  id        String   @id @default(cuid())
  address   String   @unique
  riskScore Float?
  createdAt DateTime @default(now())

  tokens    Token[]
}

// ---------------------------------------------------------------------------
// Ingestion (Fase 1)
// ---------------------------------------------------------------------------

model ObservedEvent {
  id               String    @id @default(cuid())
  signature        String
  instructionIndex Int
  wallet           String
  eventType        String
  slot             BigInt?
  blockTime        DateTime?
  payload          Json?
  receivedAt       DateTime  @default(now())

  // Enrichment F1.5 (decoder pump.fun/PumpSwap) ? tudo nullable:
  // eventos antigos e nao decodificaveis ficam null/UNKNOWN, nunca inferidos.
  direction      String? // 'buy' | 'sell'
  amountSol      Decimal?
  tokenAmount    Decimal?
  mint           String?
  counterparty   String? // bonding curve (? curve) ou pool PumpSwap
  enrichSource   String? // ex.: 'pump-decoder-v1'
  enrichedAt     DateTime?
  enrichAttempts Int      @default(0)

  // Chave de dedup (idempotencia - AGENTS.md regra 6)
  @@unique([signature, instructionIndex, wallet])
  @@index([mint])
  @@index([eventType])
}

// ---------------------------------------------------------------------------
// Signals (Fases 4?7)
// ---------------------------------------------------------------------------

model Signal {
  id              String   @id @default(cuid())
  correlationId   String
  strategyId      String
  strategyVersion String
  mint            String
  direction       String // "buy" | "sell"
  score           Float?
  status          String   @default("pending") // pending | approved | vetoed | expired
  createdAt       DateTime @default(now())

  features        SignalFeature[]
  orders          Order[]

  @@index([strategyId, strategyVersion, createdAt])
}

model SignalFeature {
  id        String @id @default(cuid())
  signalId  String
  signal    Signal @relation(fields: [signalId], references: [id])
  name      String
  value     Float

  @@unique([signalId, name])
}

// ---------------------------------------------------------------------------
// Execution (Fases 9?10) ? nunca habilitar LIVE sem aprova??o humana
// ---------------------------------------------------------------------------

model Order {
  id            String   @id @default(cuid())
  correlationId String
  signalId      String?
  signal        Signal?  @relation(fields: [signalId], references: [id])
  mode          String // "PAPER" | "SHADOW" | "LIVE"
  mint          String
  side          String
  amountLamports BigInt
  status        String   @default("created") // created | submitted | confirmed | failed | vetoed
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  transactions  Transaction[]
  positions     Position[]
}

model Transaction {
  id            String   @id @default(cuid())
  correlationId String
  orderId       String?
  order         Order?   @relation(fields: [orderId], references: [id])
  signature     String?  @unique
  status        String // "built" | "sent" | "confirmed" | "failed"
  feeLamports   BigInt?
  priorityFeeLamports BigInt?
  tipLamports   BigInt?
  sentAt        DateTime?
  confirmedAt   DateTime?
  createdAt     DateTime @default(now())
}

model Position {
  id            String   @id @default(cuid())
  correlationId String
  orderId       String?
  order         Order?   @relation(fields: [orderId], references: [id])
  mint          String
  status        String   @default("open") // open | closed
  openedAt      DateTime @default(now())
  closedAt      DateTime?
  grossPnlLamports BigInt?
  netPnlLamports   BigInt?
}

model ExecutionMetric {
  id            String   @id @default(cuid())
  correlationId String
  name          String
  value         Float
  recordedAt    DateTime @default(now())
}

// ---------------------------------------------------------------------------
// Backtest / ML (Fases 5?7)
// ---------------------------------------------------------------------------

model BacktestRun {
  id              String   @id @default(cuid())
  strategyId      String
  strategyVersion String
  params          Json?
  startedAt       DateTime @default(now())
  finishedAt      DateTime?
  status          String   @default("running")

  trades          BacktestTrade[]
}

model BacktestTrade {
  id           String      @id @default(cuid())
  runId        String
  run          BacktestRun @relation(fields: [runId], references: [id])
  mint         String
  entryAt      DateTime
  exitAt       DateTime?
  grossPnlLamports BigInt?
  netPnlLamports   BigInt?
}

model ModelVersion {
  id                  String   @id @default(cuid())
  modelId             String
  modelVersion        String
  featureSchemaVersion String
  artifactUri         String?
  trainedAt           DateTime @default(now())

  predictions         ModelPrediction[]

  @@unique([modelId, modelVersion])
}

model ModelPrediction {
  id             String       @id @default(cuid())
  modelVersionId String
  modelVersion   ModelVersion @relation(fields: [modelVersionId], references: [id])
  correlationId  String?
  inputHash      String?
  output         Json
  predictedAt    DateTime     @default(now())
}

// ---------------------------------------------------------------------------
// Sistema / Risco / Auditoria (transversal)
// ---------------------------------------------------------------------------

model SystemEvent {
  id        String   @id @default(cuid())
  kind      String
  severity  String   @default("info")
  message   String
  details   Json?
  createdAt DateTime @default(now())
}

model RiskEvent {
  id            String   @id @default(cuid())
  correlationId String?
  kind          String // ex.: "veto", "circuit_breaker", "kill_switch"
  reason        String
  details       Json?
  createdAt     DateTime @default(now())
}

model AuditLog {
  id        String   @id @default(cuid())
  actor     String
  action    String
  target    String?
  details   Json?
  createdAt DateTime @default(now())
}
[0m
[0mÔåÆ [0mRead packages/database/src/repositories/observed-events.ts
[0m
[0m$ [0mgit ls-tree -r 3d6c9db --name-only | Select-String -Pattern "migration|package.json|vitest|tsconfig"
apps/api/package.json
apps/dashboard/package.json
backtester/package.json
backtester/tsconfig.json
docs/architecture/adr/ADR-004-vitest-fast-check-pytest-hypothesis.md
docs/security/red-team/poc/vitest.poc.config.mjs
package.json
packages/config/package.json
packages/config/tsconfig.json
packages/database/package.json
packages/database/prisma/migrations/000000000000_init/migration.sql
packages/database/prisma/migrations/20260924142844_f15_enrichment/migration.sql
packages/database/prisma/migrations/migration_lock.toml
packages/database/tsconfig.json
packages/dex/package.json
packages/dex/tsconfig.json
packages/logging/package.json
packages/logging/tsconfig.json
packages/metrics/package.json
packages/metrics/tsconfig.json
packages/pumpfun/package.json
packages/pumpfun/src/generated/pump/events/completePumpAmmMigrationEvent.ts
packages/pumpfun/tsconfig.json
packages/solana/package.json
packages/solana/tsconfig.json
packages/types/package.json
packages/types/tsconfig.json
services/data-ingestion/package.json
services/data-ingestion/tsconfig.json
services/execution-engine/package.json
services/execution-engine/tsconfig.json
services/feature-engine/package.json
services/feature-engine/tsconfig.json
services/notification-service/package.json
services/notification-service/tsconfig.json
services/position-manager/package.json
services/position-manager/tsconfig.json
services/risk-engine/package.json
services/risk-engine/tsconfig.json
services/signal-engine/package.json
services/signal-engine/tsconfig.json
services/token-monitor/package.json
services/token-monitor/tsconfig.json
services/wallet-monitor/package.json
services/wallet-monitor/tsconfig.json
tsconfig.base.json
tsconfig.json
vitest.config.ts
[0m
[0mÔåÆ [0mRead packages/database/src/repositories/errors.ts
[0mÔåÆ [0mRead packages/database/package.json
[0mÔåÆ [0mRead package.json
[0mÔåÆ [0mRead vitest.config.ts
[0mÔåÆ [0mRead packages/database/src/index.ts
[0mÔåÆ [0mRead packages/database/tsconfig.json
[0m
[0m$ [0mgit worktree list
C:/Users/enzo/Desktop/copytrade 2                      b2a2048 [master]
C:/Users/enzo/Desktop/copytrade-wt-f1-5-enrichment     73eac5e [sprint/f1-5-enrichment]
C:/Users/enzo/Desktop/copytrade-wt-f1-postgres-live    cbd49d4 [sprint/f1-postgres-live]
C:/Users/enzo/Desktop/copytrade-wt-f1-security-fixes   19ffacc [sprint/f1-security-fixes]
C:/Users/enzo/Desktop/copytrade-wt-f1-security-fixes-2 df98063 [sprint/f1-security-fixes-2]
[0m
[0m$ [0mgit log --oneline -8 sprint/f1-5-enrichment; git merge-base --is-ancestor 3d6c9db sprint/f1-5-enrichment; if 
($?) { "3d6c9db IS ancestor of branch head" }
73eac5e chore(ops): check-secrets isenta docs/ (relatorios com dados on-chain publicos)
38cf505 feat(f1.5): enricher+runner+wiring, retention opt-in, lock por idade, scripts de gate, docs
20d93cf feat(f1.5): helius-provider per-wallet subscriptions + anti-leak de sockets + maxSupportedTransactionVersion=1 
+ testes
3d6c9db feat(f1.5): schema ObservedEvent com enrichment + migration + repositories com guarda TOCTOU + testes
7d7e3d1 feat(f1.5): decoder pump.fun/PumpSwap via codama da IDL oficial (pump-public-docs @81091419)
3e51d3e review(sprint/f1-5-enrichment): REJEITADO (gates falharam) cfca4f89323cbfb68e0dcfa8aafe23a39e69301f
b7364bc review(sprint/f1-5-enrichment): REJEITADO (gates falharam) 0167a0d89fe0f21d6aa83d51e4738a1f4c5e96ae
10f6011 review(sprint/f1-5-enrichment): REJEITADO (gates falharam) 812ef1a7f376b01de7f7dbc21ffae9f645e1edc3
3d6c9db IS ancestor of branch head
[0m
[0m$ [0mgit show 3d6c9db:packages/database/src/index.ts
/**
 * @ct/database ÔÇö PrismaClient singleton.
 * Nunca commitar DATABASE_URL (ver docs/security/secret-policy.md).
 */
import { PrismaClient } from '@prisma/client';

declare global {
  // eslint-disable-next-line no-var
  var __ctPrisma: PrismaClient | undefined;
}

export const prisma: PrismaClient =
  globalThis.__ctPrisma ??
  new PrismaClient({
    log: ['warn', 'error'],
  });

if (process.env['NODE_ENV'] !== 'production') {
  globalThis.__ctPrisma = prisma;
}

export { PrismaClient };
export type * from '@prisma/client';

// Repositories ÔÇö client sempre injetado (test├ível, sem singleton escondido).
export {
  insertObservedEvent,
  existsObservedEvent,
  markEnriched,
  markUndecodable,
  findUnenriched,
  incrementEnrichAttempt,
  type InsertResult,
  type ObservedEventInsertData,
  type ObservedEventDelegate,
  type EnrichmentData,
  type UnenrichedRow,
  type ObservedEventEnrichDelegate,
} from './repositories/observed-events.js';
export {
  recordSystemEvent,
  type SystemEventInput,
  type SystemEventSeverity,
} from './repositories/system-events.js';
export {
  appendAuditLog,
  type AuditLogInput,
} from './repositories/audit-log.js';
export {
  RepositoryError,
  isUniqueViolation,
  PRISMA_UNIQUE_VIOLATION,
} from './repositories/errors.js';
[0m
[0m$ [0mTest-Path "C:\Users\enzo\Desktop\copytrade-wt-f1-5-enrichment\node_modules"; Test-Path 
"C:\Users\enzo\Desktop\copytrade-wt-f1-5-enrichment\node_modules\.bin\vitest.CMD"; Test-Path 
"C:\Users\enzo\Desktop\copytrade 2\node_modules"
True
True
True
[0m
[0m$ [0mgit log --oneline 3d6c9db..73eac5e -- packages/database/; "---"; git diff 3d6c9db 73eac5e --stat -- 
packages/database/
---
[0m
[0m$ [0mgit log --oneline 3d6c9db..73eac5e -- packages/pumpfun/ packages/solana/ services/data-ingestion/ | 
Select-Object -First 20
38cf505 feat(f1.5): enricher+runner+wiring, retention opt-in, lock por idade, scripts de gate, docs
20d93cf feat(f1.5): helius-provider per-wallet subscriptions + anti-leak de sockets + maxSupportedTransactionVersion=1 
+ testes
[0m
[93m[1m! [0mpermission requested: external_directory (C:\Users\enzo\Desktop\copytrade-wt-f1-5-enrichment\*); 
auto-rejecting
[0mÔ£ù [0mnpx vitest run packages/database/src/repositories/observed-events.enrich.test.ts 2>&1 failed
[91m[1mError: [0mThe user rejected permission to use this specific tool call.
The database package is untouched by later commits, so the worktree head includes this commit's files unchanged. Let me run the gates in the sprint worktree.

